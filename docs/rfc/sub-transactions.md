---
status: superseded
superseded-by: ../architecture/sub-transactions.md
layer: kernel
description: Moved — the sub-transactions RFC shipped as ctx.atomic and now lives in architecture/.
---

# Sub-transactions at the engine seam — moved

This RFC was decided and built (`ctx.atomic`, `runSub`, the atomic contract suite,
boundary-lint R7 — #770, #786), so per the [`rfc/` rule](../README.md) it now lives in
[`architecture/sub-transactions.md`](../architecture/sub-transactions.md), where a reader
looks for shipped work. This path stays only so older links resolve.
