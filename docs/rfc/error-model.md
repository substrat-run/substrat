---
status: superseded
superseded-by: ../architecture/error-model.md
layer: kernel
description: Moved — the error-model RFC shipped in @substrat-run/contracts and now lives in architecture/.
---

# The error model — moved

This RFC was decided and built (phases 1–4: the registry, `SubstratError`, `toProblem`,
the wire envelope and problem+json on every transport — #113), so per the
[`rfc/` rule](../README.md) it now lives in
[`architecture/error-model.md`](../architecture/error-model.md), where a reader looks for
shipped work. This path stays only so older links resolve.
