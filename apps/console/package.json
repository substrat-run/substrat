{
  "name": "@substrat-run/console",
  "version": "0.1.2",
  "private": true,
  "description": "The control-plane admin console (control-plane.md §4.5)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@substrat-run/contracts": "workspace:^",
    "@substrat-run/kernel": "workspace:^",
    "@substrat-run/ui": "workspace:^",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^7.0.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
