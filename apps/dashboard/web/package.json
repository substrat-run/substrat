{
  "name": "@substrat-run/dashboard-web",
  "version": "0.1.2",
  "private": true,
  "license": "Apache-2.0",
  "description": "The Dashboard SPA — Substrat's tenant-facing self-service surface (docs/design/dashboard-ui.md). Served by the dashboard worker.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@substrat-run/contracts": "workspace:^",
    "@substrat-run/ui": "workspace:^",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^7.0.0",
    "vite": "^6.0.0"
  }
}
