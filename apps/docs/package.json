{
  "name": "@substrat-run/docs",
  "private": true,
  "version": "0.1.12",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "cf:deploy": "vitepress build && wrangler pages deploy",
    "cf:deploy:test": "vitepress build && wrangler pages deploy --branch test",
    "typecheck": "exit 0",
    "test": "vitest run"
  },
  "devDependencies": {
    "@fontsource/geist-mono": "^5.2.8",
    "@fontsource/geist-sans": "^5.2.5",
    "mermaid": "^11.16.0",
    "vitepress": "^1.6.3",
    "vitepress-plugin-mermaid": "^2.0.17",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
