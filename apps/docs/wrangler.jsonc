{
  "name": "substrat-docs",
  // No "account_id" here — wrangler's Pages config validation rejects the key.
  // The account comes from CLOUDFLARE_ACCOUNT_ID (repo Actions variable in CI,
  // or the logged-in wrangler session locally).
  "pages_build_output_dir": ".vitepress/dist"
}
