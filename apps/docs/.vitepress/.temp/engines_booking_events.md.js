import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Events","description":"The twelve events the booking engine emits, from `booking.held` to `booking.no-show`, and the PII split that keeps participant identity out of every payload. Consumes nothing.","frontmatter":{"description":"The twelve events the booking engine emits, from `booking.held` to `booking.no-show`, and the PII split that keeps participant identity out of every payload. Consumes nothing."},"headers":[],"relativePath":"engines/booking/events.md","filePath":"engines/booking/events.md","lastUpdated":1787063543000}');
const _sfc_main = { name: "engines/booking/events.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h1><h2 id="emitted" tabindex="-1">Emitted <a class="header-anchor" href="#emitted" aria-label="Permalink to &quot;Emitted&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Event</th><th>When</th></tr></thead><tbody><tr><td><code>booking.resource-created</code></td><td>a bookable resource is added</td></tr><tr><td><code>booking.held</code></td><td>a tentative hold is placed</td></tr><tr><td><code>booking.confirmed</code></td><td>a hold becomes firm</td></tr><tr><td><code>booking.expired</code></td><td>a lapsed hold is surfaced</td></tr><tr><td><code>booking.cancelled</code></td><td>held or confirmed → cancelled</td></tr><tr><td><code>booking.moved</code></td><td>rescheduled — carries <code>from</code> and <code>to</code></td></tr><tr><td><code>booking.started</code></td><td>service begins</td></tr><tr><td><code>booking.completed</code></td><td>the terminal success transition</td></tr><tr><td><code>booking.no-show</code></td><td>the party did not turn up</td></tr><tr><td><code>booking.participant-joined</code></td><td>someone joined</td></tr><tr><td><code>booking.participant-left</code></td><td>someone left</td></tr><tr><td><code>booking.opened</code></td><td>places were put on offer on an existing reservation, or withdrawn</td></tr></tbody></table><p>Consumes nothing.</p><h2 id="the-pii-split-that-shapes-every-payload" tabindex="-1">The PII split that shapes every payload <a class="header-anchor" href="#the-pii-split-that-shapes-every-payload" aria-label="Permalink to &quot;The PII split that shapes every payload&quot;">​</a></h2><p>Three platform rules collide here, and the resolution is worth understanding before you write a consumer.</p><p>Fat events are required. A match may have four participants. And the event envelope permits <strong>exactly one</strong> <code>subjectId</code>, mandatory whenever <code>piiClass !== &#39;none&#39;</code>, because crypto-shredding must be able to key the erasure. A four-person roster cannot be keyed to one subject, and the envelope has no plural.</p><p>So the roster does not ride the aggregate event:</p><table tabindex="0"><thead><tr><th>Event</th><th><code>piiClass</code></th><th>Carries</th></tr></thead><tbody><tr><td><code>participant-joined</code> / <code>-left</code></td><td><code>pseudonymous</code>, <code>subjectId</code> = that party</td><td>the one <code>partyRef</code> + share</td></tr><tr><td>every reservation lifecycle event</td><td><code>none</code></td><td>resource, interval, quantity, <strong><code>participantCount</code></strong></td></tr></tbody></table><p>This is better privacy design than the alternative, not a workaround:</p><ul><li><strong>The club&#39;s business record survives an erasure.</strong> &quot;Court 1 was booked 17:00–18:30 and completed&quot; names nobody, so it is retained as a legitimate business record while the personal link is shredded with the per-participant events.</li><li><strong>Each pseudonymous fact is individually shreddable</strong>, keyed to exactly the subject it concerns — which is what the envelope was asking for.</li><li><strong><code>partyRef</code> is a <code>DataSubjectId</code></strong>, not a free string. Participants are people; the type says so.</li></ul><h3 id="reconstructing-a-roster" tabindex="-1">Reconstructing a roster <a class="header-anchor" href="#reconstructing-a-roster" aria-label="Permalink to &quot;Reconstructing a roster&quot;">​</a></h3><p>Correlate <code>participant-joined</code> / <code>-left</code> with the lifecycle event on <code>reservationId</code>. That is ordinary stream processing over one module&#39;s own events — <strong>not</strong> a cross-module read, so the fat-event rule is intact.</p><h3 id="split-billing-does-not-need-this" tabindex="-1">Split billing does not need this <a class="header-anchor" href="#split-billing-does-not-need-this" aria-label="Permalink to &quot;Split billing does not need this&quot;">​</a></h3><p>A vertical composes invoicing&#39;s in-scope functions in the <strong>same transaction</strong>, where it already holds the roster. It never needed the event.</p><h2 id="no-booking-match-played" tabindex="-1">No <code>booking.match-played</code> <a class="header-anchor" href="#no-booking-match-played" aria-label="Permalink to &quot;No \`booking.match-played\`&quot;">​</a></h2><p><code>booking.completed</code> is the same fact. One transition should not emit two events; a vertical wanting match vocabulary emits its own.</p><h2 id="evolution-rules" tabindex="-1">Evolution rules <a class="header-anchor" href="#evolution-rules" aria-label="Permalink to &quot;Evolution rules&quot;">​</a></h2><ul><li>Payload fields are <strong>frozen once shipped</strong>. Rename, remove or retype means a <code>schemaVersion</code> bump and dual-emit through a deprecation window.</li><li>New operation inputs are optional with behaviour-preserving defaults.</li><li>Permission keys are never renamed.</li><li>Consumers parse payloads with their own Zod schema, never the producer&#39;s types.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/booking/events.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const events = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  events as default
};
