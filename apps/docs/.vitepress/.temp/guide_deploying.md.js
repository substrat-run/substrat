import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Deploying a vertical","description":"","frontmatter":{},"headers":[],"relativePath":"guide/deploying.md","filePath":"guide/deploying.md","lastUpdated":1787127415000}');
const _sfc_main = { name: "guide/deploying.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="deploying-a-vertical" tabindex="-1">Deploying a vertical <a class="header-anchor" href="#deploying-a-vertical" aria-label="Permalink to &quot;Deploying a vertical&quot;">​</a></h1><p><a href="/guide/running-locally.html">Running locally</a> ends on a promise: the SQLite adapter you run on your laptop and the Cloudflare adapter you deploy on are the same kernel above <a href="/concepts/scope-host.html">the scope-host contract</a> — <em>only the composition root changes</em>. This page is how you cross that gap. The tool is the <strong><code>substrat</code> CLI</strong>, and the shape of the crossing is two ideas: a <strong>push</strong> uploads a version; a <strong>promotion</strong> points the <code>prod</code> channel at it and makes scopes serve it. For a vertical you own, both are yours.</p><p>The model this page assumes — push vs deploy, admission, the one <code>prod</code> channel, in-place updates, previews, backout — is written up conceptually in <a href="/concepts/deploying.html">the deploy model</a>. For non-production environments (test, canary, PR previews) see <a href="/guide/environments-and-previews.html">Environments &amp; previews</a>. This page is the how-to.</p><h2 id="push-is-not-deploy-—-but-promotion-is-yours" tabindex="-1">Push is not deploy — but promotion is yours <a class="header-anchor" href="#push-is-not-deploy-—-but-promotion-is-yours" aria-label="Permalink to &quot;Push is not deploy — but promotion is yours&quot;">​</a></h2><p>The single idea to hold onto:</p><blockquote><p>A push uploads a version; it does not serve. A <strong>promotion</strong> points the <code>prod</code> channel at a version and makes scopes run it. For a <strong>private</strong> vertical — one you own, not listed on the marketplace — a push lands <strong>admitted</strong> automatically and you promote <code>prod</code> yourself. So <code>substrat push --promote prod</code> is a complete deploy, and merge-to-main can be the deploy. (<code>prod</code> is the <em>only</em> channel — a test or preview environment is a <a href="/guide/environments-and-previews.html">scope with data</a>, not a second pointer.)</p></blockquote><p>There are two separate questions hiding in &quot;can this go live&quot;, and Substrat answers them at two different boundaries (decision D-36):</p><ul><li><strong>&quot;May this code run on our infrastructure?&quot;</strong> — answered <strong>mechanically</strong> by the <a href="https://github.com/substrat-run/substrat/blob/main/docs/architecture/self-serve-deploy.md" target="_blank" rel="noreferrer">sandbox contract</a>: the declared bindings, Workers-for-Platforms isolation, and quotas. This is <strong>admission</strong>, and for a private vertical the contract is the whole answer, so a push is admitted the moment it validates (an auto-admission note records that no human vouched).</li><li><strong>&quot;May <em>other tenants</em> run this code against <em>their</em> data?&quot;</strong> — a human decision, and it only arises when you <strong>publish</strong> a vertical to the marketplace. That is where the staff gate lives now: <code>substrat publish</code> (the <code>setVerticalListed</code> decision) is a human vouch, and from then on that vertical&#39;s pushes land <strong>pending</strong> and its prod promotion is a staff decision again.</li></ul><p>A private vertical has exactly one tenant exposed to its code — the workspace that wrote it — so there is no one to protect from an unvetted bundle but yourself, and you are the human at your own checkpoint. This is still the same <a href="/concepts/modules.html#two-human-checkpoints">two-checkpoint discipline</a> that governs migrations and permissions: a promotion that changes the permission or migration surface is refused until you acknowledge the diff (<code>--ack-permissions</code> / <code>--ack-migrations</code>).</p><p>One credential principle underpins all of it: <strong>the author never holds a Cloudflare token.</strong> The control plane holds the Workers-for-Platforms credential and does the upload; the CLI builds locally and POSTs a bundle. (Decision D-34.)</p><h2 id="install" tabindex="-1">Install <a class="header-anchor" href="#install" aria-label="Permalink to &quot;Install&quot;">​</a></h2><p>The CLI is published to npm as <a href="https://www.npmjs.com/package/@substrat-run/cli" target="_blank" rel="noreferrer"><code>@substrat-run/cli</code></a> (Apache-2.0):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -g</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/cli</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # or: pnpm add -g @substrat-run/cli</span></span></code></pre></div><p>That gives you the <code>substrat</code> bin. Inside this monorepo it is also wired at the root as <code>pnpm substrat</code>, so the examples below work either way.</p><h2 id="sign-in-—-substrat-login" tabindex="-1">Sign in — <code>substrat login</code> <a class="header-anchor" href="#sign-in-—-substrat-login" aria-label="Permalink to &quot;Sign in — \`substrat login\`&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> login</span></span></code></pre></div><p>The default is a <strong>browser loopback login</strong>: the CLI starts a one-shot server on <code>127.0.0.1</code>, opens your browser to the control plane&#39;s CLI broker (<code>{cp}/auth/cli</code>), which signs you in through <a href="/concepts/identity.html">AuthHero</a> and redirects back with a PKCE-bound <code>code</code>. The CLI exchanges the code for a session token and stores it in <code>~/.substrat/config.json</code>. The token never transits a URL — only the code does — and the loopback server accepts exactly one callback, then closes.</p><p>For CI, where there is no browser, the credential is a <strong>tenant-scoped push token</strong> (<code>spt1.…</code>) in the <code>SUBSTRAT_SERVICE_TOKEN</code> environment variable — the dashboard&#39;s one-click CI setup mints and installs it for you (<a href="#deploy-from-ci">below</a>).</p><p>Either way, auth resolves in this order at push time: explicit <code>--token</code> / <code>SUBSTRAT_SERVICE_TOKEN</code> → a stored browser session → a stored service token. Two consequences worth knowing:</p><ul><li>An <strong>exported <code>SUBSTRAT_SERVICE_TOKEN</code> wins over a fresh <code>substrat login</code></strong> — the session is ignored. The CLI warns when this happens; unset the variable to use the session, or pass <code>--token</code> to make the override explicit.</li><li>The control-plane URL resolves <code>--cp</code> → <code>SUBSTRAT_CP_URL</code> → the stored config, and it must be the <strong>API base</strong> — <code>https://console.substrat.net/api</code>, not the console page. Getting HTML where JSON was expected is the symptom, and the CLI&#39;s error message names it.</li></ul><p>You are always authenticated <strong>as yourself</strong> — a push is attributable to the human or service that ran it, never a hand-picked actor.</p><h2 id="your-workspace" tabindex="-1">Your workspace <a class="header-anchor" href="#your-workspace" aria-label="Permalink to &quot;Your workspace&quot;">​</a></h2><p>A vertical is owned by a <strong>workspace</strong> (a tenant), not a bare user — the same account you sign into the <a href="/platform/dashboard.html">dashboard</a> with. On <code>login</code> the CLI resolves which workspaces you belong to and stores a default; <code>substrat whoami</code> prints them:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> whoami</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># signed in as you@acme.com</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#   acme-co  (Acme Co)</span></span></code></pre></div><p>Which workspace a <strong>push</strong> acts for is pinned per project, not per machine: a <code>&quot;substrat&quot;: { &quot;tenant&quot;: &quot;acme-co&quot; }</code> block in the vertical&#39;s <code>package.json</code> (the first interactive push offers to write it for you; <code>--tenant</code> / <code>SUBSTRAT_TENANT</code> override). The stored login default is deliberately <em>not</em> used for pushes — the first push of a slug claims it for a workspace, and a global default silently pointing at the wrong one would claim it for the wrong owner. You never type your workspace <em>into</em> a slug — the control plane forms the prefix for you (next section). New here? Sign up once in the dashboard to create your workspace, then the CLI just works.</p><h2 id="ship-it-—-substrat-push" tabindex="-1">Ship it — <code>substrat push</code> <a class="header-anchor" href="#ship-it-—-substrat-push" aria-label="Permalink to &quot;Ship it — \`substrat push\`&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> my-vertical</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;&amp; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> push</span></span></code></pre></div><p>Run it from the vertical&#39;s directory and it needs no flags: the <strong>slug</strong> and <strong>name</strong> come from a <code>&quot;substrat&quot;: { &quot;slug&quot;, &quot;name&quot; }</code> block in <code>package.json</code> (or are derived from the package name), and the <strong>version</strong> defaults to the registry&#39;s latest, patch-bumped — so you never hand-track it. Override any with <code>--slug</code>, <code>--name</code>, or <code>--version</code>.</p><p>What you declare is <strong>what your vertical needs from the runtime, in Substrat terms</strong> — a <code>runtimeNeeds</code> block in the same <code>substrat</code> section. You never write Cloudflare deploy config; the CLI derives it at push time:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;substrat&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;slug&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;helpdesk&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;runtimeNeeds&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;entry&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;src/worker.ts&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;needsNodeCompat&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;build&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;pnpm --dir app build &amp;&amp; node scripts/gen-assets.mjs&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;stores&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;binding&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;SCOPE&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;class&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;ScopeDO&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;binding&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;AUTH&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;class&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;IdentityDO&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      ]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><ul><li><code>entry</code> — your worker&#39;s entry module.</li><li><code>needsNodeCompat</code> — set it if you use Node built-ins at runtime (Better Auth does).</li><li><code>build</code> — an optional command to run before bundling (an SPA build, asset generation).</li><li><code>stores</code> — your vertical&#39;s <strong>own</strong> durable state classes and the binding each is reached through (<code>env.SCOPE</code>). This is the whole vocabulary on purpose: the sandbox contract refuses everything except your own stores anyway, so there is nothing else to say. The compatibility date is the <strong>platform&#39;s</strong> runtime baseline — you never pick it.</li></ul><p>(If you already maintain a <code>wrangler.jsonc</code> — the demos in this repo do, for local <code>wrangler dev</code> — the CLI still reads it when no <code>runtimeNeeds</code> block is present. When both exist, <code>runtimeNeeds</code> wins.)</p><p>The same <code>substrat</code> block also carries your <strong>install spec</strong> — what the dashboard grants and renders when someone installs your vertical, no catalog edit needed:</p><ul><li><code>entitlements</code> — the full entitlement set an install grants the installing tenant (your own SKU flag plus any engine you compose, e.g. <code>[&quot;helpdesk&quot;, &quot;workorder&quot;]</code>). <strong>Absent, the platform derives your slug</strong> — so a vertical whose module gates on <code>entitlementKey: &#39;&lt;slug&gt;&#39;</code> (the convention) installs entitled to itself out of the box. Declare the list explicitly the moment you gate on anything else.</li><li><code>ownerGrants</code> — the permissions the installing owner holds inside the fresh scope on day one.</li><li><code>envSpec</code> / <code>surfaces</code> — the install form&#39;s config fields and your declared UI surfaces.</li></ul><p>The same block is also where you <strong>request platform capabilities</strong> — the things the sandbox deliberately won&#39;t let you bind for yourself:</p><ul><li><code>sendsEmail</code> — set it (<code>&quot;sendsEmail&quot;: true</code>) if your vertical sends transactional mail (password resets, verification, invites). You get <strong>no</strong> <code>send_email</code> binding: a dispatch script can&#39;t hold one, and the sandbox refuses it. Instead the platform sends on your behalf through a relay, and your code just uses the ordinary <code>EmailTransport</code> seam — the auth-server&#39;s Better-Auth <code>sendResetPassword</code> callback is the reference. The declaration is a <strong>request</strong>; it does nothing until a staff member grants the <code>emailSender</code> capability in the console.</li><li><code>provisions</code> — the verticals your manager app creates tenants of (the tenant-provisioner request), turned on the same way (<code>setVerticalTenantProvisioner</code>).</li></ul><p>A capability request is refreshed on every push and confers nothing by itself; the matching grant is a directory flag your push can never set or keep — so shipping code can never quietly acquire outbound authority. This is the wiring model: <strong>declare the request, get it granted, call the platform seam — never bind the raw resource.</strong></p><p>Entitlements are delivered to your vertical <strong>with provisioning</strong> and projected locally; your per-operation gate fails closed on anything the tenant doesn&#39;t hold. If a live install ever ends up missing one (granted later, or repaired), the control plane re-delivers through your <code>/internal/reconcile</code> route — part of the required <code>/internal</code> contract for hosted verticals (<code>/internal/provision</code>, <code>/internal/reconcile</code>, <code>/internal/configure</code>, and the snapshot/export/restore family). A vertical without <code>/internal/reconcile</code> cannot be repaired in place.</p><p>You do <strong>not</strong> hand-write those routes. The whole <code>/internal</code> surface — plus the <code>{ error }</code> response envelope the control plane relies on to read a failure — is authored once in <a href="/reference/vertical-host.html"><code>@substrat-run/vertical-host</code></a> and mounted in one call:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { mountPlatformSurface } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/vertical-host&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mountPlatformSurface</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(app, {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  platformSecret</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">env</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> env.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">PLATFORM_SECRET</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  hostFor,                       </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// (env) =&gt; your CloudflareScopeHost</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  roles: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">ROLES</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ownerRoleKey: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">OWNER_ROLE_KEY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  onProvision,                   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// your pending-owner / site-registry side effect</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  resolveOwner,                  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// owner-of-record for a reconcile (omit ⇒ 501)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  onConfigure,                   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// per-instance config store (omit ⇒ 501)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p>Mounting it is what satisfies the contract — the routes cannot drift out of sync or ship without the error envelope, because there is only one copy. <code>create-substrat</code> scaffolds this call for you; the demos (<code>demos/meridian</code>, <code>demos/manyfold</code>) are worked reference implementations.</p><p>A push then:</p><ol><li><strong>Builds the bundle</strong> with <code>wrangler deploy --dry-run --outdir</code> against the derived config — running your <code>build</code> command first. workerd cannot bundle in the isolate, so the build always happens on your side; the endpoint only ever receives a <em>built</em> worker.</li><li><strong>Assembles the manifest</strong> that travels with the bundle — your own store classes and bindings, the runtime baseline and flags, the entry module — from the same derived config the bundler consumed, so what you declared and what you shipped cannot drift.</li><li><strong>Computes digests</strong> — manifest, permission (from the bindings), migration (from the DO classes) — the same digest-diff surface the checkpoints read.</li><li><strong>POSTs the bundle + manifest</strong> to <code>{cp}/verticals/{slug}/deploy</code>, authenticated with your own credential.</li></ol><p>The endpoint validates your declared bindings against the sandbox contract — a customer bundle that tries to declare a <code>CONTROL_PLANE</code> binding or a platform secret is refused <em>before</em> it reaches the namespace — uploads to the <code>substrat-verticals</code> Workers-for-Platforms namespace under a <code>deploymentRef</code>, and records the version. For a <strong>private</strong> vertical that version lands <strong>admitted</strong> (the sandbox contract is the whole gate); a <strong>listed</strong> vertical&#39;s lands <strong>pending</strong> for staff admission. On success the CLI prints the version id, its admission state, and the <code>deploymentRef</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>✓ pushed acme-co/helpdesk. version 01J… (0.2.1) is admitted; deploymentRef=acme-co-helpdesk-01j…</span></span>
<span class="line"><span>  promote it to a channel to go live (or push with --promote prod).</span></span></code></pre></div><p>To make the same push a full deploy, add <code>--promote prod</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> push</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --promote</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prod</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># ✓ pushed acme-co/helpdesk. version 01J… (0.2.1) is admitted; deploymentRef=…</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># ✓ acme-co/helpdesk → prod now points at 0.2.1</span></span></code></pre></div><p>That is the shape the generated merge-to-main workflow uses: a private vertical&#39;s push is already admitted, so <code>--promote prod</code> succeeds immediately and the merge <em>is</em> the deploy. (A listed vertical&#39;s push lands pending instead, and the <code>--promote prod</code> in the same run is refused, naming the staff gate.)</p><h3 id="the-workspace-prefix" tabindex="-1">The <code>&lt;workspace&gt;/</code> prefix <a class="header-anchor" href="#the-workspace-prefix" aria-label="Permalink to &quot;The \`&lt;workspace&gt;/\` prefix&quot;">​</a></h3><p>You push a <strong>bare</strong> <code>--slug helpdesk</code>; the vertical&#39;s registry id is <code>acme-co/helpdesk</code> — your workspace slug, prepended by the control plane from your authenticated session. You never type it. The point is that the name is unique <em>by construction</em>: every workspace can own a <code>helpdesk</code> without a global land-grab, the same way project names are scoped to your account on Vercel. (This prefixes only the registry id and the <code>deploymentRef</code> — never an app&#39;s hostname, which is per <em>instance</em> and chosen when someone creates one.)</p><p>Ownership is claimed on first push and fixed there: a later push to <code>helpdesk</code> from a different workspace is <em>its own</em> <code>other-co/helpdesk</code>, and no one else can push versions of yours.</p><p>The flip side: <strong>the credential picks the lineage.</strong> A workspace-prefixed <code>acme-co/helpdesk</code> and a bare, platform-registered <code>helpdesk</code> are different verticals, even if they build from the same repository — pushes to one never update the other. Builder credentials (your login session, a push token) always land in your workspace&#39;s namespace; only platform staff address bare slugs. If a vertical seems to exist twice, or a push &quot;succeeds&quot; but the version never shows up where you&#39;re looking, check which lineage each side is resolving before anything else.</p><h2 id="serving-in-place-—-updates-carry-data-forward" tabindex="-1">Serving in place — updates carry data forward <a class="header-anchor" href="#serving-in-place-—-updates-carry-data-forward" aria-label="Permalink to &quot;Serving in place — updates carry data forward&quot;">​</a></h2><p>A vertical serves <strong>in place</strong> from one stable serving script, and version updates carry the scope&#39;s data <strong>forward</strong> (decision D-37). This is the part that makes &quot;promote prod&quot; safe to do from your laptop:</p><ul><li>A prod promote <strong>re-uploads</strong> the promoted bundle onto the vertical&#39;s stable serving script. The scope&#39;s Durable Object and its SQLite <strong>stay put</strong> — data does not move, so an update is not a rebind-to-empty-storage.</li><li>Because the data is still there, the kernel&#39;s append-only <strong>migrations run against production data</strong>, exactly as they were designed to. A version is badged <strong>code-only</strong> or <strong>schema-change</strong> at publish so you know which kind of promote you are doing; a schema-change promote wants its migration diff acknowledged.</li><li>Secrets survive the deploy (<code>keep_bindings</code>).</li><li><strong>Backout is a time-boxed rewind.</strong> Right before an upgrade migrates, the scope DO bookmarks the instant (Durable-Object point-in-time recovery). If a promotion goes wrong, an audited <strong>rewind</strong> rolls the scope&#39;s data back to that bookmark — time-boxed to <strong>~24h</strong> unless forced, the first-hours backout. The considered, longer path is <a href="/concepts/deploying.html#backup-restore-and-backout">backup / restore</a> (<code>substrat scope restore</code>). PITR rewinds the <em>whole</em> database, so a stale bookmark is refused where it cannot be skipped.</li></ul><p>Legacy scopes that predate the stable serving script hop onto it once with <code>substrat scope adopt-serving &lt;scopeId&gt;</code> (export → restore → flip, data-first; idempotent, and <code>--vertical &lt;slug&gt;</code> backfills every scope of a vertical).</p><h2 id="see-what-you-ve-pushed-—-substrat-versions" tabindex="-1">See what you&#39;ve pushed — <code>substrat versions</code> <a class="header-anchor" href="#see-what-you-ve-pushed-—-substrat-versions" aria-label="Permalink to &quot;See what you&#39;ve pushed — \`substrat versions\`&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> versions</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> helpdesk</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># VERSION  ADMISSION  CHANNELS      ID</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 0.2.1    admitted   prod          01J…</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 0.2.0    admitted                 01J…</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 0.1.0    admitted                 01J…</span></span></code></pre></div><p>A bare slug again — the control plane resolves it under your workspace. The same view is in the dashboard&#39;s <strong>Deployments</strong> tab (below), so you can watch admission state and channels without the CLI. (If a prod promote&#39;s in-place serve ever failed, <code>versions</code> flags the split — the channel points at the new version but the scopes still run the old one — as <code>prod(promoted)</code> vs <code>prod(serving)</code>, so a stalled serve is visible rather than silently assumed live.)</p><h2 id="promote-to-prod" tabindex="-1">Promote to prod — <code>substrat promote</code> <a class="header-anchor" href="#promote-to-prod" aria-label="Permalink to &quot;Promote to prod — \`substrat promote\` {#promote-to-prod}&quot;">​</a></h2><p>Once a version is <strong>admitted</strong> — which for your private verticals is immediately — you point <code>prod</code> at it yourself. <code>prod</code> is the only channel, so <code>--channel</code> defaults to it and you rarely type it:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> promote</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> helpdesk</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --version</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> 01J…</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ack-migrations</span></span></code></pre></div><p>Promotion re-points the prod scope at a new version — a <strong>rebind</strong>: the same scope and data, serving new code. For a vertical you own privately you self-serve it (there are no <em>other</em> tenants to keep in lockstep — that concern, D-30, is a shared vertical&#39;s many tenants, which a private vertical cannot have). Every promotion appends to the vertical&#39;s channel history — what went live, what it replaced, who, and exactly when — which is what the dashboard&#39;s rollback picker reads and what a rewind anchors to.</p><p>There is no <code>dev</code> or <code>staging</code> to promote to — a non-production environment is a <a href="/guide/environments-and-previews.html">preview</a>, a scope with data at its own URL, not a second pointer at the same code. A promote to any channel but <code>prod</code> is refused, naming the preview command.</p><p>The one thing a promote will stop for is a <strong>changed surface</strong>: a promotion whose permission or migration digest differs from what is live is refused until you acknowledge the diff (<code>--ack-permissions</code> / <code>--ack-migrations</code>) — read the diff it names first. That is the migration / permission checkpoint, applied at the deploy boundary.</p><p>The staff gate returns only when you <strong>widen the audience</strong>: <code>substrat publish &lt;slug&gt;</code> lists the vertical on the marketplace, and from then on its pushes land pending and its prod promotion is a staff decision again — because now <em>other</em> tenants can run your code against <em>their</em> data.</p><h2 id="preview-a-pull-request-—-substrat-preview" tabindex="-1">Preview a pull request — <code>substrat preview</code> <a class="header-anchor" href="#preview-a-pull-request-—-substrat-preview" aria-label="Permalink to &quot;Preview a pull request — \`substrat preview\`&quot;">​</a></h2><p>Before a change is merged you can see it running — the PR&#39;s code against a <strong>fork of your production data</strong>, on its own URL:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pr-42</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">          # push this tree, fork prod, serve the pair</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># ✓ preview &#39;pr-42&#39; created → https://helpdesk-acme--pr-42.global.substrat.run</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ls</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                            # what&#39;s live</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> delete</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pr-42</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">            # reap it (idempotent)</span></span></code></pre></div><p><code>create</code> pushes the working tree (so the version it binds is exactly the PR&#39;s code), forks your vertical&#39;s prod scope, binds the pushed version to the fork, and mints a non-canonical <code>--&lt;tag&gt;</code> hostname alongside your prod URL. Re-running the same <code>--tag</code> (what a new push to the PR does) <strong>rebinds the new version onto the same fork</strong>, so successive pushes roll their migrations forward on one copy — the rehearsal you actually want before a migration merges. <code>--refresh</code> starts over from a clean fork of prod. Every preview carries a TTL (<code>--ttl 72h</code> by default) so an abandoned one is garbage-collected even if it is never deleted.</p><p>This is wired into the generated GitHub workflow for you: <strong>opening or updating a PR creates or updates its preview and comments the URL; closing the PR reaps it.</strong> If your vertical was set up before previews existed, re-run the dashboard&#39;s one-click CI setup (or copy the regenerated <code>.github/workflows/substrat-deploy.yml</code>) to pick up the PR jobs.</p><p>A preview forks <em>your own tenant&#39;s</em> scope and serves no install, so you may preview a vertical you own whether it is <strong>private or listed</strong> — publishing widens who may <em>install</em> your code, not who may preview their own pending version (#513). A vertical&#39;s <em>first</em> environment, before any prod scope exists to fork, is <code>substrat preview create --tag … --empty</code> — a clean-room scope, no source (#514). The one hard limit is jurisdiction: forking pins the copy&#39;s <em>execution</em>, so an <code>eu</code>/<code>us</code> source scope is refused until Regional Services, the same residency gate as <code>scope pull</code>. The fork carries real data and its <code>--&lt;tag&gt;</code> URL is non-canonical and not public — treat it as production data.</p><p>Previews are the substrate for every non-production environment — sticky-per-PR and per-build URLs, a long-lived test environment on a custom domain, canary rollout, a release candidate. Those patterns, and the CI recipe that wires them, are <a href="/guide/environments-and-previews.html">Environments &amp; previews</a>.</p><h2 id="deploy-from-ci" tabindex="-1">Deploy from CI — the push token <a class="header-anchor" href="#deploy-from-ci" aria-label="Permalink to &quot;Deploy from CI — the push token {#deploy-from-ci}&quot;">​</a></h2><p>Your laptop authenticates with a browser login; a pipeline cannot. The CI credential is a <strong>tenant-scoped push token</strong> — a long-lived machine credential, recognizable by its <code>spt1.</code> prefix, that authenticates as a <em>builder for exactly one workspace</em>. It can do what you can do as a builder — push, promote your private verticals (prod included), manage previews — and nothing more: it never reaches another workspace&#39;s namespace, never admits a version, and never promotes a <strong>listed</strong> vertical to prod (those stay staff decisions). It rides the same <code>SUBSTRAT_SERVICE_TOKEN</code> variable the CLI already reads, so the workflow needs nothing special.</p><p>Two tokens deliberately do <em>not</em> fill this role: your login session is per-human and short-lived, and the platform&#39;s internal service token is staff-equivalent — it must never land in a customer repository (and would push to the wrong lineage anyway, per the previous section).</p><p>You rarely handle the push token yourself. The <a href="/platform/dashboard.html">dashboard</a>&#39;s Deployments view has a <strong>one-click CI setup</strong>: connect the GitHub App, pick a repository and branch, and it</p><ol><li>mints the push token,</li><li>writes it as the repo&#39;s <code>SUBSTRAT_SERVICE_TOKEN</code> Actions secret (before the workflow, so the first run already finds its credential), and</li><li>commits <code>.github/workflows/substrat-deploy.yml</code> — which triggers that first run immediately.</li></ol><p>The committed workflow is the whole loop from this page: a push to the chosen branch installs dependencies (your lockfile picks the package manager) and runs <code>substrat push --promote prod</code> — for a private vertical, merge-to-main <em>is</em> the deploy; opening or updating a PR creates its <a href="#preview-a-pull-request-—-substrat-preview">preview</a> and comments the URL; closing the PR reaps it. A manual copy-paste path shows the same file if you&#39;d rather commit it yourself.</p><p>If you never connect the GitHub App — you own your CI, you are not on GitHub-hosted runners, or you want the release-train shape instead of merge-deploys-prod — generate the same file locally:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> init</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ci</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> github</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                        # merge to the deploy branch releases</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> init</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ci</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> github</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --release</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> changesets</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # only a package.json version move releases</span></span></code></pre></div><p>Both paths render from one generator, so the committed workflow and the generated one cannot drift. Two extras are opt-in through repository <em>variables</em> rather than a regenerated file: <code>SUBSTRAT_TEST_SCOPE_ID</code> makes every merge rebind a <a href="/guide/environments-and-previews.html#a-long-lived-test-environment">long-lived test environment</a>, and <code>SUBSTRAT_PER_BUILD_PREVIEW</code> adds a frozen <a href="/guide/environments-and-previews.html#sticky-per-pr-and-per-build-urls">per-build URL</a> to each PR comment alongside the sticky one. See <a href="/reference/cli.html#init"><code>substrat init</code></a>.</p><p>One thing to know while the first run is in flight: <strong>a vertical registers on its first successful push</strong>. Until then there is no version row in the dashboard to hang an error off — if nothing appears, the place to look is the repository&#39;s Actions tab, not the Deployments view.</p><h2 id="watch-it-in-the-dashboard" tabindex="-1">Watch it in the dashboard <a class="header-anchor" href="#watch-it-in-the-dashboard" aria-label="Permalink to &quot;Watch it in the dashboard&quot;">​</a></h2><p>Everything above is mirrored in the <a href="/platform/dashboard.html">dashboard</a>&#39;s <strong>Deployments</strong> view: the verticals your workspace has pushed, each version&#39;s admission state, and where <code>prod</code> points — with self-serve prod promotion for a vertical you own, the channel history behind the rollback picker, and a <strong>Previews / Environments</strong> surface for the non-prod work (<a href="/guide/environments-and-previews.html">test env, PR previews, canary</a>). Push from the CLI, manage from either.</p><h2 id="the-whole-path" tabindex="-1">The whole path <a class="header-anchor" href="#the-whole-path" aria-label="Permalink to &quot;The whole path&quot;">​</a></h2><p><strong>push → (auto-admitted) → promote → serve in place</strong> — laptop to production with the author never holding a Cloudflare credential, data carried forward across every update:</p><table tabindex="0"><thead><tr><th>Step</th><th>Who</th><th>Where</th></tr></thead><tbody><tr><td><code>push</code> a bare slug → <code>&lt;workspace&gt;/&lt;slug&gt;</code> version</td><td>you (the owner)</td><td>CLI</td></tr><tr><td>admission — automatic for a private vertical (the sandbox contract <em>is</em> the gate)</td><td>the control plane</td><td>—</td></tr><tr><td>promote <code>prod</code> (re-points your live scopes; the only channel)</td><td>you</td><td>CLI or dashboard</td></tr><tr><td>serve in place — DOs/data stay put, migrations run forward</td><td>the vertical&#39;s serving script</td><td>—</td></tr><tr><td>backout — time-boxed PITR rewind, then <code>scope restore</code></td><td>you</td><td>CLI / dashboard</td></tr><tr><td>resolve hostname → scope → dispatch</td><td>the <a href="/platform/router.html">router</a></td><td>—</td></tr></tbody></table><p>When you later <code>substrat publish</code> a vertical to the marketplace, the staff gate reappears at that boundary: its pushes land pending and prod promotion becomes a staff decision, because that is the point where <em>other</em> tenants run your code against their data.</p><h2 id="where-this-is-going" tabindex="-1">Where this is going <a class="header-anchor" href="#where-this-is-going" aria-label="Permalink to &quot;Where this is going&quot;">​</a></h2><p>Self-serve end to end for a vertical you own — push it, promote prod, serve in place with data carried forward — is what ships today. The remaining evolution is on the <em>publish</em> side: admitting an <strong>untrusted, listed</strong> builder&#39;s source safely — building a customer&#39;s code in a disposable sandbox so the digest checkpoints become verified rather than advisory — at which point even the marketplace staff gate can relax. That trust model is the <a href="https://github.com/substrat-run/substrat/blob/main/docs/architecture/self-serve-deploy.md" target="_blank" rel="noreferrer">self-serve deploy design note</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/deploying.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const deploying = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  deploying as default
};
