import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Control plane","description":"","frontmatter":{},"headers":[],"relativePath":"platform/control-plane.md","filePath":"platform/control-plane.md","lastUpdated":1786381079000}');
const _sfc_main = { name: "platform/control-plane.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="control-plane" tabindex="-1">Control plane <a class="header-anchor" href="#control-plane" aria-label="Permalink to &quot;Control plane&quot;">​</a></h1><p>The <strong>shared directory</strong> every vertical registers against — the one deployment the whole platform has in common. A singleton <code>ControlPlaneDO</code> (tenant registry, scope lifecycle, entitlements, roles, identities, the append-only admin audit log) fronted by the audited <code>createControlPlaneApi</code> HTTP surface. Verticals register their tenant/scope here; the <a href="/platform/console.html">Console</a> reads and acts through it; the <a href="/platform/router.html">Router</a> resolves hostnames against it.</p><p>Nothing domain-shaped runs here. It owns no engine, no vertical tables — only the directory. (A module-less <code>ScopeDO</code> binding exists solely because the coordinator&#39;s <code>provisionScope</code> still instantiates one; decoupling that is later work.)</p><h2 id="where-it-sits" tabindex="-1">Where it sits <a class="header-anchor" href="#where-it-sits" aria-label="Permalink to &quot;Where it sits&quot;">​</a></h2><p>It is the concrete form of the <a href="/concepts/platform.html">platform layer</a>: the durable authority for <a href="/concepts/tenancy.html">tenancy</a> and, at write time, <a href="/concepts/permissions.html#where-tuples-live-a-scope-reads-only-its-own-state">permissions</a>. Under scope-local permissions it is a <strong>write-time</strong> authority that projects roles and tenant-level tuples into scopes — deliberately <em>off</em> the request hot path, so a scope never reads it to answer a permission check.</p><p>The transport in front of the DO is <a href="https://github.com/substrat-run/substrat/tree/main/packages/control-plane-api" target="_blank" rel="noreferrer"><code>@substrat-run/control-plane-api</code></a> — the audited <code>HostAdmin</code> surface, the same one the CLI&#39;s deploy endpoint and the Console call.</p><h2 id="what-it-manages-now" tabindex="-1">What it manages now <a class="header-anchor" href="#what-it-manages-now" aria-label="Permalink to &quot;What it manages now&quot;">​</a></h2><p>Beyond the base registry, a few capabilities have landed that are worth naming:</p><ul><li><strong>Tenant delete with a grace window.</strong> <code>deleting</code> is a reversible, read-closed containment state that stamps <code>deletingAt</code>; a scheduled <strong>grace-window sweep</strong> ages the tenant off that timestamp and then <code>reapTenant</code> clears the directory PII/config rows (identities, membership tuples, roles, entitlements, orgs) while keeping the <code>tenants</code> row as a burned-slug tombstone and the admin log whole.</li><li><strong>Reaping an archived scope&#39;s DO storage.</strong> Cloudflare never garbage-collects a Durable Object, so <code>reapScope</code> (archived → reaped) wipes the scope DO&#39;s storage explicitly — an archived app stops costing storage only once it is reaped.</li><li><strong>Entitlements delivered with provisioning.</strong> Entitlements are per-tenant SKU flags the kernel enforces per operation (<code>manifest.entitlementKey</code>); a scope reads its currently-held entitlements at request time, and the control plane is what projects them.</li><li><strong>The hosted-vertical sandbox as a positive allowlist.</strong> A pushed vertical&#39;s declared bindings are checked against a positive allowlist (<code>ADMISSIBLE_BINDING_TYPES</code>) — anything not named is refused. It is this sandbox contract, not a staff read of an opaque digest, that lets a <strong>private</strong> vertical&#39;s version land admitted automatically (its blast radius is its own tenant).</li><li><strong>Platform-mediated egress: the email relay.</strong> The sandbox refuses <em>egress-shaped</em> bindings on purpose — <code>send_email</code>, <code>ai</code>, <code>browser</code>, and friends stay off the allowlist because reaching the outside world is a platform concern, not a per-vertical one (and a Workers-for-Platforms dispatch script cannot bind <code>send_email</code> anyway). So a vertical never sends mail directly. Instead it POSTs to the control plane&#39;s <code>POST /internal/email/send</code> <strong>relay</strong>, and the control plane — the one worker that holds an outbound-mail credential — sends on its behalf, but <strong>only</strong> if that vertical holds the staff-granted <code>emailSender</code> capability. This is the general shape for giving a hosted vertical a privileged capability without handing it the credential: a manifest <em>request</em> (<code>substrat.sendsEmail</code>), a staff <em>grant</em> (<code>setVerticalEmailSender</code>, the twin of the tenant-provisioner grant), and a platform-held relay that checks the grant on every call. Authentication reuses the <code>PLATFORM_SECRET</code> the uploader already injects into every dispatch script; the relay re-derives <em>which</em> vertical is calling from the named <code>(tenant, scope)</code> and checks the grant against that, so holding the shared secret is not enough. The <code>from</code> address is always the platform&#39;s onboarded sender.</li><li><strong>Platform-mediated credential handover: the connection relay.</strong> The inbound twin of the email relay. Connecting a provider (Scrive, Fortnox) is a <em>tenant admin&#39;s</em> act, so the natural place to paste a provider credential is the vertical&#39;s own admin screen — but a hosted vertical must never store one (plaintext in a scope row would ride every export, backup, and PITR window). Instead the vertical permission-checks the act with its own <code>ctx.check</code>, hands the secret to its harness as an effect stripped from the response, and the harness POSTs <code>POST /internal/connections/upsert</code> — <code>{tenant, scope, provider, secret, grants?, createdBy}</code> — to the control plane, which seals it into the connection store with the platform&#39;s <code>SecretBox</code>. Upserts are keyed (tenant, vertical, provider, account): a live connection is rotated <strong>in place</strong>, so its id and every permission granted to it survive — credential rotation is self-serve, no support ticket. The same trust derivation as the email relay applies (the shared secret never says <em>which</em> vertical; the platform&#39;s own scope record does), and the audit records the authorizing tenant principal (<code>createdBy</code>/<code>rotatedBy</code>) — metadata only, never the credential. Unlike the email relay there is no staff-granted capability: the tenant hands over its <em>own</em> credential for its <em>own</em> vertical, and the store&#39;s grant guards already pin the blast radius to that (tenant, vertical).</li></ul><h2 id="backup-and-recovery" tabindex="-1">Backup and recovery <a class="header-anchor" href="#backup-and-recovery" aria-label="Permalink to &quot;Backup and recovery&quot;">​</a></h2><p>Four different failures, four different instruments — and the useful thing is knowing which one covers what, because only one of them is a backup in the usual sense.</p><table tabindex="0"><thead><tr><th>What is lost</th><th>What covers it</th></tr></thead><tbody><tr><td>A scope&#39;s data, wrongly changed</td><td><strong>Durable Object point-in-time recovery</strong> — ~30 days, continuous, per scope. A destructive rewind of the live app.</td></tr><tr><td>A scope, reaped</td><td><strong>The copy the reap takes first.</strong> A reap wipes DO storage irreversibly, so the control plane stores a full-fidelity dump <em>before any byte goes</em> and records its address on the admin-log entry.</td></tr><tr><td>A scope, wanted elsewhere</td><td><strong><code>scope pull</code> / snapshots</strong> — a non-destructive copy that leaves the live app alone (see <a href="/concepts/snapshots.html">Snapshots</a>).</td></tr><tr><td><strong>The directory itself</strong></td><td><strong>The scheduled directory backup</strong>, below.</td></tr></tbody></table><p>That last row is the one the others cannot cover. The directory is a single Durable Object — lose it and no scope can be <em>found</em>, even though every scope&#39;s data is sitting there intact. PITR does not help, because PITR rewinds a database that still exists; nothing to point it at is a different failure. And no scope can rebuild the map from below: a scope does not know its own tenancy, hostname, or bound version.</p><p>So the control plane backs itself up. The scheduled sweep dumps the whole directory — tenants, scopes, hostnames, verticals, entitlements, identities, <em>and</em> the audit log — to an object store outside the DO, <strong>once a day, keeping 30 copies</strong>. The cadence is derived by reading the newest stored copy rather than from a separate timer, so a missed run is caught up on the next pass rather than skipped, and the retention window is pruned only <em>after</em> a new copy lands — a failed backup can never be the thing that deletes the last good one.</p><p><strong>RPO ≤ 24h, RTO ≤ 1h</strong> for a directory loss, and the restore has been rehearsed rather than merely designed: the round trip runs in the <a href="/reference/contract-tests.html">contract test suite</a> against both the Cloudflare and SQLite adapters — capture, diverge, restore, then keep serving through the directory that was just rewritten.</p><p><strong>Restoring replaces.</strong> The dump&#39;s contents <em>become</em> the directory; anything created since the copy was taken is gone. A merge would silently interleave two histories of the same tenant, so replace is the honest semantic — and the restore refuses a directory that still holds tenants unless the request explicitly says to overwrite. That guard exists for the realistic hazard: a restore replayed against a control plane that has already recovered.</p><p><strong>What it does not cover.</strong> The backup bucket lives in the platform&#39;s own account, so this survives losing the <em>directory</em> — a bug, a bad migration, a deleted DO — but not losing the <em>account</em>. The store is a provider-neutral seam so an off-account target is a drop-in, but as shipped that is the honest boundary. A restore also does not bring back what was never in the directory: the staff roster (its own D1 database), worker secrets, or the key that any sealed credential was sealed with.</p><p><strong>Self-hosting note.</strong> On the SQLite adapter there is no DO point-in-time recovery to fall back on, so <code>exportDirectory</code>/<code>restoreDirectory</code> are not a second line of defence there — they are the only one. Both adapters implement the same pair, and the same contract tests prove it.</p><h2 id="auth-posture-—-fail-closed" tabindex="-1">Auth posture — fail closed <a class="header-anchor" href="#auth-posture-—-fail-closed" aria-label="Permalink to &quot;Auth posture — fail closed&quot;">​</a></h2><p>Secure by default: a real <code>wrangler deploy</code> sets no dev-actor escape hatch, so every request <strong>fails closed (401)</strong> until authenticated. Locally, <code>pnpm --filter @substrat-run/control-plane dev</code> turns on <code>ALLOW_DEV_ACTOR</code> and trusts an <code>x-platform-actor</code> header as a stand-in — never mounted in production.</p><p>Who may act is data, not config: the <code>staff_actor</code> table in D1 holds one <code>PlatformActorId</code> per human, so the admin log can name <em>who</em> suspended a tenant. Access is revoked with a <strong>tombstone</strong>, never a <code>DELETE</code> (K-21) — the row is the evidence that access was once granted, which is what an audit asks for. An empty roster means nobody can act; fail-closed is the correct posture. The roster also gates staff account creation, so a departed operator cannot simply re-register.</p><p>The one honest gap: staff auth is still email + password with no MFA and no SSO, on a surface that can suspend every tenant. That is a tracked open question, not a settled design.</p><h2 id="run-it" tabindex="-1">Run it <a class="header-anchor" href="#run-it" aria-label="Permalink to &quot;Run it&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/control-plane</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # wrangler dev, no account; ALLOW_DEV_ACTOR on</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/control-plane</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> test</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # workerd test</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/control-plane</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # Workers Paid plan (DO SQLite)</span></span></code></pre></div></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("platform/control-plane.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const controlPlane = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  controlPlane as default
};
