import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Callout (field service)","description":"","frontmatter":{},"headers":[],"relativePath":"verticals/callout.md","filePath":"verticals/callout.md","lastUpdated":1784804439000}');
const _sfc_main = { name: "verticals/callout.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="callout-field-service" tabindex="-1">Callout (field service) <a class="header-anchor" href="#callout-field-service" aria-label="Permalink to &quot;Callout (field service)&quot;">​</a></h1><p><code>demos/callout</code> — a small Swedish field-service firm (ElMontage AB, el/VVS installation): work orders, technician time &amp; material, checklists, and invoice basis. It is the platform&#39;s <strong>reference vertical</strong> — the one a new vertical is built against.</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Callout is the <strong>canonical composition</strong> — the star-topology showpiece. It is where you see the central claim of the platform work end to end: engines cooperating <strong>through events and opaque <code>EntityRef</code>s, with zero imports between them</strong>. Complete a work order and, on the spine, an invoice line appears — built by an engine that never imported the one that emitted the event.</p><p>Three things it demonstrates that the other demos build on:</p><ul><li><strong>The star topology, running.</strong> Callout composes <em>three</em> engines — <a href="/engines/workorder/"><code>workorder</code></a>, <a href="/engines/invoicing/"><code>invoicing</code></a>, <a href="/engines/protocol/"><code>protocol</code></a> — and none imports another. <code>workorder.completed</code> lands on the spine; invoicing consumes it and builds billable lines by <strong>snapshot, not join</strong>, each with drill-down provenance. Editing time entries after completion is refused by the engine invariant — and wouldn&#39;t change the invoice line anyway, because it was snapshotted.</li><li><strong>The pricing moment</strong> — vertical logic meeting an engine transition in one transaction. The <code>callout/complete-workorder</code> operation runs the vertical&#39;s own compliance guard (a <code>montage</code> order needs a signed self-inspection protocol, via <code>requireSigned</code>), reads the engine-reported lines, prices them from the vertical&#39;s <code>callout_price_list</code> (min quantities, internal articles dropped), and <em>then</em> calls the engine&#39;s <code>completeWorkOrder(ctx, { billable })</code>. The engine invariant stays intact; pricing is 100% vertical-owned (K-16). Pricing is exactly the kind of thing an engine must never learn.</li><li><strong>The protocol extraction, in place.</strong> Callout&#39;s checklists began as vertical code and were extracted into the protocol engine at milestone B — a migration (<code>0003</code>) drops the old <code>callout_protocol_*</code> tables once the data moves into the engine&#39;s own. The extraction handoff is visible in the module.</li></ul><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>@substrat-run/demo-callout</code></td></tr><tr><td><strong>Engines composed</strong></td><td><a href="/engines/workorder/"><code>workorder</code></a> · <a href="/engines/invoicing/"><code>invoicing</code></a> · <a href="/engines/protocol/"><code>protocol</code></a></td></tr><tr><td><strong>Own tables</strong></td><td><code>callout_customers</code> · <code>callout_facilities</code> · <code>callout_price_list</code></td></tr><tr><td><strong>Roles</strong></td><td><code>office-admin</code> (15 keys) · <code>technician</code> (4 keys: <code>workorder:read</code>/<code>report</code>, <code>protocol:fill</code>/<code>read</code>) — portal customers hold no role, only an entity-narrowed <code>workorder:read</code> grant per customer</td></tr><tr><td><strong>Permission surface</strong></td><td><a href="https://github.com/substrat-run/substrat/blob/main/demos/callout/PERMISSIONS.md" target="_blank" rel="noreferrer"><code>PERMISSIONS.md</code></a> — 19 keys, 4 modules, 2 roles</td></tr><tr><td><strong>Apps</strong></td><td>node API (<code>:8871</code>) + React SPA (<code>:5271</code>), started together</td></tr><tr><td><strong>Status</strong></td><td>Working — the first <strong>CP-less, sandbox-clean pushable</strong> vertical, deployed on Cloudflare</td></tr></tbody></table><h2 id="the-cast-what-s-denied" tabindex="-1">The cast &amp; what&#39;s denied <a class="header-anchor" href="#the-cast-what-s-denied" aria-label="Permalink to &quot;The cast &amp; what&#39;s denied&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Who</th><th>Holds</th><th>Cannot</th></tr></thead><tbody><tr><td><strong>Anna</strong> — kontor</td><td><code>office-admin</code>, tenant level</td><td>— (runs the whole flow: create → assign → complete → export invoice basis)</td></tr><tr><td><strong>Harald</strong> — tekniker</td><td><code>technician</code> at the work scope</td><td><strong>assign</strong> an order, complete it, or sign a protocol — he can only start jobs and report time/material</td></tr><tr><td><strong>Berit</strong> — BRF Grunden portal customer</td><td>no role; entity-narrowed <code>workorder:read</code> on her org</td><td>see any order that isn&#39;t her org&#39;s — the portal walk is a <strong>tuple proof-walk</strong>, not a UI filter</td></tr><tr><td><strong>Styrbjörn</strong> — another portal customer</td><td>the same, for his org</td><td>see Berit&#39;s orders</td></tr><tr><td><strong>Mallory</strong> — office-admin of <em>another tenant</em></td><td><code>office-admin</code> in her own tenant</td><td>anything of ElMontage&#39;s — the cross-tenant denial</td></tr></tbody></table><p>Signing is deliberately kept from the technician: <code>protocol:fill</code> is not <code>protocol:sign</code>. Filling a checklist and attesting it are different authorities, and the demo splits them.</p><h2 id="deploying-—-the-cp-less-reference" tabindex="-1">Deploying — the CP-less reference <a class="header-anchor" href="#deploying-—-the-cp-less-reference" aria-label="Permalink to &quot;Deploying — the CP-less reference&quot;">​</a></h2><p>Callout is the vertical the <a href="/guide/deploying.html">deploy path</a> was proven on. Its <code>wrangler.jsonc</code> declares only its <strong>own</strong> stores — its <code>SCOPE</code> Durable Object class and an <code>AUTH_DB</code> — and <em>no</em><code>CONTROL_PLANE</code> binding, no service binding, no platform secret: <code>assertSandboxContract</code> would refuse an upload that reached for any of them. It evaluates permissions from its own storage (<a href="/concepts/permissions.html#where-tuples-live-a-scope-reads-only-its-own-state">scope-local permissions</a>) and trusts the router-asserted node for tenancy. Its React SPA is <strong>bundled into the worker</strong> (a build step generates <code>src/assets.ts</code>), so it needs no static-assets binding either. This is the shape a self-serve vertical takes.</p><h2 id="run-it" tabindex="-1">Run it <a class="header-anchor" href="#run-it" aria-label="Permalink to &quot;Run it&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> callout-demo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># API  http://localhost:8871</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># web  http://localhost:5271</span></span></code></pre></div><p>The executable spec is <code>test/scenario.test.ts</code> — the nine-step headless scenario, including the cross-tenant denial — plus <code>test/provision.test.ts</code> over the permission shape. On Cloudflare, <code>cf:dev</code> runs it on local Durable Objects and <code>cf:deploy</code> ships it (Workers Paid plan for DO SQLite); the same route table drives both, with only the adapter swapped SQLite ↔ DO/D1.</p><h2 id="deliberately-out-of-scope" tabindex="-1">Deliberately out of scope <a class="header-anchor" href="#deliberately-out-of-scope" aria-label="Permalink to &quot;Deliberately out of scope&quot;">​</a></h2><p>Scheduling/dispatch (only an assignment field is shown), inventory, supplier invoices / EDI / payroll, and a real Fortnox connector — export writes a stubbed file. The scheduling engine is a <a href="/engines/#engines-today">named next candidate</a>, not a gap pretending to be filled.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("verticals/callout.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const callout = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  callout as default
};
