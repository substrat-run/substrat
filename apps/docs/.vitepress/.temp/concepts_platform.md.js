import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"The platform layer","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/platform.md","filePath":"concepts/platform.md","lastUpdated":1787048747000}');
const _sfc_main = { name: "concepts/platform.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="the-platform-layer" tabindex="-1">The platform layer <a class="header-anchor" href="#the-platform-layer" aria-label="Permalink to &quot;The platform layer&quot;">​</a></h1><p>A Substrat vertical is not a SaaS product that happens to share some libraries. It is an application deployed <strong>onto a platform</strong> — and the platform below it owns tenancy, routing, custom domains, identity, entitlements, and the event history tier. This page is about that layer: what it gives you, what it deliberately does <em>not</em> share, and why the boundary falls where it does.</p><div class="info custom-block"><p class="custom-block-title">Status</p><p>The platform layer runs in production. Shipping on both adapters: <strong>scope provisioning</strong>, the <strong>directory</strong>, the <strong>tenant registry</strong>, <strong>tenant + scope lifecycle transitions</strong> (with fail-closed gating), the <strong>entitlement gate</strong>, and the <strong>host admin</strong> surface — every mutation of which takes a platform actor and writes an append-only <strong>audit log</strong>. Since this page was first written the <a href="/platform/router.html">router</a> and custom-domain issuance, the ops <a href="/platform/console.html">console</a>, the builder <a href="/platform/dashboard.html">dashboard</a>, <a href="/guide/environments-and-previews.html">per-PR previews</a>, directory backup/restore, and the platform-owned outbound seams (connections, egress policy, email) have all landed. What is still designed-and-unbuilt is called out where it appears.</p></div><h2 id="one-platform-n-deployments" tabindex="-1">One platform, N deployments <a class="header-anchor" href="#one-platform-n-deployments" aria-label="Permalink to &quot;One platform, N deployments&quot;">​</a></h2><p>Each vertical gets <strong>its own kernel-runtime deployment</strong>, hosting that vertical&#39;s scopes. Different verticals are separate deployments with separate storage namespaces. It&#39;s tempting to read that as <em>every vertical re-erects the whole platform</em>. It doesn&#39;t — and the split between what&#39;s shared and what isn&#39;t is the design:</p><table tabindex="0"><thead><tr><th></th><th>Shared — the platform owns it</th><th>Per-vertical</th></tr></thead><tbody><tr><td><strong>Routing</strong></td><td>Resolves <code>hostname → (tenant, scope, vertical, surface)</code>, then dispatches</td><td>The surfaces a manifest declares</td></tr><tr><td><strong>Custom domains</strong></td><td>Hostname issuance, DNS validation, certificate lifecycle</td><td>—</td></tr><tr><td><strong>Tenancy</strong></td><td>Tenant registry, scope directory, provisioning lifecycle</td><td>—</td></tr><tr><td><strong>Identity</strong></td><td>Auth callbacks, principal derivation, capability minting</td><td>—</td></tr><tr><td><strong>Entitlements</strong></td><td>The store and the module-load gate</td><td>The <code>entitlementKey</code> each manifest declares</td></tr><tr><td><strong>History &amp; analytics</strong></td><td>The event spine and its history tier</td><td>—</td></tr><tr><td><strong>Copies</strong></td><td>Snapshots, forks, per-PR previews, per-scope PITR, directory backup/restore</td><td>—</td></tr><tr><td><strong>The outside world</strong></td><td>Connections and their sealed credentials, the outbound egress seam, the email relay</td><td>The declared allowlist and the connectors a manifest requires</td></tr><tr><td><strong>Admin</strong></td><td>Directory, audit log, access log, ops-failure log; the <a href="/platform/console.html">console</a> and the builder <a href="/platform/dashboard.html">dashboard</a></td><td>—</td></tr><tr><td><strong>Execution</strong></td><td>—</td><td><strong>The scope&#39;s code</strong>: kernel + engines + your modules, and their migrations</td></tr></tbody></table><p>Everything a vertical would hate to rebuild is already shared. The only per-vertical thing is <em>the code that runs inside a scope</em>.</p><blockquote><p><strong>The scope&#39;s code is the app binary; the platform beneath it is one platform.</strong></p></blockquote><p>So building a second vertical does not mean standing up a second Substrat. It means shipping a second binary onto the same substrate — new modules, new vocabulary, new screens, against the same tenancy, the same permission model, the same event spine, the same domains machinery.</p><h2 id="why-the-deployments-don-t-merge" tabindex="-1">Why the deployments don&#39;t merge <a class="header-anchor" href="#why-the-deployments-don-t-merge" aria-label="Permalink to &quot;Why the deployments don&#39;t merge&quot;">​</a></h2><p>The obvious follow-up: if the platform is shared anyway, why not run every vertical&#39;s modules in <strong>one</strong> deployment and let entitlements decide which are active per scope?</p><p>It&#39;s a coherent design — it&#39;s how ordinary multi-tenant SaaS works — and it is rejected, for one reason that dominates the others:</p><ul><li><strong>Migrations would be globally ordered across unrelated verticals.</strong> Every scope would carry every vertical&#39;s modules, and module registration order is a <a href="/concepts/modules.html">migration-ordering contract</a>. A change to <em>another</em> vertical&#39;s migration list would touch <em>your</em> scopes.</li><li><strong>Blast radius would merge.</strong> A bad deploy of someone else&#39;s module code would take down your scopes.</li><li><strong>Upgrades would go lockstep.</strong> A shared binary means every vertical upgrades together — so you could not stay on engine v2 while another vertical moves to v3. Verticals on Substrat are often owned by <em>different companies</em>. Forcing one to upgrade because another shipped is the forced-upgrade treadmill that has degraded every extensible business platform that tried it.</li></ul><p>The shared-bundle alternative also trades a <strong>structural</strong> guarantee for a <strong>configuration</strong> guarantee — isolation would hold because the entitlement config says so, rather than because nothing is addressable. Substrat consistently refuses that trade: vertical code never gets a raw handle to the storage namespace, it gets a <a href="/concepts/scope-host.html">capability stub</a> for one scope, so cross-scope access isn&#39;t <em>denied</em> — it&#39;s <em>unreachable</em>.</p><h2 id="scope-lifecycle" tabindex="-1">Scope lifecycle <a class="header-anchor" href="#scope-lifecycle" aria-label="Permalink to &quot;Scope lifecycle&quot;">​</a></h2><p><code>provisioning → active → suspended ⇄ active → archiving → archived</code></p><p>Provisioning is idempotent and journaled, safe to re-run and safe to drive from a reconciliation sweep (<a href="/concepts/tenancy.html">Tenants &amp; scopes</a>). Provisioning requires an existing <strong>active tenant</strong> — a scope can never be orphaned. The rest of the lifecycle is control-plane work; every transition is validated (an illegal one fails closed) and audited:</p><ul><li><strong>Suspend</strong> fails <code>getScope</code> closed for every scope under the tenant. It&#39;s how an incident or a non-payment is contained without deleting anything — the same fail-closed path that stops a confused-deputy bug.</li><li><strong>Archive</strong> exports the scope&#39;s storage and releases it, keeping the registry row and the event history forever. <strong>Un-archive is a restore, not a flag flip.</strong></li><li><strong>Jurisdiction is immutable.</strong> Fixed at provisioning; a scope&#39;s execution domain can never relocate. There is no edit affordance because there is no edit.</li></ul><h2 id="platform-intents" tabindex="-1">Asking the platform for something: intents, not calls <a class="header-anchor" href="#platform-intents" aria-label="Permalink to &quot;Asking the platform for something: intents, not calls {#platform-intents}&quot;">​</a></h2><p>A vertical sometimes needs a privileged action it structurally cannot perform: provision a sibling scope (a new Manyfold &quot;site&quot;), send mail, reach a third party. The obvious design is an upward call — the vertical holds a platform credential and invokes the control plane. It is rejected for the same reason the shared bundle above is: a credential in vertical code is a configuration guarantee, and the whole point is that the reach should not exist.</p><p>Instead the vertical <strong>enqueues an intent</strong> into its own scope, and the platform comes and gets it:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// inside an operation, AFTER the vertical&#39;s own permission check</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> id</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">requestPlatform</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  kind: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;provision-sibling&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  payload: { slug, name, owner: principal },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><ul><li>The row lands in this scope&#39;s <code>_substrat_platform_requests</code> spine table, <strong>atomic with the operation</strong> — if the handler throws, the intent never happened.</li><li>Origin fields (<code>id</code>, <code>requestedAt</code>, <code>requestedBy</code>) are stamped kernel-side, exactly as an event envelope is. The vertical cannot claim to be someone else.</li><li>The platform&#39;s drain reads that scope&#39;s DO, so it <strong>knows the tenant inherently</strong> — the tenant is not in the payload and cannot be forged into it.</li><li><code>kind</code> selects the platform-side handler, which validates the payload. To the kernel the payload is opaque, like an event&#39;s.</li><li>A scope may hold at most <code>MAX_PENDING_PLATFORM_REQUESTS</code> (32) pending intents; past that <code>requestPlatform</code> throws. A stuck or runaway vertical cannot flood the drain.</li></ul><p>Authorization is the vertical&#39;s; isolation is the platform&#39;s. That split is why the intent is enqueued <em>after</em> the vertical&#39;s own <code>ctx.check</code>.</p><p><strong>And the outcome comes back.</strong> <code>ctx.platformRequests(filter)</code> reads this scope&#39;s own intent journal — newest first, filterable by <code>kind</code> and <code>status</code> — returning the same record the platform settled, with its <code>result</code> or <code>lastError</code>. That read exists for one concrete reason: a contract whose signature request settled <code>failed</code> can say so on its own screen, instead of showing a document that appears to be out for signature and is not. The kernel owns every write to the table, so a status is only ever the platform&#39;s answer.</p><h2 id="scheduled-work" tabindex="-1">Scheduled work <a class="header-anchor" href="#scheduled-work" aria-label="Permalink to &quot;Scheduled work {#scheduled-work}&quot;">​</a></h2><p>The platform runs one recurring pass — the <strong>platform sweep</strong> — that does every unit of scheduled work the system has: draining retryable effects, reconciling connectors, reaping expired snapshots, and running each vertical&#39;s declared <a href="/concepts/modules.html#recurring-work-schedules"><code>schedules</code></a>. It is <em>the scheduler&#39;s unit of work</em>; it holds no timer of its own. A deployment drives it — a node server calls <code>startPlatformSweeper</code> at boot, a Cloudflare deployment arms a singleton <code>PlatformSweeperDO</code> alarm (a dispatch namespace doesn&#39;t honour <code>wrangler</code> crons, so an alarm is the timer such a deployment can own).</p><p>Because module operations run in the vertical&#39;s own runtime — where its code and its scopes&#39; data live — the sweep runs <strong>there</strong>, not in the control plane (whose scope storage is empty by design). For each vertical that declares schedules, the pass enumerates its live scopes and invokes each due operation under a system actor, recording per-scope outcomes and stepping over any failure rather than letting it sink the pass. A schedule fires no more often than its cadence, tracked per scope; a fork or snapshot is skipped, so a test copy never runs real recurring side effects.</p><h2 id="entitlements-gate-modules-not-features" tabindex="-1">Entitlements gate modules, not features <a class="header-anchor" href="#entitlements-gate-modules-not-features" aria-label="Permalink to &quot;Entitlements gate modules, not features&quot;">​</a></h2><p>Every <a href="/concepts/modules.html">manifest</a> declares an <code>entitlementKey</code>. The platform holds a set of entitlements per tenant, and checks it <strong>when a scope invokes an operation</strong> — default-deny, uncached today (a DO-cached variant is an open benchmark).</p><p>A module whose key the tenant does not hold <strong>does not register</strong> — its operations simply do not resolve, exactly as if the module had never existed. This is the same mechanism as a manifest <code>withdraws</code> declaration, and it is deliberately blunter than a feature flag: there is no half-loaded engine, no operation that exists but refuses, no code path where an unlicensed invariant is half-enforced. A tenant either has the work-order engine or does not have it.</p><p>That bluntness is what makes the <em>load gate</em> safe to enforce at the boundary rather than sprinkled through business logic.</p><p><strong>The flag also carries a plan.</strong> Since #33 an entitlement is not only an on/off SKU: it carries a <code>plan</code> — <code>quota</code>, <code>expiresAt</code>, and a <code>tier</code> — that a vertical reads at request time through <code>ctx.entitlement(key)</code> / <code>ctx.entitlements()</code> to gate features and enforce quota <em>within</em> a module it already holds. So entitlements are the load gate <strong>and</strong> a feature/plan surface — the earlier &quot;not a feature flag&quot; line was too absolute. The kernel enforces two things itself: presence (the load gate above) and <strong>expiry</strong>, which fails closed at the gate exactly as a revoke would; <code>quota</code> and <code>tier</code> are expression only — the vertical reads the number and enforces its own meaning. For a <strong>hosted</strong> vertical these are read from a <strong>scope-local projection</strong>, so gating a feature needs no control-plane binding. It is the same mechanism as the load gate and, like it, <strong>uncached today</strong> (a DO-cached variant is the open benchmark).</p><h2 id="why-the-admin-console-isn-t-a-vertical" tabindex="-1">Why the admin console isn&#39;t a vertical <a class="header-anchor" href="#why-the-admin-console-isn-t-a-vertical" aria-label="Permalink to &quot;Why the admin console isn&#39;t a vertical&quot;">​</a></h2><p>Substrat&#39;s admin surface — provisioning scopes, suspending tenants, granting entitlements — runs <strong>outside</strong> the module system, and this is worth understanding because it&#39;s a good test of whether the isolation story is real.</p><p>A super-admin, by definition, acts across every tenant. So: could you just build it as a vertical, with the platform as its own tenant?</p><p><strong>No — and not because it would be dangerous. Because it would be inert.</strong> A vertical&#39;s code reaches data through exactly one path: a capability stub for one <code>(tenant, scope)</code> pair that the kernel minted for it. It holds no handle to the storage namespace and no way to name a scope it wasn&#39;t invoked for — including, in production, scopes belonging to <em>other verticals&#39; deployments</em>, which it cannot address at all. An admin vertical would sit there with no way to reach the thing it exists to manage. To give it that reach you&#39;d have to build a privileged out-of-band path — which is the control plane, arrived at by a longer road.</p><p>This is the isolation guarantee doing its job on its own author. The interesting half: <em>record-keeping</em> about tenants — who they are, which plan, what staff did — is ordinary scope-shaped data, and can perfectly well be a vertical on Substrat like any other. It&#39;s <em>acting</em> across tenants that cannot be.</p><h2 id="what-this-means-for-you" tabindex="-1">What this means for you <a class="header-anchor" href="#what-this-means-for-you" aria-label="Permalink to &quot;What this means for you&quot;">​</a></h2><ul><li><strong>Don&#39;t build tenancy, domains, audit, or identity.</strong> They&#39;re below you. Verticals that rebuild them are a smell that the kernel drew a line wrong. Authentication in particular is a swappable edge adapter — see <a href="/concepts/identity.html">authentication &amp; identity</a>.</li><li><strong>Your scopes carry your code only.</strong> Your migrations are ordered against your modules and the engines you depend on — never against a stranger&#39;s vertical.</li><li><strong>You upgrade on your schedule.</strong> Per-vertical deployments exist so that engine versions, migrations, and blast radius stay yours.</li><li><strong>Declare an <code>entitlementKey</code> and mean it.</strong> It is the unit at which your module is sold, licensed, and switched off.</li></ul><p>See <a href="/concepts/tenancy.html">Tenants &amp; scopes</a> for the entities and addressing model, and <a href="/concepts/modules.html">Modules &amp; the manifest</a> for what a deployment is made of.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/platform.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const platform = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  platform as default
};
