import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"What a good API looks like","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/api-design.md","filePath":"concepts/api-design.md","lastUpdated":1787673225000}');
const _sfc_main = { name: "concepts/api-design.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="what-a-good-api-looks-like" tabindex="-1">What a good API looks like <a class="header-anchor" href="#what-a-good-api-looks-like" aria-label="Permalink to &quot;What a good API looks like&quot;">​</a></h1><p>Substrat is opinionated about API shape on purpose. Not because there is one true REST, but because the alternative is that every vertical re-decides pagination, error bodies, time and identifiers — and then a fleet of them disagrees, forever, in ways that only surface when someone tries to write one client against two.</p><p>The goal of this page is a specific one: <strong>a well-shaped API should be what falls out of the defaults, not what a careful team remembers to do.</strong> Where a convention is enforced mechanically, that is said plainly. Where it is still a convention, that is said too.</p><div class="info custom-block"><p class="custom-block-title">Status</p><p>Shipping today: the operation spine, boundary parsing, value types, the context clock, lost-update safety, request idempotency, additive evolution, the generated OpenAPI document, and the <strong>error model</strong> — every surface answers <code>application/problem+json</code>. The <strong>pagination</strong> convention ships in <code>contracts</code> and is adopted across the platform&#39;s own surfaces, but not yet by every engine and vertical. Nothing on this page is aspirational without saying so.</p></div><h2 id="the-shape-of-one-operation" tabindex="-1">The shape of one operation <a class="header-anchor" href="#the-shape-of-one-operation" aria-label="Permalink to &quot;The shape of one operation&quot;">​</a></h2><p>Everything below is a variation on one idea, so it is worth seeing the whole thing once:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;callout/create-workorder&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  summary: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Open a work order against a facility&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  permission: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;facility:manage&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  input: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">object</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    facilityId: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    description: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">min</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    priority: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">enum</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">([</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;normal&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;urgent&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]).</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">optional</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  output: workOrder,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  http: { method: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;POST&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, path: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;/workorders&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>That declaration is not documentation <em>about</em> the operation. It <strong>is</strong> the operation&#39;s contract: the same Zod objects validate the request at runtime, type the handler at compile time, and generate the OpenAPI document served at <code>/openapi.json</code>. There is no second place where the truth is written down, so there is no second place for it to drift.</p><h2 id="the-defaults" tabindex="-1">The defaults <a class="header-anchor" href="#the-defaults" aria-label="Permalink to &quot;The defaults&quot;">​</a></h2><h3 id="_1-one-operation-one-permission-one-event" tabindex="-1">1. One operation, one permission, one event <a class="header-anchor" href="#_1-one-operation-one-permission-one-event" aria-label="Permalink to &quot;1. One operation, one permission, one event&quot;">​</a></h3><p>Every operation&#39;s first line is its permission check:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">assertAllowed</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">check</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;facility:manage&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">));</span></span></code></pre></div><p>Every mutation emits a <strong>fat</strong> event — one carrying enough payload that a consumer never needs to read back across a module boundary to understand what happened. Together these give you two things most systems retrofit badly: an authorization story that cannot be bypassed by adding a route, and an audit trail that is a byproduct of doing the work rather than a feature someone remembered.</p><p>How much of that is mechanism rather than discipline is worth being precise about, because the honest answer is &quot;most, not all&quot;:</p><ul><li><strong>Declaring the permission is a compile error to skip.</strong> An operation carries either a <code>permission</code> or a <code>narrows</code> with a stated reason — never both, and never neither. An entity-narrowed check must say what it narrows to, and which field the entity id comes from; naming a field that does not exist does not compile.</li><li><strong>The permission surface is re-emitted by CI.</strong> <code>pnpm lint:permissions --check</code> renders each vertical&#39;s <code>PERMISSIONS.md</code> from the same objects the code uses, so a widened role cannot merge without appearing in the pull-request diff.</li><li><strong>The handler&#39;s <code>assertAllowed</code> line is still a convention.</strong> <code>tools/boundary-lint.mjs</code> enforces the data, network, spine-write and star-topology boundaries — it does not check that a handler calls its declared permission. That gap is closed by the declaration and by review, not by a linter.</li></ul><h3 id="_2-parse-don-t-trust" tabindex="-1">2. Parse, don&#39;t trust <a class="header-anchor" href="#_2-parse-don-t-trust" aria-label="Permalink to &quot;2. Parse, don&#39;t trust&quot;">​</a></h3><p>Input crosses the boundary through a Zod schema or it does not cross. Not &quot;validate the suspicious fields&quot; — parse the whole input into a typed value, once, at the edge. Everything downstream is then working with a value the type system already believes in.</p><h3 id="_3-values-that-survive-the-wire" tabindex="-1">3. Values that survive the wire <a class="header-anchor" href="#_3-values-that-survive-the-wire" aria-label="Permalink to &quot;3. Values that survive the wire&quot;">​</a></h3><ul><li><strong>Money is a decimal string plus a currency</strong>, never a float, and arithmetic goes through the shared helpers. See <a href="/concepts/money.html">Money</a>.</li><li><strong>Identifiers are ULIDs.</strong> Sortable by creation time, generated without coordination, and — usefully — they double as pagination cursors.</li><li><strong>Instants are ISO 8601 with an offset</strong>, stamped once and never re-derived.</li></ul><p>The rule underneath all three: a value should mean the same thing after a JSON round trip as it did before one. Floats and naive local timestamps both fail that test.</p><h3 id="_4-lists-are-pages-not-dumps" tabindex="-1">4. Lists are pages, not dumps <a class="header-anchor" href="#_4-lists-are-pages-not-dumps" aria-label="Permalink to &quot;4. Lists are pages, not dumps&quot;">​</a></h3><p>A list endpoint that returns everything is a bug with a delay on it. It passes review, it passes tests, and then one tenant&#39;s table gets large.</p><p>The platform convention is <strong>keyset pagination</strong> — a cursor over the list&#39;s own sort key, never an offset. The request carries the walk in the query string, and the response carries it in <strong>headers</strong>, so the body stays the list it always was:</p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">GET</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> /api/customers?limit=20&amp;cursor=01J8Z3K7Q9WRT0P</span></span></code></pre></div><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">200 OK</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Link</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &lt;https://api…/customers?limit=20&amp;cursor=01J9A2M4X8QER1S&gt;; rel=&quot;next&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">X-Total-Count</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> 340</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[ </span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}">…</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ]</span></span></code></pre></div><p>The <code>Link</code> is <a href="https://www.rfc-editor.org/rfc/rfc8288" target="_blank" rel="noreferrer">RFC 8288</a>, the same header GitHub serves, and it hands the client a URL to <strong>follow</strong> rather than one to assemble — so the filters and the page size travel with it and cannot be dropped by accident. Its absence is how the walk ends: no <code>rel=&quot;next&quot;</code>, no further request. <code>X-Total-Count</code> appears only for a list that asked for a total.</p><p><strong>Why headers and not a <code>{ entries, nextCursor }</code> body.</strong> Because the body is a published contract and the walk is not. Wrapping the body renames a live endpoint&#39;s response — <code>[…]</code> becomes <code>{ &quot;entries&quot;: […] }</code> — so adopting paging broke every consumer a vertical could not see, and the rational move was to leave an unbounded list unbounded, which is the opposite of the point. It also could not be done at all for a list whose published shape was a bare array: a body cannot be an array and an object at once. In headers, adopting paging changes nothing a client was already reading.</p><p>A caveat that follows from the choice: a browser client on a <strong>different origin</strong> cannot read <code>Link</code> or <code>X-Total-Count</code> unless the server lists them in <code>Access-Control-Expose-Headers</code> — and the symptom is not an error, it is a list that looks like it has exactly one page. <code>PAGE_EXPOSED_HEADERS</code> in <code>@substrat-run/contracts</code> is the list to expose.</p><p><strong>Inside the platform, a page is still a value.</strong> <code>ctx</code>-side and <code>stub.invoke</code> callers get <code>Page&lt;T&gt;</code> — <code>{ entries, nextCursor }</code> — because an operation is transport-agnostic and a test, a seed or another operation has no HTTP response to read a header off. The handler returns <code>pageOf(...)</code>; the HTTP mount projects it. That is also why the platform&#39;s own control-plane API still answers with the envelope in the body: its only consumers are the console and dashboard, versioned and deployed with it, so it has no migration problem to solve and no unknown client to protect.</p><p>There is <strong>one</strong> way to page, not two. Page numbers and <code>offset</code> are not offered, and that is a decision rather than an omission: on live data rows shift between requests, so an offset window silently skips and duplicates rows. A cursor names a <em>position in the ordering</em> instead of a count of rows that have scrolled past, so a row inserted mid-walk cannot push another onto a page you already read.</p><p>The cost is honest and worth stating: keyset gives you <em>next</em>, not <em>jump to page 7</em>. A page number is not recoverable from a cursor, and asking for one is usually a sign the screen wants a report rather than a list.</p><p><strong>A total count is available, and is opt-in.</strong> Keyset cannot produce one for free — that is the trade for correctness under concurrent writes — so a total is a second query per request. Business software asks for it constantly (a table of work orders with no <code>1–20 of 340</code> reads as broken), so the platform supports it rather than pretending nobody needs it. It just declines to charge every list for it:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">paged</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sortKey</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;id&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">total</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span></code></pre></div><p>The handler then returns <code>countedPageOf(...)</code> instead of <code>pageOf(...)</code>, and the compiler holds it to that; the total reaches the client as <code>X-Total-Count</code>. Two things to know about the number: it counts the <strong>filtered</strong> set — the same <code>WHERE</code> the page ran under, never the table, which is the mistake that looks right until a second list exists — and it is a snapshot, so rows written mid-walk can make page one&#39;s total disagree with the rows eventually seen. That is inherent to counting a moving set, not something to design around.</p><p>Two defaults worth knowing: HTTP list reads <strong>default to a page</strong> (20, capped at 200), because egress is where an ever-growing table has to stop being a dump. Kernel-side reads default to <strong>unbounded</strong>, because internal callers — provisioning, catalogs, sweeps — mean &quot;everything&quot;, and a silent cap there would let them mistake a page for the whole set.</p><p><strong>A search is not a list, and does not go on the list endpoint.</strong> Once a list is paged, a client filtering the page it received is searching the first page only — so a picker over a large table needs a real index, not a <code>q</code> parameter bolted onto the list. Give it its own route:</p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">GET</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> /api/customers/search?q=ander&amp;limit=10</span></span></code></pre></div><p>The two reads have genuinely different contracts. A list is ordered by a declared sort key and paged by a cursor over it; a search is ordered by <em>relevance</em>, which has no stable sort key and therefore no honest cursor — paging a ranked result set reorders rows, and rows go missing or double. So a search is <strong>capped</strong> and says so, and the caller narrows the term rather than paging. One endpoint cannot carry both contracts, which is why every API that has tried ends up with two anyway.</p><p><code>/customers/search</code> does not collide with <code>/customers/{id}</code>: a static segment is registered ahead of its parameter sibling, the same rule OpenAPI uses to resolve a concrete path before a templated one. Declaring the operation is what gets you that ordering — a hand-written route table has to do it by hand.</p><p>How the index itself is declared and read is in <a href="/concepts/reads.html#finding-a-row-by-what-someone-typed">Reads &amp; scaling</a>.</p><h4 id="declaring-it" tabindex="-1">Declaring it <a class="header-anchor" href="#declaring-it" aria-label="Permalink to &quot;Declaring it&quot;">​</a></h4><p>An operation declares <code>paged</code> and its <code>output</code> carries the <strong>entry</strong> shape; the platform supplies the envelope, the query parameters and the handler&#39;s return type:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;acme/list-customers&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  output: entities.customer.fields,   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// the ENTRY, not an array</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  paged: { sortKey: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;id&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, order: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;desc&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, total: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  http: { method: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;GET&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, path: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;/customers&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">},</span></span></code></pre></div><p><code>sortKey</code> is compile-checked against that entry&#39;s fields, and the handler is typed to return <code>Page&lt;Entry&gt;</code> — so declaring <code>paged</code> and returning a bare array does not compile. See <a href="/concepts/model.html#paged-reads">The model</a> for the handler side.</p><div class="warning custom-block"><p class="custom-block-title">Declared here, adopted incrementally</p><p>The convention ships in <code>@substrat-run/contracts</code> and is used across the control plane, dashboard and console. The <code>paged</code> declaration exists and the todo demo uses it end to end, but most engine and vertical list operations have <strong>not</strong> adopted it yet — several still return unbounded arrays. Tracked in <a href="https://github.com/substrat-run/substrat/issues/129" target="_blank" rel="noreferrer">#129</a> and <a href="https://github.com/substrat-run/substrat/issues/811" target="_blank" rel="noreferrer">#811</a>, which also adds the declared filter/sort vocabulary a cursor needs to stay correct under a caller-chosen sort.</p></div><div class="tip custom-block"><p class="custom-block-title">The one place offset survives</p><p>The console&#39;s <strong>scope-table browser</strong> pages with <code>limit</code>/<code>offset</code>, deliberately. It is random access into a table for a human reading rows — &quot;jump to 5,000&quot; is the actual requirement, and drift between requests is not a correctness problem there. It is a debugging surface, not a product API, and it is the exception that has to justify itself.</p></div><h3 id="_5-failures-are-data" tabindex="-1">5. Failures are data <a class="header-anchor" href="#_5-failures-are-data" aria-label="Permalink to &quot;5. Failures are data&quot;">​</a></h3><p>An error is part of your API surface, not an accident that happens to it. <code>500 Something went wrong</code> is unactionable for a human and worse for an agent; <code>validation_failed on field &#39;email&#39;</code> can be recovered from without a person reading a log.</p><p>The shape is <a href="https://www.rfc-editor.org/rfc/rfc9457" target="_blank" rel="noreferrer">RFC 9457</a> <code>problem+json</code> with a closed code taxonomy:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;type&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:   </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;https://substrat.net/errors/permission-denied&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;title&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:  </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;Permission denied&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;status&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">403</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;detail&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;permission denied: customer:manage&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;code&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:   </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;permission_denied&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;permission&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;customer:manage&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>The codes are a small closed set — <code>unauthenticated</code>, <code>permission_denied</code>, <code>forbidden</code>, <code>not_found</code>, <code>conflict</code>, <code>validation_failed</code>, <code>precondition_failed</code>, <code>rate_limited</code>, <code>unavailable</code>, <code>internal</code> — because an open set is a suggestion. A module narrows one with a <code>reason</code> slug it owns rather than inventing a code, which is the same boundary discipline engines follow everywhere else.</p><p>One rule that is not a detail: <strong><code>internal</code> never carries a message.</strong> An unrecognised throw is by definition one nobody reviewed for what it discloses, and a multi-tenant surface is the wrong place to find out.</p><p><strong>A body with no <code>code</code> is telling you something.</strong> When a transport is relaying a status it did not raise — a throw nobody typed, a downstream vertical&#39;s own refusal — the body is RFC 9457&#39;s <code>about:blank</code> form: the status, the message, and no taxonomy entry. Inventing a code there would put our vocabulary on a failure we cannot describe, and a client switching on <code>code</code> would match it. So the absence is the honest answer, and it is also the signal that a throw site is still untyped.</p><p>You get all of this by throwing <code>substratError(&#39;conflict&#39;, &#39;the cart is empty&#39;, { reason: &#39;cart_empty&#39; })</code> and letting <code>problemResponse</code> render it. <code>code</code> is the platform&#39;s, <code>reason</code> is yours.</p><div class="info custom-block"><p class="custom-block-title">The deprecated duplicate</p><p>Every body also carries <code>error</code>, a copy of <code>detail</code>, because every SPA in this repo read <code>{ error }</code> before the model landed. It exists for one migration window and then goes — read <code>detail</code>.</p></div><h3 id="_6-time-comes-from-the-context" tabindex="-1">6. Time comes from the context <a class="header-anchor" href="#_6-time-comes-from-the-context" aria-label="Permalink to &quot;6. Time comes from the context&quot;">​</a></h3><p>An operation should not read the wall clock. It should ask its context what time it is, so that tests can freeze it and replay means something:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> now</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">now</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span></code></pre></div><p>This looks like a nicety until you try to test anything with a window in it — leave balances, metering periods, booking availability — and find that the only way to assert the interesting case is to wait for it. A host takes a <code>clock</code>, so a scenario hands in <code>frozenClock</code> or <code>manualClock</code> and asserts the elapsed case exactly, instead of sleeping.</p><p>It is a mechanism rather than a convention: <code>new Date()</code> and <code>Date.now()</code> in module code are <code>boundary-lint</code> <strong>R6</strong> violations, the same class of ban as a <code>node:*</code> import. Code that must read the real wall clock — a JWT whose <code>exp</code> a remote server judges — opts out in a reviewable <code>boundary-lint-allow R6</code> block.</p><p>The value is also stable for the whole invocation, which is a second thing worth having: the rows a write leaves and the events announcing them agree about when they happened.</p><h3 id="_7-writes-are-safe-to-retry" tabindex="-1">7. Writes are safe to retry <a class="header-anchor" href="#_7-writes-are-safe-to-retry" aria-label="Permalink to &quot;7. Writes are safe to retry&quot;">​</a></h3><p>A client that times out does not know whether the work happened. It retries, and there is a second work order. Agents make this acute rather than novel: they retry more aggressively than people, and they do it faster.</p><p>The client sends a token it chose. The same token on the retry returns the first response instead of doing the work again:</p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">POST</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> /api/workorders</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Idempotency-Key</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dispatch-4471</span></span></code></pre></div><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">200 OK</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">Idempotency-Replayed</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> true</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{ </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;id&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;01J8Z…&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#B31D28", "--shiki-light-font-style": "italic", "--shiki-dark": "#FDAEB7", "--shiki-dark-font-style": "italic" })}">…</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span></code></pre></div><p><strong>Nothing is declared, and that is the difference from §7b.</strong> Every operation on an unsafe method honours the header, because a retried write creating a second entity is a hazard on all of them — where a lost update is a hazard on the field-bag shape alone, which is why <code>concurrency</code> is opt-in and this is not. The client opts in by sending a key; the server never requires one. A <code>GET</code> is already idempotent, so the header is not honoured there: doing so would mean serving a recorded <em>body</em> for a read, which is a cache with none of a cache&#39;s rules.</p><p><strong>The recording is written inside the operation&#39;s own transaction</strong>, and three properties fall out of that one placement rather than from mechanisms of their own:</p><ul><li><strong>A failed request is retried, not replayed.</strong> The operation threw, the transaction rolled back, and the recording went with it. There is nothing to find, so the retry executes — correctly, because nothing happened the first time. Recording failures would have meant deciding which of them are permanent, and a retry after a 500 is the most ordinary thing a client does.</li><li><strong>A replayed response describes work that actually committed.</strong> There is no window in which the recording exists and the rows it describes do not.</li><li><strong>A concurrent retry cannot slip past.</strong> Invocations serialise per scope, so the second request takes its turn after the first has committed. Every other implementation of this needs an in-flight state and a &quot;still processing&quot; 409; this one does not.</li></ul><p><strong>A key names one request.</strong> Send the same key with a different body and it is refused with <code>409 conflict</code>, never served the earlier response — a client that received an answer to a question it did not ask will act on it. Two deliberate identical writes are two writes, and a client that wanted one sends one key. A key is also scoped to the caller who sent it: two clients will both choose <code>1</code>, and a lookup that crossed that boundary would replay one principal&#39;s response to another.</p><p>Two limits, stated rather than discovered:</p><ul><li><strong>A replay is not a fresh authorization.</strong> The recorded response is returned without running the handler, and the permission check lives inside the handler. What bounds it is that a caller can only ever reach responses it received itself, and that a key is remembered for <strong>24 hours</strong>. The alternative — re-running the operation so the permission can be re-checked — is the duplicate execution the key exists to prevent.</li><li><strong>A response too large to record (over 128 KiB) refuses its replay</strong> with <code>409</code> rather than executing again. Fail closed: an error the caller can act on beats doing the work twice, and the original did complete.</li></ul><p><strong>Opting out is a line someone wrote.</strong> An operation whose response must not be recorded — a freshly minted secret, a one-time token — says so, and the platform then refuses the header rather than silently storing the response or silently executing twice:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;acme/mint-token&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  idempotency: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// the response is a credential; do not record it</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  …</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Opt-out rather than opt-in because the two read differently in a diff: a missing opt-in is invisible, while <code>idempotency: false</code> is something a reviewer can ask about. It is the same reasoning <code>narrows</code> applies to a permission that is deliberately not node-level — state the exception, never the rule.</p><div class="tip custom-block"><p class="custom-block-title">Not the event spine&#39;s idempotency</p><p>Consumers have been required-idempotent since the beginning, and the contract suite checks it: a consumer may see an event more than once and must settle to the same state. That is the spine re-delivering. This is a client re-sending. Different boundary, different actor, no relationship beyond the word.</p></div><h3 id="_7b-a-read-modify-write-says-what-it-is-writing-over" tabindex="-1">7b. A read-modify-write says what it is writing over <a class="header-anchor" href="#_7b-a-read-modify-write-says-what-it-is-writing-over" aria-label="Permalink to &quot;7b. A read-modify-write says what it is writing over&quot;">​</a></h3><p>Two people open the same record, both save, and the second write silently destroys the first. There is no error, no log line, and nobody notices until the data is gone. This is the oldest bug in multi-user software and the one hardest to see afterwards — the row looks fine, it is just wrong.</p><p>An operation that is read-modify-write declares <code>concurrency</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;callout/update-facility&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  permission: { key: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;facility:manage&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, entity: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;facility&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, idFrom: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;facilityId&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  input: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">object</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    facilityId: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    name: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">optional</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    address: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">optional</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    accessNote: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">optional</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  concurrency: { over: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;facility&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, idFrom: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;facilityId&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  emits: { entity: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;facility&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, entityIdFrom: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;id&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;callout.facility-updated&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, … },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  http: { method: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;PATCH&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, path: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;/facilities/{facilityId}&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Declared once, it does three things. Every response carries the entity&#39;s version as an <code>ETag</code>. An unsafe method compares the caller&#39;s <code>If-Match</code> against that version <strong>inside the operation&#39;s transaction</strong> and refuses a stale one with <code>412 precondition_failed</code>. And the generated browser client remembers the tag from a read and sends it on the next write to the same entity, so an app writes no header code.</p><p><strong>Opt-in, not blanket.</strong> Most declared operations are command-shaped — <code>todo/rename-list</code> takes a name, not a whole entity it read and echoed back — and two concurrent renames do not lose an update. A mandatory precondition there is a forced round-trip guarding nothing.</p><p><strong>But not left to memory.</strong> The shape that <em>does</em> lose updates is visible in the model: one required field naming the row, every other field optional over that entity&#39;s own columns. An operation of that shape with no <code>concurrency</code> is refused at module load, the same way a bare-array list output with no <code>paged</code> is.</p><div class="tip custom-block"><p class="custom-block-title">Why the guarded operation must emit</p><p>An entity&#39;s version is the ULID of the last event about it — there is no version column (<a href="https://github.com/substrat-run/substrat/issues/901" target="_blank" rel="noreferrer">#901</a>). So a guarded write that announces nothing is <em>worse</em> than an unguarded one: both writers pass their <code>If-Match</code>, neither moves the version, both commit, and both get a 200 with an <code>ETag</code> asserting the write was serialised. <code>concurrency.over</code> is therefore compile-checked against the operation&#39;s declared <code>emits</code>.</p><p>Adopting this in Callout is what revealed that <code>create-facility</code> had never emitted at all — so every facility it created had no version, and no conditional update against one could ever have succeeded.</p></div><p>Two smaller rules follow from the same reasoning:</p><ul><li><strong>The 412 does not carry the current version.</strong> Handing it back turns the obvious client fix into a blind retry that overwrites whatever caused the refusal. Re-reading is what gives a user something to merge.</li><li><strong><code>ETag</code> must be in <code>Access-Control-Expose-Headers</code></strong> for a cross-origin browser client — <code>CONCURRENCY_EXPOSED_HEADERS</code> names it. Unexposed, the tag reads as <code>null</code>, no <code>If-Match</code> is ever sent, and the protection switches itself off silently in exactly the deployment shape where two editors are most likely.</li></ul><h3 id="_8-surfaces-only-grow" tabindex="-1">8. Surfaces only grow <a class="header-anchor" href="#_8-surfaces-only-grow" aria-label="Permalink to &quot;8. Surfaces only grow&quot;">​</a></h3><p>Once shipped, an operation&#39;s surface evolves <strong>additively</strong>:</p><ul><li>new inputs are optional, with behavior-preserving defaults;</li><li>emitted event payload fields are frozen — renaming, removing or retyping one means a <code>schemaVersion</code> bump and a dual-emit deprecation window;</li><li>permission keys are never renamed.</li></ul><p>This is the rule that makes a fleet of independently-deployed verticals survivable. It is also the rule that makes <em>deciding conventions early</em> matter so much: an additive-only system is one where the cost of a wrong default compounds.</p><h3 id="_9-the-document-is-generated-and-ci-diffs-it" tabindex="-1">9. The document is generated, and CI diffs it <a class="header-anchor" href="#_9-the-document-is-generated-and-ci-diffs-it" aria-label="Permalink to &quot;9. The document is generated, and CI diffs it&quot;">​</a></h3><p>Every vertical serves <code>/openapi.json</code> and a rendered reference at <code>/api/docs</code>, both built from the operation catalog — the same Zod schemas the handlers parse. The emitted document is checked in, and CI re-emits it to fail on drift.</p><p>The reason to care: a hand-written spec is a description of what someone believed the API did on the day they wrote it. A generated one cannot be wrong without the code being wrong.</p><h2 id="what-well-architected-means-here" tabindex="-1">What &quot;well-architected&quot; means here <a class="header-anchor" href="#what-well-architected-means-here" aria-label="Permalink to &quot;What &quot;well-architected&quot; means here&quot;">​</a></h2><p>Underneath the specifics there is one idea, and it is the same one the <a href="/concepts/modules.html">three-layer rule</a> expresses: <strong>prefer mechanisms to conventions.</strong></p><p>A convention is a thing a careful person remembers. A mechanism is a thing a careless person cannot get wrong. Substrat&#39;s bet is that most of what people call architecture discipline is actually the absence of mechanism — so the platform spends its complexity budget on compile-time joins, boundary lints, checked-in artifacts that CI re-emits, and contract tests every adapter must pass, rather than on documents describing how to behave.</p><p>Two things follow that are worth stating, because they cut against instinct:</p><ul><li><strong>A good default is one you cannot silently opt out of.</strong> Pagination that is available is pagination that half the endpoints skip.</li><li><strong>Two human checkpoints stay human on purpose</strong> — a migration diff and a permission diff. CI going red is what makes the reading unskippable; it is not itself the approval. Some judgments should not be automated away, and knowing which ones is part of the design.</li></ul><h2 id="the-defaults-at-a-glance" tabindex="-1">The defaults at a glance <a class="header-anchor" href="#the-defaults-at-a-glance" aria-label="Permalink to &quot;The defaults at a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Default</th><th>Shape</th><th>Status</th></tr></thead><tbody><tr><td>Permission declared</td><td><code>permission</code> or <code>narrows</code> + reason</td><td>Compile error to omit</td></tr><tr><td>Permission checked first</td><td><code>assertAllowed(await ctx.check(…))</code></td><td>Convention + review</td></tr><tr><td>Fat events on mutation</td><td>payload complete for consumers</td><td>Convention + review</td></tr><tr><td>Boundary parsing</td><td>Zod at the edge</td><td>Shipped</td></tr><tr><td>Money</td><td>decimal string + currency</td><td>Shipped</td></tr><tr><td>Identifiers</td><td>ULID</td><td>Shipped</td></tr><tr><td>Pagination</td><td>keyset cursor, declared with <code>paged</code>; entries in the body, the walk in <code>Link</code> / <code>X-Total-Count</code></td><td>Declaration shipped; <a href="https://github.com/substrat-run/substrat/issues/811" target="_blank" rel="noreferrer">#811</a> to adopt everywhere</td></tr><tr><td>Lost-update safety</td><td><code>concurrency</code> + <code>If-Match</code> / <code>ETag</code>, 412 on a stale tag</td><td>Shipped; compile error to omit on a field-bag update</td></tr><tr><td>Errors</td><td>RFC 9457 problem+json, closed codes</td><td>Shipped; <code>about:blank</code> where a status is all we have</td></tr><tr><td>Clock</td><td><code>ctx.now()</code></td><td>Shipped; <code>new Date()</code> in module code is a lint error</td></tr><tr><td>Idempotent writes</td><td><code>Idempotency-Key</code>, response replayed for 24h</td><td>Shipped; honoured on every write, <code>idempotency: false</code> to opt out</td></tr><tr><td>Evolution</td><td>additive only</td><td>Convention + review</td></tr><tr><td>API document</td><td>generated, CI-diffed</td><td>Shipped</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/api-design.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const apiDesign = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  apiDesign as default
};
