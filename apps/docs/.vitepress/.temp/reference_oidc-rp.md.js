import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/oidc-rp","description":"","frontmatter":{},"headers":[],"relativePath":"reference/oidc-rp.md","filePath":"reference/oidc-rp.md","lastUpdated":1784804439000}');
const _sfc_main = { name: "reference/oidc-rp.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-oidc-rp" tabindex="-1">@substrat-run/oidc-rp <a class="header-anchor" href="#substrat-run-oidc-rp" aria-label="Permalink to &quot;@substrat-run/oidc-rp&quot;">​</a></h1><p>The Substrat platform&#39;s OpenID Connect <strong>relying party</strong> — written once so the security-critical verifier is not copied per app. The <a href="/platform/dashboard.html">Dashboard</a> and the <a href="/platform/console.html">control-plane Console</a> both authenticate through it; the <code>substrat login</code> CLI brokers the same flow. It proves <em>who</em> a caller is (the ID token <code>sub</code>, and <code>email</code>) and nothing more — <a href="/concepts/permissions.html">authorization stays in the kernel</a>: roles, grants, and tenancy are never this package&#39;s concern.</p><p>It is the concrete form of the <a href="/concepts/identity.html#two-real-choices-made-differently">identity seam</a> for the platform&#39;s own surfaces. Demos keep their own Better Auth stores; the platform apps share this one relying party against AuthHero.</p><h2 id="what-it-is" tabindex="-1">What it is <a class="header-anchor" href="#what-it-is" aria-label="Permalink to &quot;What it is&quot;">​</a></h2><ul><li><strong>Standard Authorization-Code + PKCE</strong>, against the platform&#39;s AuthHero instance (an Auth0-compatible OIDC authority).</li><li><strong>Discovery-driven.</strong> The only wired-in value is the issuer URL; endpoints and signing keys come from <code>{issuer}/.well-known/openid-configuration</code>. The ID token is signature-verified against the issuer JWKS.</li><li><strong>Confidential client</strong> — the code-for-token exchange happens server-side with the client secret, never in the browser.</li><li><strong>Stateless.</strong> No KV, no D1. The short-lived PKCE/state/nonce rides a signed <em>flow</em> cookie (<code>sb_oidc_flow</code>, 10-minute lifetime); the session is a signed JWT cookie (<code>sb_session</code>, 7-day lifetime). Both are HMAC-signed with <code>SESSION_SECRET</code>.</li><li><strong>workerd-safe</strong> — <code>jose</code> + Web Crypto only, no <code>node:*</code>. It runs in the same isolate as the app that mounts it.</li></ul><h2 id="config-is-entirely-runtime" tabindex="-1">Config is entirely runtime <a class="header-anchor" href="#config-is-entirely-runtime" aria-label="Permalink to &quot;Config is entirely runtime&quot;">​</a></h2><p>Nothing is checked in; every value is a secret:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>OIDC_ISSUER          # e.g. https://auth.substrat.run — the only wired-in value</span></span>
<span class="line"><span>OIDC_CLIENT_ID</span></span>
<span class="line"><span>OIDC_CLIENT_SECRET   # wrangler secret put …</span></span>
<span class="line"><span>SESSION_SECRET       # signs the flow + session cookies</span></span>
<span class="line"><span>BASE_URL             # optional — force the redirect origin, else derive from the request</span></span></code></pre></div><h2 id="surface" tabindex="-1">Surface <a class="header-anchor" href="#surface" aria-label="Permalink to &quot;Surface&quot;">​</a></h2><p>The high-level entry point is <code>mountOidcRoutes(app, opts?)</code>, which wires three routes onto a Hono app — identically for both platform apps, because the only per-app difference is what happens <em>after</em> a session exists:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { mountOidcRoutes, sessionFromHeaders } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/oidc-rp&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mountOidcRoutes</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(app, { onSuccess: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;/&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, onError: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;/?error=auth&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> });</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// mounts:  GET /api/auth/login  ·  GET /api/auth/callback  ·  GET /api/auth/logout</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// elsewhere, resolve the current user from a request:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> user</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> await</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> sessionFromHeaders</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(c.env, c.req.raw.headers); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// SessionUser | null</span></span></code></pre></div><p>The per-app step that stays in the app is exactly the interesting one: the Dashboard does a <strong>JIT tenant bootstrap</strong> on first login (a new user provisions their own tenant), while the Console does a <strong>staff-roster lookup</strong> (only a known staff actor gets in). Same session, two admission policies.</p><p>Lower-level pieces are exported for callers that don&#39;t want the mounted routes — <code>beginLogin</code> / <code>completeLogin</code> (the two halves of the round-trip), <code>mintSession</code> / <code>verifySession</code>, <code>sessionFromHeaders</code>, and <code>safePath</code> (the open-redirect guard that permits only a same-origin absolute path for a <code>returnTo</code>).</p><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Real and in production — it is what signs you in to the platform apps today. <code>0.x</code>: the surface is stable enough that two apps share it unchanged, but versioned as pre-1.0 until the platform&#39;s own auth surface settles.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/oidc-rp.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const oidcRp = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  oidcRp as default
};
