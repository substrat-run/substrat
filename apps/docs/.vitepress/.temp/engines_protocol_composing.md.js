import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Composing & extending","description":"Registering the protocol engine and supplying the templates it deliberately ships none of, wrapping instantiation in your own operations, and the completion guard in both its forms.","frontmatter":{"description":"Registering the protocol engine and supplying the templates it deliberately ships none of, wrapping instantiation in your own operations, and the completion guard in both its forms."},"headers":[],"relativePath":"engines/protocol/composing.md","filePath":"engines/protocol/composing.md","lastUpdated":1787063543000}');
const _sfc_main = { name: "engines/protocol/composing.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="composing-extending" tabindex="-1">Composing &amp; extending <a class="header-anchor" href="#composing-extending" aria-label="Permalink to &quot;Composing &amp; extending&quot;">​</a></h1><h2 id="using-it-as-is" tabindex="-1">Using it as-is <a class="header-anchor" href="#using-it-as-is" aria-label="Permalink to &quot;Using it as-is&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">registerModule</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(protocolModule);</span></span></code></pre></div><p>Then supply template content — the engine ships none.</p><h2 id="wrapping-it-with-vertical-logic" tabindex="-1">Wrapping it with vertical logic <a class="header-anchor" href="#wrapping-it-with-vertical-logic" aria-label="Permalink to &quot;Wrapping it with vertical logic&quot;">​</a></h2><p>The registered operations are default bindings over exported in-scope functions. Your own operations call them in the same transaction, and you own the permission check:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { instantiateProtocol, PROTOCOL_PERM } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/engine-protocol&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">defineOperation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;cykel/start-condition-report&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">input</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  assertAllowed</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">check</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">PROTOCOL_PERM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.create));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  return</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> instantiateProtocol</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    templateKey: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;condition-report&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    entity: input.bike, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// any EntityRef — the vertical owns the vocabulary</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  });</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p>The vertical declares the <code>protocol → &lt;parent&gt;</code> entity relation in <em>its</em> manifest — the engine can&#39;t know whether protocols hang off work orders, bikes, or apartments — and supplies the template content.</p><h2 id="the-completion-guard-—-two-forms" tabindex="-1">The completion guard — two forms <a class="header-anchor" href="#the-completion-guard-—-two-forms" aria-label="Permalink to &quot;The completion guard — two forms&quot;">​</a></h2><p>&quot;Obligatorisk för status&quot; (a work order can&#39;t complete until its protocol is signed) is the canonical cross-engine requirement, and there are two ways to wire it.</p><h3 id="composed-in-code" tabindex="-1">Composed, in code <a class="header-anchor" href="#composed-in-code" aria-label="Permalink to &quot;Composed, in code&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { requireSigned } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/engine-protocol&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">defineOperation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;acme/workorder-complete&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">input</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  assertAllowed</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">check</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">WORKORDER_PERM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.complete));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  requireSigned</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, input.order, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;self-inspection-electrical&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// throws if not signed</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  return</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> completeWorkOrder</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, input);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p>The work-order engine knows nothing of protocols, the protocol engine checks its own tables, and the <em>vertical</em> wires the two — star topology intact. The <code>requireSigned</code> call site <strong>is</strong> the compliance gate.</p><h3 id="declared-in-the-manifest" tabindex="-1">Declared, in the manifest <a class="header-anchor" href="#declared-in-the-manifest" aria-label="Permalink to &quot;Declared, in the manifest&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">guards</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  before: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;bike-shop/close-repair&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  predicate: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;protocol/all-signed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  config: { templateKey: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;condition-report&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, entityType: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;bike&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, entityIdFrom: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;bikeId&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}]</span></span></code></pre></div><p>Same rule, but it appears in the <strong>reviewable manifest diff</strong> — dropping a compliance gate becomes visible rather than a deleted line in a handler.</p><p>Prefer the declarative form when the rule really is &quot;this operation is blocked until X&quot;. Prefer the composed form when the vertical needs to name a moment it actually owns — a bike shop&#39;s <em>pickup</em>, where the customer accepts the report, is more than the engine&#39;s transition, and a guard on <code>workorder/close</code> would describe it wrongly.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p><strong>There is none at registration time.</strong> <code>ModuleRegistration</code> has five fields — <code>manifest</code>, <code>migrations</code>, <code>operations</code>, <code>consumers</code>, <code>predicates</code> — and none is config. There is no <code>createProtocolModule({...})</code> factory and no <code>config</code> field on the manifest.</p><p><em>Configuration is dynamic; composition is code.</em></p><p>This engine shows the two sanctioned alternatives better than any other:</p><table tabindex="0"><thead><tr><th>You want</th><th>Use</th></tr></thead><tbody><tr><td>a parameterised rule</td><td><strong><code>guards[].config</code></strong> — declared in the vertical&#39;s manifest, kernel-opaque, parsed by <code>allSignedGuardConfig</code>. Its <code>entityIdFrom</code> late-binds into your vocabulary.</td></tr><tr><td>tenant-specific content</td><td><strong>runtime data</strong> — <code>defineTemplate</code> puts templates in the tenant&#39;s own scope. Which protocols exist is data, not config.</td></tr><tr><td>only part of the engine</td><td><code>withdraws</code> in your manifest, then re-offer behind your own operation</td></tr><tr><td>different behaviour around a transition</td><td>your own operation calling the in-scope function</td></tr><tr><td>the engine off for a tenant</td><td>revoke the <code>protocol</code> entitlement</td></tr></tbody></table><p>The template mechanism is the lesson worth generalising: content that varies per tenant is <strong>data in the scope</strong>, not options in a constructor. An engine with a <code>templates:</code> config array would need a redeploy to add a checklist; <code>defineTemplate</code> needs an operation call.</p><h3 id="adding-data-to-a-protocol" tabindex="-1">Adding data to a protocol <a class="header-anchor" href="#adding-data-to-a-protocol" aria-label="Permalink to &quot;Adding data to a protocol&quot;">​</a></h3><p>Never add a column upstream. A vertical needing extra fields adds <strong>its own side table keyed by the engine&#39;s instance id</strong>. Another module&#39;s tables are private; the stable surface is entity ids, <code>EntityRef</code>s, and event payloads.</p><h2 id="reaching-the-outside-world" tabindex="-1">Reaching the outside world <a class="header-anchor" href="#reaching-the-outside-world" aria-label="Permalink to &quot;Reaching the outside world&quot;">​</a></h2><p>Nothing in this engine talks to anything external, and it never will: module code may not <code>fetch</code>, and connectors handle the outside world.</p><p>The external signing flow is split across that boundary deliberately:</p><ol><li>Your vertical calls <code>requestSignatures(ctx, { instanceId, method: &#39;scrive&#39;, parties })</code>. That freezes the content, computes the hash <strong>once</strong>, writes a request row per party, and emits a fat <code>protocol.signatures-requested</code> carrying everything a connector needs to dispatch — the hash, the parties, the content ref. All inside your transaction.</li><li>An <strong>executor</strong> outside the scope picks that event off the outbox and makes the HTTP call. It holds platform authority, which module code must never have.</li><li>Days later the provider reports a signature, and something invokes <code>recordSignature(ctx, { requestId, signatory, signedAt, contentHash, evidenceRef })</code>.</li></ol><div class="warning custom-block"><p class="custom-block-title">Step 3 has no caller yet</p><p>Steps 1 and 2 are buildable today — the outbox and <code>ExecutorHandler</code> are real. Step 3 is not, and the gap is in the <strong>kernel</strong>, not this engine:</p><ul><li>there is <strong>no webhook ingress</strong> anywhere. <code>apps/router</code> resolves hostname → target and has no such surface; <code>kernel-design.md</code> reserves the idea and nothing implements it. No signature verification, no replay protection, no connection store.</li><li>there is <strong>no inbound authority seam</strong>. <code>ExecutorHandler</code> has no return path into a scope, and the only stub minter — <code>ScopeHost.getScope(principal, …)</code> — demands a <code>PrincipalId</code>. A provider callback is not a principal.</li></ul><p>So <code>recordSignature</code> is reachable only by a principal holding <code>protocol:record-signature</code>, a key deliberately held by <strong>no human role</strong> in any demo. It is shaped to be callable by that ingress when it lands; it is not a claim that BankID works today.</p><p>Tracked as <a href="https://github.com/substrat-run/substrat/issues/96" target="_blank" rel="noreferrer">#96</a> (webhook ingress) and <a href="https://github.com/substrat-run/substrat/issues/97" target="_blank" rel="noreferrer">#97</a> (inbound authority seam).</p></div><p>A worked example of steps 1 and the shape of 3: <a href="/verticals/meridian.html#a-document-the-anstallningsavtal">Meridian&#39;s anställningsavtal</a>.</p><h3 id="the-shape-to-copy" tabindex="-1">The shape to copy <a class="header-anchor" href="#the-shape-to-copy" aria-label="Permalink to &quot;The shape to copy&quot;">​</a></h3><p>The engine defines what evidence <em>is</em> and what must be true of it — the hash matches the frozen content, the signatory matches the request, the timestamp is the provider&#39;s. A connector supplies the evidence. What the engine does <strong>not</strong> do is pretend an asynchronous, non-principal signature fits a synchronous, principal-shaped operation: that assumption is what left the freeze window open, and the correction was a new noun (the signature request), not a better comment.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/protocol/composing.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const composing = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  composing as default
};
