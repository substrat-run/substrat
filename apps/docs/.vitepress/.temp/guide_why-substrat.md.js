import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Why runtime enforcement?","description":"Templates, code generators and BaaS platforms all deliver their guarantees as conventions, and conventions erode on the next edit. Why Substrat enforces them in the runtime instead.","frontmatter":{"description":"Templates, code generators and BaaS platforms all deliver their guarantees as conventions, and conventions erode on the next edit. Why Substrat enforces them in the runtime instead."},"headers":[],"relativePath":"guide/why-substrat.md","filePath":"guide/why-substrat.md","lastUpdated":1787063543000}');
const _sfc_main = { name: "guide/why-substrat.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="why-runtime-enforcement" tabindex="-1">Why runtime enforcement? <a class="header-anchor" href="#why-runtime-enforcement" aria-label="Permalink to &quot;Why runtime enforcement?&quot;">​</a></h1><p>Every adjacent way of getting a &quot;production-grade foundation&quot; delivers its guarantees as <strong>conventions</strong>:</p><ul><li><strong>Templates and boilerplates</strong> give you correct code once — and every edit after that, human or AI, erodes it. Nothing stops the fifth iteration of a form handler from querying across tenants.</li><li><strong>Code generators</strong> produce the foundation and leave. &quot;Zero lock-in, no runtime dependencies&quot; also means zero enforcement the day after generation.</li><li><strong>BaaS platforms</strong> enforce at runtime, but make the guarantee contingent on rules the builder must write correctly — row-level-security policies are precisely the thing inexperienced builders and LLMs misconfigure most.</li></ul><p>The industry pattern is well documented: analyses of AI-generated apps keep finding the same failure list — missing row-level security, broken auth boundaries, no tenant isolation, no audit trail. Prompting the model to &quot;be secure&quot; measurably doesn&#39;t fix it.</p><h2 id="the-structural-insight" tabindex="-1">The structural insight <a class="header-anchor" href="#the-structural-insight" aria-label="Permalink to &quot;The structural insight&quot;">​</a></h2><p><strong>The layer where LLMs are weakest — tenancy, auth, migrations, integrations, compliance — is the layer where mistakes are catastrophic. The layer where LLMs are strongest — screens, forms, workflows, reports — is the layer where mistakes are cosmetic.</strong></p><p>Substrat puts humans and hard guarantees under the line, and AI velocity above it.</p><h2 id="defaults-not-configuration" tabindex="-1">Defaults, not configuration <a class="header-anchor" href="#defaults-not-configuration" aria-label="Permalink to &quot;Defaults, not configuration&quot;">​</a></h2><p>The subtler failure mode than &quot;no enforcement&quot; is <strong>enforcement you can misconfigure</strong>: platforms where isolation is real but contingent on builder-declared policy — a public ACL here, a system-mode default there. Substrat guarantees are defaults of the substrate, not configuration surfaces:</p><ul><li>There is no API that returns another scope&#39;s data with the wrong flag set — the API for reaching a scope <em>is</em> the isolation mechanism.</li><li>There is no &quot;remember to log this&quot; — the event envelope is stamped kernel-side on every <code>emit</code>, and the audit-relevant fields (tenant, scope, actor, time) are not parameters.</li><li>The permission checker&#39;s secure default is <strong>deny everything</strong>. The permissive test checker is exported as <code>UNSAFE_allowAllChecker</code> — the name is the warning.</li></ul><h2 id="why-this-survives-better-models" tabindex="-1">Why this survives better models <a class="header-anchor" href="#why-this-survives-better-models" aria-label="Permalink to &quot;Why this survives better models&quot;">​</a></h2><p>Even a future AI that writes flawless tenancy code doesn&#39;t solve the <strong>trust</strong> problem: someone has to underwrite that isolation, audit, and GDPR hold structurally — to a customer, an auditor, or a procurement checklist. A property of the substrate can be tested, demonstrated, and certified once, below all the code that changes daily.</p><p>That&#39;s also why the enforcement is verified mechanically: every adapter that hosts scopes must pass <a href="/reference/contract-tests.html">the same conformance suite</a> — isolation, serialization, fail-closed addressing, stamped envelopes — unchanged, forever.</p><h2 id="the-two-human-checkpoints" tabindex="-1">The two human checkpoints <a class="header-anchor" href="#the-two-human-checkpoints" aria-label="Permalink to &quot;The two human checkpoints&quot;">​</a></h2><p>Substrat is built for verticals that iterate at AI speed, but two things stay under human review even in a fully agent-driven shop:</p><ol><li><strong>Schema migrations</strong> — the blast radius of a bad migration is data, not pixels.</li><li><strong>Permission definitions</strong> — who can do what, where in the tree, reviewed as a human-readable diff.</li></ol><p>Everything else iterates freely with contained blast radius. The module manifest and the permission model are designed to make exactly these two reviews small and legible: permissions are declared with descriptions in the manifest, and every permission decision carries a proof path that explains it.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/why-substrat.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const whySubstrat = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  whySubstrat as default
};
