import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Absence engine","description":"","frontmatter":{},"headers":[],"relativePath":"engines/absence/index.md","filePath":"engines/absence/index.md","lastUpdated":1786727111000}');
const _sfc_main = { name: "engines/absence/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="absence-engine" tabindex="-1">Absence engine <a class="header-anchor" href="#absence-engine" aria-label="Permalink to &quot;Absence engine&quot;">​</a></h1><p><code>@substrat-run/engine-absence</code> — approved absence as an <strong>append-only entry ledger over an opaque subject</strong>. Balances are folds, approval is the only path onto the ledger, and the engine deliberately knows nothing about who its subjects are, how leave accrues, or which days count — all of which are the vertical&#39;s.</p><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>@substrat-run/engine-absence</code></td></tr><tr><td><strong>Entitlement key</strong></td><td><code>absence</code></td></tr><tr><td><strong>Owns</strong></td><td>the append-only ledger, balance-as-a-fold, the per-type balance floor, the request approval state machine, stale-request expiry</td></tr><tr><td><strong>Emits</strong></td><td>6 events, <code>absence.requested</code> → <code>absence.decided</code> (<a href="./events.html">events</a>)</td></tr><tr><td><strong>Consumes</strong></td><td>nothing — it is a source, not a sink</td></tr><tr><td><strong>Permissions</strong></td><td>4 (<code>absence:read</code> · <code>request</code> · <code>approve</code> · <code>configure</code>)</td></tr><tr><td><strong>Status</strong></td><td>product seed (0.x) — surfaces change until the first vertical ships</td></tr></tbody></table><h2 id="what-it-owns" tabindex="-1">What it owns <a class="header-anchor" href="#what-it-owns" aria-label="Permalink to &quot;What it owns&quot;">​</a></h2><ul><li><strong>The ledger is append-only, always.</strong> An accrual, a booking, a correction, a carryover, or a reversal is a <strong>new entry</strong> — never an edit, never a delete. A mistake is corrected by a compensating entry, so the record of what was believed when survives.</li><li><strong>Balance is a pure fold.</strong> <code>balanceAsOf(subject, leaveType, asOf?)</code> sums signed deltas over entries effective by that date. There is no stored counter to drift, no cache to invalidate, nothing to reconcile.</li><li><strong>Only an approved request books.</strong> The state machine — <code>requested → approved | rejected</code>, with <code>requested | approved → cancelled</code> — cannot skip, and the <code>booking</code> and <code>reversal</code> entry kinds are mintable <strong>only</strong> through <code>decideAbsence</code> and <code>cancelAbsence</code>. &quot;The ledger moves only through approval&quot; is a construction, not a review comment.</li><li><strong>The floor is the engine&#39;s, per leave type.</strong> A booking that would fold the balance below the type&#39;s floor is rejected at decision time — not by the UI. Floor <code>0</code> refuses overdraft; a <strong>negative floor admits advance leave</strong> (<em>förskottssemester</em>) with no code change.</li><li><strong>The subject is opaque.</strong> Every write names a <code>subject</code>: an <code>EntityRef</code> the vertical provides (an employee, a plannable crew resource) plus the <code>DataSubjectId</code> that keys crypto-shredding. The engine never dereferences the ref and <strong>never owns a directory</strong>.</li><li><strong>Stale requests expire under a system actor.</strong> The manifest declares an <code>absence/expire-stale</code> schedule: a leave still <code>requested</code> after its start date is cancelled by the platform sweep, attributed to <code>{ system: &#39;@substrat-run/engine-absence&#39; }</code> — never to a manager who never looked at it.</li></ul><h3 id="the-property-worth-understanding" tabindex="-1">The property worth understanding <a class="header-anchor" href="#the-property-worth-understanding" aria-label="Permalink to &quot;The property worth understanding&quot;">​</a></h3><p><strong>Dates here are calendar days, inclusive on both ends</strong> — deliberately unlike the <a href="/engines/booking/">booking engine&#39;s</a> half-open instants. Booking allocates physical time; absence covers human days. The <code>days</code> decimal on a request is <strong>vertical-computed</strong> (a Monday–Friday request may be <code>5</code>, or <code>4</code> after excluding a red day): the engine folds it, it never derives it. Correspondingly, <code>availability()</code> answers <em>&quot;an approved absence covers this date&quot;</em> — never <em>&quot;this many hours free&quot;</em>. Composing that verdict with a holiday calendar is the vertical&#39;s last mile.</p><h2 id="what-it-will-not-do" tabindex="-1">What it will not do <a class="header-anchor" href="#what-it-will-not-do" aria-label="Permalink to &quot;What it will not do&quot;">​</a></h2><ul><li><strong>No directory.</strong> No employee table, no names, no employment terms, no notion of who reports to whom. Existence checks on the subject happen in <em>your</em> operation, before the engine call.</li><li><strong>No leave-type semantics.</strong> The engine stores a key, a floor, and an active flag. Labels, statutory day counts, what <em>VAB</em> or <em>baja</em> mean, per-country divergence — all vertical vocabulary (<a href="./composing.html">composing</a>).</li><li><strong>No accrual formulas, no carryover caps.</strong> Accruals and carryovers are entries you record; the rules that generate them are yours.</li><li><strong>No calendars.</strong> Weekends, red days, and public holidays never enter the engine.</li><li><strong>No scheduling.</strong> &quot;Is this person double-booked on a shift&quot; is a different seam (interval capacity — the booking engine, or a future scheduling engine). This engine answers availability <em>per an approved ledger</em>, nothing more.</li></ul><h2 id="where-it-came-from" tabindex="-1">Where it came from <a class="header-anchor" href="#where-it-came-from" aria-label="Permalink to &quot;Where it came from&quot;">​</a></h2><p>The ledger shape was written <strong>vertical-first</strong> inside the Meridian HR demo, to that spec&#39;s own extraction plan: freeze the invariants as plain module code, and extract the engine only when a <em>second consumer with a different shape</em> arrives. It did — field-crew resource planning, where the subject is a plannable <code>resource</code> rather than an employee — and the extraction moved the invariants here while Meridian kept its vocabulary and screens. The subject-opacity line above is what made that composition possible, and it is load-bearing: it is why the same engine serves an HR product and a route planner without either forking it.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/absence/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
