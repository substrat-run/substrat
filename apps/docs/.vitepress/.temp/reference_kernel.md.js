import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/kernel","description":"","frontmatter":{},"headers":[],"relativePath":"reference/kernel.md","filePath":"reference/kernel.md","lastUpdated":1787594628000}');
const _sfc_main = { name: "reference/kernel.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-kernel" tabindex="-1">@substrat-run/kernel <a class="header-anchor" href="#substrat-run-kernel" aria-label="Permalink to &quot;@substrat-run/kernel&quot;">​</a></h1><p>The kernel&#39;s <strong>behavioral seams</strong> in pure TypeScript. This package imports no platform APIs — Cloudflare specifics live only in adapters, and every adapter must pass the same <a href="/reference/contract-tests.html">conformance suite</a>.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/kernel</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/contracts</span></span></code></pre></div><h2 id="scope-host-scope-host-ts" tabindex="-1">Scope host (<code>scope-host.ts</code>) <a class="header-anchor" href="#scope-host-scope-host-ts" aria-label="Permalink to &quot;Scope host (\`scope-host.ts\`)&quot;">​</a></h2><p>The adapter seam. Full semantics in <a href="/concepts/scope-host.html">Operations &amp; the scope host</a>.</p><table tabindex="0"><thead><tr><th>Export</th><th>Kind</th><th>Purpose</th></tr></thead><tbody><tr><td><code>ScopeHost</code></td><td>interface</td><td><code>getScope</code>, <code>getConnectorScope</code>, <code>getSystemScope</code> (the scheduler&#39;s door — a stub whose authority is a module on a timer, #383), <code>provisionScope</code>, <code>registerModule</code>, <code>defineOperation</code>, <code>registeredSchedules</code> / <code>runDueSchedules</code> (the recurring-work seam the platform sweep drives), <code>admin</code>, <code>close</code></td></tr><tr><td><code>ScopeStub</code></td><td>interface</td><td>the capability — the only way code outside a scope reaches it</td></tr><tr><td><code>OperationContext</code></td><td>interface</td><td>what a handler sees: ambient <code>tenantId</code>/<code>scopeId</code>/<code>principal</code>, <code>sql</code>, <code>emit</code>, <code>check</code>, <code>search</code> (ids from the declared FTS index), <code>entitlement</code>/<code>entitlements</code> (read a held SKU/quota at request time — the gate a CP-less vertical uses), <code>link</code>, <code>grant</code>/<code>revoke</code> (delegation, never elevation), <code>requestPlatform</code>/<code>platformRequests</code>, <code>versionOf</code> (an entity&#39;s version — the ULID of the last event about it), <code>sealToConnection</code></td></tr><tr><td><code>OperationHandler&lt;I, O&gt;</code></td><td>type</td><td><code>(ctx, input) =&gt; O | Promise&lt;O&gt;</code></td></tr><tr><td><code>ConsumerHandler</code></td><td>type</td><td>event consumer; at-least-once, must be idempotent</td></tr><tr><td><code>ModuleRegistration</code></td><td>interface</td><td>manifest + migrations + operations + consumers</td></tr><tr><td><code>SqlMigration</code></td><td>interface</td><td><code>{ version, sql }</code> — ordered, journaled per module</td></tr><tr><td><code>ScopedSql</code>, <code>SqlValue</code></td><td>types</td><td>synchronous scope-local SQL: <code>query&lt;T&gt;()</code>, <code>exec()</code></td></tr><tr><td><code>ExecutorHandler</code></td><td>type</td><td><code>(admin, event) =&gt; void</code> — out-of-band host code effecting what a module asked for via an event (K-22). Registered with <code>registerExecutor</code>; receives <code>HostAdmin</code>, never <code>ctx</code>, because it acts with platform authority</td></tr><tr><td><code>HostAdmin</code></td><td>interface</td><td>the audited control-plane surface. Every mutation takes a <code>PlatformActorId</code> and writes an append-only audit row; directory reads take one too and record to the K-24 access log. The capability groups it now spans: <strong>permissions &amp; membership</strong> (<code>defineRole</code>, <code>listRoles</code>, <code>assignRole</code>, <code>unassignRole</code>, <code>grant</code>, <code>grantToOrg</code>, <code>grantToConnection</code>, <code>grantToSystem</code> — the schedule&#39;s system-principal grant (#383), <code>addMember</code>, <code>removeMember</code>, <code>listMembers</code>); <strong>organizations</strong> (<code>createOrg</code>, <code>listOrgs</code>, <code>getOrg</code>); <strong>the vertical + version registry</strong> (<code>registerVertical</code>, <code>publishVersion</code>, <code>admitVersion</code>/<code>rejectVersion</code>, <code>promoteVersion</code>, <code>bindScopeVersion</code>, <code>setVerticalListed</code>, <code>verticalServing</code> — push-is-not-live admission, channels, and the stable serving script); <strong>the hostname map</strong> (<code>bindHostname</code>, <code>setHostnameStatus</code>, <code>setHostnameIssuance</code>, <code>listHostnames</code>, <code>resolveHostname</code> — the router&#39;s unlogged per-request read); <strong>tenant registry</strong> (<code>createTenant</code>, <code>setTenantName</code>, <code>setTenantStatus</code>, <code>listTenants</code>, <code>getTenant</code>, <code>reapTenant</code>); <strong>scope directory + lifecycle</strong> (<code>listScopes</code>, <code>getScopeRecord</code>, <code>activateScope</code>, <code>suspendScope</code>/<code>unsuspendScope</code>, <code>archiveScope</code>/<code>unarchiveScope</code>, <code>reapScope</code>, <code>rewindScope</code>); <strong>scope introspection</strong> (<code>listScopeTables</code>, <code>readScopeTable</code>, <code>queryScope</code>, <code>exportScope</code> — the read-only &quot;Data&quot; window into a scope&#39;s own database); <strong>entitlements</strong> (<code>grantEntitlement</code>, <code>revokeEntitlement</code>, <code>listEntitlements</code>); <strong>the connections hub</strong> (<code>createConnection</code>, <code>openConnection</code>, <code>listConnections</code>, <code>updateConnectionSecret</code>, <code>revokeConnection</code>, connector-state get/put/list — the integrations credential store); <strong>identity</strong> (<code>registerIdentityPool</code>, <code>linkIdentity</code>, <code>unlinkIdentity</code>, <code>resolveIdentity</code>, <code>listIdentityTenants</code>); and the logs (<code>auditLog</code>, <code>accessLog</code>)</td></tr><tr><td><code>ProvisionScopeInput</code></td><td>interface</td><td>tenant, scope, optional shape + jurisdiction (provisioned via <code>provisionScope(actor, input)</code>)</td></tr></tbody></table><h2 id="permission-checker-permission-checker-ts" tabindex="-1">Permission checker (<code>permission-checker.ts</code>) <a class="header-anchor" href="#permission-checker-permission-checker-ts" aria-label="Permalink to &quot;Permission checker (\`permission-checker.ts\`)&quot;">​</a></h2><p>The evaluation seam — the model is kernel-owned, the engine is swappable. See <a href="/concepts/permissions.html">Permissions</a>.</p><table tabindex="0"><thead><tr><th>Export</th><th>Purpose</th></tr></thead><tbody><tr><td><code>PermissionChecker</code></td><td><code>check(principal, permission, node, entity?) → Promise&lt;Decision&gt;</code></td></tr><tr><td><code>assertAllowed(decision)</code></td><td>throws <code>PermissionDenied</code> unless allowed; the standard first line of an operation. Narrows the type to the proof-carrying allow.</td></tr><tr><td><code>PermissionDenied</code></td><td>the error class</td></tr><tr><td><code>denyAllChecker</code></td><td><strong>secure default</strong> — denies everything</td></tr><tr><td><code>UNSAFE_allowAllChecker</code></td><td>test-only; grants everything via a synthetic proof tuple. The name is the warning.</td></tr></tbody></table><h2 id="ulid" tabindex="-1"><code>ulid()</code> <a class="header-anchor" href="#ulid" aria-label="Permalink to &quot;\`ulid()\`&quot;">​</a></h2><p>A dependency-free ULID generator — the ID scheme used everywhere (<code>ids</code> in <a href="/reference/contracts.html"><code>@substrat-run/contracts</code></a>).</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { ulid } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/kernel&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> id</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ulid</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// &#39;01JZX6ZH2E...&#39;</span></span></code></pre></div><h2 id="the-other-seams" tabindex="-1">The other seams <a class="header-anchor" href="#the-other-seams" aria-label="Permalink to &quot;The other seams&quot;">​</a></h2><p>Everything below is in the kernel for the same reason: it is a rule two or more implementations have to agree on, and a second copy of it is a chance for them to disagree. All of it is web-standard only — <code>globalThis.crypto</code>, <code>TextEncoder</code>, <code>URL</code> — because a package that assumed Node would break the portability claim the adapters exist to prove.</p><h3 id="erasure-and-secrets" tabindex="-1">Erasure and secrets <a class="header-anchor" href="#erasure-and-secrets" aria-label="Permalink to &quot;Erasure and secrets&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Module</th><th>Export</th><th>What it is</th></tr></thead><tbody><tr><td><code>subject-keys.ts</code></td><td><code>createSubjectKeys</code></td><td><strong>The mechanism under <code>piiClass</code>.</strong> A per-subject data key seals that subject&#39;s payloads at the moment a <em>copy</em> is written, so &quot;erase from every copy we hold&quot; becomes &quot;destroy one key&quot;. A live scope needs no crypto — Tier 1 is mutable, erasing there is an ordinary redaction. The copies the platform keeps and cannot rewrite are the problem: a reap backup, a stored dump, the Tier-2 event lake. Those are full-fidelity on purpose, which is exactly why a <code>DELETE</code> can never reach one. A tombstone means a destroyed key is never re-minted</td></tr><tr><td><code>secret-box.ts</code></td><td><code>webCryptoSecretBox</code>, <code>unconfiguredSecretBox</code>, <code>isSecretBoxConfigured</code>, <code>SecretBoxUnconfiguredError</code></td><td>The symmetric seal/open adapter the connection store rests on. The kernel decides that per-tenant credentials are encrypted at rest and that plaintext never touches the directory; <em>what</em> encrypts is swappable — Web Crypto locally, a Secrets Store binding or an external KMS when hosted. <code>unconfiguredSecretBox</code> refuses to store a credential rather than storing it in the clear</td></tr><tr><td><code>sealed-box.ts</code></td><td><code>sealTo</code>, <code>openSealed</code>, <code>generateSealingKeyPair</code>, <code>ConnectionSealingKeyUnavailableError</code></td><td>The <strong>asymmetric</strong> sibling, answering a question a scope cannot otherwise ask: <em>hand this to a recipient I cannot talk to</em>. Every path out of a hosted scope is a spine row, so a symmetric key minted in-scope would have to travel the same rows as the value it protects. A connection&#39;s <strong>public</strong> half can be projected down, and a public key is enough to write with — so the scope seals, and the connector opens at egress with a private half that never left the directory</td></tr></tbody></table><h3 id="trusting-the-edges" tabindex="-1">Trusting the edges <a class="header-anchor" href="#trusting-the-edges" aria-label="Permalink to &quot;Trusting the edges&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Module</th><th>Export</th><th>What it is</th></tr></thead><tbody><tr><td><code>routed-node.ts</code></td><td><code>readRoutedNode</code>, <code>RouterAssertionError</code></td><td>The vertical&#39;s side of the router contract: read the <code>(tenant, scope, surface)</code> the router asserted over its service binding. A request with <strong>no</strong> assertion is legitimate — that is a standalone deploy</td></tr><tr><td><code>platform-call.ts</code></td><td><code>assertPlatformCall</code>, <code>PLATFORM_SECRET_HEADER</code>, …</td><td>The opposite direction: <em>is the platform itself calling?</em> Provisioning is control-plane-driven — only the vertical can create a usable scope DO — and here there is no legitimate unauthenticated case, because an open provisioning endpoint lets a stranger mint tenants inside your vertical. So this one <strong>fails closed with no configuration at all</strong></td></tr><tr><td><code>read-only-sql.ts</code></td><td><code>assertReadOnlyQuery</code></td><td>The textual gate in front of the <a href="/platform/console.html">scope SQL console</a>. <code>readScopeTable</code> is safe by construction; a console taking user SQL is not, so read-only-ness is enforced per statement in two layers — this shared scan (both adapters, same rejections, so a query that runs in dev runs in prod) and an adapter-authoritative backstop behind it. The scan reads bare tokens <em>outside</em> comments, string literals and quoted identifiers, so a <code>;</code> inside a string never trips it</td></tr></tbody></table><h3 id="fleet-arithmetic" tabindex="-1">Fleet arithmetic <a class="header-anchor" href="#fleet-arithmetic" aria-label="Permalink to &quot;Fleet arithmetic&quot;">​</a></h3><p>These are pure functions over the directory projection, deliberately shared so two callers can never disagree about what a number means.</p><table tabindex="0"><thead><tr><th>Module</th><th>Export</th><th>What it is</th></tr></thead><tbody><tr><td><code>platform-sweep.ts</code></td><td><code>runPlatformSweep</code>, <code>startPlatformSweeper</code></td><td>One pass of every scheduled thing the platform does — draining retryable effects, reconciling connectors, reaping expired snapshots, running each vertical&#39;s declared schedules. It holds <strong>no timer</strong>: a node deployment calls <code>startPlatformSweeper</code>, a Cloudflare one arms a sweeper DO&#39;s alarm</td></tr><tr><td><code>migration-progress.ts</code></td><td><code>migrationFleet</code>, <code>migrationProgress</code>, <code>migrationSummary</code>, <code>MIGRATION_FLAG_THRESHOLD</code></td><td>What &quot;487/500 migrated, 13 pending, 0 failed&quot; means, computed once for both the sweep&#39;s report and the ops view. It reads the directory projection against the registered frontier — <strong>no scope is woken to answer a fleet question</strong></td></tr><tr><td><code>meters.ts</code></td><td><code>foldMeterReading</code></td><td>What counts as billable. Every adapter has the same three directory tables and could each write the same <code>GROUP BY</code>; they must not, because the billable rule is a <em>commercial</em> definition and two copies in two dialects is how the two fleets end up quoting different numbers for the same month</td></tr><tr><td><code>provider-error.ts</code></td><td><code>isTerminalProviderError</code>, <code>providerErrorStatus</code>, <code>RETRYABLE_CLIENT_STATUSES</code></td><td>Is a failed outbound call worth trying again? Deliberately <strong>structural</strong> — any error carrying a numeric <code>status</code> — so the drain never imports a provider&#39;s error class to classify it. It exists because a provider answering <code>409 requires valid personal number field</code> was once retried a hundred times over two days: that is not a fault to wait out, it is the provider telling the caller its request is wrong, and attempt 101 carries identical bytes</td></tr><tr><td><code>scope-record.ts</code></td><td><code>resolveScopeRecord</code></td><td>The directory row <code>provisionScope</code> writes, with every optional resolved — in the kernel so the two adapters cannot default differently</td></tr></tbody></table><h2 id="guarantees-adapters-must-uphold" tabindex="-1">Guarantees adapters must uphold <a class="header-anchor" href="#guarantees-adapters-must-uphold" aria-label="Permalink to &quot;Guarantees adapters must uphold&quot;">​</a></h2><p>Any <code>ScopeHost</code> implementation must provide — verified by <a href="/reference/contract-tests.html"><code>@substrat-run/contract-tests</code></a>:</p><ul><li><strong>Strict serialization per scope</strong> — one operation at a time, to completion.</li><li><strong>Structured-clone boundary</strong> — inputs/results cloned both directions, even in-process.</li><li><strong>Kernel-stamped events</strong> — id, timestamp, tenant, scope, actor stamped below the API surface.</li><li><strong>Fail-closed addressing</strong> — mismatched <code>(tenantId, scopeId)</code> throws, never resolves elsewhere.</li><li><strong>PII invariant at emit</strong> — PII-classed events without <code>subjectId</code> are rejected.</li><li><strong>An entity&#39;s version is the last event&#39;s ULID</strong> — <code>versionOf</code> moves on every event about the entity, never moves for an event about another, and survives a shred: the payload is erased and the envelope kept, so an erased entity can still refuse a stale write. There is no version column anywhere, and deliberately not going to be one.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/kernel.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const kernel = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  kernel as default
};
