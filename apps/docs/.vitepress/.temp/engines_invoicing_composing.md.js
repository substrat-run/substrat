import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Composing & extending","description":"","frontmatter":{},"headers":[],"relativePath":"engines/invoicing/composing.md","filePath":"engines/invoicing/composing.md","lastUpdated":1784293491000}');
const _sfc_main = { name: "engines/invoicing/composing.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="composing-extending" tabindex="-1">Composing &amp; extending <a class="header-anchor" href="#composing-extending" aria-label="Permalink to &quot;Composing &amp; extending&quot;">​</a></h1><h2 id="using-it-as-is" tabindex="-1">Using it as-is <a class="header-anchor" href="#using-it-as-is" aria-label="Permalink to &quot;Using it as-is&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">registerModule</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(invoicingModule);</span></span></code></pre></div><p>Then <strong>emit the events it consumes</strong> — that&#39;s the whole integration. There is no wiring step between the work-order engine and this one, because there is no connection between them: one emits, the kernel journals, this one consumes.</p><p>To make a vertical&#39;s own domain billable, emit a <code>commerce.order-placed</code>-shaped event from your operation. You never import this engine to do it.</p><h2 id="extending-it" tabindex="-1">Extending it <a class="header-anchor" href="#extending-it" aria-label="Permalink to &quot;Extending it&quot;">​</a></h2><p>Today, barely — and this is the honest gap in this engine.</p><p>There are <strong>no in-scope functions</strong> (<a href="./surface.html#in-scope-functions">surface</a>), so the composition pattern the other engines offer isn&#39;t available here. You cannot wrap <code>export</code> in your own vocabulary, and you cannot export a basis and write to your own tables in one transaction, without re-implementing the operation.</p><p>What you <em>can</em> do:</p><table tabindex="0"><thead><tr><th>You want</th><th>Use</th></tr></thead><tbody><tr><td>billing from a new domain</td><td>emit an event; add a consumer upstream — the <code>commerce.order-placed</code> path is the worked example</td></tr><tr><td>extra fields on an underlag</td><td>your own side table keyed by the underlag id — <strong>never</strong> a column upstream</td></tr><tr><td>to hide export from most roles</td><td>keep <code>invoicing:export</code> off the role; it&#39;s already a separate key</td></tr><tr><td>the engine off for a tenant</td><td>revoke the <code>invoicing</code> entitlement — but read the caveat in <a href="./surface.html#entitlement">surface</a> first</td></tr></tbody></table><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p><strong>There is none.</strong> <code>ModuleRegistration</code> has five fields and none is config; there is no <code>createInvoicingModule({...})</code> factory and no <code>config</code> field on the manifest. An engine cannot be told anything at registration time.</p><p><em>Configuration is dynamic; composition is code.</em></p><p>The one behaviour you might reasonably want to configure — <em>&quot;only invoice-payment orders bill&quot;</em> — is <strong>hard-coded</strong> in the <code>commerce.order-placed</code> consumer:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (p.paymentMethod </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!==</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;invoice&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><p>A vertical that settles differently can&#39;t change that rule; it would emit a different event or carry a different <code>paymentMethod</code>. Whether that belongs in the engine at all is a fair question — it is a business decision living in shared machinery, which is exactly what the engine/vertical line is supposed to keep out.</p><h2 id="reaching-the-outside-world" tabindex="-1">Reaching the outside world <a class="header-anchor" href="#reaching-the-outside-world" aria-label="Permalink to &quot;Reaching the outside world&quot;">​</a></h2><p>Nothing in this engine talks to anything external, and it never will: module code may not <code>fetch</code>, and connectors handle the outside world.</p><p><strong><code>invoicing.underlag-exported</code> is the seam.</strong> Its consumer is by design an accounting connector (Fortnox/Visma-class) that turns the frozen basis into a real invoice. That the event exists, carries <code>Money</code>, and fires exactly once per export is the whole contract.</p><p>The immutability invariant exists <em>for</em> this seam: a connector must be able to trust that what it read can&#39;t change underneath it, and that exporting twice is impossible.</p><h3 id="so-how-would-stripe-attach" tabindex="-1">So how would Stripe attach? <a class="header-anchor" href="#so-how-would-stripe-attach" aria-label="Permalink to &quot;So how would Stripe attach?&quot;">​</a></h3><p>Not to this engine, and the answer depends on who&#39;s paying whom:</p><table tabindex="0"><thead><tr><th>Case</th><th>Bucket</th><th>Where it lives</th></tr></thead><tbody><tr><td>A tenant&#39;s customer pays a card/Swish charge</td><td><strong>connector</strong></td><td>the integrations hub — a capability tenants use</td></tr><tr><td>Substrat charges its own tenants</td><td><strong>adapter</strong></td><td>infrastructure the kernel consumes, swappable behind a pure interface</td></tr></tbody></table><p>Fortnox lands in <em>both</em> buckets depending on the same question: a connector when it&#39;s a tenant&#39;s bookkeeping, an adapter when it&#39;s the platform&#39;s own invoicing rail.</p><p>A connector is a <strong>fourth bucket</strong> — not kernel, not engine, not vertical. It lives in the integrations hub, and the hub itself is kernel-owned while the connectors in it are not. The test that decides it: <em>effects on the outside world are connectors.</em></p><div class="warning custom-block"><p class="custom-block-title">Connectors are not built yet</p><p>The connector interface, connection store, token refresh, webhook ingress, and per-tenant connection config are <strong>planned, not implemented</strong> — there is no connector code in the repo. The <code>_substrat_outbox</code> is real and transactional, but it drains only to in-process consumers; there is no external sink and <code>drained_at</code> is written nowhere.</p><p>Today, &quot;export to Fortnox&quot; in the demos is a <strong>file-write stub</strong>. This engine is pre-shaped for a seam that doesn&#39;t have a floor under it yet — which is deliberate sequencing, but don&#39;t read <code>underlag-exported</code> as something you can wire Stripe to this afternoon.</p></div><p>Note also what <em>doesn&#39;t</em> belong behind that seam: reskontra and avisering (ledger, dunning) stay out of the engine entirely. The basis goes out; the bookkeeping stays in the bookkeeping system.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/invoicing/composing.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const composing = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  composing as default
};
