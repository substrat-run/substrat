import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"How Substrat compares","description":"","frontmatter":{},"headers":[],"relativePath":"guide/comparisons.md","filePath":"guide/comparisons.md","lastUpdated":1787486158000}');
const _sfc_main = { name: "guide/comparisons.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="how-substrat-compares" tabindex="-1">How Substrat compares <a class="header-anchor" href="#how-substrat-compares" aria-label="Permalink to &quot;How Substrat compares&quot;">​</a></h1><p>Substrat sits in a category most people meet for the first time here, so the fastest way to understand it is against the tools you already know. This page places it next to each familiar neighbor and names the <em>one axis</em> where it diverges.</p><p>It is deliberately not a scoreboard. Every tool below is good at what it was built for, and several are excellent; the point is to show what Substrat is by showing what it trades away and what it keeps. If your problem is the shape one of these neighbors was built for, use that neighbor — the last section says so plainly.</p><h2 id="the-three-way-choice-everyone-else-forces" tabindex="-1">The three-way choice everyone else forces <a class="header-anchor" href="#the-three-way-choice-everyone-else-forces" aria-label="Permalink to &quot;The three-way choice everyone else forces&quot;">​</a></h2><p>Today a team building vertical B2B software has to pick two of three things — governance, code ownership, and an open, agent-friendly runtime:</p><table tabindex="0"><thead><tr><th>Approach</th><th>Examples</th><th>What you get</th><th>What you give up</th></tr></thead><tbody><tr><td><strong>Governance without code</strong></td><td>OutSystems, Mendix, Power Apps, ServiceNow App Engine</td><td>Real permissions, audit, compliance — as a hosted platform</td><td>A proprietary visual model; no code you own; agent-hostile</td></tr><tr><td><strong>Code without governance</strong></td><td>Supabase, boilerplates, prompt-to-app tools</td><td>Full code ownership, fast start, agent-friendly</td><td>Every catastrophic mistake — tenant leaks, missing audit — is yours</td></tr><tr><td><strong>Both, walled garden</strong></td><td>Salesforce, ServiceNow</td><td>Governance <em>and</em> depth, at the top of the market</td><td>Proprietary runtime, enterprise pricing, build-inside-our-world</td></tr></tbody></table><p>Substrat is the missing fourth corner: <strong>governance as a runtime substrate, under code you own, on an open runtime, shaped for multi-tenant vertical SaaS.</strong> The guarantees live <a href="/guide/why-substrat.html">below the API surface</a>, so it stops mattering who — or what — wrote the code above them, and you still own that code.</p><h2 id="the-table-for-scanning" tabindex="-1">The table, for scanning <a class="header-anchor" href="#the-table-for-scanning" aria-label="Permalink to &quot;The table, for scanning&quot;">​</a></h2><p>Read it as <em>what each product optimizes for</em>, not as a scoreboard. Several of these are better than Substrat at what they set out to do, and the third column says so.</p><table tabindex="0"><thead><tr><th>Product</th><th>What it really is</th><th>Where it beats Substrat</th><th>Where Substrat differs</th></tr></thead><tbody><tr><td><strong>Ruby on Rails</strong></td><td>The reference full-stack framework</td><td>Maturity, ecosystem, hiring pool, twenty years of answers. Rails + a good team beats Substrat on almost any <em>single</em>-tenant app</td><td>Rails gives you conventions, and conventions erode with every edit. No tenancy, permissions, audit or GDPR machinery below the app — that is yours to build and keep correct</td></tr><tr><td><strong>Wasp</strong></td><td>Rails-for-TypeScript: a declarative spec compiled to a React/Node/Prisma app</td><td>Excellent DX, MIT, no hosting lock-in, strong AI-coding compatibility, and a SaaS template that gets you selling</td><td>Convergent instinct — a typed spec the compiler checks; our <a href="/concepts/model.html"><code>model.ts</code></a> rhymes with theirs — and opposite depth. Wasp is a framework you deploy; Substrat is a runtime that <em>enforces</em>. We keep their perks (deterministic generation, pre-vetted auth, local run, migrations) rather than trading them away for the guarantees</td></tr><tr><td><strong>Supabase</strong></td><td>Backend-as-a-service with row-level security</td><td>Better DX, bigger ecosystem, far better for app-shaped products</td><td>App-shaped, not vertical-SaaS-shaped. RLS is precisely the surface builders and LLMs misconfigure most</td></tr><tr><td><strong>Convex</strong></td><td>Reactive TypeScript backend — database, functions, scheduling and sync in one runtime — with sandboxed <strong>Components</strong> as installable backend modules</td><td>The closest architectural peer here, and ahead of us on axes we care about: reactivity as a primitive, a component catalogue that ships today, transactions that span component calls, SOC 2 Type II + HIPAA, and a backend you can self-host. Their DX is better than ours and it isn&#39;t close</td><td>Convex closed the RLS hole the same way we did, so the difference isn&#39;t enforcement — it&#39;s <em>shape</em>. No tenancy tree, no scope-per-customer, no audit spine below the API, no erasure machinery. Their components are technical primitives (workflow, rate limiter, aggregate); a <a href="/engines/">domain engine</a> that owns a work order&#39;s state machine is a different animal</td></tr><tr><td><strong>MakerKit / ShipFast / Open SaaS</strong></td><td>Boilerplates, one-time purchase</td><td>Unbeatable economics <em>if</em> the guarantees can be conventions</td><td>Guarantees erode with every edit; no nested tenancy, no provisioning, no engines</td></tr><tr><td><strong>Lovable / Bolt / Replit</strong></td><td>Prompt-to-app</td><td>Enormous distribution, polish, iteration speed</td><td>They generate the dangerous parts too. Their remedy is <em>scanning</em> — checking policies exist, not that they hold. Here you bring your own model and your own agent, on code that runs locally</td></tr><tr><td><strong>Base44, Floot</strong></td><td>AI-native app builders with auth/roles as platform primitives</td><td>The nearest AI-native articulation of the same idea, with real products and real users</td><td>Single-app shaped — no tenancy tree, no engines, no B2B SaaS shape — and weak portability. Substrat code boots on SQLite with no platform in the loop</td></tr><tr><td><strong>Retool / Superblocks</strong></td><td>Internal tools with governance</td><td>Owns the internal-tool shape outright — genuinely better there</td><td>No nested tenancy for <em>selling</em> SaaS; runtime-locked</td></tr><tr><td><strong>OutSystems / Mendix / Power Apps</strong></td><td>Enterprise low-code</td><td>Genuinely solve permissions, audit and governance as a hosted platform — and prove the willingness to pay</td><td>A proprietary visual model: agent-hostile, internal-app-shaped, single-level tenancy, no usable eject</td></tr><tr><td><strong>Microsoft Dataverse</strong></td><td>Proprietary data platform with runtime-enforced security roles</td><td>The most mature RBAC in this table by a distance — row <em>and</em> column security, business-unit hierarchies, team ownership, audit built in</td><td>Business units are org structure <em>inside one tenant</em>, not tenancy for selling SaaS to many customers; per-user licensing lands on your customer, which breaks vertical-SaaS economics</td></tr><tr><td><strong>Salesforce / ServiceNow</strong></td><td>The top of the market</td><td>Ecosystem, certifications, integrator army, indemnification</td><td>Proprietary runtime, enterprise pricing, no real exit. Apex defaults to <em>system</em> mode — enforcement contingent on the developer</td></tr><tr><td><strong>Odoo / Frappe</strong></td><td>Platform-with-modules</td><td>The biggest module ecosystems on earth; a real business today</td><td>You inherit their ORM, worldview and upgrade treadmill; single-org shaped</td></tr><tr><td><strong>Medusa v2</strong></td><td>E-commerce with strict module isolation</td><td>Independently converged on the module/engine model, shipping in production</td><td>E-commerce-scoped, no native multi-tenancy, no enforcement layer around the modules</td></tr><tr><td><strong>Baseplate.dev</strong></td><td>Deterministic codegen you eject from</td><td>Zero lock-in — literally its proudest feature</td><td>The exact opposite pole: they generate the foundation and leave; we <em>are</em> the foundation and stay</td></tr><tr><td><strong>DIY: WorkOS + Nango + Inngest + Stripe</strong></td><td>Assemble it yourself</td><td>Best-in-class at each piece; no platform bet</td><td>Every seam between them is yours to keep correct forever, and none of them enforces anything about the code above</td></tr></tbody></table><p><strong>What none of the AI builders in this table have</strong> is an oracle independent of the code they generate: they produce the app and its tests from the same act of generation, so the tests inherit the same misunderstanding and pass. Frameworks don&#39;t generate your tests at all, which is honest but leaves the second opinion to your discipline on a Friday afternoon. Deriving code from the model and <a href="/guide/ai-guardrails.html#guard-04-two-descriptions-that-are-allowed-to-disagree">tests from the concept</a> is cheap, and it catches the exact failure mode — small, confident, inconsistent mistakes — that makes generated systems untrustworthy.</p><h2 id="the-neighbors-one-by-one" tabindex="-1">The neighbors, one by one <a class="header-anchor" href="#the-neighbors-one-by-one" aria-label="Permalink to &quot;The neighbors, one by one&quot;">​</a></h2><h3 id="full-stack-frameworks" tabindex="-1">Full-stack frameworks <a class="header-anchor" href="#full-stack-frameworks" aria-label="Permalink to &quot;Full-stack frameworks&quot;">​</a></h3><p><em>Ruby on Rails, Wasp, Laravel.</em></p><p>The honest baseline, and the one most readers should measure against first: for a single-tenant app, a good framework and a good team beat Substrat, and it isn&#39;t close. What a framework gives you is <strong>conventions</strong> — and conventions are exactly what erodes when the fifth iteration of a handler is written at speed, by a person or an agent, and drops a <code>WHERE</code> clause nothing was enforcing.</p><p>Wasp is the closest in instinct: a declarative spec the compiler checks, compiled down to a working app — the same conviction that a project should have one high-level description a human and an agent can both hold in their head. Substrat&#39;s <a href="/concepts/model.html"><code>model.ts</code></a> rhymes with <code>main.wasp.ts</code> deliberately.</p><p>The divergence is depth, not direction. Wasp is a framework you deploy; Substrat is a runtime that <em>enforces</em>, with tenancy, permissions, audit and GDPR below the API surface rather than in the app you generate. And the trade usually demanded for that — giving up the framework perks — is one we tried hard not to take: deterministic generation, auth you never write, local run, connectors, and platform-owned migrations are all still here.</p><p>If your problem is one app for one organization, take the framework.</p><h3 id="templates-boilerplates" tabindex="-1">Templates &amp; boilerplates <a class="header-anchor" href="#templates-boilerplates" aria-label="Permalink to &quot;Templates &amp; boilerplates&quot;">​</a></h3><p><em>MakerKit, ShipFast, Open SaaS, Bullet Train.</em></p><p>The closest articulation of &quot;an LLM-friendly foundation you own&quot; — and the honest one about its own economics (a one-time purchase, then it&#39;s yours). The difference is durability: a template gives you correct code <em>once</em>, and every edit after that, human or AI, can erode it. Nothing at runtime stops the fifth iteration of a handler from querying across tenants. Substrat&#39;s guarantees are enforced by the substrate on every call, not printed into a starting point. Templates also stop at the single-app shape — no nested tenancy, no provisioning, no shared domain engines.</p><h3 id="prompt-to-app" tabindex="-1">Prompt-to-app <a class="header-anchor" href="#prompt-to-app" aria-label="Permalink to &quot;Prompt-to-app&quot;">​</a></h3><p><em>Lovable, Bolt, Replit, and similar.</em></p><p>These generate the whole app, including the dangerous parts. That&#39;s their ceiling for B2B: the parts an LLM gets wrong most often — tenant isolation, auth boundaries, audit — are exactly the parts they generate rather than stand on. Their remedy, security <em>scanning</em>, checks that policies exist, not that they hold. Substrat inverts the split: the dangerous 30% is a hardened substrate; the safe, high-velocity 70% — screens, forms, workflows — is where the generation happens. The two are complementary, not rivals: a prompt-to-app tool pointed at Substrat&#39;s manifest generates <em>above</em> the guarantees instead of reinventing them.</p><h3 id="backend-as-a-service" tabindex="-1">Backend-as-a-service <a class="header-anchor" href="#backend-as-a-service" aria-label="Permalink to &quot;Backend-as-a-service&quot;">​</a></h3><p><em>Supabase.</em></p><p>BaaS does enforce at runtime — but it makes the guarantee contingent on rules the builder writes correctly. Row-level-security policies are precisely the surface inexperienced builders and LLMs misconfigure most; the isolation is real <em>if</em> every policy is right. Substrat&#39;s isolation is not a policy you author — <a href="/concepts/scope-host.html">the API for reaching a scope</a> <em>is</em> the isolation mechanism, and its secure default is deny. BaaS is also app-shaped rather than vertical-SaaS-shaped: no nested tenancy tree, no module system, no domain engines.</p><h3 id="reactive-backends-with-modules" tabindex="-1">Reactive backends with modules <a class="header-anchor" href="#reactive-backends-with-modules" aria-label="Permalink to &quot;Reactive backends with modules&quot;">​</a></h3><p><em>Convex.</em></p><p>Worth separating from the row above, because the usual BaaS critique doesn&#39;t land here. Convex reached the same conclusion we did — <a href="https://stack.convex.dev/why-convex-doesnt-need-row-level-security" target="_blank" rel="noreferrer">that RLS is a ticking timebomb</a>, because authorization split into a second language in a second place is a thing you have to get right twice and update twice — and closed the hole the same way, by putting every database access behind server functions. Their Components are the nearest mechanism to our engines from the other direction: mini-backends with private tables the host app cannot read, an explicit API, runtime validation of arguments <em>and</em> return values, and calls that commit as sub-transactions inside the caller&#39;s.</p><p>So the difference is shape rather than enforcement. Convex is built for one app: there is no tenancy tree, no scope per customer, no audit spine below the API, no erasure machinery, and authorization is a convention you repeat in every function rather than a check whose absence fails a lint. And their catalogue holds <em>technical</em> primitives — workflow, rate limiter, aggregate, migrations — where a <a href="/engines/">domain engine</a> owns a business invariant like a state machine that cannot skip a state. If your product is one app that wants live data, Convex is an excellent answer and a better developer experience than ours.</p><h3 id="low-code-enterprise-app-platforms" tabindex="-1">Low-code / enterprise app platforms <a class="header-anchor" href="#low-code-enterprise-app-platforms" aria-label="Permalink to &quot;Low-code / enterprise app platforms&quot;">​</a></h3><p><em>OutSystems, Mendix, Appian, Power Apps, ServiceNow App Engine.</em></p><p>The strongest existing answer on governance — they genuinely solve permissions, audit, and compliance as a hosted platform, and the market pays enterprise prices for it. The trade is that all of it lives inside a proprietary visual model: AI copilots generate <em>into</em> that model, deepening lock-in rather than producing code you own; the shape is internal-app, not multi-tenant SaaS you sell; and even the best tenancy in the class is single-level. Their existence is the demand evidence for the category. Substrat targets the same governance, but as a substrate under TypeScript you own, <a href="/guide/ai-agents.html">agent-first by design</a>, with nested tenancy as the reason it exists.</p><h3 id="salesforce-servicenow" tabindex="-1">Salesforce &amp; ServiceNow <a class="header-anchor" href="#salesforce-servicenow" aria-label="Permalink to &quot;Salesforce &amp; ServiceNow&quot;">​</a></h3><p><em>The top of the market.</em></p><p>Proof the category works at scale — governance, depth, and now AI app generation, sold to the largest enterprises. And proof of the trade Substrat refuses: a proprietary runtime, enterprise pricing, US-hosted, build-inside-our-world with no real exit. Substrat is the code-first, developer-owned, EU-shaped version for the SME and mid-market segment that tier prices out — a different customer, deliberately.</p><h3 id="odoo-frappe" tabindex="-1">Odoo &amp; Frappe <a class="header-anchor" href="#odoo-frappe" aria-label="Permalink to &quot;Odoo &amp; Frappe&quot;">​</a></h3><p><em>The platform-with-modules thesis, executed.</em></p><p>The nearest thing in spirit to &quot;apps on a shared platform,&quot; and worth understanding closely because the resemblance is surface-level. On Odoo you adopt an entire ORM, worldview, and upgrade cadence; apps extend each other by inheritance and share one database, so the platform is a single-organization shape — reseller multi-tenancy is something integrators build themselves, per tenant. Frappe&#39;s everything-is-a-DocType metadata model is the dynamic-schema approach Substrat <a href="/concepts/modules.html">deliberately avoids</a>: modules own their own typed tables and migrations instead. The engine model draws the opposite boundary from an Odoo app — see <a href="/engines/#what-an-engine-is-not">what an engine is <em>not</em></a>.</p><h3 id="medusa" tabindex="-1">Medusa <a class="header-anchor" href="#medusa" aria-label="Permalink to &quot;Medusa&quot;">​</a></h3><p><em>The architectural cousin.</em></p><p>The one neighbor Substrat resembles by convergence rather than contrast. Medusa v2&#39;s modules are strictly isolated, associate through link tables instead of foreign keys, and carry per-module migrations — independently arriving at the same engine model, and shipping in production. It validates the architecture. The differences are scope and layer: Medusa is e-commerce-shaped, runs one instance per tenant rather than native multi-tenancy, and has no enforcement layer around the modules. Substrat generalizes the module isolation and wraps it in <a href="/concepts/tenancy.html">nested tenancy</a> and runtime enforcement.</p><h3 id="assembling-the-pieces-yourself" tabindex="-1">Assembling the pieces yourself <a class="header-anchor" href="#assembling-the-pieces-yourself" aria-label="Permalink to &quot;Assembling the pieces yourself&quot;">​</a></h3><p><em>Clerk / WorkOS for identity, Nango for integrations, Inngest for jobs, Stripe for billing.</em></p><p>Every ingredient Substrat bundles exists as an excellent standalone product, and a capable team can wire them together. What that assembly doesn&#39;t give you is the <em>seam</em>: identity, tenancy, permissions, events, and integrations enforced as one coherent substrate, with domain engines on top and one audited path through all of it. You own the glue — and the glue between governance systems is exactly where the catastrophic mistakes live. Substrat is the opinionated bundle where the seams are the product.</p><h2 id="when-substrat-is-the-wrong-tool" tabindex="-1">When Substrat is the wrong tool <a class="header-anchor" href="#when-substrat-is-the-wrong-tool" aria-label="Permalink to &quot;When Substrat is the wrong tool&quot;">​</a></h2><p>The boundary is as much a part of the definition as the category. Reach for something else when:</p><ul><li><strong>You&#39;re building a single-tenant internal tool.</strong> Without a tenancy tree or a per-tenant compliance surface, most of the kernel is dead weight — Retool, Power Apps, and the BaaS platforms own that shape.</li><li><strong>One tenant is data- or scale-heavy.</strong> The scope-per-customer model suits many operationally-rich tenants, not one tenant with hundreds of millions of hot rows.</li><li><strong>The moat is deep domain logic, not foundation cost.</strong> Accounting, payroll, core banking — decades of domain depth. Integrate those; don&#39;t rebuild them.</li><li><strong>The foundation isn&#39;t your binding constraint.</strong> Consumer-scale apps, ML-first products, realtime-collaboration tools — different physics, where tenancy and audit aren&#39;t the bottleneck.</li></ul><p>Substrat fits where apps are <strong>structurally repetitive but operationally rich</strong>: workflow-shaped B2B products whose foundation — tenancy, permissions, audit, GDPR — is the expensive, identical part, and whose differentiation is vocabulary, states, and compliance content. Where the foundation isn&#39;t the cost driver, the substrate stops paying rent.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/comparisons.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const comparisons = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  comparisons as default
};
