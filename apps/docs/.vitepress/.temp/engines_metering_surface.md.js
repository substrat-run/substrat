import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Operations, functions & permissions","description":"","frontmatter":{},"headers":[],"relativePath":"engines/metering/surface.md","filePath":"engines/metering/surface.md","lastUpdated":1786737227000}');
const _sfc_main = { name: "engines/metering/surface.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="operations-functions-permissions" tabindex="-1">Operations, functions &amp; permissions <a class="header-anchor" href="#operations-functions-permissions" aria-label="Permalink to &quot;Operations, functions &amp; permissions&quot;">​</a></h1><h2 id="operations" tabindex="-1">Operations <a class="header-anchor" href="#operations" aria-label="Permalink to &quot;Operations&quot;">​</a></h2><p>Registered bindings, each one a permission check plus a call into the in-scope function below.</p><table tabindex="0"><thead><tr><th>Operation</th><th>Permission</th><th>Does</th></tr></thead><tbody><tr><td><code>metering/configure-meter</code></td><td><code>metering:configure</code></td><td>register a meter (kind/unit frozen thereafter); update description/active</td></tr><tr><td><code>metering/list-meters</code></td><td><code>metering:read</code></td><td>list registered meters</td></tr><tr><td><code>metering/record</code></td><td><code>metering:record</code></td><td>append a usage entry — idempotent on <code>(meter, dedupeKey)</code></td></tr><tr><td><code>metering/total</code></td><td><code>metering:read</code></td><td>window aggregate for one meter — the same code path a close freezes</td></tr><tr><td><code>metering/list-entries</code></td><td><code>metering:read</code></td><td>raw entries, filterable by meter / subject / window</td></tr><tr><td><code>metering/close-period</code></td><td><code>metering:close</code></td><td>freeze <code>[from, to)</code> into lines, advance the horizon, emit the fat event</td></tr><tr><td><code>metering/list-periods</code></td><td><code>metering:read</code></td><td>the close journal</td></tr><tr><td><code>metering/period-lines</code></td><td><code>metering:read</code></td><td>a closed period&#39;s frozen lines</td></tr></tbody></table><h2 id="in-scope-functions" tabindex="-1">In-scope functions <a class="header-anchor" href="#in-scope-functions" aria-label="Permalink to &quot;In-scope functions&quot;">​</a></h2><p>The composable surface. A vertical calls these <strong>inside its own operation and its own permission check</strong>, in one transaction — this is how you extend the engine without forking it.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">configureMeter</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { key, kind, unit, description?, active? })  → Meter</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">listMeters</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx)                                                  → Meter[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">recordUsage</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { meter, qty, subject?, occurredAt?,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">                   dedupeKey, note? })                           → { entry, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">deduped</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: boolean }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">usageTotal</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { meter, from, to })                             → { qty, entryCount } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">|</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">listEntries</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { meter?, subject?, from?, to? })               → UsageEntry[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">closePeriod</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { from, to })                                   → { period, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">lines</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: PeriodLine[] }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">listPeriods</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx)                                                 → MeteringPeriod[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">periodLines</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { periodId })                                   → PeriodLine[]</span></span></code></pre></div><p>Notes worth knowing:</p><ul><li><strong><code>recordUsage</code> returns <code>{ entry, deduped }</code></strong> — <code>deduped: true</code> means the key had already been recorded (same qty), the existing entry came back, and <strong>no event was emitted</strong>. A retry loop can call it blindly.</li><li><strong>The dedupe key is a contract, not a suggestion.</strong> Derive it from the observation&#39;s own identity — a turn id, an aggregation-bucket id (<code>requests:2026-08-14T13</code>) — never from a random value, or the idempotency is theatre.</li><li><strong><code>occurredAt</code> defaults to now</strong> and must be at or after the close horizon. Record late usage at observation time; never backdate into a closed window (the engine refuses).</li><li><strong>Gauges reject negative samples</strong>; counters take signed deltas — a correction is a compensating negative entry.</li><li><strong><code>usageTotal</code> returning <code>null</code></strong> means the meter has nothing to say for the window: a counter with no entries, or a gauge never sampled at all. A gauge with no <em>in-window</em> samples returns its carried-forward level with <code>entryCount: 0</code>.</li><li><strong><code>subject</code> is optional and opaque</strong> — an <code>EntityRef</code> the engine stores and never dereferences, for attribution and filtering (<a href="./composing.html">composing</a> covers when to use it versus a side table).</li></ul><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><p><code>metering:read</code> · <code>metering:record</code> · <code>metering:configure</code> · <code>metering:close</code></p><ul><li><strong><code>metering:record</code></strong> is the ingest key — what a vertical&#39;s turn-completion or sampling operation holds. It does not grant closing.</li><li><strong><code>metering:close</code></strong> freezes billing evidence and advances the horizon — hold it to the same standard as invoicing&#39;s export permission, because a close is what turns entries into a bill.</li><li><strong><code>metering:configure</code></strong> registers meter vocabulary. Kind/unit freezing means this key cannot rewrite history, only extend it.</li><li><strong><code>metering:read</code></strong> covers meters, entries, totals and closed periods alike — usage data is one audience (the tenant&#39;s admins), not many.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/metering/surface.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const surface = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  surface as default
};
