import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Events","description":"","frontmatter":{},"headers":[],"relativePath":"engines/invoicing/events.md","filePath":"engines/invoicing/events.md","lastUpdated":1784293491000}');
const _sfc_main = { name: "engines/invoicing/events.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h1><p>This engine is mostly a <strong>sink</strong>. Lines arrive by event, never by call — which is why the consumed side is the more interesting half.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">events</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  emits</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;invoicing.underlag-updated&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, schemaVersion: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;invoicing.underlag-exported&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, schemaVersion: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  consumes</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder.completed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, schemaVersion: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// the original producer</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;commerce.order-placed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, schemaVersion: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// added for the shop vertical</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><h2 id="consumed" tabindex="-1">Consumed <a class="header-anchor" href="#consumed" aria-label="Permalink to &quot;Consumed&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Event</th><th>From</th><th>Handler behaviour</th></tr></thead><tbody><tr><td><code>workorder.completed</code></td><td>the work-order engine</td><td>append lines, <code>source_type: &#39;workorder&#39;</code></td></tr><tr><td><code>commerce.order-placed</code></td><td>the shop vertical</td><td>append lines, <code>source_type: &#39;order&#39;</code> — <strong>only if <code>paymentMethod === &#39;invoice&#39;</code></strong></td></tr></tbody></table><p>Each has its <strong>own</strong> consumer with its <strong>own</strong> Zod view of the payload. Zero imports from either producer — the engine has never heard of the packages that emit these.</p><p>Adding the second was a <strong>purely additive change</strong>: a new <code>consumes</code> entry, a new parser, a new handler. No migration, no permission, the <code>workorder.completed</code> path untouched. That&#39;s engine reuse across verticals, demonstrated rather than asserted.</p><p>The mechanics — replay guard, find-or-create, dead-lettering — are in <a href="./model.html#snapshot-not-join">Domain model &amp; invariants</a>.</p><h2 id="emitted" tabindex="-1">Emitted <a class="header-anchor" href="#emitted" aria-label="Permalink to &quot;Emitted&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Event</th><th>v</th><th>piiClass</th><th>Payload</th></tr></thead><tbody><tr><td><code>invoicing.underlag-updated</code></td><td>1</td><td>none</td><td><code>{ underlagId, addedLines, source: EntityRef }</code></td></tr><tr><td><code>invoicing.underlag-exported</code></td><td><strong>2</strong></td><td>none</td><td><code>{ underlagId, number, total: Money }</code></td></tr></tbody></table><p><code>invoicing.underlag-exported</code> is the natural hook for the next step in a vertical: an accounting connector that turns the frozen basis into a real invoice. See <a href="./composing.html#reaching-the-outside-world">Composing</a>.</p><h2 id="versioning" tabindex="-1">Versioning <a class="header-anchor" href="#versioning" aria-label="Permalink to &quot;Versioning&quot;">​</a></h2><div class="warning custom-block"><p class="custom-block-title"><code>underlag-exported</code> is at schemaVersion 2 — a replace, not a dual-emit</p><p>v1 carried <code>total</code> as a bare amount string with no currency — an amount that isn&#39;t an amount, on the one event an accounting connector consumes. v2 makes it <code>Money</code>. If you consume this event, read <code>total.amount</code> and <code>total.currency</code> rather than the old string.</p><p>This is a deliberate exception to the usual deprecation-window rule, and the reason generalises to <strong>every</strong> engine here:</p><p><strong>Consumer dispatch keys on event <code>type</code> alone.</strong> The <code>schemaVersion</code> in a manifest&#39;s <code>consumes</code> is discarded at registration; the dispatch query is <code>WHERE o.type = ?</code>. So emitting v1 and v2 together would deliver <em>both</em> to every consumer of the type — and for an export event, that means a connector could invoice the same basis twice, silently.</p><p>A clean replace fails <strong>loudly</strong> instead: a v1 consumer&#39;s strict parse rejects v2 and dead-letters, which is visible. Loud beats silent when the alternative is double-billing.</p></div><p>The general rule still holds elsewhere: payload fields are frozen once shipped, new fields are optional and additive, and rename/remove/retype means a version bump. Just know that &quot;dual-emit through a deprecation window&quot; isn&#39;t actually available until version routing exists — it&#39;s a live open question in the kernel design, not a solved problem.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/invoicing/events.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const events = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  events as default
};
