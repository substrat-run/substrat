import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"What is a vertical?","description":"","frontmatter":{},"headers":[],"relativePath":"verticals/index.md","filePath":"verticals/index.md","lastUpdated":1785339849000}');
const _sfc_main = { name: "verticals/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="what-is-a-vertical" tabindex="-1">What is a vertical? <a class="header-anchor" href="#what-is-a-vertical" aria-label="Permalink to &quot;What is a vertical?&quot;">​</a></h1><p>A <strong>vertical</strong> is the business: the software a service firm, a workshop, a shop, or an HR team actually runs on. It owns everything with a user&#39;s fingerprints on it — <strong>vocabulary, pricing, roles, screens, workflows</strong> — and it composes the <a href="/engines/">engines</a> and the kernel underneath.</p><p>Where an engine is a reusable <em>contract</em>, a vertical is a <em>composition plus a product</em>. It is where the three layers meet: the kernel&#39;s guarantees, the engines&#39; invariants, and the business&#39;s own logic, wired together in one place you can read.</p><h2 id="what-a-vertical-owns-and-what-it-borrows" tabindex="-1">What a vertical owns (and what it borrows) <a class="header-anchor" href="#what-a-vertical-owns-and-what-it-borrows" aria-label="Permalink to &quot;What a vertical owns (and what it borrows)&quot;">​</a></h2><ul><li><strong>Borrows invariants</strong> from engines by calling their in-scope functions inside its <em>own</em> operations — same transaction, so an engine&#39;s state machine and the vertical&#39;s own tables commit together or not at all.</li><li><strong>Does its own permission check</strong> as the first line of every operation; engines never decide who may call a vertical.</li><li><strong>Adds side tables keyed by an engine&#39;s ids</strong> when it needs extra data on an engine entity — never a column upstream (a module&#39;s tables are private).</li><li><strong>Owns the screens</strong> — the app is the vertical&#39;s, composed over one API; the kernel and engines ship headless.</li></ul><p>The design test cuts both ways: if a vertical ever needs to <em>fork</em> an engine, the engine drew its line wrong; if a vertical hand-rolls tenancy, audit, or permissions, it&#39;s reaching below its layer.</p><h2 id="a-curated-set-not-a-pile" tabindex="-1">A curated set, not a pile <a class="header-anchor" href="#a-curated-set-not-a-pile" aria-label="Permalink to &quot;A curated set, not a pile&quot;">​</a></h2><p>These demos are chosen, not accumulated. Each one exists to prove a <strong>different way of using the platform</strong> — a distinct point in the design space — so the set as a whole shows the range, and no two demos teach the same lesson.</p><table tabindex="0"><thead><tr><th>Vertical</th><th>Package</th><th>The shape it uniquely shows</th><th>Engines composed</th></tr></thead><tbody><tr><td><strong><a href="/verticals/meridian.html">Meridian</a></strong> (HR)</td><td><code>demos/meridian</code></td><td>The <strong>shape-breaker</strong> — its <em>core</em> domain has no engine, so leave, time and expenses are vertical code on the kernel alone (<code>protocol</code> is composed for onboarding only); multi-country scopes diverging from one codebase; one <strong>role-adaptive app</strong> (employee + manager in the same surface)</td><td><code>protocol</code> only</td></tr><tr><td><strong><a href="/verticals/callout.html">Callout</a></strong> (field service)</td><td><code>demos/callout</code></td><td>The <strong>canonical composition</strong> — two engines cooperating through events with zero imports between them (the star-topology showpiece), plus the <em>pricing moment</em> where vertical logic meets an engine transition</td><td><code>workorder</code> · <code>invoicing</code> · <code>protocol</code></td></tr><tr><td><strong><a href="/verticals/handlebar.html">Handlebar</a></strong> (bike workshop)</td><td><code>demos/handlebar</code></td><td><strong>Engine reuse</strong> — the same engines under new vocabulary; the second shape that <em>forced the protocol engine to be extracted</em> from Callout</td><td><code>workorder</code> · <code>invoicing</code> · <code>protocol</code></td></tr><tr><td><strong><a href="/verticals/shop.html">Kallkälla</a></strong> (coffee shop)</td><td><code>demos/shop</code></td><td><strong>Two audiences, one source of truth</strong> — a customer storefront and a staff back-office as separate apps over one API; <code>invoicing</code> reused far outside field service</td><td><code>invoicing</code> (+ its own commerce module)</td></tr><tr><td><strong><a href="/verticals/rallypoint.html">RallyPoint</a></strong> (padel club)</td><td><code>demos/rally</code></td><td>A <strong>second invariant shape</strong> — allocation over an interval rather than a state machine, with the lost race rejected and no locking code anywhere; <strong>multi-venue tenancy</strong> (one tenant-level admin across venues, reception pinned to one); a <strong>consumer who holds no role at all</strong>, reaching their own booking through entity-narrowed grants</td><td><code>booking</code> · <code>invoicing</code> · <code>invites</code></td></tr><tr><td><strong><a href="/verticals/manyfold.html">Manyfold</a></strong> (headless CMS)</td><td><code>demos/manyfold</code></td><td><strong>Content lifecycle on the kernel alone</strong> — a draft → review → publish state machine that can&#39;t skip, append-only revisions, freeze-on-publish with a content hash, and references resolved at delivery, all <strong>composing no engine at all</strong>; <strong>multi-scope, &quot;site = scope&quot;</strong> — one tenant runs many sites, each its own scope, the same login a different role in each</td><td><em>none</em> — kernel only</td></tr></tbody></table><p>The through-line: <strong>structurally repetitive, operationally rich</strong> — the foundation is the same, the vocabulary and shape are not. That is exactly the segment Substrat is for.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("verticals/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
