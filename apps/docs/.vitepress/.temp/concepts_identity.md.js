import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Authentication &amp; identity","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/identity.md","filePath":"concepts/identity.md","lastUpdated":1785339849000}');
const _sfc_main = { name: "concepts/identity.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="authentication-amp-identity" tabindex="-1">Authentication &amp; identity <a class="header-anchor" href="#authentication-amp-identity" aria-label="Permalink to &quot;Authentication &amp;amp; identity&quot;">​</a></h1><p>The kernel <strong>authorizes</strong>; it never <strong>authenticates</strong>. Every operation runs against an ambient <code>PrincipalId</code> that is already established by the time the kernel sees it — the kernel never learns <em>how</em> a caller proved who they are, only <em>who they are</em>. Authentication is a swappable adapter at the edge (decision D-16).</p><h2 id="the-line-authenticate-vs-authorize" tabindex="-1">The line: authenticate vs authorize <a class="header-anchor" href="#the-line-authenticate-vs-authorize" aria-label="Permalink to &quot;The line: authenticate vs authorize&quot;">​</a></h2><ul><li><strong>Authorization is the kernel&#39;s</strong> — roles, grants, tenancy, the tuple evaluator (<a href="/concepts/permissions.html">permissions</a>). This is enforced on every operation and cannot be delegated to an outside system.</li><li><strong>Authentication is an adapter&#39;s</strong> — it takes a request (a session cookie, a bearer token, an OIDC <code>sub</code>) and resolves it to a <code>PrincipalId</code> + home node. Nothing more.</li><li>An external IdP&#39;s <strong>organizations/roles are a projection</strong> of kernel tuples, never the source of truth. If you let an IdP&#39;s RBAC decide access, you have two permission systems fighting — exactly what the three-layer rule forbids.</li></ul><h2 id="the-neutral-seam" tabindex="-1">The neutral seam <a class="header-anchor" href="#the-neutral-seam" aria-label="Permalink to &quot;The neutral seam&quot;">​</a></h2><p>The control-plane directory holds one provider-agnostic mapping:</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">_substrat_identities (</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  provider</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">     TEXT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- &#39;better-auth&#39; | &#39;oidc:&lt;issuer&gt;&#39; | …</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  external_id  </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">TEXT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- the provider&#39;s stable user id (e.g. the OIDC \`sub\`)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  principal_id </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">TEXT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- the Substrat principal it resolves to</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  tenant_id    </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">TEXT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  scope_id     </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">TEXT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- NULL = tenant-level home</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  PRIMARY KEY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (tenant_id, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">provider</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, external_id)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre></div><p>exposed on the host admin as two methods:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">linkIdentity</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(actor, { provider, externalId, principal, tenantId, scopeId? }): </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> // audited; idempotent per (tenantId, provider, externalId)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">resolveIdentity</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(tenantId, provider, externalId): { principal, scopeId } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">|</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> undefined</span></span></code></pre></div><p>Because the mapping is <strong>keyed by provider</strong>, several auth adapters — and several OIDC upstreams — coexist without collision. Adding one is additive: no schema change, no permission, no kernel change.</p><p><strong>Pools are registered, and declare their topology.</strong> Before a provider may link anything it registers as <code>central</code> (one pool serving many tenants — the same <code>externalId</code> everywhere is the same human) or <code>tenant-bound</code> (one pool, one tenant — the same <code>externalId</code> elsewhere is a <em>different</em> human). An unregistered provider is refused rather than defaulted: without that fact the kernel would be guessing whether two people are one.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> host.admin.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">registerIdentityPool</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(actor, {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  provider: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;oidc:https://issuer.example&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  topology: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;tenant-bound&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  tenantId,                       </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// null for a central pool</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p>Topology, not audience. A padel player on a branded multi-club platform is a <em>consumer</em> who wants the central topology, while a shopper on a white-label store is a consumer who must not have it — so the audience is shorthand and the topology is the enforceable fact. A provider string names exactly one pool, so separate per-tenant deployments take distinct provider strings (<code>oidc:&lt;issuer&gt;</code>).</p><p><strong>And keyed by tenant.</strong> The full key is <code>(tenantId, provider, externalId)</code>, with the tenant as an <em>input</em> to the lookup rather than something derived from the identity. That matters as soon as tenants have their own identity pools: an external subject id is unique only <em>within</em> its pool, so two white-label shops can both issue user <code>123</code> to different people. A globally-keyed mapping would resolve the second as the first.</p><p>The same key is what lets one login belong to several tenants — a consultant administering five customers is one external identity with one row per tenant, and one principal per tenant. Shared identity, separate authority.</p><p>The caller always knows which tenant it is asking about: the request arrived on that tenant&#39;s hostname, or carried a pool-scoped token.</p><p><strong>&quot;Which tenants is this login in?&quot;</strong> is a separate call, and central-only:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> host.admin.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">listIdentityTenants</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(actor, provider, externalId); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// → TenantId[]</span></span></code></pre></div><p>It throws on a tenant-bound pool. Not because the answer would leak — the caller already knows that pool&#39;s tenant — but because asking is a category error: where the same <code>externalId</code> names different people per tenant, a tenant list has no meaning. Enumerate, then resolve within the one you picked.</p><h2 id="auth-adapters-at-the-edge" tabindex="-1">Auth adapters at the edge <a class="header-anchor" href="#auth-adapters-at-the-edge" aria-label="Permalink to &quot;Auth adapters at the edge&quot;">​</a></h2><p>An auth adapter is anything that turns a request into a principal:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> AuthAdapter</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  resolve</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">headers</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Headers</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Promise</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">AuthResult</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// null = &quot;not mine&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>The server tries its mounted adapters in order; the first to recognise the request wins, and the resolved principal is handed to <code>getScope</code>. Adapters are <strong>chosen by config</strong>, so a deployment can run one, another, or several at once:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AUTH=better-auth,public    # a real session, else an anonymous browse-only fallback</span></span>
<span class="line"><span>AUTH=oidc                  # an OIDC / authhero adapter</span></span>
<span class="line"><span>AUTH=better-auth,oidc      # both doors — resolve to the same principal model underneath</span></span></code></pre></div><p><strong>OIDC is not a separate burden.</strong> Better Auth can federate upstream identity itself (social, generic OIDC, enterprise SSO), so &quot;log in with authhero/Google/SSO&quot; becomes config on the <em>same</em> adapter — or a second adapter against the <em>same</em> <code>resolveIdentity</code>. Either way the kernel is untouched: doing the seam neutrally is what buys the choice.</p><h3 id="two-real-choices-made-differently" tabindex="-1">Two real choices, made differently <a class="header-anchor" href="#two-real-choices-made-differently" aria-label="Permalink to &quot;Two real choices, made differently&quot;">​</a></h3><p>The neutral seam is not hypothetical — the codebase exercises both ends of it deliberately:</p><ul><li><strong>The platform&#39;s own apps</strong> — the <a href="/platform/dashboard.html">Dashboard</a> and the <a href="/platform/console.html">control-plane Console</a> — authenticate against <strong>AuthHero OIDC</strong> (Authorization-Code + PKCE) through the shared <a href="/reference/oidc-rp.html"><code>@substrat-run/oidc-rp</code></a> relying party. One IdP, one session model, across every platform surface — including the <code>substrat login</code> CLI, which brokers the same flow. This is the &quot;log in with SSO&quot; corner, chosen because platform staff and tenant admins are one identity population the platform runs itself.</li><li><strong>The demos stay on Better Auth</strong>, each with its own user store, because a demo vertical models a business that owns <em>its</em> customers&#39; logins. Same <code>AuthAdapter</code> contract, opposite end of the choice.</li></ul><p>That the same kernel serves both, unchanged, is the seam paying out.</p><h2 id="identity-sync-on-first-login" tabindex="-1">Identity sync on first login <a class="header-anchor" href="#identity-sync-on-first-login" aria-label="Permalink to &quot;Identity sync on first login&quot;">​</a></h2><p>The first time an adapter resolves an external user it hasn&#39;t seen, it provisions them — the plan&#39;s §4.3 flow — then binds the identity:</p><ol><li>mint a <code>PrincipalId</code>;</li><li>assign the role(s) that user should hold;</li><li>create the domain records they own (e.g. a customer);</li><li>issue any entity-narrowed grants (e.g. read-your-own-orders);</li><li><code>linkIdentity((tenantId, provider, externalId) → principal)</code>.</li></ol><p>From then on <code>resolveIdentity</code> short-circuits to the same principal — a stable identity with real, enforced permissions.</p><h2 id="in-the-demo" tabindex="-1">In the demo <a class="header-anchor" href="#in-the-demo" aria-label="Permalink to &quot;In the demo&quot;">​</a></h2><p>The <a href="https://github.com/substrat-run/substrat/tree/main/demos/shop" target="_blank" rel="noreferrer">Kallkälla Kaffe</a> shop uses <strong>Better Auth</strong> as its first adapter (email/password, its own store, entirely separate from the scope-host DBs) plus a browse-only anonymous fallback. Pre-seeded logins let you sign in as each persona and <em>feel</em> the permission model — the credentials are below.</p><table tabindex="0"><thead><tr><th>Log in as</th><th>Password</th><th>Role</th><th>Sees</th></tr></thead><tbody><tr><td><code>astrid@kallkalla.se</code></td><td><code>demo1234</code></td><td>shop-admin</td><td>everything — catalogue, stock, orders, invoicing</td></tr><tr><td><code>gustav@kallkalla.se</code></td><td><code>demo1234</code></td><td>warehouse</td><td>orders + stock; <strong>invoicing is denied</strong></td></tr><tr><td><code>elin@cafepascal.se</code></td><td><code>demo1234</code></td><td>customer</td><td>the shop + <strong>only her own</strong> orders</td></tr><tr><td><code>otto@kontoret.se</code></td><td><code>demo1234</code></td><td>customer</td><td>the shop + only his own orders</td></tr><tr><td><em>sign up</em></td><td>—</td><td>customer</td><td>a fresh principal, provisioned on first login</td></tr><tr><td><em>(not logged in)</em></td><td>—</td><td>public</td><td>browse the catalogue only</td></tr></tbody></table><p>Signing in as Gustav and watching <em>Invoice basis</em> disappear from the nav — and 403 if you ask for it directly — is the whole thesis in one click: <strong>Better Auth authenticated you, the kernel authorized you.</strong></p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/identity.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const identity = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  identity as default
};
