import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Events","description":"The six events the absence engine emits, the fat payloads that spare consumers a cross-module read, and the subject key that keeps crypto-shredding possible. Consumes nothing.","frontmatter":{"description":"The six events the absence engine emits, the fat payloads that spare consumers a cross-module read, and the subject key that keeps crypto-shredding possible. Consumes nothing."},"headers":[],"relativePath":"engines/absence/events.md","filePath":"engines/absence/events.md","lastUpdated":1787063543000}');
const _sfc_main = { name: "engines/absence/events.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h1><h2 id="emitted" tabindex="-1">Emitted <a class="header-anchor" href="#emitted" aria-label="Permalink to &quot;Emitted&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Event</th><th>When</th></tr></thead><tbody><tr><td><code>absence.leave-type-configured</code></td><td>a leave type&#39;s key/floor/active registered or updated</td></tr><tr><td><code>absence.entry-recorded</code></td><td>any ledger entry lands — accrual, correction, carryover, booking, reversal</td></tr><tr><td><code>absence.requested</code></td><td>a request is filed</td></tr><tr><td><code>absence.decided</code></td><td>a request is approved (carries the booking) or rejected</td></tr><tr><td><code>absence.cancelled</code></td><td>a request is withdrawn or an approved absence cancelled (carries the reversal id)</td></tr><tr><td><code>absence.expired</code></td><td>the schedule cancelled a request left unapproved past its start date</td></tr></tbody></table><p>Consumes nothing.</p><h2 id="payload-contracts" tabindex="-1">Payload contracts <a class="header-anchor" href="#payload-contracts" aria-label="Permalink to &quot;Payload contracts&quot;">​</a></h2><p>All payloads are <strong>fat</strong>: they carry the subject ref, the leave-type key, the ids and the deltas a consumer needs, so no consumer ever reads back across the module boundary.</p><ul><li><code>absence.entry-recorded</code> — <code>{ entryId, subject, leaveTypeKey, entryKind, delta, effectiveDate, requestId }</code>. Note that <strong>bookings and reversals emit this too</strong> (from inside <code>decideAbsence</code>/<code>cancelAbsence</code>): a consumer that only cares about ledger movement can subscribe to this one event and see every delta, whatever minted it.</li><li><code>absence.requested</code> — <code>{ requestId, subject, leaveTypeKey, startDate, endDate, days }</code>.</li><li><code>absence.decided</code> — <code>{ requestId, subject, leaveTypeKey, decision, bookingId }</code>, plus <code>days</code>/<code>startDate</code>/<code>endDate</code> on approval. One transition, one event — there is no separate <code>absence.booked</code>; the booking&#39;s id is on the decision.</li><li><code>absence.cancelled</code> — <code>{ requestId, subject, leaveTypeKey, priorStatus, reversalId }</code>. <code>priorStatus</code> tells a consumer whether balance moved (<code>approved</code> → a reversal exists) or not (<code>requested</code> → <code>reversalId: null</code>).</li><li><code>absence.expired</code> — <code>{ requestId, subject, startDate }</code>, emitted under the system actor, which is how an audit reader distinguishes the schedule from a person.</li></ul><h2 id="pii-and-the-subject-key" tabindex="-1">PII and the subject key <a class="header-anchor" href="#pii-and-the-subject-key" aria-label="Permalink to &quot;PII and the subject key&quot;">​</a></h2><p>Every subject-bearing event is <code>piiClass: &#39;pseudonymous&#39;</code> with <code>subjectId</code> set to the <strong>vertical-supplied <code>DataSubjectId</code></strong> stored on the row — the erasure key travels with every fact about a person, so crypto-shredding that subject shreds their absence trail in one keyed sweep while the scope&#39;s aggregate numbers survive.</p><p><code>absence.leave-type-configured</code> is <code>piiClass: &#39;none&#39;</code> — policy, not people.</p><p>The engine never invents that id: the vertical passes it on every write (Meridian passes the employee id its whole hr.* spine already shreds on; a route planner passes its resource id). Choosing an id that actually maps to the person is the vertical&#39;s responsibility, stated here because nothing downstream can fix a wrong choice.</p><h2 id="versioning" tabindex="-1">Versioning <a class="header-anchor" href="#versioning" aria-label="Permalink to &quot;Versioning&quot;">​</a></h2><p>Every event is emitted at <code>schemaVersion: 1</code>. Payload fields are <strong>frozen once shipped</strong>: additions are fine, renames/removals/retypes mean a <code>schemaVersion</code> bump with a dual-emit deprecation window — the platform-wide rule, restated because ledger events are exactly the kind downstream payroll and reporting consumers quietly grow to depend on.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/absence/events.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const events = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  events as default
};
