import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Composing & extending","description":"","frontmatter":{},"headers":[],"relativePath":"engines/workorder/composing.md","filePath":"engines/workorder/composing.md","lastUpdated":1784462441000}');
const _sfc_main = { name: "engines/workorder/composing.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="composing-extending" tabindex="-1">Composing &amp; extending <a class="header-anchor" href="#composing-extending" aria-label="Permalink to &quot;Composing &amp; extending&quot;">​</a></h1><h2 id="using-it-as-is" tabindex="-1">Using it as-is <a class="header-anchor" href="#using-it-as-is" aria-label="Permalink to &quot;Using it as-is&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">registerModule</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(workorderModule);</span></span></code></pre></div><p>That&#39;s the whole registration surface. <code>registerModule</code> takes exactly one argument, and it&#39;s a frozen constant.</p><h2 id="wrapping-it-with-vertical-logic" tabindex="-1">Wrapping it with vertical logic <a class="header-anchor" href="#wrapping-it-with-vertical-logic" aria-label="Permalink to &quot;Wrapping it with vertical logic&quot;">​</a></h2><p>The registered operations are <strong>default bindings</strong>. For custom flows, call the exported in-scope functions from your own operation — same transaction, same serialization domain, and the permission check becomes yours:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { createWorkOrder, PERM } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/engine-workorder&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">defineOperation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;acme/felanmalan-to-order&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">input</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  assertAllowed</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">check</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">PERM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.create));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ctx.sql.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;UPDATE acme_tickets SET status = ? WHERE id = ?&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;converted&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, input.ticketId]);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  return</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> createWorkOrder</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    facility: </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ticketFacility</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, input.ticketId),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    customer: </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ticketCustomer</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, input.ticketId),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    kind: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;felanmalan&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    title: input.title,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  });</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p>The ticket update and the order creation commit together or not at all.</p><p><code>demos/callout/src/routes.ts</code> maps the seam precisely: <code>assign</code>/<code>start</code>/<code>close</code> go straight to the engine, while <code>create-workorder</code> and <code>complete-workorder</code> (which needs billable lines priced) route through <code>callout/*</code>.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p><strong>There is none, and that&#39;s deliberate.</strong> <code>ModuleRegistration</code> has five fields — <code>manifest</code>, <code>migrations</code>, <code>operations</code>, <code>consumers</code>, <code>predicates</code> — and none is config. There is no <code>createWorkorderModule({...})</code> factory and no <code>config</code> field on the manifest. An engine cannot be told anything at registration time.</p><p><em>Configuration is dynamic; composition is code.</em> When you need the engine to behave differently, you reach for one of these instead:</p><table tabindex="0"><thead><tr><th>You want</th><th>Use</th></tr></thead><tbody><tr><td>only part of the engine</td><td><code>withdraws</code> in your manifest — suppress a default binding, re-offer it behind your own operation</td></tr><tr><td>different behaviour around a transition</td><td>your own operation calling the in-scope function (above)</td></tr><tr><td>a parameterised rule</td><td>a guard predicate with <code>guards[].config</code> — kernel-opaque, parsed by the predicate that owns it</td></tr><tr><td>tenant-specific content</td><td>runtime data in your own tables</td></tr><tr><td>the engine off entirely for a tenant</td><td>revoke the <code>workorder</code> entitlement</td></tr></tbody></table><p><code>entitlementKey</code> is a <strong>binary SKU gate</strong>, not a config knob: a tenant holds it or the operations don&#39;t resolve. It&#39;s checked per invoke and fails closed. One caveat — bare <code>defineOperation</code> glue carries no manifest and is <strong>ungated</strong>, so a vertical operation wrapping an in-scope function bypasses the entitlement check.</p><h3 id="adding-data-to-an-order" tabindex="-1">Adding data to an order <a class="header-anchor" href="#adding-data-to-an-order" aria-label="Permalink to &quot;Adding data to an order&quot;">​</a></h3><p>Never add a column upstream. A vertical needing extra fields on a work order adds <strong>its own side table keyed by the engine&#39;s order id</strong>. Another module&#39;s tables are private; the stable surface is entity ids, <code>EntityRef</code>s, and event payloads.</p><h2 id="portal-reads" tabindex="-1">Portal reads <a class="header-anchor" href="#portal-reads" aria-label="Permalink to &quot;Portal reads&quot;">​</a></h2><p>The engine declares the edge that makes entity-narrowed access work:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">entityRelations</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [{ entityType: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, parentType: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;facility&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }]</span></span></code></pre></div><p><code>createWorkOrder</code> records it with <code>ctx.link(orderRef, input.facility)</code>, so a <a href="/concepts/permissions.html#capability-grants">capability grant</a> on a facility reaches the work orders under it. A portal customer gets an entity-narrowed <code>workorder:read</code> on their own facility and <code>workorder/get</code> — which checks per-entity — does the rest.</p><h2 id="reaching-the-outside-world" tabindex="-1">Reaching the outside world <a class="header-anchor" href="#reaching-the-outside-world" aria-label="Permalink to &quot;Reaching the outside world&quot;">​</a></h2><p>Nothing in this engine talks to anything external, and it never will: module code may not <code>fetch</code>, and connectors handle the outside world.</p><p>The seam is <code>workorder.completed</code> on the event spine. A <strong>connector</strong> — a third-party capability living in the kernel-owned integrations hub, neither engine nor vertical — is what would carry it outward.</p><div class="warning custom-block"><p class="custom-block-title">Connectors are not built yet</p><p>The connector interface, webhook ingress, and per-tenant connection config are <strong>planned, not implemented</strong> — there is no connector code in the repo today. The <code>_substrat_outbox</code> is real and transactional, but it drains only to in-process consumers; there is no external sink. Engines are pre-shaped for a seam that doesn&#39;t have a floor under it yet.</p></div></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/workorder/composing.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const composing = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  composing as default
};
