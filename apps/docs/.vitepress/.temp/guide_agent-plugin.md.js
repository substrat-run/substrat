import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"The Claude Code plugin","description":"","frontmatter":{},"headers":[],"relativePath":"guide/agent-plugin.md","filePath":"guide/agent-plugin.md","lastUpdated":1787098519000}');
const _sfc_main = { name: "guide/agent-plugin.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="the-claude-code-plugin" tabindex="-1">The Claude Code plugin <a class="header-anchor" href="#the-claude-code-plugin" aria-label="Permalink to &quot;The Claude Code plugin&quot;">​</a></h1><p>A scaffolded Substrat project already teaches an agent what it needs: <code>npm create substrat</code> writes the rules, the build playbook, and a session hook that pins the agent to the docs for the kernel version installed there. The plugin exists for the case before that — an agent opened on a directory we did not generate, which has none of it.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">claude</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> plugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> marketplace</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> https://substrat.net/marketplace.json</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">claude</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> plugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> substrat@substrat-run</span></span></code></pre></div><p>In Claude Desktop, install it from <strong>+</strong> → <strong>Plugins</strong> → <strong>Add plugin</strong>. Plugins are not available in WSL sessions.</p><p>The marketplace also resolves from the repository, if you prefer that form:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">claude</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> plugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> marketplace</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> substrat-run/substrat</span></span></code></pre></div><p>Both register the same catalog. The URL form downloads one small file; the repository form clones the monorepo, so prefer the URL unless you have a reason not to.</p><h2 id="what-you-get" tabindex="-1">What you get <a class="header-anchor" href="#what-you-get" aria-label="Permalink to &quot;What you get&quot;">​</a></h2><p><strong><code>/substrat</code></strong> — the guided build flow. It works out which of three situations you are in and routes accordingly: an existing Substrat vertical (read its rules and playbook), an empty directory (scaffold one first, then the same), or an existing project that is not a vertical (say so, rather than improvise — Substrat is not a library you add to an app).</p><p><strong>A <code>SessionStart</code> hook</strong> — silent in any project that is not a Substrat vertical, so it costs you nothing elsewhere. In one that is, it tells the agent where the rules live and which docs slice describes the kernel actually installed. Substrat is pre-1.0 and its interfaces change without notice, so an agent working from pages it cached two minors ago is the expensive failure: confident, plausible, wrong.</p><h2 id="what-it-deliberately-does-not-contain" tabindex="-1">What it deliberately does not contain <a class="header-anchor" href="#what-it-deliberately-does-not-contain" aria-label="Permalink to &quot;What it deliberately does not contain&quot;">​</a></h2><p>The build flow itself. That lives in the project, in <code>.substrat/playbook.md</code>, beside the <a href="./agent-rules.html">agent rules</a> in <code>AGENTS.md</code> — and that copy is pinned to the kernel version installed there. A copy inside the plugin would go stale on its own schedule, and the rules an agent must not violate are the worst possible thing to be stale about. The plugin routes to the project&#39;s copy and never substitutes for it.</p><p>This is why the plugin adds nothing to a project you scaffolded: that project already carries its own copy of the skill and the hook. Installing it anyway is harmless — the project&#39;s copies win — but it is not something you need to do.</p><h2 id="other-tools" tabindex="-1">Other tools <a class="header-anchor" href="#other-tools" aria-label="Permalink to &quot;Other tools&quot;">​</a></h2><p>The plugin is a Claude Code adapter. Every other client reads the same neutral core, which the scaffold writes into the project directly:</p><table tabindex="0"><thead><tr><th>Tool</th><th>How it finds the rules</th></tr></thead><tbody><tr><td>Claude Code</td><td><code>CLAUDE.md</code>, <code>.claude/skills/</code>, <code>.claude/settings.json</code>, <code>.claude/launch.json</code></td></tr><tr><td>Cursor</td><td><code>.cursor/rules/substrat.mdc</code>, <code>.cursor/commands/new-vertical.md</code></td></tr><tr><td>opencode</td><td><code>.opencode/command/new-vertical.md</code></td></tr><tr><td>Codex, Kiro</td><td><code>AGENTS.md</code>, natively — nothing to install</td></tr></tbody></table><p>For an agent that has not scaffolded anything and is reading rather than building, <a href="https://substrat.net/llms.txt" target="_blank" rel="noreferrer"><code>substrat.net/llms.txt</code></a> is the docs index and every page on this site has a <code>.md</code> twin at the same URL.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/agent-plugin.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const agentPlugin = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  agentPlugin as default
};
