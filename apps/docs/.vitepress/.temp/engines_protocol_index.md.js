import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Protocol engine","description":"","frontmatter":{},"headers":[],"relativePath":"engines/protocol/index.md","filePath":"engines/protocol/index.md","lastUpdated":1784560672000}');
const _sfc_main = { name: "engines/protocol/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="protocol-engine" tabindex="-1">Protocol engine <a class="header-anchor" href="#protocol-engine" aria-label="Permalink to &quot;Protocol engine&quot;">​</a></h1><p><code>@substrat-run/engine-protocol</code> — <strong>protocols and checklists with the sign → immutable invariant</strong>: self-inspections, installation protocols, service checklists, per-item condition reports. A protocol is the compliance artifact a field-service or workshop business must be able to produce, unaltered, years later.</p><p>The engine owns only the invariants. Template <strong>content</strong> — which protocols exist, what sections and items they contain, which are mandatory when — is 100% vertical-owned. An instance binds to any <code>EntityRef</code> — a work order, a bike&#39;s condition report, or an employee&#39;s <strong>onboarding checklist</strong>, all three shipping in the demo verticals — and the engine never knows the vertical&#39;s vocabulary. (The HR vertical even signs onboarding as the <em>employee</em>, not a supervisor: same engine, the vertical&#39;s grant decides who signs.)</p><p>Both content kinds ship in <a href="/verticals/meridian.html#engines-composed">Meridian</a>: onboarding as a <code>checklist</code>, and the <strong>anställningsavtal</strong> as a <code>document</code> signed by an employer principal and a new hire who has no account yet. It is the worked example for everything below.</p><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>@substrat-run/engine-protocol</code></td></tr><tr><td><strong>Entitlement key</strong></td><td><code>protocol</code></td></tr><tr><td><strong>Owns</strong></td><td>version-pinned templates, append-only responses, freeze→immutable, a verifiable content hash, signature requests</td></tr><tr><td><strong>Emits</strong></td><td>9 events, <code>protocol.instantiated</code> → <code>protocol.voided</code> (<a href="./events.html">events</a>)</td></tr><tr><td><strong>Consumes</strong></td><td>nothing</td></tr><tr><td><strong>Permissions</strong></td><td>9 (<code>protocol:create</code> · <code>fill</code> · <code>bind</code> · <code>request-signature</code> · <code>record-signature</code> · <code>sign</code> · <code>countersign</code> · <code>read</code> · <code>void</code>)</td></tr><tr><td><strong>Contributes</strong></td><td>the <code>protocol/all-signed</code> guard predicate — the only config-shaped surface in any engine</td></tr><tr><td><strong>Status</strong></td><td>product seed (0.x) — the extraction proof</td></tr></tbody></table><h2 id="what-it-owns" tabindex="-1">What it owns <a class="header-anchor" href="#what-it-owns" aria-label="Permalink to &quot;What it owns&quot;">​</a></h2><ol><li><strong>Freeze freezes.</strong> Content is writable only while an instance is <code>open</code> — whether it froze at an in-app signature or at dispatch to an external signing provider.</li><li><strong>Verifiable content hash.</strong> SHA-256 over template content plus the frozen content — replayable against stored rows by anyone. One recipe per content kind.</li><li><strong>Counter-signature on frozen content.</strong> A second signature whose hash must equal the primary&#39;s, so it can never silently attach to changed content.</li><li><strong>Append-only responses.</strong> An edit is a new row; the history <em>is</em> audit material.</li><li><strong>Version-pinned templates.</strong> An instance pins <code>(key, version)</code> at instantiation forever.</li><li><strong>Void, not delete.</strong> A superseded protocol is voided with a reason, never removed.</li><li><strong>A signature is over a <em>frozen</em> hash, by a named signatory, at a stated time.</strong> The signatory may be a principal or an <strong>external person with no account</strong>, which is what makes a BankID/Scrive flow expressible without pretending it is a synchronous in-app tap.</li></ol><p>Details: <a href="./model.html">Domain model &amp; invariants</a>.</p><h2 id="what-it-will-not-do" tabindex="-1">What it will not do <a class="header-anchor" href="#what-it-will-not-do" aria-label="Permalink to &quot;What it will not do&quot;">​</a></h2><ul><li><strong>Template content</strong> — sections, items, vocabulary, branschprotokoll packs are the vertical&#39;s. The engine validates fills against the pinned template shape; it authors no templates.</li><li><strong>Talk to external signature providers</strong> — the engine emits a request and a connector dispatches it; it imports no vendor SDK and makes no network call. Note the <em>inbound</em> half (webhook ingress, and an authority seam letting a non-principal callback invoke an operation) is <strong>not built</strong> — see <a href="https://github.com/substrat-run/substrat/issues/96" target="_blank" rel="noreferrer">#96</a> and <a href="https://github.com/substrat-run/substrat/issues/97" target="_blank" rel="noreferrer">#97</a>.</li><li><strong>Render or reconcile a document</strong> — a <code>document</code> instance holds a ref and a hash, never bytes, and recomputing that hash from your own rows is your obligation, not the engine&#39;s.</li><li><strong>Branching/conditional templates, scheduled instantiation, PDF rendering, a photo pipeline, offline sync, a template marketplace</strong> — explicit v0 non-goals.</li></ul><h2 id="is-this-a-good-match" tabindex="-1">Is this a good match? <a class="header-anchor" href="#is-this-a-good-match" aria-label="Permalink to &quot;Is this a good match?&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Reach for it when</th><th>Look elsewhere when</th></tr></thead><tbody><tr><td>Someone must <strong>attest</strong> to a set of answers, and that attestation must hold up later</td><td>You just need a form; a table and a timestamp are cheaper</td></tr><tr><td>&quot;Prove this wasn&#39;t altered after signing&quot; is a real question</td><td>Nobody will audit it</td></tr><tr><td>The person who fills is not always the person who signs</td><td>One actor does everything</td></tr><tr><td>A second party (customer at pickup) confirms the same content</td><td>There&#39;s no counterparty</td></tr><tr><td>Parties sign <strong>asynchronously</strong>, via a provider, possibly without accounts</td><td>Everyone signs in-app, in session</td></tr><tr><td>The signed thing is a <strong>document you own</strong> (an avtal), not a checklist</td><td>It is neither</td></tr><tr><td>The checklist&#39;s <em>content</em> is yours and changes over time</td><td>You want the engine to ship the checklists — it won&#39;t</td></tr></tbody></table><p>The clarifying question: <strong>is the signature load-bearing?</strong> If the value is in the answers, you want a form. If the value is in <em>who stood behind the answers, and that they haven&#39;t changed since</em> — that&#39;s this engine.</p><p>This engine is the <strong>extraction proof</strong>: it was forced out of vertical code only when a <em>second</em> vertical (a bike shop&#39;s per-bike condition report) needed the same sign-immutability invariant in a different shape. Engines get extracted when a second consumer proves the line, not when someone predicts one.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/protocol/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
