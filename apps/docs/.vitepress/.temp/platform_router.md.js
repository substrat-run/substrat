import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Router","description":"","frontmatter":{},"headers":[],"relativePath":"platform/router.md","filePath":"platform/router.md","lastUpdated":1785339849000}');
const _sfc_main = { name: "platform/router.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="router" tabindex="-1">Router <a class="header-anchor" href="#router" aria-label="Permalink to &quot;Router&quot;">​</a></h1><p><strong>One worker in front of every vertical.</strong> It resolves an inbound <code>hostname → (tenant, scope, vertical, surface)</code> against the <a href="/platform/control-plane.html">control plane</a>&#39;s directory and forwards the request to the right vertical over a service binding, asserting the resolved node in <code>x-substrat-*</code> headers. Before it existed, a provisioned scope had no URL — the console faked one with an env var.</p><p>It is built on <a href="/reference/adapter-cloudflare.html"><code>createRouteResolver</code></a> (the adapter&#39;s <code>routing</code> subpath). K-26/K-27.</p><h2 id="why-one-router" tabindex="-1">Why one router <a class="header-anchor" href="#why-one-router" aria-label="Permalink to &quot;Why one router&quot;">​</a></h2><ul><li><strong>Not one per vertical.</strong> Cert and DNS lifecycle in one place means a new vertical gets custom domains for free instead of repeating the Cloudflare-for-SaaS dance each time.</li><li><strong>Not one per jurisdiction.</strong> The router is stateless and holds nothing regional, so a per-region router would duplicate the cert/DNS lifecycle and buy nothing. <em>Verticals</em>, by contrast, <strong>do</strong> deploy per jurisdiction (K-30): <code>substrat-fsm-eu</code> binds EU storage and cannot reach US data — a worker that <em>cannot</em> beats one that merely chooses not to. The router itself does <strong>not</strong> enforce region: <code>verticalFor(env, target)</code> keys on the resolved route&#39;s <code>deploymentRef</code> (the Workers-for-Platforms dispatch handle) and falls back to the static <code>VERTICAL_&lt;SLUG&gt;</code> binding — <code>target.region</code> is carried but never re-checked here. Residency is pinned by configuration, not by a code branch in the router: Regional Services pins TLS termination and processing at the edge, <em>ahead</em> of this worker, and the DO jurisdiction pins storage and execution (K-7). Re-checking region in the router would be a third enforcement point that can only ever disagree with those two.</li></ul><p>This does not erode the decision against bundling verticals into one DO class (D-30): a router <em>forwards</em>; deployments stay separate, and upgrade on their own schedules.</p><h2 id="the-trust-boundary" tabindex="-1">The trust boundary <a class="header-anchor" href="#the-trust-boundary" aria-label="Permalink to &quot;The trust boundary&quot;">​</a></h2><p>A vertical trusts the <code>x-substrat-*</code> headers <strong>absolutely</strong> — they name the tenant whose data it will serve. Two things keep that safe, and both are required:</p><ol><li><strong>Vertical workers have no public route</strong> (<code>workers_dev: false</code>, no route) — reachable only by service binding from the router.</li><li><strong><code>ROUTER_SECRET</code></strong>, the same value on the router and every vertical, presented as <code>x-substrat-router</code> and verified in the kernel (<code>readRoutedNode</code>).</li></ol><p>(2) exists because (1) is a deployment fact and <code>workers.dev</code> is on by default — one forgotten toggle makes (1) false with nothing in the code noticing, and the consequence is a cross-tenant read. The router also <strong>strips every inbound <code>x-substrat-*</code> header</strong> (by prefix) before setting its own, so a client cannot forge a node.</p><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>The hostname → node resolution, the trust boundary, and dispatch are built. Hostname <em>provisioning</em> is built too: <a href="https://github.com/substrat-run/substrat/tree/main/packages/control-plane-api" target="_blank" rel="noreferrer"><code>packages/control-plane-api</code></a>&#39;s custom-hostname provisioner drives the Cloudflare-for-SaaS <code>custom_hostnames</code> API end to end — <code>create</code> registers a hostname and returns the DNS records the tenant publishes, <code>check</code> polls validation + certificate state, <code>remove</code> retires it — with public-suffix enforcement (a tenant can&#39;t claim <code>co.uk</code>), wired through the control plane&#39;s <code>/hostnames</code> routes. A platform hostname is live immediately under the wildcard cert; a custom domain lands <code>pending</code> and walks the DNS-validation lifecycle, and only an <code>active</code> binding resolves. The provisioner is injected, so a self-host or dev environment with no CF-for-SaaS zone simply records the binding <code>pending</code> and never issues.</p><p>Dispatch runs both shapes. When the resolved route carries a <code>deploymentRef</code>, the router dispatches through the Workers-for-Platforms namespace (<code>env.DISPATCH.get(deploymentRef)</code>) — this is how customer-pushed verticals are reached, and it is built. The static <code>VERTICAL_&lt;SLUG&gt;</code> service-binding map — the milestone-one shape — remains as the fallback for a route with no bound version. All of it lives inside <code>verticalFor</code>; nothing else in the router changes between the two.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("platform/router.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const router = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  router as default
};
