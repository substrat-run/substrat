import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/contracts","description":"","frontmatter":{},"headers":[],"relativePath":"reference/contracts.md","filePath":"reference/contracts.md","lastUpdated":1787230840000}');
const _sfc_main = { name: "reference/contracts.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-contracts" tabindex="-1">@substrat-run/contracts <a class="header-anchor" href="#substrat-run-contracts" aria-label="Permalink to &quot;@substrat-run/contracts&quot;">​</a></h1><p>The <strong>source of truth</strong> for every data shape that crosses a Substrat boundary. Schemas are written in <a href="https://zod.dev" target="_blank" rel="noreferrer">Zod</a>, so the reviewed artifact <em>is</em> the runtime validator — OpenAPI and JSON Schema documents are emitted from this package, never hand-maintained beside it.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/contracts</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> zod</span></span></code></pre></div><h2 id="ids-ids-ts" tabindex="-1">IDs (<code>ids.ts</code>) <a class="header-anchor" href="#ids-ids-ts" aria-label="Permalink to &quot;IDs (\`ids.ts\`)&quot;">​</a></h2><p>Branded ULID types — opaque, sortable, no PII, and unmixable at compile time:</p><table tabindex="0"><thead><tr><th>Schema</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>tenantId</code></td><td><code>TenantId</code></td><td></td></tr><tr><td><code>scopeId</code></td><td><code>ScopeId</code></td><td>a <code>ScopeId</code> won&#39;t typecheck as a <code>TenantId</code></td></tr><tr><td><code>principalId</code></td><td><code>PrincipalId</code></td><td>a tenant subject</td></tr><tr><td><code>platformActorId</code></td><td><code>PlatformActorId</code></td><td>a control-plane staff subject — branded apart from <code>PrincipalId</code> so the two can&#39;t be confused</td></tr><tr><td><code>eventId</code></td><td><code>EventId</code></td><td></td></tr><tr><td><code>dataSubjectId</code></td><td><code>DataSubjectId</code></td><td>keys crypto-shredding erasure</td></tr><tr><td><code>moduleId</code></td><td><code>ModuleId</code></td><td>npm-package-shaped: <code>@substrat-run/engine-workorder</code></td></tr><tr><td><code>instant</code></td><td><code>Instant</code></td><td>ISO 8601 with timezone; stamped kernel-side</td></tr><tr><td><code>permissionKey</code></td><td><code>PermissionKey</code></td><td>module-namespaced: <code>workorder:create</code></td></tr><tr><td><code>slug</code></td><td>—</td><td>URL-safe identifier</td></tr></tbody></table><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { tenantId, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> TenantId } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/contracts&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> t</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> TenantId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> tenantId.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">parse</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(input); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// validated + branded</span></span></code></pre></div><p>IDs are deliberately meaning-free: they appear in logs and billing systems outside any jurisdiction, so they must never encode anything.</p><h2 id="tenancy-tenancy-ts" tabindex="-1">Tenancy (<code>tenancy.ts</code>) <a class="header-anchor" href="#tenancy-tenancy-ts" aria-label="Permalink to &quot;Tenancy (\`tenancy.ts\`)&quot;">​</a></h2><p><code>tenant</code> / <code>Tenant</code>, <code>scope</code> / <code>Scope</code>, plus <code>tenantStatus</code>, <code>scopeStatus</code>, <code>storageShape</code> (<code>&#39;A&#39; | &#39;B&#39;</code>) and <code>jurisdiction</code> (<code>&#39;eu&#39; | &#39;us&#39; | &#39;global&#39;</code>, plus <code>provisionableJurisdiction</code> — the subset the control plane currently accepts), and <code>createTenantInput</code>. Also <code>org</code> / <code>Org</code> and <code>createOrgInput</code> — organizations inside a tenant, which membership tuples point at and <code>grantToOrg</code> targets. <code>migrationFailure</code> on a scope is non-null when its last migration attempt failed, which is what stops a scope that serves nothing from rendering as healthy. See <a href="/concepts/tenancy.html">Tenants &amp; scopes</a>.</p><h2 id="control-plane-control-plane-ts" tabindex="-1">Control plane (<code>control-plane.ts</code>) <a class="header-anchor" href="#control-plane-control-plane-ts" aria-label="Permalink to &quot;Control plane (\`control-plane.ts\`)&quot;">​</a></h2><p><code>adminAction</code> (the enum of audited control-plane mutations) and <code>adminLogEntry</code> / <code>AdminLogEntry</code> — one append-only audit row: actor, action, target, before/after, timestamp. <code>tenantId</code> is nullable for platform-level actions that target no tenant; <code>causedBy</code> holds the id of the event that caused the action, when one did, which is what joins the two halves of the <a href="/concepts/events.html#the-connector-seam">connector seam</a>.</p><p>Identity lives here too: <code>identityLink</code>, <code>resolvedIdentity</code>, <code>identityPool</code> / <code>poolTopology</code> (<code>&#39;central&#39; | &#39;tenant-bound&#39;</code> — whether the same external subject id in two tenants is one human or two), and <code>orgMembership</code>, whose <code>revokedAt</code> is a tombstone rather than a deletion. See <a href="/concepts/platform.html">The platform layer</a> and <a href="/concepts/identity.html">Authentication &amp; identity</a>.</p><h2 id="events-events-ts" tabindex="-1">Events (<code>events.ts</code>) <a class="header-anchor" href="#events-events-ts" aria-label="Permalink to &quot;Events (\`events.ts\`)&quot;">​</a></h2><ul><li><code>entityRef</code> / <code>EntityRef</code> — the opaque <code>(entityType, entityId)</code> reference everything generic binds to.</li><li><code>piiClass</code> — <code>&#39;none&#39; | &#39;pseudonymous&#39; | &#39;direct&#39;</code>, required on every event.</li><li><code>domainEventInput</code> — what module code passes to <code>emit()</code>; origin fields deliberately absent.</li><li><code>domainEvent</code> — the full kernel-stamped envelope.</li><li><code>actor</code> — a <code>PrincipalId</code> or <code>{ system: ModuleId }</code>.</li></ul><p>The schema enforces the crypto-shredding invariant: a PII-classed event without a <code>subjectId</code> fails validation. See <a href="/concepts/events.html">Events &amp; audit</a>.</p><h2 id="permissions-permission-ts" tabindex="-1">Permissions (<code>permission.ts</code>) <a class="header-anchor" href="#permissions-permission-ts" aria-label="Permalink to &quot;Permissions (\`permission.ts\`)&quot;">​</a></h2><p>The authored surface: <code>node</code>, <code>roleDefinition</code>, <code>roleAssignment</code>, <code>capabilityGrant</code>. The evaluation representation: <code>objectRef</code>, <code>relationTuple</code> (internal to checkers). The results: <code>decision</code> / <code>Decision</code> (proof-carrying discriminated union) and <code>effectivePermissions</code>. See <a href="/concepts/permissions.html">Permissions</a>.</p><h2 id="module-manifest-manifest-ts" tabindex="-1">Module manifest (<code>manifest.ts</code>) <a class="header-anchor" href="#module-manifest-manifest-ts" aria-label="Permalink to &quot;Module manifest (\`manifest.ts\`)&quot;">​</a></h2><p><code>moduleManifest</code> / <code>ModuleManifest</code> — the self-description every module ships: permissions, events (emits/consumes), migrations + skew window, attachment targets, entity relations, entitlement key, searchables (<code>{ entityType, fields, tokenizer? }</code> — the table and id column are filled in by <code>manifestEntities</code> from the entity registry, and the kernel derives a per-scope FTS5 index from the result), UI contributions, <code>envSpec</code> (<code>envVarSpec[]</code> — the declared environment that the Dashboard&#39;s Env form renders from), and <code>schedules</code> (<code>scheduleSpec[]</code> — recurring work: <code>{ operation, cadence: { everyMinutes }, input?, permissions }</code> the platform sweep runs on every live scope under a system actor). Field-by-field walkthrough in <a href="/concepts/modules.html">Modules &amp; the manifest</a>.</p><p><code>checkSubject</code> — who a permission check is <em>about</em>: <code>{ kind: &#39;principal&#39; | &#39;connection&#39; | &#39;system&#39;, id }</code>. <code>systemGrant</code> grants a permission to a module&#39;s system principal (<code>{ moduleId, permission, node, … }</code>), the scheduler analogue of <code>connectionGrant</code>.</p><h2 id="vertical-version-registry-registry-ts" tabindex="-1">Vertical &amp; version registry (<code>registry.ts</code>) <a class="header-anchor" href="#vertical-version-registry-registry-ts" aria-label="Permalink to &quot;Vertical &amp; version registry (\`registry.ts\`)&quot;">​</a></h2><p>The registry shapes (#31): <code>vertical</code> / <code>Vertical</code> (slug, <code>source</code>, <code>ownerTenant</code>, declared <code>envSpec</code>, install spec — entitlements/grants/capabilities/surfaces), <code>verticalVersion</code> (digests + admission status — so <em>push is not live</em>), and the channel records promotion moves a scope between. This is what turns the scope&#39;s nullable <code>vertical</code> string into a pinnable record. See <a href="/concepts/platform.html">The platform layer</a>.</p><h2 id="deploy-manifest-deploy-ts" tabindex="-1">Deploy manifest (<code>deploy.ts</code>) <a class="header-anchor" href="#deploy-manifest-deploy-ts" aria-label="Permalink to &quot;Deploy manifest (\`deploy.ts\`)&quot;">​</a></h2><p><code>deployManifest</code> — the JSON a <code>substrat push</code> sends beside the module files (D-39), re-parsed at the control-plane trust boundary and run through the §4 sandbox contract. Carries <code>runtimeNeeds</code> (own-DO <code>storeNeed</code>s, per-tenant <code>tenantStoreNeed</code>s), the permission registry, and <code>RUNTIME_BASELINE</code>. One schema, two parses, no drift.</p><h2 id="connections-connections-ts" tabindex="-1">Connections (<code>connections.ts</code>) <a class="header-anchor" href="#connections-connections-ts" aria-label="Permalink to &quot;Connections (\`connections.ts\`)&quot;">​</a></h2><p>The integrations hub&#39;s store (#101): <code>connection</code> / <code>Connection</code> and <code>connectionStatus</code>, keyed on <strong>(tenant, vertical, provider)</strong> so one vendor&#39;s host code never reaches another&#39;s credential. Everything here is metadata — the sealed credential never appears in these shapes. See <a href="/concepts/events.html#the-connector-seam">The connector seam</a>.</p><h2 id="scope-introspection-introspection-ts" tabindex="-1">Scope introspection (<code>introspection.ts</code>) <a class="header-anchor" href="#scope-introspection-introspection-ts" aria-label="Permalink to &quot;Scope introspection (\`introspection.ts\`)&quot;">​</a></h2><p>The read-only &quot;Data&quot; window into a scope&#39;s own database (kernel-design §5.4): <code>scopeTable</code>, <code>scopeTablePage</code> / <code>readScopeTableInput</code>, and the free-SQL <code>queryScope</code> shapes — table-shaped and bounded, no write path to forge the spine.</p><h2 id="routing-routing-ts" tabindex="-1">Routing (<code>routing.ts</code>) <a class="header-anchor" href="#routing-routing-ts" aria-label="Permalink to &quot;Routing (\`routing.ts\`)&quot;">​</a></h2><p>The hostname map (K-26): <code>RouteTarget</code> (<code>hostname → (tenant, scope, vertical, surface, region)</code> — what the environment router resolves), <code>hostnameBinding</code> / <code>HostnameStatus</code> and its issuance/DNS records, plus <code>declaredSurface</code> and <code>surfaceName</code> (a scope fronts many surfaces — storefront + back office — from one database).</p><h2 id="openapi-openapi-ts" tabindex="-1">OpenAPI (<code>openapi.ts</code>) <a class="header-anchor" href="#openapi-openapi-ts" aria-label="Permalink to &quot;OpenAPI (\`openapi.ts\`)&quot;">​</a></h2><p><code>ApiCatalog</code> / <code>ApiOperationDoc</code> and the builder that renders a vertical&#39;s operation catalog — the same Zod schemas its handlers parse — as an OpenAPI 3.1 document, so the published contract cannot drift from the enforcement (D-22).</p><h2 id="money-money-ts" tabindex="-1">Money (<code>money.ts</code>) <a class="header-anchor" href="#money-money-ts" aria-label="Permalink to &quot;Money (\`money.ts\`)&quot;">​</a></h2><p><code>money</code> / <code>Money</code> (decimal-string amount + ISO 4217 currency, both branded) and the sanctioned arithmetic: <code>addMoney</code>, <code>mulMoney</code>, <code>moneyOf</code>, <code>addDecimal</code>, <code>mulDecimal</code>, <code>compareDecimal</code> — exact micro-unit (6 dp) bigint arithmetic, half-up rounding. See <a href="/concepts/money.html">Money</a>.</p><h2 id="attachments-attachments-ts" tabindex="-1">Attachments (<code>attachments.ts</code>) <a class="header-anchor" href="#attachments-attachments-ts" aria-label="Permalink to &quot;Attachments (\`attachments.ts\`)&quot;">​</a></h2><p><code>visibility</code> — <code>&#39;internal&#39; | &#39;customer&#39;</code>, the mandatory classification on every attachment item that could reach a customer portal.</p><h2 id="versioning" tabindex="-1">Versioning <a class="header-anchor" href="#versioning" aria-label="Permalink to &quot;Versioning&quot;">​</a></h2><p>The package is semver&#39;d and every event and manifest carries explicit schema versions. Pre-1.0, shapes change without notice; from the first shipped vertical onward, breaking changes to emitted schemas are CI-diffed and linted.</p><h2 id="the-model" tabindex="-1">The model <a class="header-anchor" href="#the-model" aria-label="Permalink to &quot;The model&quot;">​</a></h2><p>One TypeScript module declares a vertical&#39;s entities, operations and permissions; the compiler checks the joins between them. Full walkthrough in <a href="/concepts/model.html">The model</a>.</p><table tabindex="0"><thead><tr><th>export</th><th>what it does</th></tr></thead><tbody><tr><td><code>defineEntities</code></td><td>declares entities — <code>table</code>, <code>fields</code>, <code>parents</code>, <code>key</code>, <code>erasable</code>. <code>parents</code> is checked against the map&#39;s own keys; <code>key</code> and <code>erasable</code> against each entity&#39;s own fields</td></tr><tr><td><code>defineOperations(entities, permissions, engines?)</code></td><td>declares operations against those entities, a declared permission set, and any composed engine registries</td></tr><tr><td><code>manifestEntities(entities, refs)</code></td><td>composes the entity-referencing manifest fragments; derives <code>entityRelations</code> from each entity&#39;s <code>parents</code></td></tr><tr><td><code>permissionsUsedBy</code> · <code>eventsEmittedBy</code></td><td>derive the manifest&#39;s <code>permissions</code> and <code>events.emits</code> from the operations</td></tr><tr><td><code>emitModel</code></td><td>renders the registry to deterministic JSON — the artifact <code>pnpm lint:model --check</code> gates</td></tr><tr><td><code>EntityRow&lt;T, K&gt;</code></td><td>a declared entity&#39;s row type, for <code>ctx.sql.query&lt;…&gt;</code></td></tr><tr><td><code>OperationImpl&lt;Ops, Ctx&gt;</code></td><td>the handler map an operation set requires; bind with <code>satisfies</code></td></tr><tr><td><code>journalColumns(sql)</code></td><td>test tooling — columns per table from a migration journal, following <code>ADD COLUMN</code>, <code>DROP TABLE</code> and <code>RENAME TO</code></td></tr></tbody></table><p><code>model.json</code> is for consumers that must not execute your code, or that want diffability. A code generator reads the TypeScript: <code>z.toJSONSchema</code> drops <code>.refine()</code> and <code>.brand()</code>, so a generator reading the JSON would emit validators weaker than you declared.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/contracts.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const contracts = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  contracts as default
};
