import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/adapter-cloudflare","description":"","frontmatter":{},"headers":[],"relativePath":"reference/adapter-cloudflare.md","filePath":"reference/adapter-cloudflare.md","lastUpdated":1787048747000}');
const _sfc_main = { name: "reference/adapter-cloudflare.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-adapter-cloudflare" tabindex="-1">@substrat-run/adapter-cloudflare <a class="header-anchor" href="#substrat-run-adapter-cloudflare" aria-label="Permalink to &quot;@substrat-run/adapter-cloudflare&quot;">​</a></h1><p>The <strong>Durable-Object scope host</strong> — the production backing for the same scope-host contract the <a href="/reference/adapter-sqlite.html">pure-SQLite adapter</a> implements. One SQLite-backed Durable Object per scope, a durable control-plane DO for the directory, and a stateless coordinator that mints capability stubs.</p><p>Both adapters pass the <strong>same</strong> <a href="/reference/contract-tests.html">conformance suite</a> unchanged (decision 14), so a vertical developed and CI-tested on pure SQLite runs on Cloudflare with no code change. Several verticals run deployed on it today, <a href="/concepts/identity.html">OIDC-only</a> against a shared relying party.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/adapter-cloudflare</span></span></code></pre></div><h2 id="the-pieces" tabindex="-1">The pieces <a class="header-anchor" href="#the-pieces" aria-label="Permalink to &quot;The pieces&quot;">​</a></h2><ul><li><strong><code>ScopeDO</code></strong> — one scope = one SQLite-backed Durable Object. <code>defineScopeDO([…modules])</code> bundles the kernel, engines, and the vertical&#39;s modules into the DO at build time (a DO can&#39;t receive handler closures over RPC). Each operation runs inside <code>ctx.storage.transaction(async …)</code> — the DO analogue of <code>BEGIN IMMEDIATE … COMMIT</code>, which rolls back on a throw <em>across <code>await</code>s</em> — with strict per-scope serialization, lazy migrations on wake, manifest guards, and the outbox→consumer dispatch loop.</li><li><strong><code>ControlPlaneDO</code></strong> — the durable directory: tenants, scope lifecycle, roles, entitlements, identities, tenant-level tuples, and the append-only admin audit log. Under scope-local permissions (below) it is a <strong>write-time</strong> authority that projects into scopes, not a read-time dependency — and a CP-less vertical binds no control plane at all.</li><li><strong><code>CloudflareScopeHost</code></strong> — the coordinator. Stateless (rebuilt per request); it validates and gates against the control plane, then mints a <code>ScopeStub</code> that RPCs <code>invoke</code> into the scope&#39;s DO. Implements the same <code>ScopeHost</code> contract as the pure adapter. Its <code>controlPlane</code> binding is <strong>optional</strong>: omit it (with <code>scopeLocalPermissions</code>) for a CP-less vertical that provisions via <code>provisionScopeLocal</code> and trusts the router-asserted node.</li><li><strong><code>createRouteResolver</code></strong> (the <code>@substrat-run/adapter-cloudflare/routing</code> subpath) — resolves an inbound <code>hostname → RouteTarget</code> (tenant, scope, <code>deploymentRef</code>, <code>verticalSlug</code>) for Workers-for-Platforms dynamic dispatch. The environment <a href="/platform/router.html">router</a> is built on it.</li><li><strong><code>definePlatformSweeperDO</code></strong> — the alarm-driven Durable Object that runs the platform&#39;s scheduled maintenance. A singleton (<code>PLATFORM_SWEEPER_NAME</code>) whose <code>alarm()</code> runs one <code>runPlatformSweep</code> pass — reaping expired previews, an archived scope&#39;s DO storage, and connection reconciliations — then re-arms itself. A DO alarm, not a cron, because a hosted vertical pushed into a dispatch namespace gets no <code>triggers.crons</code>; <code>ensureArmed()</code> is idempotent and self-arms from code. The workerd analogue of the kernel&#39;s node-side <code>startPlatformSweeper</code>.</li><li><strong><code>defineScopeSweeperDO</code></strong> — the <em>vertical&#39;s own</em> timer, and the piece a CP-less vertical cannot do without. Same singleton shape (<code>SCOPE_SWEEPER_NAME</code>, one alarm, one pass at a time), but the roster is this deployment&#39;s scopes and the pass is the work that must happen inside them: draining retryable executor deliveries and firing the <a href="/concepts/modules.html#recurring-work-schedules">schedules</a> the manifest declares. The platform sweeper reaps and reconciles from above; this one runs <em>your</em> recurring work where your data is.</li><li><strong>Per-tenant stores</strong> — <code>createD1TenantStores</code> and <code>createR2BlobStores</code>, the platform&#39;s reach into a minted D1 database or R2 bucket. Worth understanding for what they are <em>not</em>: this is the <strong>control-plane</strong> path — minting at provision, out-of-band SQL, ops inspection. Request-time access is not HTTP at all. The serving script carries a real <code>d1</code> / <code>r2_bucket</code> binding per store, named by the contracts helpers, attached by the control plane at provision and re-derived from the ledger on every serving upload. A vertical is <em>handed</em> a store; it never mints one, and never learns a database id or bucket name.</li></ul><h2 id="usage-a-worker" tabindex="-1">Usage (a Worker) <a class="header-anchor" href="#usage-a-worker" aria-label="Permalink to &quot;Usage (a Worker)&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { defineScopeDO, ControlPlaneDO, CloudflareScopeHost } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/adapter-cloudflare&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { workorderModule } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/engine-workorder&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> ScopeDO</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> defineScopeDO</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">([workorderModule </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/*, …engines, vertical */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { ControlPlaneDO };</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> default</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  async</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> fetch</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">req</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">env</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> host</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> CloudflareScopeHost</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({ scope: env.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">SCOPE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, controlPlane: env.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">CONTROL_PLANE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> });</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // authenticate → getScope → invoke (the Callout demo wires a full Hono API + Better Auth)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> stub</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">getScope</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(principal, tenantId, scopeId);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> Response.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">json</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> stub.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">invoke</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder/list&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, {}));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">};</span></span></code></pre></div><p><code>wrangler.jsonc</code> binds <code>SCOPE</code> + <code>CONTROL_PLANE</code> as SQLite-backed Durable Objects (<code>new_sqlite_classes</code>). Run it on real <code>workerd</code> with <code>wrangler dev</code> (no account needed); deploy with <code>wrangler deploy</code> (DO SQLite needs a Workers Paid plan).</p><p>Two options change the topology:</p><ul><li><strong><code>scopeLocalPermissions: true</code></strong> turns on projection-on-write — the host projects a tenant&#39;s roles and tenant-level tuples into its scopes on every tenant-level write, and those scopes evaluate permissions from their own storage. This takes the control-plane DO off the request hot path (see <a href="/concepts/permissions.html#where-tuples-live-a-scope-reads-only-its-own-state">Permissions</a>). Default off. Enabling it for scopes provisioned earlier wants a one-time <code>reconcileTenantProjection</code> back-fill.</li><li><strong>omitting <code>controlPlane</code></strong> makes a <strong>CP-less</strong> vertical: it binds no control-plane DO, holds its role definitions locally, receives only scope-level assignments, provisions via <code>provisionScopeLocal</code>, and trusts the router-asserted node for tenancy/lifecycle. This is what lets a vertical deploy as its own isolated Workers-for-Platforms script with no platform binding — the shape <a href="/verticals/callout.html">Callout</a> ships in.</li></ul><h2 id="how-the-semantics-map" tabindex="-1">How the semantics map <a class="header-anchor" href="#how-the-semantics-map" aria-label="Permalink to &quot;How the semantics map&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Contract guarantee</th><th>Implementation here</th></tr></thead><tbody><tr><td>strict serialization per scope (K-6)</td><td>per-DO operation queue — the DO input gate over-delivers, so serialization is enforced explicitly</td></tr><tr><td>scope storage isolation</td><td>one SQLite-backed DO per scope</td></tr><tr><td>transactional operation + rollback (K-4)</td><td><code>ctx.storage.transaction(async …)</code> — commits on success, rolls back on a throw across <code>await</code>s</td></tr><tr><td>structured-clone boundary</td><td>the coordinator→DO RPC boundary itself</td></tr><tr><td>fail-closed addressing + lifecycle gates</td><td>validated in the <code>ControlPlaneDO</code> before the stub is minted</td></tr><tr><td>permission checks</td><td>tuple checker, evaluated entirely from the scope&#39;s own storage — scope tuples written locally, tenant tuples + roles <strong>projected</strong> in by the control plane at write time (<code>scopeLocalPermissions</code>); no per-request control-plane read</td></tr></tbody></table><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>The shared contract suites run <strong>green in workerd against real Durable Objects</strong> (one runtime-late-registration test is skipped — a deployed DO bundle is code-time), the control plane is durable, and a fleet of verticals is deployed on it. Landed since this page first listed them as deferred:</p><ul><li><strong>Scope-local permissions</strong> (<code>scopeLocalPermissions</code>, <code>provisionScopeLocal</code>) — the control plane off the request hot path, projection-on-write, and a CP-less host mode.</li><li><strong>The <code>hostname → (tenant, scope, deploymentRef)</code> router</strong> — <code>createRouteResolver</code>, feeding Workers-for-Platforms dynamic dispatch, with custom domains issued end to end.</li><li><strong>Per-tenant D1 and R2</strong> — minted by the platform at provision, handed to the vertical as a binding.</li><li><strong>Both sweepers</strong> — the platform&#39;s maintenance pass and the vertical&#39;s own scope sweep, each an alarm-driven singleton because a dispatch-namespace script gets no crons.</li><li><strong>The outbound seams</strong> — connector dispatch riding platform intents, the declared egress allowlist, and the platform email relay, so a CP-less vertical reaches the outside world without ever holding a credential.</li></ul><p>Still deferred, honestly:</p><ul><li>the directory is a single control-plane DO; the tenant-root-DO + global-D1 split (kernel-design §3.2) is a later scaling/blast-radius refinement, and the many-scope fan-out cost of projection for the platform&#39;s own tenant is an explicit open question;</li><li>per-jurisdiction DO ids (K-7) are not built yet;</li><li>Shape B (DO control plane fronting per-tenant D1) is not built.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/adapter-cloudflare.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const adapterCloudflare = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  adapterCloudflare as default
};
