import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Kallkälla (coffee shop)","description":"","frontmatter":{},"headers":[],"relativePath":"verticals/shop.md","filePath":"verticals/shop.md","lastUpdated":1784804439000}');
const _sfc_main = { name: "verticals/shop.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="kallkalla-coffee-shop" tabindex="-1">Kallkälla (coffee shop) <a class="header-anchor" href="#kallkalla-coffee-shop" aria-label="Permalink to &quot;Kallkälla (coffee shop)&quot;">​</a></h1><p><code>demos/shop</code> — a small-batch coffee roaster in Stockholm (Kallkälla Kaffe AB) running a web shop: beans (whole/ground × 250 g/1 kg) and brewing gear, checkout <strong>mot faktura</strong> (on invoice).</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Kallkälla is deliberately the <em>third</em> vertical, doing a different job than the work-order shops (<a href="/verticals/callout.html">Callout</a>, <a href="/verticals/handlebar.html">Handlebar</a>). It proves three things they can&#39;t:</p><ul><li><strong>A published engine reused across a genuinely different domain.</strong> <a href="/engines/invoicing/"><code>invoicing</code></a> builds an invoice basis from a <strong>retail order</strong>, not a service work order — the same snapshot-not-join consumer, fed by a different event. Star-topology reuse, outside the domain the engine was extracted from.</li><li><strong>Additive engine evolution, on stage.</strong> Invoicing learns a <em>second</em> input event (<code>commerce.order-placed</code>) <strong>additively</strong> — a new <code>consumes</code> entry and a second consumer with its own Zod parse — while the existing <code>workorder.completed</code> path stays frozen and untouched. This is the <a href="/concepts/modules.html">additive-evolution rule</a> demonstrated, not asserted.</li><li><strong>A new invariant shape: no oversell.</strong> Not a state machine — a <strong>reservation ledger</strong>. You cannot sell more than exists.</li></ul><h3 id="the-no-oversell-invariant" tabindex="-1">The no-oversell invariant <a class="header-anchor" href="#the-no-oversell-invariant" aria-label="Permalink to &quot;The no-oversell invariant&quot;">​</a></h3><p><code>available = on_hand − Σ(active reservations)</code>. A cart add succeeds only if <code>available ≥ qty</code>, else it throws out-of-stock. Reservations are lines on an <strong>open cart</strong>; expiry is <strong>lazy</strong> — elapsed holds are excluded on read and swept opportunistically on the next write, so there is no timer or cron (default hold 900 s). Checkout re-verifies <code>on_hand</code> in case a hold lapsed.</p><p>It is enforced in the vertical&#39;s <strong>own commerce module</strong>, not an engine — and it is atomic for the same reason RallyPoint&#39;s booking is: operations serialize per scope (K-6), so &quot;read available, then reserve&quot; never interleaves. This is the extraction seam for a future <code>engine-inventory</code> / <code>engine-order</code> — named for the <em>second</em> retail vertical (decision 27), not built ahead of one. Its companion invariant: an order is <strong>immutable after placement</strong> (<code>cart → placed → fulfilled → closed</code>, no skips).</p><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>@substrat-run/demo-shop</code></td></tr><tr><td><strong>Engines composed</strong></td><td><a href="/engines/invoicing/"><code>invoicing</code></a> (+ the vertical&#39;s own commerce module)</td></tr><tr><td><strong>Own tables</strong></td><td><code>shop_products</code> · <code>shop_variants</code> · <code>shop_stock</code> · <code>shop_customers</code> · <code>shop_discounts</code> · <code>shop_carts</code> · <code>shop_cart_lines</code> · <code>shop_orders</code> · <code>shop_order_lines</code></td></tr><tr><td><strong>Roles</strong></td><td><code>public</code> (browse) · <code>shopper</code> (browse + checkout) · <code>warehouse</code> (fulfil, read, stock) · <code>shop-admin</code> (all) — portal customers hold an entity-narrowed <code>order:read</code> grant</td></tr><tr><td><strong>Permission surface</strong></td><td><a href="https://github.com/substrat-run/substrat/blob/main/demos/shop/PERMISSIONS.md" target="_blank" rel="noreferrer"><code>PERMISSIONS.md</code></a> — 10 keys, 2 modules, 4 roles</td></tr><tr><td><strong>Auth</strong></td><td><a href="/concepts/identity.html#in-the-demo">Better Auth</a> — email/password, its own tenant-bound store, plus an anonymous browse-only fallback</td></tr><tr><td><strong>Apps</strong></td><td><strong>three</strong> processes over one API: API (<code>:8873</code>), storefront (<code>:5273</code>), back-office (<code>:5274</code>, <code>ADMIN_PORT</code>)</td></tr><tr><td><strong>Status</strong></td><td>Working — demo seed</td></tr></tbody></table><h2 id="two-audiences-one-source-of-truth" tabindex="-1">Two audiences, one source of truth <a class="header-anchor" href="#two-audiences-one-source-of-truth" aria-label="Permalink to &quot;Two audiences, one source of truth&quot;">​</a></h2><p>The storefront and the back-office are <strong>separate Vite apps</strong> — different chrome, different audience — both proxying <code>/api</code> to <strong>one</strong> API, so every action runs the same kernel permission check behind both. The split is presentation and audience, never a second source of truth. This is the sharpest illustration in the repo that &quot;customer-facing&quot; and &quot;staff-facing&quot; are surfaces over one authority.</p><h2 id="the-cast-what-s-denied" tabindex="-1">The cast &amp; what&#39;s denied <a class="header-anchor" href="#the-cast-what-s-denied" aria-label="Permalink to &quot;The cast &amp; what&#39;s denied&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Who</th><th>Holds</th><th>Cannot</th></tr></thead><tbody><tr><td><strong>Astrid</strong></td><td><code>shop-admin</code>, tenant level</td><td>— (catalog, prices, stock, discounts, fulfil, invoicing)</td></tr><tr><td><strong>Gustav</strong></td><td><code>warehouse</code></td><td><strong>set prices</strong>, create discounts, or touch <strong>invoicing</strong> — he adjusts stock and fulfils orders</td></tr><tr><td><strong>Elin</strong> — Café Pascal</td><td>entity-narrowed <code>order:read</code> on her own customer</td><td>see Otto&#39;s orders, write anything, or invoice — she sees only her own orders</td></tr><tr><td><strong>Otto</strong> — Kontoret</td><td>the same, for his customer</td><td>see Elin&#39;s orders</td></tr><tr><td><em>(not logged in)</em></td><td><code>public</code> — browse-only</td><td>anything but browsing the catalogue — a thin role on the same code path, not an auth bypass</td></tr><tr><td><strong>Rurik</strong> — admin of rival tenant <em>Bönfeber</em></td><td><code>shop-admin</code> in his own tenant</td><td>anything of Kallkälla&#39;s — the cross-tenant denial</td></tr></tbody></table><p>Signing in as Gustav and watching <em>Invoice basis</em> disappear from the nav — and 403 if you ask for it directly — is the whole thesis in one click: <strong>Better Auth authenticated you, the kernel authorized you.</strong></p><h2 id="run-it" tabindex="-1">Run it <a class="header-anchor" href="#run-it" aria-label="Permalink to &quot;Run it&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/demo-shop</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># API         http://localhost:8873</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># storefront  http://localhost:5273</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># back-office  http://localhost:5274</span></span></code></pre></div><p>Two suites: <code>test/scenario.test.ts</code> (the oversell throw on the last-unit race, checkout mot faktura with a discount, frozen order lines, the invoicing underlag with provenance, lazy TTL release, the no-skip state machine, and every denial vector) and <code>test/provision.test.ts</code>.</p><h2 id="deliberately-out-of-scope" tabindex="-1">Deliberately out of scope <a class="header-anchor" href="#deliberately-out-of-scope" aria-label="Permalink to &quot;Deliberately out of scope&quot;">​</a></h2><p>Payment capture (Swish/card), tax, shipping rates, multi-warehouse, and returns — a card/Swish order would settle through a deferred payment connector, and only <strong>mot faktura</strong> produces an invoice basis. The extracted inventory/order engines wait on a second retail vertical.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("verticals/shop.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const shop = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  shop as default
};
