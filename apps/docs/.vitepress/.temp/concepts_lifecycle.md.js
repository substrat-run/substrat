import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Lifecycles","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/lifecycle.md","filePath":"concepts/lifecycle.md","lastUpdated":1787314487000}');
const _sfc_main = { name: "concepts/lifecycle.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="lifecycles" tabindex="-1">Lifecycles <a class="header-anchor" href="#lifecycles" aria-label="Permalink to &quot;Lifecycles&quot;">​</a></h1><p>An entity with a <code>status</code> column has a state machine. Before #844 that machine was written down twice: once as the column&#39;s enum, and once as guards scattered through the operations that move it.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// the enum, in entities.ts</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">status</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">enum</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">([</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;planned&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;in_progress&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;completed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;closed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]),</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// the machine, in index.ts — six call sites, held to that enum by nothing</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">requireStatus</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(row, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;planned&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;in_progress&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span></code></pre></div><p>A <strong>lifecycle</strong> is that machine, declared once, beside the entities and operations it already draws on.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { defineLifecycles } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/contracts&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> workorderLifecycles</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> defineLifecycles</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  workorderEntities,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  workorderOperations,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  workorder: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    field: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;status&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    initial: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;planned&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    states: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      planned: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        on: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder/start&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;in_progress&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        allow: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder/assign&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder/report-time&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder/report-material&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      in_progress: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        on: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder/complete&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;completed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        allow: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder/report-time&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder/report-material&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        extensible: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      completed: { on: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder/close&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;closed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> } },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      closed: { terminal: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><h2 id="on-and-allow-are-different-things" tabindex="-1"><code>on</code> and <code>allow</code> are different things <a class="header-anchor" href="#on-and-allow-are-different-things" aria-label="Permalink to &quot;\`on\` and \`allow\` are different things&quot;">​</a></h2><p><code>on</code> is an <strong>edge</strong>: the operation moves the entity to the named state.</p><p><code>allow</code> is a <strong>precondition</strong>: the operation is legal in this state and changes nothing.</p><p>The distinction is not cosmetic, and it came from counting. Of the nine <code>requireState</code> call sites in the booking engine, several gate operations that move no state at all — attaching a note to a reservation checks that it is <code>held</code> or <code>confirmed</code> and leaves it exactly where it was. A format with only edges would have described the majority of those guards as transitions, and the emitted diagram would have grown a self-loop for every note anyone can write.</p><p><strong>Absence means &quot;not governed&quot;, never &quot;forbidden&quot;.</strong> <code>workorder/get</code> appears nowhere above because no state gates a read. A lifecycle describes which mutations a state admits; an operation it has never heard of is simply not its business.</p><h2 id="what-the-compiler-checks" tabindex="-1">What the compiler checks <a class="header-anchor" href="#what-the-compiler-checks" aria-label="Permalink to &quot;What the compiler checks&quot;">​</a></h2><p>The declaration is checked against the two things it names, which is the whole reason it is TypeScript rather than a schema file:</p><table tabindex="0"><thead><tr><th>You write</th><th>It is checked against</th></tr></thead><tbody><tr><td><code>field: &#39;status&#39;</code></td><td>the entity&#39;s own fields</td></tr><tr><td>a state name</td><td>the values that field can hold</td></tr><tr><td><code>initial</code></td><td>the declared states</td></tr><tr><td>an edge target</td><td>the declared states</td></tr><tr><td>an operation id</td><td>the module&#39;s declared operations</td></tr></tbody></table><p>Two of those bite in both directions. A state the column cannot hold is refused — and so is a value the column <em>can</em> hold that the machine has never heard of, which is where an entity goes to get stuck. Add a fifth value to the enum and forget the machine, and the build fails.</p><div class="tip custom-block"><p class="custom-block-title">Why one check runs at runtime</p><p>A <code>states</code> object carrying a key the enum does not have compiles clean however the constraint is written — TypeScript applies no excess-property check when a value satisfies a generic constraint. That check therefore runs at module load, where it can actually bite. A check that reads like it works and does not is worse than an absent one.</p></div><h2 id="what-a-lifecycle-cannot-say" tabindex="-1">What a lifecycle cannot say <a class="header-anchor" href="#what-a-lifecycle-cannot-say" aria-label="Permalink to &quot;What a lifecycle cannot say {#what-a-lifecycle-cannot-say}&quot;">​</a></h2><p>The omissions are the design. There are no actions, no effects, no <code>context</code>, no parallel regions, no timers and no expression language.</p><p>An edge names the operation that performs it and stops. The operation keeps its body, its permission check, its writes and its events. The moment an edge can carry a condition, this is BPMN in TypeScript — <a href="/guide/comparisons.html">the tarpit</a> the platform named in the same breath as adopting durable execution.</p><p><strong>Durable execution is a separate concern.</strong> Retries, sleeps and fan-out are the outbox and the sweeper, not this. A lifecycle says which states exist and which operation moves between them. It never says <em>when</em>, and it never runs anything.</p><p><strong>Guards are not declared here either.</strong> A guard is wired in the manifest as <code>{ before, predicate, config }</code> and evaluated by the kernel inside the guarded operation&#39;s own transaction. Every edge names its operation, so a guard on the operation already <em>is</em> a guard on the edge — the emitters join the two rather than making anyone write it twice.</p><h2 id="substates" tabindex="-1">Substates <a class="header-anchor" href="#substates" aria-label="Permalink to &quot;Substates&quot;">​</a></h2><p>A state marked <code>extensible: true</code> admits vertical substates: <code>in_progress</code> refines into <code>awaiting_parts</code> or <code>pending_customer_approval</code> without the engine learning either word. Transitions <em>within</em> an engine state are the vertical&#39;s; transitions <em>between</em> engine states stay the engine&#39;s, so no substate path can skip <code>completed</code>.</p><p>An invariant-bearing state declares nothing. <code>completed</code> and <code>closed</code> carry billing consequences, and the absence of the flag is how they say so.</p><h2 id="what-it-is-used-for" tabindex="-1">What it is used for <a class="header-anchor" href="#what-it-is-used-for" aria-label="Permalink to &quot;What it is used for&quot;">​</a></h2><p><strong>Enforcement.</strong> <code>assertTransition</code> replaces every hand-written guard and throws the platform&#39;s own <code>conflict</code> with <code>reason: &#39;invalid_transition&#39;</code> — which is also how two demos that were throwing a bare <code>Error</code>, and returning a 500 where every engine returns a 409, came back onto the contract.</p><p><strong>Review.</strong> <code>pnpm lint:model</code> re-emits the machine into <code>model.json</code>, and CI re-emits with <code>--check</code>. A redirected edge or a state that stops admitting substates has to appear in a PR diff, the same way <a href="/concepts/permissions.html"><code>lint:permissions</code></a> makes a widened role appear in one. Widening a state machine is no less consequential than widening a role, and until now it was a one-line change to an <code>if</code>.</p><p><strong>Derivation.</strong> The machine emits to an <a href="https://stately.ai/docs" target="_blank" rel="noreferrer">XState v5</a> config — states, edges, nested substates, <code>type: &#39;final&#39;</code>, and guards joined in from the manifest. That is a one-way emit for diagrams and for a test oracle: XState&#39;s <code>transition()</code> is a pure function, so it can confirm the emitted machine and the engine&#39;s own guard agree on every state/event pair without XState ever leaving devDependencies.</p><div class="warning custom-block"><p class="custom-block-title">The emitted machine is not a source</p><p>Editing it in a visual editor and reading it back would make that editor a second authoring surface. Anything checked in belongs behind a <code>--check</code> re-emit, so an edit goes red rather than becoming the truth.</p></div><h2 id="adopting-one" tabindex="-1">Adopting one <a class="header-anchor" href="#adopting-one" aria-label="Permalink to &quot;Adopting one&quot;">​</a></h2><ol><li>Declare it in a file that imports the entities and the operations — not in <code>entities.ts</code>, which the operations already import and which would close the loop into a cycle.</li><li>Replace each hand-written guard with a call naming the <strong>verb</strong> rather than re-deriving the set of states that permits it.</li><li>Export an emitted model so <code>lint:model</code> picks it up:</li></ol><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> workorderModel</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> emitModel</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(workorderEntities, { lifecycles: workorderLifecycles });</span></span></code></pre></div><p>Adopted: <code>engine-workorder</code>, <code>engine-booking</code>, <code>engine-invoicing</code>, <code>demos/manyfold</code>, <code>demos/shop</code>.</p><p>Booking is worth reading as the harder example: seven states, three operations that are <code>allow</code> rather than edges, a state reachable by lapse rather than by transition, and a transition performed by composition. It needed no addition to the format.</p><p>Two are deliberately not adopted:</p><ul><li><strong><code>engine-protocol</code></strong> gates content mutation, not just transitions, and each of its refusals carries its own reason — <code>content_frozen</code>, <code>wrong_status</code>, <code>already_voided</code>. Routing those through <code>assertTransition</code> would flatten three useful answers into one vaguer one. It needs the <code>transitionFor</code> treatment below, applied case by case.</li><li><strong><code>engine-absence</code></strong> has a machine (<code>requested → approved | rejected | cancelled</code>) and no entity to hang it on: its registry declares <code>absence_leave_types</code> and nothing else, so <code>absence_requests</code> is not a declared entity. Registering it comes first.</li></ul><h2 id="when-the-module-has-a-better-reason-than-invalid-transition" tabindex="-1">When the module has a better reason than &quot;invalid transition&quot; <a class="header-anchor" href="#when-the-module-has-a-better-reason-than-invalid-transition" aria-label="Permalink to &quot;When the module has a better reason than &quot;invalid transition&quot;&quot;">​</a></h2><p><code>assertTransition</code> is the default. Where a module&#39;s own refusal says more, ask the declaration the <em>legality</em> question with <code>transitionFor</code> and keep your own reason:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">transitionFor</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(invoicingLifecycles.underlag, underlag.status, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;invoicing/export&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  throw</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> conflict</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;immutable_after_export&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">\`underlag \${</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">underlag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">number</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">} is &#39;\${</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">underlag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">status</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">}&#39; …\`</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>The declaration stays load-bearing — it is still the single description of what is legal — and the caller still hears the invariant that actually stopped them.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/lifecycle.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const lifecycle = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  lifecycle as default
};
