import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderSuspense, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Meridian (HR)","description":"","frontmatter":{},"headers":[],"relativePath":"verticals/meridian.md","filePath":"verticals/meridian.md","lastUpdated":1787048747000}');
const _sfc_main = { name: "verticals/meridian.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="meridian-hr" tabindex="-1">Meridian (HR) <a class="header-anchor" href="#meridian-hr" aria-label="Permalink to &quot;Meridian (HR)&quot;">​</a></h1><p><code>demos/meridian</code> — a multi-country <strong>HR vertical</strong>: leave and absence, project time reporting, expenses, onboarding, and the <strong>anställningsavtal</strong>, in <strong>one role-adaptive app</strong> that is an employee&#39;s self-service tool and a manager&#39;s console at once.</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Meridian is the deliberate <strong>shape-breaker</strong>. Every other demo leans on a domain engine for the work at its centre; Meridian has <em>none to lean on</em> — there is no absence engine, no time-tracking engine, no expenses engine. So leave/absence, time reporting, and expenses are all <strong>vertical code</strong>, and that is the point: it proves the kernel&#39;s guarantees — nested tenancy, permissions, audit, GDPR — hold with <strong>zero engine support</strong>, and that Substrat&#39;s value isn&#39;t quietly borrowed from the work-order state machine.</p><p>It&#39;s interesting for three reasons the field-service demos can&#39;t show:</p><ul><li><strong>A core domain with no engine.</strong> The kernel carries leave, time and expenses alone. It does compose one engine — <a href="/engines/protocol/"><code>protocol</code></a>, for onboarding checklists <strong>and the employment contract</strong> — but that sits beside the work rather than under it, and is itself the third proof of protocol reuse.</li><li><strong>Two countries from one codebase.</strong> Sweden (25 days + saved days, VAB) and Spain (22 days, <em>registro de jornada</em>) are two scopes under one tenant, diverging by <strong>data and per-scope config</strong>, not a fork.</li><li><strong>One app, two audiences.</strong> An employee sees only their own record; a team lead who is <em>also</em> an employee gets a <strong>Manage</strong> section beside their own <strong>My work</strong> — the same app, the same permission checks, adapting to who signs in.</li></ul><p>And it surfaces the platform&#39;s next engine: the <strong>absence / entry-ledger</strong> candidate (<a href="/engines/#engines-today">engines</a>) — because its absence ledger and its time ledger are the <em>same append-only shape</em> the work-order engine already owns.</p><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>demos/meridian</code> (private)</td></tr><tr><td><strong>Tenancy shape</strong></td><td>2 tenants — Nordljus AB (Sweden + Spain scopes) · Solmark AB (the cross-tenant attack victim)</td></tr><tr><td><strong>Engines composed</strong></td><td><a href="/engines/protocol/"><code>protocol</code></a> — onboarding checklists <strong>and</strong> the anställningsavtal, both bound to an <code>employee</code> ref</td></tr><tr><td><strong>Own tables</strong></td><td><code>hr_employees</code> · <code>hr_employment_terms</code> (append-only) · <code>hr_leave_types</code> · <code>hr_absence_ledger</code> (append-only) · <code>hr_leave_requests</code> · <code>hr_projects</code> · <code>hr_time_entries</code> (append-only) · <code>hr_expenses</code> · <code>hr_holidays</code></td></tr><tr><td><strong>Roles</strong></td><td><code>hr-admin</code> (tenant) · <code>manager</code> / <code>payroll</code> (scope) — employees are entity-narrowed grants, not a role</td></tr><tr><td><strong>Permission surface</strong></td><td><a href="https://github.com/substrat-run/substrat/blob/main/demos/meridian/PERMISSIONS.md" target="_blank" rel="noreferrer"><code>PERMISSIONS.md</code></a> — 21 keys, 3 roles</td></tr><tr><td><strong>App</strong></td><td>one role-adaptive React app (employee mobile + manager web), fjord-blue design system, light + dark</td></tr><tr><td><strong>Status</strong></td><td>Working — 14-case scenario green on the pure-SQLite adapter</td></tr></tbody></table><h2 id="engines-composed" tabindex="-1">Engines composed <a class="header-anchor" href="#engines-composed" aria-label="Permalink to &quot;Engines composed&quot;">​</a></h2><p>Only one, and it&#39;s the reuse story: <strong><code>protocol</code></strong> — used <strong>twice, in both of its content kinds</strong>, which makes Meridian the demo that shows the difference.</p><h3 id="a-checklist-onboarding" tabindex="-1">A checklist: onboarding <a class="header-anchor" href="#a-checklist-onboarding" aria-label="Permalink to &quot;A checklist: onboarding&quot;">​</a></h3><p><code>hr/start-onboarding</code> calls the engine&#39;s <code>instantiateProtocol(ctx, …)</code> bound to an <strong><code>employee</code></strong> <code>EntityRef</code>, and the vertical declares the <code>protocol → employee</code> relation so the permission walk reaches the employee&#39;s own-record grant. A nice twist: onboarding is signed by the <strong>employee</strong> themselves (their own acknowledgement), not a supervisor — the same engine as Callout, where the arbetsledare signs; the vertical&#39;s grant decides who holds <code>protocol:sign</code>.</p><h3 id="a-document-the-anstallningsavtal" tabindex="-1">A document: the anställningsavtal <a class="header-anchor" href="#a-document-the-anstallningsavtal" aria-label="Permalink to &quot;A document: the anställningsavtal&quot;">​</a></h3><p>An employment contract is not checklist-shaped. It has articles — a role, a salary, an occupancy rate, a start date, a notice period — and they live in <strong>this vertical&#39;s</strong><code>hr_employment_terms</code> table. The engine never sees them. It gets a hash.</p><p>This checklist item used to exist:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{ </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">key</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;anstallningsavtal&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">label</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Anställningsavtal signerat&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;check&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span></code></pre></div><p>It&#39;s gone, and its removal is the lesson. <strong>A checkbox recording that a contract was signed is not evidence that it was.</strong> The tempting fix — one item reading &quot;I accept this contract&quot;, with the real terms in the vertical — is worse than it looks: the engine&#39;s hash would cover that sentence and nothing else, producing a signature that <em>looks</em> like evidence and isn&#39;t.</p><p>So the contract is a <code>document</code> protocol. <code>hr/issue-employment-contract</code> does three things in one transaction: instantiate, <code>bindDocument</code> with <code>(contentRef, contentHash)</code>, and <code>requestSignatures</code>.</p><div class="warning custom-block"><p class="custom-block-title">What the engine proves, and what it doesn&#39;t</p><p>It proves a signature was made over exactly this hash, at this time, by this signatory, and that the hash has not moved since. It <strong>cannot</strong> prove <code>hr_employment_terms</code> still hashes to it — it never read that table.</p><p>Re-deriving the hash is the <strong>vertical&#39;s</strong> obligation. That is why <code>hr/verify-contract</code> exists, and why the template&#39;s <code>hashRecipe</code> is a required field spelling the recipe out for whoever opens this in five years.</p></div><h3 id="two-signatories-two-kinds" tabindex="-1">Two signatories, two kinds <a class="header-anchor" href="#two-signatories-two-kinds" aria-label="Permalink to &quot;Two signatories, two kinds&quot;">​</a></h3><p>The asymmetry is the point, and HR is where it&#39;s most obviously true:</p><table tabindex="0"><thead><tr><th>Party</th><th>Kind</th><th>Why</th></tr></thead><tbody><tr><td><strong>Arbetsgivaren</strong></td><td><code>principal</code></td><td>has an account, signs as themselves — the issuing party, so <code>primary</code></td></tr><tr><td><strong>Den anställde</strong></td><td><code>external</code></td><td>a new hire has <strong>no login on the day they sign</strong></td></tr></tbody></table><p><code>hr_employees.principal_ref</code> has always been nullable — &quot;the login principal, if this person has one&quot;. Karin Berg is seeded with none: offered a job, contract out at Scrive, not yet an employee. Her signatory ref is the opaque <strong>employee id</strong>, the same <code>DataSubjectId</code><code>hr.employee-created</code> already crypto-shreds on — <strong>never <code>national_id</code></strong>, which is this vertical&#39;s declared shred target and would become permanent in a table nothing can edit.</p><p>While the contract is out it sits in <code>pending_signature</code>: <strong>frozen</strong>. No rebind, no fill. It reaches <code>signed</code> only when <em>both</em> parties have signed — one signature, or a decline, is not completion.</p><div class="warning custom-block"><p class="custom-block-title">Nothing completes this in the <em>local</em> demo, and that is not a platform gap</p><p><code>protocol/record-signature</code> is what a Scrive webhook calls. Both blockers this page used to name are now closed: webhook ingress terminates on the platform (<a href="https://github.com/substrat-run/substrat/issues/96" target="_blank" rel="noreferrer">#96</a>) and a non-principal caller reaches a scope operation through a <a href="/concepts/permissions.html">connection&#39;s stub</a> (<a href="https://github.com/substrat-run/substrat/issues/97" target="_blank" rel="noreferrer">#97</a>) — a subject that opens the door and confers nothing, whose events are stamped with the connection that caused them.</p><p>What is still true is the <em>local</em> story: <code>pnpm … dev</code> has no provider on the other end, so a contract dispatched there stays frozen and pending. Completing it needs the <a href="/connectors/scrive.html">Scrive connector</a> and a real signing session.</p><p><code>hr-admin</code> holds <code>protocol:bind</code> and <code>protocol:request-signature</code> — it can freeze and dispatch. It does <strong>not</strong> hold <code>protocol:record-signature</code>, and neither does any other human role: asserting that someone signed with BankID is the provider&#39;s word, not HR&#39;s. The scenario test mints a throwaway principal to stand in for the connector, and that stub deliberately lives in the <strong>test</strong> rather than the seed — a seeded &quot;connector principal&quot; in a reference implementation would quietly become an authority nobody designed.</p></div><p>Everything else is vertical code — and the most interesting part is what <em>isn&#39;t</em> an engine yet. <strong>Two ledgers, one shape:</strong></p><ul><li><code>hr_absence_ledger</code> — vacation/absence, where an entry moves a <strong>balance</strong> (accrual <code>+</code>, booking <code>−</code>, correction, carryover). Balance is a fold; the current value is never stored.</li><li><code>hr_time_entries</code> — worked hours booked to a <strong>project</strong> ref, where an entry moves no balance.</li></ul><p>Both are append-only (a correction is a new row), both fold to a value, both emit an event per write — the <em>identical discipline</em> the work-order engine already owns for order-bound time. That three unrelated capabilities want the same invariant is the signal that an <strong>entry-ledger engine</strong> should be extracted once a second HR-shaped consumer forces it. Its generic core may even belong in the kernel (a ledger-entries attachment contract); the domain semantics — accrual, the approval state machine, the no-negative-beyond-policy floor — stay in the engine. Until then it is honest vertical code with the invariants written cleanly, so the extraction is later mechanical.</p><p>The variable-pay <strong>payroll export</strong> is the Callout <em>invoice basis</em> pattern re-cast: approved absence + expenses leave as a file for a payroll provider. Payroll itself is a deep-domain moat — integrated, never rebuilt.</p><h2 id="the-cast-what-s-denied" tabindex="-1">The cast &amp; what&#39;s denied <a class="header-anchor" href="#the-cast-what-s-denied" aria-label="Permalink to &quot;The cast &amp; what&#39;s denied&quot;">​</a></h2><p>Six principals span the whole matrix — pure employee, dual-role, pure manager, and an attacker:</p><table tabindex="0"><thead><tr><th>Persona</th><th>Holds</th><th>Sees</th></tr></thead><tbody><tr><td><strong>Elin</strong> (SE) · <strong>Pablo</strong> (ES)</td><td>entity-narrowed grant on their own <code>employee</code> record</td><td><strong>My work</strong> only — their own balance, timesheet, expenses, onboarding</td></tr><tr><td><strong>Karin</strong> (SE)</td><td><em>nothing</em> — no principal at all</td><td>she is a signatory, not a user: her contract is out for BankID signature before she has an account</td></tr><tr><td><strong>Mats</strong> — team lead</td><td><code>manager</code> role <strong>and</strong> his own employee record</td><td><strong>My work + Manage</strong> — the dual-role case</td></tr><tr><td><strong>Petra</strong></td><td><code>payroll</code> role</td><td>the variable-pay export</td></tr><tr><td><strong>Hedda</strong></td><td><code>hr-admin</code> role (tenant-level)</td><td><strong>Manage</strong> across both country scopes</td></tr><tr><td><strong>Mallory</strong></td><td><code>hr-admin</code> in <em>Solmark AB</em></td><td>nothing of Nordljus — the attacker</td></tr></tbody></table><p>The denials the <a href="#run-it">scenario</a> asserts — because denials <em>are</em> the demo:</p><ul><li>an employee <strong>cannot approve their own leave</strong>, read a colleague&#39;s balance, or enumerate the team (<code>hr/roster</code> is gated on node-level <code>absence:read</code>, which employees hold only per-entity);</li><li>a manager <strong>sees their department but never compensation</strong> — the roster read carries no <code>national_id</code> or salary;</li><li><strong>cross-tenant</strong>: Mallory operating from Solmark&#39;s scope reaches <em>none</em> of Nordljus&#39;s data — the isolation holds structurally, not by a filter someone remembered to add.</li></ul><p>The full surface — every key, which role holds it, and the entity-narrowed grant shapes — is the checked-in <a href="https://github.com/substrat-run/substrat/blob/main/demos/meridian/PERMISSIONS.md" target="_blank" rel="noreferrer"><code>PERMISSIONS.md</code></a>, re-emitted by CI so it can&#39;t drift from what runs.</p><h2 id="the-app" tabindex="-1">The app <a class="header-anchor" href="#the-app" aria-label="Permalink to &quot;The app&quot;">​</a></h2><p>One React app, composed over the vertical&#39;s single API, that <strong>adapts to the principal and the viewport</strong>. The nav is gated on what you <em>are</em> — an employee record grants <strong>My work</strong>, manager/HR permissions grant <strong>Manage</strong> — and the layout switches with the screen:</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-353",
        class: "mermaid",
        graph: "flowchart%20TB%0A%20%20subgraph%20one%5B%22One%20role-adaptive%20app%20%C2%B7%20one%20%2Fapi%20%C2%B7%20one%20permission%20check%22%5D%0A%20%20%20%20direction%20TB%0A%20%20%20%20W%5B%22My%20work%3Cbr%2F%3EHome%20%C2%B7%20Time%20off%20%C2%B7%20Timesheet%20%C2%B7%20Expenses%20%C2%B7%20Me%22%5D%0A%20%20%20%20M%5B%22Manage%20%C2%B7%20dept%3Cbr%2F%3EInbox%20%C2%B7%20Team%20calendar%20%C2%B7%20Timesheets%20%C2%B7%20Onboarding%20%C2%B7%20Team%22%5D%0A%20%20end%0A%20%20Elin%5B%22Elin%20%2F%20Pablo%3Cbr%2F%3E(employee)%22%5D%20--%3E%20W%0A%20%20Mats%5B%22Mats%3Cbr%2F%3E(team%20lead)%22%5D%20--%3E%20W%0A%20%20Mats%20--%3E%20M%0A%20%20Hedda%5B%22Hedda%3Cbr%2F%3E(HR%20admin)%22%5D%20--%3E%20M%0A%20%20W%20-.%20%22bottom%20tabs%20on%20mobile%22%20.-%20L%5B%22Responsive%20shell%22%5D%0A%20%20M%20-.%20%22sidebar%20on%20desktop%22%20.-%20L%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<ul><li><strong>Employee (mobile-first)</strong> — a vacation-balance hero, and four one-handed, low-typing flows: request time off (with live balance impact), log time (append-only, hours stepper), submit expense (camera-first), and e-sign onboarding.</li><li><strong>Manager (desktop-led)</strong> — an approvals <strong>Inbox</strong> (decline always takes a reason), a team <strong>absence calendar</strong>, <strong>timesheet</strong> review, <strong>onboarding</strong> tracking, and a salary-free <strong>team</strong> roster. Onboarding cards distinguish the two protocol kinds: a checklist shows a progress bar, a contract shows <em>&quot;Frozen at Scrive · 2 signatures outstanding&quot;</em> — because rendering a document as <code>0/1</code> would misdescribe what is actually happening to it.</li><li><strong>Design system</strong> — the fjord-blue &quot;Meridian&quot; palette, a colourblind-safe leave-type palette (every colour paired with a shape), light and dark. The employee and manager surfaces read as one system because they <em>are</em> one app.</li></ul><h2 id="run-it" tabindex="-1">Run it <a class="header-anchor" href="#run-it" aria-label="Permalink to &quot;Run it&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/demo-meridian</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span></span></code></pre></div><p>Starts the API on <code>:8875</code> and the app on <code>:5275</code>. Open it, resize the window to watch the shell move between bottom-tabs and sidebar, and switch personas (Elin → Mats → Hedda) to watch the sections adapt.</p><p>The executable spec is the scenario test — fourteen cases replayed headlessly on the pure-SQLite adapter:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/demo-meridian</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> test</span></span></code></pre></div><p>It asserts the ledger fold (request → approve drops the balance 25 → 22), the approval state machine (can&#39;t skip, can&#39;t re-decide), the no-negative floor, the self-service and cross-tenant <strong>denials</strong>, onboarding reuse, Sweden/Spain diverging from the same code, and the contract flow end to end — that a document is bound by hash and never read by the engine, that <code>pending_signature</code> refuses a rebind, that two signatories of <em>different kinds</em> are needed before it is signed, that a provider reporting a hash we never froze <strong>fails closed</strong>, and that HR cannot speak for the provider.</p><p>The <code>PERMISSIONS.md</code> and the append-only migrations (<code>0001-init</code>, <code>0002-employment-terms</code>) are the human checkpoints the platform makes unskippable.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("verticals/meridian.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const meridian = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  meridian as default
};
