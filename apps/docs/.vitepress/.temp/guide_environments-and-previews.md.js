import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderComponent, ssrRenderStyle } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Environments & previews","description":"","frontmatter":{},"headers":[],"relativePath":"guide/environments-and-previews.md","filePath":"guide/environments-and-previews.md","lastUpdated":1787424503000}');
const _sfc_main = { name: "guide/environments-and-previews.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_InstanceResolution = resolveComponent("InstanceResolution");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="environments-previews" tabindex="-1">Environments &amp; previews <a class="header-anchor" href="#environments-previews" aria-label="Permalink to &quot;Environments &amp; previews&quot;">​</a></h1><p>A vertical you own has exactly <strong>one channel — <code>prod</code></strong> (<a href="/concepts/deploying.html#the-one-channel-prod">the deploy model</a>). There is no <code>dev</code>, no <code>staging</code>. That is not a missing feature; it is the point. On most platforms an <em>environment</em> is a second deployment — another machine running other code. Substrat inverted both halves: code is shared and nearly free (one hibernating Durable Object class per version), and the isolation boundary is the <strong>scope</strong>. So the thing that separates one environment from another here is <em>which scope</em>, not <em>which deployment</em> — and a scope with its own data already has a name and a URL. A second pointer at the same code would add nothing.</p><p>So a non-production environment on Substrat is a <strong><a href="#previews-the-non-prod-primitive">preview</a></strong>: a scope with data, bound to a version, reachable at its own hostname. This page is how you use that one primitive to get every workflow the old three-environment ladder gave you — and a few it couldn&#39;t.</p><h2 id="the-mental-model-an-instance-is-scope-×-version" tabindex="-1">The mental model: an instance is <code>(scope × version)</code> <a class="header-anchor" href="#the-mental-model-an-instance-is-scope-×-version" aria-label="Permalink to &quot;The mental model: an instance is \`(scope × version)\`&quot;">​</a></h2><p>Three primitives, and the whole page falls out of how they compose (<a href="https://github.com/substrat-run/substrat/blob/main/docs/architecture/preview-and-snapshots.md" target="_blank" rel="noreferrer">preview &amp; snapshots RFC</a> §2):</p><table tabindex="0"><thead><tr><th>Primitive</th><th>Mutability</th><th>Git analogy</th></tr></thead><tbody><tr><td><strong>version id</strong> → a <code>deploymentRef</code></td><td><strong>immutable</strong> — a pushed build never changes</td><td>a commit <strong>sha</strong></td></tr><tr><td><strong>binding</strong> (<code>bindScopeVersion</code>) — which version a scope runs</td><td><strong>mutable</strong> by design</td><td>a <strong>branch ref</strong></td></tr><tr><td><strong>hostname</strong> → resolves to a <strong>scope</strong></td><td>stable; names a scope, never a version</td><td>a checkout path</td></tr></tbody></table><p>A running app is a <strong>scope bound to a version</strong>, fronted by a hostname. The router resolves <code>hostname → scope</code>, then dispatches on <em>whatever version that scope is currently bound to</em>. Nothing more names an environment.</p><p>This is why <strong>prod is already a moving target</strong>: the prod hostname points at the prod scope, and a <a href="/guide/deploying.html#promote-to-prod">promote</a> rebinds that scope to a new version. &quot;test&quot; and &quot;prod&quot; are the <em>same shape</em> — a stable scope + a stable hostname whose binding moves. The only thing that differs is <strong>what triggers the rebind, and how gated it is</strong>:</p><ul><li><strong>prod</strong> — the binding moves on an explicit, acknowledged <a href="/guide/deploying.html#promote-to-prod">promote</a>; across a shared vertical&#39;s tenants it cascades.</li><li><strong>test</strong> — the binding moves automatically on every merge to <code>main</code>, on one scope, ungated, driven from CI.</li></ul>`);
  _push(ssrRenderComponent(_component_InstanceResolution, null, null, _parent));
  _push(`<h2 id="previews-the-non-prod-primitive" tabindex="-1">Previews: the non-prod primitive <a class="header-anchor" href="#previews-the-non-prod-primitive" aria-label="Permalink to &quot;Previews: the non-prod primitive&quot;">​</a></h2><p>A preview is a scope with data bound to a version, at its own <code>--&lt;tag&gt;</code> hostname. You create and manage them with <a href="/reference/cli.html#preview"><code>substrat preview</code></a>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pr-42</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">          # push this tree, fork prod, serve the pair</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># ✓ preview &#39;pr-42&#39; → https://helpdesk-acme--pr-42.global.substrat.run</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ls</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> delete</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pr-42</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">            # reap it (idempotent)</span></span></code></pre></div><p><code>create</code> pushes the working tree (so the bound version is exactly this code), forks the vertical&#39;s prod scope, binds the pushed version to the fork, and mints a non-canonical <code>--&lt;tag&gt;</code> hostname. Two properties do the heavy lifting:</p><ul><li><strong>A preview is idempotent per tag.</strong> Re-running the same <code>--tag</code> — what a new push to a PR does — <strong>rebinds the new version onto the same fork</strong>. Successive pushes roll their migrations <em>forward</em> on one copy, which is the rehearsal that actually de-risks a release: the fork accumulates schema changes exactly the way prod will. <code>--refresh</code> starts over from a clean fork of prod.</li><li><strong>A prerelease label never steals a release coordinate.</strong> A default preview push is labelled <code>&lt;pkg&gt;-&lt;tag&gt;.&lt;n&gt;</code> (a semver <em>prerelease</em>), and the registry&#39;s <code>nextVersion</code> only counts anchored <code>x.y.z</code> releases — so preview pushes are free: they never collide with, and never advance, the version your repo owns. (Pass <code>--version</code> to pin an exact label.)</li></ul><p>Every preview carries a TTL (<code>--ttl 72h</code> by default) so an abandoned one is garbage-collected even if never deleted — and reuse <strong>renews</strong> the deadline, so an actively-pushed preview never dies under you. <code>--ttl none</code> <strong>pins</strong> it until you delete it (see <a href="#a-long-lived-test-environment">the test environment</a> below).</p><div class="tip custom-block"><p class="custom-block-title">Previews are for a vertical you own</p><p>A preview forks <em>your own tenant&#39;s</em> scope and serves no install, so a builder may preview a vertical they own whether it is <strong>private or listed</strong> — publishing widens who may <em>install</em>, not who may preview their own pending code (#513). What a preview will not do is fork a scope pinned tighter than <code>global</code>-jurisdiction, or fork a first-party vertical you don&#39;t own.</p></div><h3 id="a-vertical-s-first-environment-—-the-clean-room" tabindex="-1">A vertical&#39;s <em>first</em> environment — the clean room <a class="header-anchor" href="#a-vertical-s-first-environment-—-the-clean-room" aria-label="Permalink to &quot;A vertical&#39;s *first* environment — the clean room&quot;">​</a></h3><p>A fork needs something to fork. A brand-new vertical has no prod scope yet — exactly when a throwaway environment is most useful. <code>--empty</code> provisions a <strong>clean-room</strong> scope instead of forking: module tables migrated, version bound, hostname minted, no data.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> sandbox</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --empty</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # a source-less environment</span></span></code></pre></div><p>It follows the tenant-app hostname convention (<code>&lt;vertical&gt;-&lt;tenant&gt;--&lt;tag&gt;.&lt;base&gt;</code>) since there is no source URL to derive from. This is the &quot;empty / seed data → clean-room preview&quot; cell of the model — a first environment before any prod exists (#514).</p><h2 id="sticky-per-pr-and-per-build-urls" tabindex="-1">Sticky-per-PR <strong>and</strong> per-build URLs <a class="header-anchor" href="#sticky-per-pr-and-per-build-urls" aria-label="Permalink to &quot;Sticky-per-PR **and** per-build URLs&quot;">​</a></h2><p>Because a hostname names a <em>scope</em>, and the router serves <em>that scope&#39;s current binding</em>, two URLs on one scope cannot resolve to two different versions. That single fact dictates the shape of good PR previews — and it is exactly git&#39;s branch-vs-sha discipline:</p><table tabindex="0"><thead><tr><th>URL</th><th>Backed by</th><th>Behaviour</th></tr></thead><tbody><tr><td><strong>Sticky PR URL</strong> — <code>…--pr-42.&lt;base&gt;</code></td><td>one long-lived fork, <strong>rebound every push</strong></td><td>always the latest push on the PR; bookmark it once. The <em>branch ref</em>.</td></tr><tr><td><strong>Per-build URL</strong> — <code>…--pr-42-&lt;run&gt;.&lt;base&gt;</code></td><td>a <strong>fresh</strong> scope per build, <strong>never rebound</strong></td><td>frozen to exactly that build, forever. The <em>sha</em>.</td></tr></tbody></table><p>The sticky URL is what a reviewer watches; the per-build URL is what you paste into a comment when a regression appears in <em>one specific build</em> and must stay that build. The moving pointer is only safe <em>because</em> every build is also addressable immutably — you can always de-reference &quot;the bug on the PR preview&quot; down to a fixed artifact.</p><p>The recipe is two <code>preview create</code> calls per push:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pr-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$PR                  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># sticky: reused, renews its TTL</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pr-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$PR</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$RUN </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">--empty</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ttl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> 24h</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # per-build: fresh, short-lived</span></span></code></pre></div><p>You do not have to write that yourself — <a href="/reference/cli.html#init"><code>substrat init --ci github</code></a> and the dashboard&#39;s <a href="/guide/deploying.html#deploy-from-ci">one-click CI setup</a> generate the same workflow, which creates the sticky preview on every PR push and comments the URL back. The per-build call is <strong>opt-in</strong>, because a frozen scope per build is a real cost: set the repository variable <code>SUBSTRAT_PER_BUILD_PREVIEW</code> to <code>1</code> and the same workflow adds it, with the PR comment then naming <strong>both</strong> URLs — the one that follows the PR and the one frozen to this build.</p><p>The per-build preview is deliberately <code>--empty</code> rather than a fork: it is thrown away within a day, so copying prod data into it on every push would be pure cost. If you need a frozen copy <em>with</em> data, that is a snapshot of the sticky preview, not a per-build one.</p><h2 id="a-long-lived-test-environment" tabindex="-1">A long-lived test environment <a class="header-anchor" href="#a-long-lived-test-environment" aria-label="Permalink to &quot;A long-lived test environment&quot;">​</a></h2><p>A &quot;test environment that always runs the latest <code>main</code>&quot; is just a <strong>pinned preview whose binding is rebound on every merge</strong>, fronted by a custom domain. Nothing new — three primitives you already have:</p><ol><li><p><strong>A durable scope.</strong> Create it pinned so the GC sweep never reaps it:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> test</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ttl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> none</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      # forks prod; or --empty for a fresh one</span></span></code></pre></div></li><li><p><strong>A custom domain on it.</strong> A hostname binds to <em>any</em> scope you own, not just prod — attach one with <a href="/reference/cli.html#scope-domain"><code>substrat scope domain</code></a>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> scope</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> domain</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">testScopeI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --domain</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> crm-test.ahero.se</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># → verifying — publish the CNAME/TXT it prints; live when active</span></span></code></pre></div><p>The scope is now protected from reaping while that domain resolves, exactly like a prod scope (<a href="/concepts/tenancy.html">the reap guard</a> counts any bound hostname).</p></li><li><p><strong>A rebind on every merge.</strong> In the merge-to-<code>main</code> job, after the build, pin the test scope to the version you just pushed:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> scope</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">testScopeI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --version</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">justPushedI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --snapshot</span></span></code></pre></div></li></ol><p>That is the whole thing. <code>crm-test.ahero.se</code> always serves the head of <code>main</code>; migrations roll forward on its accumulated data, rehearsing every prod migration a merge earlier; and prod stays behind its gated promote. All of it is manageable from the <a href="/platform/dashboard.html#previews-environments">dashboard</a> too — pin a preview, attach a domain, and (per app) bind a version — no CLI required.</p><div class="info custom-block"><p class="custom-block-title">Why &quot;tracks main&quot; stays a CI step, not a platform setting</p><p>Substrat <em>enables</em> this workflow; it deliberately does not <em>encode</em> it. A &quot;this scope auto-tracks tag X&quot; toggle would be a new environment noun — the same temptation the retired <code>dev</code>/<code>staging</code> channels gave in to, re-buried one layer down. A one-line <code>scope bind</code> in your merge job is more legible and composes with any branching model. Revisit only if you have many test slots wanting a shared rule.</p></div><h2 id="canary-pinned-tenants-—-per-scope-rollout" tabindex="-1">Canary &amp; pinned tenants — per-scope rollout <a class="header-anchor" href="#canary-pinned-tenants-—-per-scope-rollout" aria-label="Permalink to &quot;Canary &amp; pinned tenants — per-scope rollout&quot;">​</a></h2><p>The same <a href="/reference/cli.html#scope-bind"><code>scope bind</code></a> primitive is the whole rollout axis for a <strong>shared</strong> vertical with many tenant scopes. A prod promote cascades every tenant to the new version; when you want <em>tenant A first</em>, bind that one scope ahead:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> scope</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">tenantA-scopeI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --version</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">nex</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">t</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --snapshot</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # canary one tenant</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># … watch it, then promote prod to bring the rest along</span></span></code></pre></div><p><code>--snapshot</code> forks the scope&#39;s data before a migration-crossing bind, so a bad version leaves the prior one still runnable at its correct migration frontier — <a href="/concepts/deploying.html#previews-run-a-version-against-a-copy-of-the-data">fork-before-you-promote</a>, applied per scope. A binding drift is native and expected: an install pins &quot;prod-at-the-time&quot;, and <code>Update to latest</code> is the per-scope catch-up. The channel was always the <em>weaker</em> abstraction here — it can&#39;t express &quot;tenant A first&quot;; the binding can.</p><h2 id="the-release-workflow-pnpm-changesets" tabindex="-1">The release workflow: pnpm + changesets <a class="header-anchor" href="#the-release-workflow-pnpm-changesets" aria-label="Permalink to &quot;The release workflow: pnpm + changesets&quot;">​</a></h2><p>The version your repo owns lives in <code>package.json</code>, not in a registry counter — so a merge does <strong>not</strong> move it, and only a release does. This is the workflow the generated CI encodes, and it maps cleanly onto the primitives above:</p><table tabindex="0"><thead><tr><th>Moment</th><th>Version label</th><th>What runs</th><th>Where</th></tr></thead><tbody><tr><td><strong>PR opened / pushed</strong></td><td><code>&lt;next&gt;-pr-&lt;n&gt;.&lt;k&gt;</code></td><td><code>preview create --tag pr-&lt;n&gt;</code></td><td>sticky <code>…--pr-&lt;n&gt;</code>, a fork of prod</td></tr><tr><td><strong>Merge to <code>main</code></strong> (changeset lands; version does <strong>not</strong> move)</td><td><code>&lt;pkg&gt;-test.&lt;run&gt;</code></td><td><code>push</code>, then <code>scope bind &lt;testScope&gt;</code></td><td><code>crm-test.ahero.se</code>, the long-lived test env</td></tr><tr><td><strong>Version PR</strong> (&quot;chore(release): version packages&quot;)</td><td><code>&lt;next&gt;-pr-&lt;n&gt;.&lt;k&gt;</code></td><td>nothing special — it is a PR, so it gets a PR preview</td><td>that preview <strong>is</strong> the release candidate: the code that is about to become prod, on a fork of prod data, while rejecting it is still free</td></tr><tr><td><strong>Version PR merges</strong> (version moves)</td><td><code>&lt;pkg&gt;</code> exactly</td><td><code>push --version &lt;pkg&gt; --promote prod</code></td><td>prod</td></tr></tbody></table><p>Note what row 3 does <em>not</em> need: a release-candidate channel, an <code>rc</code> tag, or any new noun. A version PR is a pull request, so the ordinary PR preview already runs the release candidate against forked prod data. The one thing that differs between it and the prod push is the version <em>label</em> — the code is the same tree.</p><p>Two disciplines make this safe, and both are already true — this workflow just names them:</p><ul><li><strong>A non-release push never claims a release coordinate</strong>, because prerelease labels are skipped when the registry computes the next version. PR previews, the test env, and the release candidate all push freely without punching holes in your version sequence.</li><li><strong>The release candidate is the one moment</strong> the <em>exact</em> artifact that will become prod runs against a fork of prod&#39;s data <em>while the change is still a PR</em> — i.e. while rejecting it costs nothing. It is also where the <code>--ack-permissions</code> / <code>--ack-migrations</code> acknowledgements are <em>earned</em> rather than typed at the same instant as the deploy.</li></ul><div class="tip custom-block"><p class="custom-block-title">Don&#39;t hand-roll it</p><p>Generate it:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> init</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ci</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> github</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --release</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> changesets</span></span></code></pre></div><p><a href="/reference/cli.html#init"><code>substrat init</code></a> and the dashboard&#39;s <a href="/guide/deploying.html#deploy-from-ci">one-click CI setup</a> render the <strong>same</strong> workflow from the same generator — merge-to-main release, the test-env rebind, and the PR previews with their comment. The label discipline and the two-<code>preview</code>-per-push recipe are not something you should have to re-derive per vertical, and the reason this command exists is that the first workflow we shipped got the label part wrong: it pushed <code>--version 0.1.&lt;run number&gt;</code> on every run, claiming a real registry coordinate each time and punching holes in the version sequence.</p></div><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="/guide/deploying.html">Deploying a vertical</a> — push, admission, promote prod, serve-in-place.</li><li><a href="/concepts/deploying.html">The deploy model</a> — the concepts behind the one channel and in-place serving.</li><li><a href="/concepts/snapshots.html">Snapshots &amp; test copies</a> — the data-fork machinery a preview is built on.</li><li><a href="/reference/cli.html"><code>@substrat-run/cli</code></a> — <code>preview</code>, <code>scope bind</code>, <code>scope domain</code>, <code>promote</code>.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/environments-and-previews.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const environmentsAndPreviews = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  environmentsAndPreviews as default
};
