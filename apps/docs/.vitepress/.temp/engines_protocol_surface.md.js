import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Operations, functions & permissions","description":"","frontmatter":{},"headers":[],"relativePath":"engines/protocol/surface.md","filePath":"engines/protocol/surface.md","lastUpdated":1784804439000}');
const _sfc_main = { name: "engines/protocol/surface.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="operations-functions-permissions" tabindex="-1">Operations, functions &amp; permissions <a class="header-anchor" href="#operations-functions-permissions" aria-label="Permalink to &quot;Operations, functions &amp; permissions&quot;">​</a></h1><p>An engine has <strong>no endpoints</strong>. It exposes operations (invoked through a scope stub) and in-scope functions (called by a vertical inside its own transaction). HTTP, where it exists, is a generated artifact pointed at by the manifest&#39;s <code>api</code> field.</p><p>This engine has a third surface the others don&#39;t: it <strong>contributes a guard predicate</strong>.</p><h2 id="operations" tabindex="-1">Operations <a class="header-anchor" href="#operations" aria-label="Permalink to &quot;Operations&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Operation</th><th>Permission</th><th>Does</th></tr></thead><tbody><tr><td><code>protocol/define-template</code></td><td><code>protocol:create</code></td><td>register a new template version</td></tr><tr><td><code>protocol/list-templates</code></td><td><code>protocol:read</code></td><td>list registered templates</td></tr><tr><td><code>protocol/instantiate</code></td><td><code>protocol:create</code></td><td>start an instance on an entity</td></tr><tr><td><code>protocol/fill</code></td><td><code>protocol:fill</code></td><td>append a response (open checklist instances only)</td></tr><tr><td><code>protocol/bind-document</code></td><td><code>protocol:bind</code></td><td>bind vertical content <code>(ref, hash)</code> to an open document instance</td></tr><tr><td><code>protocol/request-signatures</code></td><td><code>protocol:request-signature</code></td><td>freeze and send to named parties for external signature</td></tr><tr><td><code>protocol/cancel-signatures</code></td><td><code>protocol:request-signature</code></td><td>withdraw an outstanding request set and thaw</td></tr><tr><td><code>protocol/record-signature</code></td><td><code>protocol:record-signature</code></td><td>record a signature a provider reported</td></tr><tr><td><code>protocol/decline-signature</code></td><td><code>protocol:record-signature</code></td><td>record a refusal or an expiry</td></tr><tr><td><code>protocol/sign</code></td><td><code>protocol:sign</code></td><td>sign in-app; freezes the instance forever</td></tr><tr><td><code>protocol/countersign</code></td><td><code>protocol:countersign</code></td><td>add a second signature on frozen content</td></tr><tr><td><code>protocol/void</code></td><td><code>protocol:void</code></td><td>supersede an instance (never deletes)</td></tr><tr><td><code>protocol/get</code></td><td><code>protocol:read</code></td><td>one instance with responses and signatures</td></tr><tr><td><code>protocol/list-for-entity</code></td><td><code>protocol:read</code></td><td>every protocol on an <code>EntityRef</code></td></tr></tbody></table><h2 id="in-scope-functions" tabindex="-1">In-scope functions <a class="header-anchor" href="#in-scope-functions" aria-label="Permalink to &quot;In-scope functions&quot;">​</a></h2><p>The most complete composable surface of any engine here — every operation has a function behind it:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { instantiateProtocol, requireSigned, PROTOCOL_PERM } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/engine-protocol&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Function</th><th>Notes</th></tr></thead><tbody><tr><td><code>defineTemplate(ctx, input)</code></td><td>register a <code>(key, version)</code></td></tr><tr><td><code>listTemplates(ctx)</code></td><td></td></tr><tr><td><code>instantiateProtocol(ctx, input)</code></td><td>binds to any <code>EntityRef</code></td></tr><tr><td><code>fillProtocol(ctx, input)</code></td><td>append-only; checklist kind only; throws once frozen</td></tr><tr><td><code>bindDocument(ctx, input)</code></td><td>document kind only; rebindable while open</td></tr><tr><td><code>requestSignatures(ctx, input)</code></td><td><strong>async</strong> — freezes content, opens a request per party</td></tr><tr><td><code>recordSignature(ctx, input)</code></td><td><strong>async</strong> — an external signatory, at the provider&#39;s time</td></tr><tr><td><code>declineSignature(ctx, input)</code></td><td>a refusal or expiry; does <strong>not</strong> thaw the instance</td></tr><tr><td><code>cancelSignatureRequests(ctx, input)</code></td><td>withdraws the set and thaws back to <code>open</code></td></tr><tr><td><code>signProtocol(ctx, input)</code></td><td><strong>async</strong> — in-app; computes the content hash via Web Crypto</td></tr><tr><td><code>countersignProtocol(ctx, input)</code></td><td><strong>async</strong> — recomputes and compares the hash</td></tr><tr><td><code>voidProtocol(ctx, input)</code></td><td></td></tr><tr><td><code>getProtocol(ctx, instanceId)</code></td><td></td></tr><tr><td><code>listProtocolsForEntity(ctx, entity)</code></td><td></td></tr><tr><td><code>protocolContentHash(template, latest, boundHash?)</code></td><td><strong>async</strong> — the hash recipe, exported so you can verify independently. Takes no <code>ctx</code>: it hashes a template plus the latest responses (or a bound document hash), nothing scope-bound</td></tr><tr><td><code>requireSigned(ctx, entity, templateKey)</code></td><td>throws unless signed — the completion-guard building block</td></tr><tr><td><code>requireCountersigned(ctx, …)</code></td><td>the counter-signed equivalent</td></tr></tbody></table><p>There is deliberately <strong>no <code>requireAllSigned</code></strong>. In the request-driven path an instance reaches <code>signed</code> only once every requested party has signed, so &quot;all parties signed&quot; <em>is</em><code>requireSigned</code> — a second predicate would read as though it checked something stronger while checking the same thing. The multi-party question is answered by the state machine.</p><p>None of these check permissions — that is the caller&#39;s job.</p><p><code>signProtocol</code>, <code>countersignProtocol</code>, <code>requestSignatures</code>, <code>recordSignature</code> and <code>protocolContentHash</code> are <strong>async</strong> because Web Crypto is; the rest are synchronous. That <code>protocolContentHash</code> is exported at all is the point of the invariant: the recipe is a contract you can replay, not a black box.</p><h2 id="the-guard-predicate" tabindex="-1">The guard predicate <a class="header-anchor" href="#the-guard-predicate" aria-label="Permalink to &quot;The guard predicate&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">predicates</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;protocol/all-signed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: allSignedPredicate }</span></span></code></pre></div><p>The engine <strong>contributes</strong> the predicate; a vertical manifest <strong>wires</strong> it. The engine declares no guard of its own — <em>what is mandatory when</em> is vertical policy, and the engine cannot know another module&#39;s operations.</p><p>This is the only genuinely config-shaped surface in any engine. Its config is declared in the <strong>vertical&#39;s</strong> manifest, kernel-opaque, and parsed by the predicate that owns it:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> allSignedGuardConfig</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">object</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  templateKey: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">min</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">),   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// vertical content: &#39;tillstandsrapport&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  entityType: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">min</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">),    </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// what the protocol hangs on: &#39;workorder&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  entityIdFrom: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">min</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">),  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// input field holding the id: &#39;orderId&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  countersigned: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">default</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p><code>entityIdFrom</code> is the trick worth stealing: it <strong>late-binds into the vertical&#39;s vocabulary</strong>, so one predicate serves any operation shape without the engine ever knowing the vertical&#39;s words. That&#39;s how you parameterise engine behaviour without a config bag — see <a href="./composing.html#configuration">Composing</a>.</p><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Key</th><th>Description</th></tr></thead><tbody><tr><td><code>protocol:create</code></td><td>Define protocol templates and start protocol instances on entities</td></tr><tr><td><code>protocol:fill</code></td><td>Record responses on an open checklist protocol (append-only)</td></tr><tr><td><code>protocol:bind</code></td><td>Bind vertical-owned document content (ref + hash) to an open document protocol</td></tr><tr><td><code>protocol:request-signature</code></td><td>Freeze and request signatures from named parties; also cancels a pending set</td></tr><tr><td><code>protocol:record-signature</code></td><td>Record a signature reported by an external provider</td></tr><tr><td><code>protocol:sign</code></td><td>Sign in-app — freezes it forever (the technician fills, the arbetsledare signs)</td></tr><tr><td><code>protocol:countersign</code></td><td>Counter-sign an already-signed protocol — a second signature on the same frozen content</td></tr><tr><td><code>protocol:read</code></td><td>Read protocol templates, instances, responses, signature requests and signatures</td></tr><tr><td><code>protocol:void</code></td><td>Void (supersede) a protocol — never deletes</td></tr></tbody></table><p>Nine keys for what looks like one workflow, because the separations are the product: fill ≠ sign is the arbetsledare rule, sign ≠ countersign is the customer at pickup, and <code>void</code> is a supervisory act.</p><p><code>protocol:record-signature</code> is the odd one and deliberately so: it speaks <strong>for an external provider</strong>, not for a person, and it is held by <strong>no human role in any demo</strong>. A staff signer must not be able to assert that some customer signed with BankID. When ingress lands (<a href="https://github.com/substrat-run/substrat/issues/96" target="_blank" rel="noreferrer">#96</a>, <a href="https://github.com/substrat-run/substrat/issues/97" target="_blank" rel="noreferrer">#97</a>), granting this key is how a deployment declares what it trusts to speak for a provider — which is why it belongs in the permission diff rather than in a config file.</p><h2 id="entitlement" tabindex="-1">Entitlement <a class="header-anchor" href="#entitlement" aria-label="Permalink to &quot;Entitlement&quot;">​</a></h2><p><code>entitlementKey: &#39;protocol&#39;</code>. Checked per invoke, fails closed. A binary SKU gate, not configuration.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/protocol/surface.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const surface = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  surface as default
};
