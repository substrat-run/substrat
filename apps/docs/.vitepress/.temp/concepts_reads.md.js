import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Reads & scaling","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/reads.md","filePath":"concepts/reads.md","lastUpdated":1787424503000}');
const _sfc_main = { name: "concepts/reads.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_ReadPaths = resolveComponent("ReadPaths");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="reads-scaling" tabindex="-1">Reads &amp; scaling <a class="header-anchor" href="#reads-scaling" aria-label="Permalink to &quot;Reads &amp; scaling&quot;">​</a></h1><p>Every scope runs a <strong>strictly serialized executor</strong>: one operation at a time, run to completion (<a href="/concepts/scope-host.html">Operations &amp; the scope host</a>). That guarantee is what lets module code do a read-modify-write without locks, transactions-for-concurrency, or retry loops. It also raises the obvious question — <em>doesn&#39;t that mean one request at a time?</em></p><p>No. But the reason it doesn&#39;t is worth understanding, because the intuitive fix is the wrong one.</p><h2 id="serialization-is-per-scope-not-per-system" tabindex="-1">Serialization is per scope, not per system <a class="header-anchor" href="#serialization-is-per-scope-not-per-system" aria-label="Permalink to &quot;Serialization is per scope, not per system&quot;">​</a></h2><p>The unit of serialization is the <strong>scope</strong>, and a scope is sized to the <em>consistency domain</em> — one housing association, one branch, one clinic — never the whole tenant (<a href="/concepts/tenancy.html">Tenants &amp; scopes</a>). A thousand scopes execute a thousand operations at once: separate execution domains, separate databases, possibly separate machines. Only two operations <em>on the same scope</em> ever queue.</p><p>That queue is also much cheaper than it sounds. A scope&#39;s data is local to its execution domain, so an operation is local SQLite work — microseconds to a couple of milliseconds — not a network round-trip per query. And module code <a href="/concepts/modules.html">cannot call the network</a>, so an operation can never hold the scope&#39;s turn while waiting on a slow third party. The queue drains fast because operations are short.</p><h2 id="why-you-can-t-just-parallelize-reads" tabindex="-1">Why you can&#39;t just parallelize reads <a class="header-anchor" href="#why-you-can-t-just-parallelize-reads" aria-label="Permalink to &quot;Why you can&#39;t just parallelize reads&quot;">​</a></h2><p>The tempting fix is to let read-only operations run concurrently. It buys you almost nothing.</p><p>A scope&#39;s execution domain is a <strong>single isolate</strong> — one thread. Admitting reads concurrently would interleave them at <code>await</code> points, not run them in parallel. And SQLite reads inside the domain are <em>synchronous local calls</em>, so there are no <code>await</code> points to interleave at. Ten concurrent reads would still execute one after another, just with more bookkeeping.</p><blockquote><p><strong>Reads get fast by getting short, not by getting concurrent.</strong> A queue of 50 reads at 30µs each is invisible. At 2ms each, it isn&#39;t. Attack the duration.</p></blockquote><h2 id="the-three-read-paths" tabindex="-1">The three read paths <a class="header-anchor" href="#the-three-read-paths" aria-label="Permalink to &quot;The three read paths&quot;">​</a></h2><p>Reach for them in this order.</p><table tabindex="0"><thead><tr><th>Path</th><th>Latency</th><th>Consistency</th><th>Use for</th></tr></thead><tbody><tr><td><strong>In-scope</strong> (default)</td><td>µs</td><td>Serializable</td><td>Everything interactive</td></tr><tr><td><strong>External read model</strong> (escape hatch)</td><td>ms</td><td>Eventually consistent</td><td>A scope whose reads outgrow its executor</td></tr><tr><td><strong>History tier</strong> (Iceberg / R2 SQL)</td><td>seconds</td><td>Eventually consistent</td><td>Reporting, audit, cross-scope</td></tr></tbody></table><h3 id="_1-in-scope-reads-—-the-default" tabindex="-1">1. In-scope reads — the default <a class="header-anchor" href="#_1-in-scope-reads-—-the-default" aria-label="Permalink to &quot;1. In-scope reads — the default&quot;">​</a></h3><p>A read inside a scope is a local indexed query: one hop, strongly consistent, tens of microseconds. Nearly all interactive reads belong here.</p><p>When a screen needs a shape the normalized tables don&#39;t serve cheaply — a dispatcher board joining jobs, customers, technicians and totals — don&#39;t reach for a cache or a replica. Keep a <strong>projection table in the scope&#39;s own database</strong>, maintained by an event consumer in the same transaction as the write it derives from:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// The vertical&#39;s own module registration.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> fsmModule</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ModuleRegistration</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  manifest: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // ...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    events: { emits: [], consumes: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder.completed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  consumers: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // Fed by the engine&#39;s event. Own table, keyed by the engine&#39;s id —</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // never a column added upstream, never a read into engine tables.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    &#39;workorder.completed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">event</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">      const</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">workOrderId</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">completedAt</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> JobCompleted.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">parse</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(event.payload);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      ctx.sql.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">        \`UPDATE fsm_job_board SET status = ?, completed_at = ? WHERE job_id = ?\`</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;completed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, completedAt, workOrderId],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      );</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">};</span></span></code></pre></div><p>The read is then one indexed scan of one flat table. There is <strong>no staleness</strong>, because there is no second store — the projection commits with the write that caused it. This is the same <a href="/concepts/modules.html">side-table pattern</a> verticals already use to extend engine entities, applied to read performance.</p><h3 id="_2-external-read-model-—-the-escape-hatch" tabindex="-1">2. External read model — the escape hatch <a class="header-anchor" href="#_2-external-read-model-—-the-escape-hatch" aria-label="Permalink to &quot;2. External read model — the escape hatch&quot;">​</a></h3><p>If a scope&#39;s read volume genuinely outgrows its executor, the <strong>outbox</strong> already gives you the way out. Event emission is transactional with the write it describes (<a href="/concepts/events.html">Events &amp; audit</a>), so a second drain sink can maintain a denormalized read model in external storage (D1, KV) without any risk of a write that never reaches it.</p><div class="warning custom-block"><p class="custom-block-title">Read-your-writes does not survive the crossing</p><p>D1&#39;s Sessions API provides sequential consistency <em>within D1&#39;s version space</em> — a <em>bookmark</em> names a D1 version, and a replica waits to catch up before answering. But the authoritative write lands in <strong>the scope</strong>, and reaches D1 only after the outbox pump runs. At the moment the operation returns, there is no D1 bookmark that means <em>&quot;after my write&quot;</em> — the two version spaces are unrelated.</p><p>So the guarantee you&#39;d be counting on is exactly the one that breaks. Recovering it means either mapping event id → D1 bookmark in the pump and waiting on that watermark, or pinning a session&#39;s reads back to the scope for a window after it writes. Choose one <strong>before</strong> adopting this path.</p></div><p>Note what this implies: inside a scope you have something <em>stronger</em> than session consistency for free — full serializability, one copy, no bookmarks to thread. Read-your-writes isn&#39;t a problem you have; it&#39;s a problem you&#39;d <strong>acquire</strong> by leaving.</p><h3 id="_3-history-tier-—-not-a-read-tier" tabindex="-1">3. History tier — not a read tier <a class="header-anchor" href="#_3-history-tier-—-not-a-read-tier" aria-label="Permalink to &quot;3. History tier — not a read tier&quot;">​</a></h3><p>Domain events flow to Iceberg on R2, queried through a tenant-scoping gateway. That tier is columnar and seconds-scale by construction: it is for <strong>reporting, reconciliation, audit, and cross-scope history</strong>. It is not a UI list view, and treating it as one is a category error.</p>`);
  _push(ssrRenderComponent(_component_ReadPaths, null, null, _parent));
  _push(`<h2 id="finding-a-row-by-what-someone-typed" tabindex="-1">Finding a row by what someone typed <a class="header-anchor" href="#finding-a-row-by-what-someone-typed" aria-label="Permalink to &quot;Finding a row by what someone typed&quot;">​</a></h2><p>A picker is not a list. <code>listCustomers</code> sorted by number is the right read at forty customers and the wrong one at forty thousand — and once a list is paged, filtering the page in the browser searches the first page only. Declare the entity <strong>searchable</strong> and the kernel builds a real index for it:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// The vertical&#39;s manifest, through \`manifestEntities\` — the same helper that checks</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// \`fields\` against the entity registry.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">...</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">manifestEntities</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(calloutEntities, {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  searchables: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    { entityType: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;customer&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, fields: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;name&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;number&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // \`substring\` matches inside a word (&quot;ande&quot; → &quot;Andersson&quot;) for a larger index.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    { entityType: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;note&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, fields: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;body&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">], tokenizer: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;substring&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}),</span></span></code></pre></div><p>From that declaration the kernel provisions a per-scope FTS5 index and the triggers that maintain it, journaled like any other migration. Nothing else changes: rows are still written with <code>ctx.sql</code>, and the index stays correct because the trigger fires no matter who writes. That also means it is <strong>read-after-write correct</strong> — a customer created in one breath is findable in the next, with no indexing lag to wait out.</p><p>The read returns ids, and you hydrate them through the query you already have:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> searchCustomers</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">q</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">limit</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 20</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  assertAllowed</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">check</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">PERM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.customerManage));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> hits</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">search</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;customer&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, q, { limit });</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (hits.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">length</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> ===</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { results: [], limit, capped: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> };</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> rows</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.sql.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">query</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">CustomerRow</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;(</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    \`SELECT * FROM callout_customers WHERE id IN (\${</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">hits</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">map</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">(() </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;?&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">).</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">join</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;, &#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">})\`</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    hits.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">map</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">((</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">h</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> h.id),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  );</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> byId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Map</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(rows.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">map</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">((</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">r</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [r.id, r]));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  // \`IN (…)\` loses the rank order — put it back, or the best match lands third.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { results: hits.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">map</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">((</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">h</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> byId.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(h.id)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">), limit, capped: hits.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">length</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> ===</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> limit };</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">};</span></span></code></pre></div><p>Three things worth knowing before you design a screen around it:</p><ul><li><strong>It is capped, not paged.</strong> A relevance order has no stable sort key, so it has no honest cursor either — paging a ranked result set reorders rows, and rows go missing or double. Ask for the top N and tell the user to narrow the term. Deep, ordered paging is what a declared sort on a list read is for.</li><li><strong>It does not check permission.</strong> Nothing on <code>ctx</code> does. Your <code>assertAllowed</code> comes first, and if your entity uses narrowed grants, filter the hits you hydrate — a ranked top-N filtered afterwards returns <em>fewer</em> than N, so over-fetch deliberately.</li><li><strong>Give it its own route.</strong> <code>GET /customers/search</code> beside <code>GET /customers</code>, not a <code>q</code> parameter on the list: the two have different pagination contracts and one endpoint cannot carry both. The static segment is registered ahead of <code>/customers/{id}</code> automatically, so the two never collide.</li></ul><p>Short terms are refused rather than answered by a scan — two characters for the default <code>prefix</code> tokenizer, three for <code>substring</code>, which is the trigram index&#39;s own floor. Enforce the same minimum in your operation&#39;s input schema so a caller gets a 400 that names the field.</p><h2 id="two-more-surfaces-and-neither-is-a-read-tier" tabindex="-1">Two more surfaces, and neither is a read tier <a class="header-anchor" href="#two-more-surfaces-and-neither-is-a-read-tier" aria-label="Permalink to &quot;Two more surfaces, and neither is a read tier&quot;">​</a></h2><p>Distinct from the three read paths, a vertical&#39;s manifest can declare stores the platform mints and injects for it:</p><ul><li><strong>A per-tenant relational store</strong> (<code>tenantStores</code>, opened at runtime through <code>host.openTenantStore</code>) — one independent SQL database per tenant. It is an <em>own-store</em> concept, the way a vertical might keep an auth database.</li><li><strong>A per-tenant blob store</strong> (<code>blobStores</code>) — object storage behind the kernel&#39;s <a href="/concepts/modules.html">attachment</a> surface, for the documents and images a scope&#39;s rows point at rather than contain.</li></ul><p>Both follow the same ownership rule, and it is the interesting part: <strong>the builder supplies no id</strong>. The vertical declares a <em>need</em>; the platform mints the database or bucket, holds the cloud credential, and attaches a binding to the serving script. A vertical is handed a store — it never names one, so it can never name someone else&#39;s.</p><p>Neither is a read-scaling tier. Reach for them when a vertical genuinely needs storage outside the per-scope execution domain, not to make scope reads faster. Scope reads still follow the three paths above.</p><h2 id="why-not-global-read-replicas" tabindex="-1">Why not global read replicas? <a class="header-anchor" href="#why-not-global-read-replicas" aria-label="Permalink to &quot;Why not global read replicas?&quot;">​</a></h2><p>Because the operational record shouldn&#39;t leave, for three reasons:</p><ul><li><strong>Residency.</strong> A scope&#39;s <code>jurisdiction</code> is fixed at provisioning and its execution domain can never relocate. Replicating an <code>eu</code> scope&#39;s data to other regions contradicts that guarantee — and D1 offers location <em>hints</em>, not jurisdiction guarantees.</li><li><strong>Consistency.</strong> Read-your-writes does not cross the outbox, per above. Bookmarks cannot repair a boundary they cannot see.</li><li><strong>The workload doesn&#39;t want it.</strong> A scope maps to <em>one business</em>, whose users cluster around it. <strong>Placing the execution domain well at provisioning beats replicating it.</strong></li></ul><p>The honest carve-out: global replication earns its keep on <strong>public, read-heavy, staleness-tolerant surfaces</strong> — customer portals, tracking links, availability views — where the projection is a derived subset rather than the operational record, and traffic can dwarf internal usage. That&#39;s a per-surface decision, not a platform read tier.</p><h2 id="when-a-scope-really-is-too-hot" tabindex="-1">When a scope really is too hot <a class="header-anchor" href="#when-a-scope-really-is-too-hot" aria-label="Permalink to &quot;When a scope really is too hot&quot;">​</a></h2><p>Serialization bounds <em>write</em> throughput on a single scope, and at some point a scope can outgrow its execution domain. The answer is never &quot;shard the scope&quot;:</p><ul><li><strong>Split it.</strong> A scope that&#39;s too hot is usually a consistency domain drawn too large. This is the same move as the <a href="/concepts/tenancy.html">granularity rule</a> — and it&#39;s the right answer more often than it looks.</li><li><strong>Migrate it to storage shape B</strong>, where the execution domain becomes a control plane (hot state, locks) fronting a separate database for bulk storage and read replicas. The choice is per scope, and <strong>invisible to module code</strong>.</li></ul><p>Both remain available for the same reason everything else here does: module code reaches data through <code>ctx.sql</code> and nothing else, so what sits underneath can change without a single operation being rewritten.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/reads.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const reads = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  reads as default
};
