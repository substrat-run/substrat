import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/vertical-auth","description":"","frontmatter":{},"headers":[],"relativePath":"reference/vertical-auth.md","filePath":"reference/vertical-auth.md","lastUpdated":1786535775000}');
const _sfc_main = { name: "reference/vertical-auth.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-vertical-auth" tabindex="-1">@substrat-run/vertical-auth <a class="header-anchor" href="#substrat-run-vertical-auth" aria-label="Permalink to &quot;@substrat-run/vertical-auth&quot;">​</a></h1><p>Pluggable authentication for a vertical — <strong>one interface the application codes against</strong>, and three interchangeable implementations behind it. It is the concrete form of the <a href="/concepts/identity.html">identity seam</a> for verticals, the counterpart to <a href="/reference/oidc-rp.html"><code>@substrat-run/oidc-rp</code></a>, which serves the platform&#39;s own surfaces.</p><p>A vertical scaffolded with <code>create-substrat</code> answers <code>/api/*</code> with <code>401</code> until real auth is wired into <code>authenticatedPrincipal</code>. This package fills that seam without the vertical hand-rolling session handling or the owner-claim logic.</p><h2 id="the-authprovider-contract" tabindex="-1">The <code>AuthProvider</code> contract <a class="header-anchor" href="#the-authprovider-contract" aria-label="Permalink to &quot;The \`AuthProvider\` contract&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> type</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { AuthProvider, AuthSubject } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/vertical-auth&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> AuthProvider</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  handle</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">request</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Request</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Promise</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Response</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;;             </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// /api/auth/* — login, logout, callbacks</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  resolve</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">headers</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Headers</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Promise</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">AuthSubject</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;;  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// request → verified subject</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> AuthSubject</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  sub</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;           </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// the provider&#39;s stable subject id</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  email</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  name</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>The provider proves <em>who</em> a caller is and nothing more. <a href="/concepts/permissions.html">Authorization stays in the kernel</a> — roles, grants and tenancy are never this package&#39;s concern. Mapping <code>sub</code> to a Substrat <code>PrincipalId</code> is a <strong>separate, per-scope</strong> step (the identity directory below), which is what lets the implementation swap without touching the app.</p><h2 id="choosing-an-implementation" tabindex="-1">Choosing an implementation <a class="header-anchor" href="#choosing-an-implementation" aria-label="Permalink to &quot;Choosing an implementation&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Export</th><th>Login happens</th><th>Use when</th></tr></thead><tbody><tr><td><code>oidcAuthProvider</code></td><td>at the issuer; the app verifies a presented JWT</td><td>an SPA already holds a token from Supabase, Auth0, AuthHero, Keycloak, Zitadel</td></tr><tr><td><code>oidcRpAuthProvider</code></td><td>server-side, in your worker</td><td>you want the full browser redirect flow and a session cookie you control</td></tr><tr><td><code>doAuthProvider</code></td><td>in your own Durable Object</td><td>the vertical owns credentials rather than delegating to an issuer</td></tr></tbody></table><p><code>oidcAuthProvider</code> is discovery-driven: the issuer URL is the only wired-in value, and the ID token is signature-verified against the issuer&#39;s JWKS. <code>oidcRpAuthProvider</code> runs Authorization-Code + PKCE through <a href="/reference/oidc-rp.html"><code>@substrat-run/oidc-rp</code></a>, so verticals and the platform share one verifier rather than two copies of the security-critical path.</p><h2 id="the-identity-directory" tabindex="-1">The identity directory <a class="header-anchor" href="#the-identity-directory" aria-label="Permalink to &quot;The identity directory&quot;">​</a></h2><p><code>IdentityDO</code> is one Durable Object <strong>per tenant</strong>, running its own SQLite. A tenant&#39;s users, sessions and credentials live in that tenant&#39;s DO — there is no shared <code>AUTH_DB</code> for a bug to leak across. It is one of the vertical&#39;s own DO classes, so it stays <a href="/concepts/deploying.html">sandbox-clean</a>: the platform refuses cross-script bindings, not a vertical&#39;s own.</p><p>Its <code>sub → principal</code> mapping is <strong>provider-agnostic</strong> and used under <em>every</em> provider, including the two OIDC ones where Better Auth stays dormant:</p><ul><li><code>setPendingOwner</code> — the first-run claim. The first authenticated subject takes the pending owner slot; access is invite-only afterwards.</li><li><code>resolvePrincipal</code> — the per-request lookup that turns a verified <code>sub</code> into the <code>PrincipalId</code> the kernel checks permissions against.</li></ul><h2 id="cookie-domain-safety" tabindex="-1">Cookie-domain safety <a class="header-anchor" href="#cookie-domain-safety" aria-label="Permalink to &quot;Cookie-domain safety&quot;">​</a></h2><p><code>resolveCookieDomain</code> decides the session cookie&#39;s <code>Domain</code> for multi-surface installs, and refuses to set one on a public suffix — the boundary where one tenant&#39;s cookie could reach another. It is guarded by the real Public Suffix List via <a href="/reference/psl.html"><code>@substrat-run/psl</code></a>, not a label-count heuristic.</p><h2 id="runtime" tabindex="-1">Runtime <a class="header-anchor" href="#runtime" aria-label="Permalink to &quot;Runtime&quot;">​</a></h2><p>Workers-targeted: <code>jose</code> + Web Crypto, no <code>node:*</code>. <code>IdentityDO</code> extends the <code>cloudflare:workers</code> <code>DurableObject</code> base, and the vertical&#39;s worker bundles this package and re-exports the DO class so wrangler can see it.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/vertical-auth.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const verticalAuth = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  verticalAuth as default
};
