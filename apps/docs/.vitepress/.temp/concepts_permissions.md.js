import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Permissions","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/permissions.md","filePath":"concepts/permissions.md","lastUpdated":1787601143000}');
const _sfc_main = { name: "concepts/permissions.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_PermissionPipeline = resolveComponent("PermissionPipeline");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h1><p>The permission <strong>model</strong> is kernel-owned — it is enforcement input, never delegated to an auth provider. The <strong>evaluation engine</strong> is an adapter behind one interface, with a built-in relationship-tuple engine as the default.</p><h2 id="the-authored-surface" tabindex="-1">The authored surface <a class="header-anchor" href="#the-authored-surface" aria-label="Permalink to &quot;The authored surface&quot;">​</a></h2><p>Humans (and agents, under review) write three kinds of things:</p><h3 id="permission-keys" tabindex="-1">Permission keys <a class="header-anchor" href="#permission-keys" aria-label="Permalink to &quot;Permission keys&quot;">​</a></h3><p>Module-namespaced strings declared in the <a href="/concepts/modules.html">manifest</a> with human-readable descriptions:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>workorder:create   Create work orders</span></span>
<span class="line"><span>workorder:report   Start work, report time and material</span></span>
<span class="line"><span>invoicing:export   Export an invoice basis (makes it immutable)</span></span></code></pre></div><h3 id="roles-nodes" tabindex="-1">Roles @ nodes <a class="header-anchor" href="#roles-nodes" aria-label="Permalink to &quot;Roles @ nodes&quot;">​</a></h3><p>A role bundles permissions; an assignment binds a principal to a role <strong>at a node of the tenancy tree</strong> — the tenant root or a specific scope — with inheritance down the tree:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.admin.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">defineRole</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(tenantId, {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  key: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;technician&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  permissions: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder:read&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder:report&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  source: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;vertical&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.admin.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">assignRole</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  principalId: tech,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  roleKey: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;technician&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  node: { tenantId, scopeId: stockholmBranch }, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// or scopeId: null = whole tenant</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><h3 id="capability-grants" tabindex="-1">Capability grants <a class="header-anchor" href="#capability-grants" aria-label="Permalink to &quot;Capability grants&quot;">​</a></h3><p>Narrow, direct, time-boxable grants — one permission, one node, optionally <strong>narrowed to one entity</strong> and its declared descendants:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.admin.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">grant</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  principalId: portalCustomer,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  permission: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder:read&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  node: { tenantId, scopeId: branch },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  entity: { entityType: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;facility&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, entityId: theirBuilding },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  expiresAt: nextMonth,      </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// optional</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  grantedBy: adminPrincipal,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p>Entity-narrowed grants are how portal users (a customer, a board member, a subcontractor) see only <em>their</em> facilities and orders inside a shared scope. Grants can also target an <strong>organization</strong>; members reach them via membership.</p><p>A <strong>connection</strong> is a first-class grant subject too — not only principals and orgs. A connector (an external provider acting through a connection) can hold a capability grant without pretending to be a person: <code>host.admin.grantToConnection(...)</code>, and a check&#39;s subject is polymorphic (<code>{ kind: &#39;principal&#39; | &#39;connection&#39; | &#39;system&#39;, id }</code>). A connection is keyed <code>(tenant, vertical, provider)</code>, so granting it one permission reaches only that tenant&#39;s scopes running that vertical — the blast radius of a leaked provider token is one permission on one vertical&#39;s data, readable in a diff.</p><p>A <strong>module&#39;s system principal</strong> is the third such subject — the caller behind <a href="/concepts/modules.html#recurring-work-schedules">scheduled work</a>. A schedule runs an operation on a timer, and it is not a person either; <code>host.admin.grantToSystem(...)</code> gives <code>{ kind: &#39;system&#39;, id: moduleId }</code> exactly the permissions the schedule declared, so the operation&#39;s own <code>ctx.check</code> resolves the same way it does for anyone — the gate stays the check, never a bypass — and the emitted events read as <code>{ system: &#39;@your/module&#39; }</code>. Like a connection it holds no memberships; its authority is exactly the grants written against <code>system:&lt;moduleId&gt;</code>, projected at provisioning and revocable per scope.</p><p>Organizations are a real directory record, not a string you make up at the call site:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> host.admin.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">createOrg</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(actor, {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  id: orgId,          </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// branded ULID — slug and name are attributes, not identity</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  tenantId,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  slug: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;acme&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,       </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// unique within the tenant</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  name: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Acme AB&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p><code>addMember</code>, <code>removeMember</code>, <code>listMembers</code> and <code>grantToOrg</code> all <strong>fail closed on an org that does not exist in that tenant</strong>. That refusal is the point of the record: a grant to an org nobody registered would otherwise look applied, resolve for nobody, and still show up in the permission diff as though access had been conferred. Because the id is a ULID rather than a name, renaming an org cannot orphan the tuples that reference it.</p><h2 id="from-declaration-to-enforcement" tabindex="-1">From declaration to enforcement <a class="header-anchor" href="#from-declaration-to-enforcement" aria-label="Permalink to &quot;From declaration to enforcement&quot;">​</a></h2><p>The <strong>keys and role templates</strong> are declared in TypeScript — the same objects the host registers (<a href="/concepts/modules.html">module manifests</a> declare the keys, provisioning declares the roles). That declared surface is not trusted to stay honest by convention; it passes three successive gates on its way to a running scope:</p>`);
  _push(ssrRenderComponent(_component_PermissionPipeline, null, null, _parent));
  _push(`<p>Three kinds of honesty, at three altitudes:</p><ul><li><strong>Review-time.</strong> <code>PERMISSIONS.md</code> is the human-readable render of the surface — the artifact for the <a href="/concepts/modules.html">permission checkpoint</a>. Because CI regenerates it with <code>--check</code> and fails on any difference, a role that gains a key cannot merge without showing up in the diff a reviewer must approve. The check reads the <em>same</em> declared objects the host registers, so the artifact cannot drift from what is enforced.</li><li><strong>Deploy-time.</strong> The surface is carried in the version&#39;s deploy manifest and content-hashed as <code>digests.permission</code>, so <strong>admission</strong> can compute a genuine permission diff between the running version and the incoming one. The declared surface is <strong>immutable per version</strong> — it is a property of the code, frozen when that version is built.</li><li><strong>Request-time.</strong> Roles and grants are <em>projected</em> into each scope&#39;s own tables at write time; the checker reads only that local projection (see below), and an absent or empty projection is a <strong>deny</strong>. Missing data can only ever remove authority, never confer it.</li></ul><p>This is the same layering the rest of the model rests on: the <strong>declared</strong> surface is a per-version code fact, <strong>operator</strong> state (a tenant&#39;s live roles) is a runtime-mutable table, and <strong>minted</strong> grants are scope-local tuples. None is a copy that can silently diverge from the others — each is the authority for its own layer.</p><h2 id="evaluation-relationship-tuples-with-a-fixed-algebra" tabindex="-1">Evaluation: relationship tuples with a fixed algebra <a class="header-anchor" href="#evaluation-relationship-tuples-with-a-fixed-algebra" aria-label="Permalink to &quot;Evaluation: relationship tuples with a fixed algebra&quot;">​</a></h2><p>Internally, the built-in checker compiles the authored surface into relationship tuples (<code>subject → relation → object</code>) and evaluates checks with a <strong>fixed, four-rule derivation algebra</strong>:</p><ol><li><strong>Role expansion</strong> — principal has role, role carries permission.</li><li><strong>Tenancy-tree inheritance</strong> — permission at a node flows down to child scopes.</li><li><strong>Entity parent edges</strong> — declared in module manifests (<code>workorder → facility</code>) and written at runtime via <code>ctx.link</code>; entity-narrowed grants flow along these edges, depth-capped.</li><li><strong>Org/group membership</strong> — grants to an organization reach its members.</li></ol><p>No negation, no configurable rewrite rules. Verticals never see or author tuples — roles and grants remain the only authored surface.</p><p><strong>Where tuples live — a scope reads only its own state.</strong> Scope and entity tuples (rules 2 and 3) live in the scope&#39;s own database and are evaluated inside its serialization domain, so there is no distributed-consistency problem for them. Tenant-level facts — role <em>definitions</em>, role assignments, tenant grants, and org membership (rules 1 and 4) — are tenant-wide, so the <strong>directory</strong> (the control plane) is their authority. But the checker never reads the directory on a request: the control plane <strong>projects</strong> those facts into scope-local tables (<code>_substrat_roles</code>, <code>_substrat_tenant_tuples</code>) at <em>write</em> time, and the checker reads the projection exactly as it reads scope tuples.</p><p>This is the important shift, and it is deliberate: <strong>the control plane is a write-time authority that projects into scopes; it is never a read-time dependency on the request path.</strong> Cost moves from the read path (every request) to the write path (rare role/grant changes) — the correct direction for a read-heavy multi-tenant system, and what makes a scope genuinely isolated: it can run as its own Cloudflare Worker with <em>no</em> control-plane binding at all. (For the earlier per-request model and why it was replaced — scaling, isolation, and the untrusted-vertical requirement — see the <a href="https://github.com/substrat-run/substrat/blob/main/docs/architecture/scope-local-permissions.md" target="_blank" rel="noreferrer">scope-local permissions design note</a>.)</p><p>The consistency consequence is small and one-directional. Scope-level grants/roles stay synchronous and immediately consistent (&quot;no zookies&quot;) — the checker runs in the scope&#39;s own serialization domain. Only <strong>tenant-level</strong> changes are now eventually consistent across a tenant&#39;s scopes, bounded by the write-time fan-out and a reconciliation sweep. An <strong>absent or empty projection is a deny</strong>, byte-for-byte the normal deny path, so missing data can only ever <em>remove</em> authority, never grant it. Revocation still fans out as tombstones (below) — now into every scope the tuple was projected to.</p><h2 id="revocation-tuples-tombstone-they-never-disappear" tabindex="-1">Revocation: tuples tombstone, they never disappear <a class="header-anchor" href="#revocation-tuples-tombstone-they-never-disappear" aria-label="Permalink to &quot;Revocation: tuples tombstone, they never disappear&quot;">​</a></h2><p>Access is withdrawn by <strong>tombstoning</strong> a tuple — it keeps its row, gains a <code>revokedAt</code>, and the checker&#39;s walk skips it. Nothing deletes a tuple.</p><p>That is deliberate. A tuple that once granted access is the evidence of <em>why</em> an access was allowed, so deleting it destroys the audit trail exactly where it is most needed: a deleted row can show neither that access was revoked nor that it was ever granted. Every revocation path in the kernel works this way — <code>removeMember</code> is the first of them — and <code>listMembers({ includeRevoked: true })</code> is the evidence view over the result.</p><p>Liveness is therefore one predicate, applied identically everywhere: a tuple grants only while it is <strong>unexpired and unrevoked</strong>. Expiry (<code>expiresAt</code> above) and revocation are siblings, not separate mechanisms. The checker interface is deliberately swappable (an OpenFGA-backed adapter is the designated alternative), and any implementation must pass the same contract tests.</p><h2 id="decisions-carry-proof" tabindex="-1">Decisions carry proof <a class="header-anchor" href="#decisions-carry-proof" aria-label="Permalink to &quot;Decisions carry proof&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Decision</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">allowed</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">proof</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> RelationTuple</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[] }   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// the chain that granted access</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">allowed</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> false</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">checked</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> PermissionKey</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">node</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Node</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> };</span></span></code></pre></div><p>An allow <strong>always</strong> carries the tuple chain that produced it — an unexplained allow is unrepresentable. This powers:</p><ul><li><strong>explain</strong> — why does this user see this?</li><li><strong>view-as-user</strong> — render any screen as any principal, with real decisions;</li><li><strong>the human-readable permission diff</strong> — the review artifact for the permission checkpoint: who gains what, where in the tree.</li></ul><p>And it carries past the request: when an operation emits, the kernel stamps onto the event envelope which permission(s) it checked-and-passed and — when the allow came through a capability grant rather than a role — a ref to the granting tuple (<code>authorization</code>, K-34). The full chain is re-derivable by <code>explain</code>; what re-derivation cannot recover once tuples have since changed is <em>which</em> permission and grant were consulted at write time. That pointer is what the envelope keeps, so a mutation carries its own proof of authority (<a href="/concepts/events.html">Events &amp; audit</a>). Decisions carry proof forward, not only at the instant they are made.</p><h2 id="denials-are-recorded" tabindex="-1">Denials are recorded <a class="header-anchor" href="#denials-are-recorded" aria-label="Permalink to &quot;Denials are recorded&quot;">​</a></h2><p>An allow leaves its trace on the event it authorized; a <strong>denial</strong> has no event to ride, so it is captured on its own. When an enforced check refuses, the kernel writes a row to a scope-local <code>_substrat_denials</code> table (<code>id</code>, <code>actor</code>, <code>permission</code>, <code>tenant_id</code>, <code>scope_id</code>, <code>operation</code>, <code>at</code>, <code>drained_at</code>) — the one moment where an actor&#39;s intent and the permission model visibly disagree, and which no other log witnesses (the admin log records changes, the outbox records <em>allowed</em> mutations). Because a denial rolls its operation back, the row is written <strong>outside</strong> that transaction, at the operation boundary after the rollback, so the record survives the very failure it describes.</p><p>This is <strong>not</strong> the K-24 staff-directory read log (<code>_substrat_access_log</code>): that records control-plane <em>reads</em> against the directory; <code>_substrat_denials</code> records refused checks inside a scope. Two different tables for two different facts. Both <em>drain</em> rather than expire — <code>drained_at</code> marks a row shipped onward, and only a drained row may be pruned.</p><h3 id="reading-them-back" tabindex="-1">Reading them back <a class="header-anchor" href="#reading-them-back" aria-label="Permalink to &quot;Reading them back&quot;">​</a></h3><p>Two staff reads on <code>HostAdmin</code>, served by the control plane as <code>GET /tenants/:t/scopes/:s/denials</code> and <code>…/denials/summary</code>, and rendered per scope in the console:</p><ul><li><strong><code>summarizeDenials</code></strong> buckets the log per (actor, permission) with a count, the number of distinct operations, and first/last occurrence. This is the view to open first, and the ordering is <strong>by count</strong> on purpose: the volume of this log is attacker-influenceable — a probing client mints unlimited rows — so a newest-first page would let whoever wrote the last hundred rows push every other actor off the screen.</li><li><strong><code>listDenials</code></strong> returns the raw rows behind a bucket, newest first, narrowed by <code>actor</code>, <code>permission</code>, <code>operation</code>, and a <code>since</code>/<code>until</code> window.</li></ul><p>The summary also carries the log&#39;s own <strong>window</strong> — its oldest and newest held rows, computed <em>ignoring</em> the filter. That is what keeps an empty result honest: because rows drain rather than expire, absence before the window&#39;s floor means &quot;no longer held&quot;, not &quot;never happened&quot;.</p><p>Both reads are actor-stamped and recorded to the K-24 access log, so reading the denial log is itself logged, and both cross-check the <code>(tenantId, scopeId)</code> pair and fail closed on a mismatch — a confused-deputy scope id resolves to nothing, never another tenant&#39;s refusals.</p><h2 id="in-operations" tabindex="-1">In operations <a class="header-anchor" href="#in-operations" aria-label="Permalink to &quot;In operations&quot;">​</a></h2><p>The standard first line of every operation:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { assertAllowed } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/kernel&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> handler</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> OperationHandler</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Input</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Output</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt; </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">input</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  assertAllowed</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">check</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder:read&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">orderRef</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(input.orderId)));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  // ...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">};</span></span></code></pre></div><p><code>ctx.check</code> evaluates the ambient principal at the ambient node. Pass an <code>EntityRef</code> for per-entity checks: the checker tries node-level first (staff see everything in the scope), then walks the declared parent edges against entity-narrowed grants (portal users see their own things).</p><h2 id="defaults" tabindex="-1">Defaults <a class="header-anchor" href="#defaults" aria-label="Permalink to &quot;Defaults&quot;">​</a></h2><ul><li><code>denyAllChecker</code> — the secure default. A host without an explicit checker allows nothing.</li><li><code>UNSAFE_allowAllChecker</code> — grants everything to everyone via a synthetic proof tuple. For tests and scratch scripts; the name is deliberately alarming. Never wire it into anything a tenant can reach.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/permissions.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const permissions = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  permissions as default
};
