import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RallyPoint (padel club)","description":"","frontmatter":{},"headers":[],"relativePath":"verticals/rallypoint.md","filePath":"verticals/rallypoint.md","lastUpdated":1787127415000}');
const _sfc_main = { name: "verticals/rallypoint.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rallypoint-padel-club" tabindex="-1">RallyPoint (padel club) <a class="header-anchor" href="#rallypoint-padel-club" aria-label="Permalink to &quot;RallyPoint (padel club)&quot;">​</a></h1><p><code>demos/rally</code> — a <strong>racket-club booking platform</strong>: courts, availability, open matches, prepaid credit and subscriptions, across several venues, in <strong>two apps over one API</strong> — a mobile app for players and a desktop console for club staff.</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>RallyPoint is the demo that proves the platform holds up on a <strong>second invariant shape</strong>. Every other vertical composes a <em>state machine</em> — an order that moves <code>planned → completed</code>. A reservation is not that. It is <strong>allocation against capacity over an interval</strong>, and the thing that must never happen is two people holding the same court at the same time.</p><p>It is interesting for four reasons the other demos can&#39;t show:</p><ul><li><strong>The lost race, with no locking code.</strong> Two people book the last slot; one is refused with <code>SlotUnavailable</code>. There is no <code>SELECT … FOR UPDATE</code>, no retry loop, no lock anywhere in <a href="/engines/booking/">the engine</a> — because a scope is a single Durable Object, so the check and the write that follows it never interleave. This is the clearest demonstration in the repo that the DO-per-scope choice buys something concrete.</li><li><strong>Atomicity across two concerns, for free.</strong> Paying for a court from a prepaid balance debits a ledger <em>and</em> confirms a reservation in one transaction in one scope. &quot;Charged but not booked&quot; and &quot;booked but not charged&quot; are both <strong>unrepresentable</strong>, not merely unlikely — the expensive part of this feature everywhere else.</li><li><strong>A consumer who holds no role at all.</strong> A player is not a principal with a role. They hold scope-wide grants for things that are genuinely public at a club (see what is free, take a free court) and <strong>entity-narrowed</strong> grants for everything touching a booking that already exists. Widening the latter to the scope would hand every player the club&#39;s entire book.</li><li><strong>Multi-venue tenancy that the UI has to respect.</strong> One tenant runs two venues; a tenant-level admin reaches both, a receptionist is pinned to one — and <em>the console shows no venue switcher to staff who have only one</em>, because reachability is the permission model answering rather than a UI preference.</li></ul><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>demos/rally</code> (private)</td></tr><tr><td><strong>Tenancy shape</strong></td><td>2 tenants — RallyPoint AB (Solna + Nacka scopes) · Padelcenter Väst AB (the cross-tenant attack victim)</td></tr><tr><td><strong>Engines composed</strong></td><td><a href="/engines/booking/"><code>booking</code></a> · <a href="/engines/invoicing/"><code>invoicing</code></a> · <a href="/engines/invites/"><code>invites</code></a></td></tr><tr><td><strong>Own tables</strong></td><td><code>rally_venue</code> · <code>rally_members</code> · <code>rally_wallet_entries</code> (append-only) · <code>rally_credit_packs</code> · <code>rally_plans</code> · <code>rally_subscriptions</code> · <code>rally_courts</code> · <code>rally_venue_hours</code> · <code>rally_court_hours</code> · <code>rally_closures</code> · <code>rally_price_rules</code> · <code>rally_bookings</code> · <code>rally_matches</code> · <code>rally_invited_player</code></td></tr><tr><td><strong>Roles</strong></td><td><code>club-admin</code> (tenant) · <code>receptionist</code> / <code>coach</code> (scope) — players are grants, not a role</td></tr><tr><td><strong>Permission surface</strong></td><td><a href="https://github.com/substrat-run/substrat/blob/main/demos/rally/PERMISSIONS.md" target="_blank" rel="noreferrer"><code>PERMISSIONS.md</code></a> — 18 keys · 4 modules · 3 roles</td></tr><tr><td><strong>Apps</strong></td><td>player (mobile, <code>:5277</code>) · manager console (desktop, <code>:5278</code>) over one API (<code>:8877</code>)</td></tr><tr><td><strong>Status</strong></td><td>demo seed — the social/player tier and payment rail are deliberately out of scope (below)</td></tr></tbody></table><h2 id="engines-composed" tabindex="-1">Engines composed <a class="header-anchor" href="#engines-composed" aria-label="Permalink to &quot;Engines composed&quot;">​</a></h2><h3 id="booking-—-the-allocation-invariant" tabindex="-1"><code>booking</code> — the allocation invariant <a class="header-anchor" href="#booking-—-the-allocation-invariant" aria-label="Permalink to &quot;\`booking\` — the allocation invariant&quot;">​</a></h3><p>The vertical never checks whether a court is free. It validates its <strong>own</strong> rules, resolves its <strong>own</strong> price, hands the engine an absolute interval, and lets the engine arbitrate:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rally/book-court</span></span>
<span class="line"><span>  ├── vertical: is the club open? is this duration offered on this court?</span></span>
<span class="line"><span>  ├── vertical: which price rule wins, and what does it cost?</span></span>
<span class="line"><span>  ├── ENGINE:   holdReservation(…)  ← throws SlotUnavailable if taken</span></span>
<span class="line"><span>  └── vertical: record price in rally_bookings, keyed by the engine&#39;s id</span></span></code></pre></div><p>A vertical that checked for a clash first would be doing it wrong — it cannot do so correctly, and the engine already does.</p><h3 id="invites-—-joining-the-club" tabindex="-1"><code>invites</code> — joining the club <a class="header-anchor" href="#invites-—-joining-the-club" aria-label="Permalink to &quot;\`invites\` — joining the club&quot;">​</a></h3><p>Clubs are <strong>tenants</strong> here, so joining one is a kernel membership, not a row the vertical can write. <code>rally/invite-player</code> composes the engine; accepting produces two records with two owners:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rally/invite-player</span></span>
<span class="line"><span>  ├── ENGINE:   sendInvite(…)          ← stores a HASH, never the address</span></span>
<span class="line"><span>  └── vertical: remember the name and party ref, keyed by the invitation id</span></span>
<span class="line"><span></span></span>
<span class="line"><span>player accepts</span></span>
<span class="line"><span>  ├── ENGINE:   emits member.add-requested   ← it cannot write the directory</span></span>
<span class="line"><span>  ├── EXECUTOR: admin.addMember(…)           ← the kernel membership</span></span>
<span class="line"><span>  └── vertical: creates the rally_members row on invites.accepted</span></span></code></pre></div><p>The kernel tuple says <em>this principal belongs to this club</em>. <code>rally_members</code> says what a club knows — name, level, wallet. Neither can own the other: one is tenant-wide directory state, the other is per-venue scope data.</p><p>Two things this vertical has to get right, and both are easy to miss:</p><ul><li><strong>Re-inviting must not fail.</strong> The engine returns the <em>same</em> invitation id for someone already invited, because it refuses to reveal that they are. A plain <code>INSERT</code> on the side table would fail on the primary key and leak exactly the fact the engine hides.</li><li><strong>The consumer must be idempotent.</strong> Delivery is at-least-once, and a second member row would give one human two wallets at the same club.</li></ul><h3 id="the-orchestration-moment-paying-from-a-balance" tabindex="-1">The orchestration moment: paying from a balance <a class="header-anchor" href="#the-orchestration-moment-paying-from-a-balance" aria-label="Permalink to &quot;The orchestration moment: paying from a balance&quot;">​</a></h3><p><code>rally/confirm-booking</code> composes the engine&#39;s <code>confirmReservation</code> and the vertical&#39;s own ledger debit <strong>in one transaction</strong>. If the balance is short, the whole thing rolls back — the reservation stays <code>held</code> <em>and</em> the ledger stays empty. There is a test that asserts exactly that, because it is the property the design claims.</p><h3 id="what-is-vertical-and-emphatically-not-engine" tabindex="-1">What is vertical, and emphatically not engine <a class="header-anchor" href="#what-is-vertical-and-emphatically-not-engine" aria-label="Permalink to &quot;What is vertical, and emphatically not engine&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Concern</th><th>Lives in</th><th>Why</th></tr></thead><tbody><tr><td>Timezones, opening hours, DST</td><td>vertical</td><td>the engine takes instants and does no calendar arithmetic</td></tr><tr><td>Pricing, seasonal rules</td><td>vertical</td><td>the engine never learns what a slot costs</td></tr><tr><td>Who owns an open game</td><td>vertical</td><td>the club, a player up front, or a player opening a booking they hold</td></tr><tr><td>Level bands, cancellation windows</td><td>vertical</td><td>the engine knows only <em>fill target</em> and <em>deadline</em></td></tr><tr><td>Wallet, credit packs, subscriptions</td><td>vertical</td><td><a href="https://github.com/substrat-run/substrat/blob/main/docs/strategy/commerce-gaps.md" target="_blank" rel="noreferrer">commerce-gaps §4.6</a> — a subscription engine designed off a demo wishlist is the exact failure mode D-27 prevents</td></tr></tbody></table><p>Two domain details worth stealing:</p><ul><li><strong>Floodlights are priced by season, not by the clock.</strong> A night surcharge exists because someone pays for the lights, and Stockholm sunset moves from ~14:45 in December to ~22:00 in June — so a fixed &quot;after 17:00&quot; rule bills for lights in June daylight and misses the December afternoon. Price rules carry a date range, and a season outranks a time of day.</li><li><strong>A subscription is a wallet topped up on a schedule.</strong> One mechanism, not two. The recurrence is a Workflow&#39;s job; the credit and the cursor advance happen in one transaction.</li><li><strong>A price table is a matrix, and its failure mode is a hole in it.</strong> Duration is an input rather than a multiplier, so every (window × length) pair is priced explicitly — and the specificity ladder is a <em>tie-breaker</em>, not a composition, so a length-only rule would replace peak pricing rather than add to it. The console renders what the rules actually <em>resolve</em> to, hour by hour, because a row where all three lengths cost the same is invisible in the rule list and obvious in the grid.</li></ul><h2 id="the-cast-what-s-denied" tabindex="-1">The cast &amp; what&#39;s denied <a class="header-anchor" href="#the-cast-what-s-denied" aria-label="Permalink to &quot;The cast &amp; what&#39;s denied&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Who</th><th>Holds</th><th>Cannot</th></tr></thead><tbody><tr><td><strong>Astrid</strong> — klubbchef</td><td><code>club-admin</code> at <strong>tenant</strong> level</td><td>nothing at Padelcenter Väst; her role is her own tenant&#39;s</td></tr><tr><td><strong>Ravi</strong> — reception, Solna</td><td><code>receptionist</code> <strong>scoped to Solna</strong></td><td>re-cut hours, create courts, read roles — and gets no venue switcher at all</td></tr><tr><td><strong>Nils</strong> — coach</td><td><code>coach</code></td><td>book anything; he reads the calendar and writes nothing</td></tr><tr><td><strong>Elin / Johan</strong> — players</td><td><strong>no role.</strong> Scope-wide <code>rally:browse</code> + <code>booking:hold</code>; entity-narrowed <code>booking:read</code>/<code>confirm</code>/<code>cancel</code> on their own member record</td><td>read the club&#39;s book, the member roster, another player&#39;s wallet</td></tr><tr><td><strong>Rutger</strong> — another company&#39;s admin</td><td><code>club-admin</code> in his own tenant</td><td>anything here — and the refusal differs by layer (below)</td></tr></tbody></table><p>Two of these are worth reading closely.</p><p><strong>The coach grant is deliberately broad</strong> — plain <code>booking:read</code> is the <em>whole</em> venue calendar, member names included, not just his own lessons. Reviewed and accepted at the permission checkpoint; narrowing it needs a per-coach entity grant, and the line to re-open it on is a club running independent coaches who must not see each other&#39;s business. A test pins the breadth, so narrowing it later is a visible change rather than a silent one.</p><p><strong>The attacker is refused differently depending on where he knocks.</strong> In-scope he names the <code>(tenant, scope)</code> pair himself, gets it wrong, and gets <code>unknown scope</code>. Through the HTTP surface the <em>server</em> supplies the pair from its venue table, so he cannot mis-pair it — he reaches a real scope and is refused by the permission model instead. A stronger answer, one layer further in.</p><h2 id="the-app" tabindex="-1">The app <a class="header-anchor" href="#the-app" aria-label="Permalink to &quot;The app&quot;">​</a></h2><p>Two surfaces, one API. The split is chrome and audience, never a second source of truth.</p><p><strong>Player app</strong> (mobile, 402px) — booking is <strong>two steps, split by what each one decides</strong>:</p><table tabindex="0"><thead><tr><th></th><th>Decides</th><th>Also carries</th></tr></thead><tbody><tr><td><strong>when</strong></td><td>a start time</td><td><em>filters</em> only — date, roof, and optionally a length</td></tr><tr><td><strong>what</strong></td><td>length (defaults to 90), court, payment</td><td>the price, quoted before committing</td></tr></tbody></table><p>A player never picks a court to <em>find</em> a time: availability is aggregated across the venue, so a start is offered if <strong>any</strong> court can take it, and the court is assigned from the filtered pool or chosen last by the few who care. Each start shows which lengths actually fit the gap (● 60 · ●● 90 · ●●● 120), so no tap can dead-end. A lost race never shows a bare error — it offers the nearest alternatives. Screen state lives in the URL, so a refresh lands where you were.</p><p>Putting length on step 1 as a <em>choice</em> was the mistake this replaced: it asked for a decision about the booking before the player had one to make, and mixed a filter with a commitment. Asking about it twice is fine — they are different questions.</p><p><strong>Manager console</strong> (desktop, 1440px+) — a resource-grid calendar, courts as columns and time as rows, with a peak band, a now-line, and cell states that are each border + fill + icon + label, <strong>never colour alone</strong>: confirmed, held-with-countdown, open match filling, maintenance stripes, outside-hours hatch, expired, past. Cells render <code>effectiveState</code>, so a lapsed hold never shows a countdown ticking past zero. The booking drawer keeps every typed value on a rejection and offers one-tap alternatives.</p><p>Both are built from a high-fidelity design handover kept in <a href="https://github.com/substrat-run/substrat/tree/main/demos/rally/spec/design" target="_blank" rel="noreferrer"><code>demos/rally/spec/design/</code></a>.</p><h2 id="run-it" tabindex="-1">Run it <a class="header-anchor" href="#run-it" aria-label="Permalink to &quot;Run it&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/demo-rally</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># API      http://localhost:8877</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># player   http://localhost:5277</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># console  http://localhost:5278</span></span></code></pre></div><p>The executable spec is three suites, <strong>45 tests</strong>:</p><ul><li><code>test/provision.test.ts</code> — provisioning one RallyPoint instance: exactly one tenant and one scope (no cast, no second company), the owner getting <code>club-admin</code> and nobody else a role, and that provisioning twice doubles nothing.</li><li><code>test/scenario.test.ts</code> — the invariants, called directly: the lost race, lazy hold expiry, seasonal pricing to the öre, wallet atomicity, portal isolation, the cross-tenant attack.</li><li><code>test/flows.test.ts</code> — the same journeys walked through the <strong>real HTTP routes</strong>, ending with a guard that fails if any registered route was never exercised. That guard exists because every bug this demo shipped was a wiring bug — an operation with no route, a link nothing read, a button with no path behind it — and a test that reaches for <code>ctx</code> is structurally blind to all of them.</li></ul><h2 id="deliberately-out-of-scope" tabindex="-1">Deliberately out of scope <a class="header-anchor" href="#deliberately-out-of-scope" aria-label="Permalink to &quot;Deliberately out of scope&quot;">​</a></h2><ul><li><strong>The cross-club player tier.</strong> Connections (&quot;players I&#39;ve played with&quot;), groups, and a portable rating are keyed to a <em>global</em> player identity owned by no tenant. A club&#39;s scope cannot answer &quot;which other clubs does she play at&quot;, and must not be able to. Those live in a downstream tier fed by the event outbox — see <a href="https://github.com/substrat-run/substrat/blob/main/docs/rfc/booking-social.md" target="_blank" rel="noreferrer">booking-social.md</a>.</li><li><strong>A payment rail.</strong> Buying credit currently mints balance without charging anything; the money is imaginary. Stripe Connect is a connector, and connectors are a framework this repo has specified and not yet built.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("verticals/rallypoint.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const rallypoint = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  rallypoint as default
};
