import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse(`{"title":"Domain model & invariants","description":"The invoicing engine's two tables, the immutable-after-export invariant, why lines snapshot their source rather than join to it, and the rule that one document carries one currency.","frontmatter":{"description":"The invoicing engine's two tables, the immutable-after-export invariant, why lines snapshot their source rather than join to it, and the rule that one document carries one currency."},"headers":[],"relativePath":"engines/invoicing/model.md","filePath":"engines/invoicing/model.md","lastUpdated":1787063543000}`);
const _sfc_main = { name: "engines/invoicing/model.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="domain-model-invariants" tabindex="-1">Domain model &amp; invariants <a class="header-anchor" href="#domain-model-invariants" aria-label="Permalink to &quot;Domain model &amp; invariants&quot;">​</a></h1><h2 id="tables" tabindex="-1">Tables <a class="header-anchor" href="#tables" aria-label="Permalink to &quot;Tables&quot;">​</a></h2><p>Created by migration <code>0001-init</code>, inside each scope&#39;s own database:</p><ul><li><strong><code>invoicing_underlag</code></strong> — id, sequential <code>number</code>, customer as <code>EntityRef</code> columns (<code>customer_type</code>/<code>customer_id</code>), <code>status</code> (<code>open</code>/<code>exported</code>), <code>created_at</code>, <code>exported_at</code>.</li><li><strong><code>invoicing_lines</code></strong> — underlag, provenance (<code>source_type</code>, <code>source_id</code>), article, description, qty, unit, unit price (amount + currency), line total, <code>created_at</code>.</li></ul><p>There is no customer table and no article table — those are opaque refs the vertical owns.</p><h2 id="the-immutability-invariant" tabindex="-1">The immutability invariant <a class="header-anchor" href="#the-immutability-invariant" aria-label="Permalink to &quot;The immutability invariant&quot;">​</a></h2><p>An underlag is <code>open</code> or <code>exported</code>:</p><ul><li>While <strong>open</strong>, consumed events append lines.</li><li><strong><code>invoicing/export</code></strong> flips it to <code>exported</code> — and from then on it is immutable. Nothing appends to it, nothing edits it, and attempting to export it again throws.</li><li>Billable work arriving <em>after</em> export (a late time report, a re-opened order) opens a <strong>new</strong> underlag. History is never rewritten; late facts become new facts.</li></ul><p>This is the engine invariant on top of the kernel&#39;s guarantees: the kernel makes events trustworthy; the engine makes the financial artifact derived from them tamper-proof.</p><h2 id="snapshot-not-join" tabindex="-1">Snapshot, not join <a class="header-anchor" href="#snapshot-not-join" aria-label="Permalink to &quot;Snapshot, not join&quot;">​</a></h2><p>Each consumed event has its <strong>own</strong> consumer with its <strong>own</strong> Zod view of the payload — never the producer&#39;s types. When a work order completes, the consumer:</p><ol><li><p><strong>Parses its own view of the payload</strong>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> completedPayload</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">object</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  orderId: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">min</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  number: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  customer: entityRef,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  billable: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">array</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">object</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({ article, description, qty, unit,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">                               unitPrice: money, lineTotal: money, sourceType, sourceId })),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  total: money,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div></li><li><p><strong>Guards against replay</strong> — if lines for that <code>source_id</code> already exist, return.</p></li><li><p><strong>Finds or creates the open underlag</strong> for that customer — one open collection per customer at a time.</p></li><li><p><strong>Copies the billable lines</strong> with provenance (<code>source_type: &#39;workorder&#39;</code>, <code>source_id: orderId</code>).</p></li><li><p><strong>Emits <code>invoicing.underlag-updated</code>.</strong></p></li></ol><p>Prices and quantities are <strong>frozen from the event payload</strong> at the moment the work order completed. If the vertical&#39;s price list changes tomorrow, yesterday&#39;s underlag doesn&#39;t — which is exactly what an invoice basis must guarantee. Provenance is kept as <code>EntityRef</code> columns, so every line can answer &quot;where did you come from?&quot; without the engine ever querying the work-order tables.</p><p>The <code>commerce.order-placed</code> consumer is the same five steps with <code>source_type: &#39;order&#39;</code> (a discount arrives as a negative line, so the total stays net), plus one domain rule: <strong>only invoice-payment orders bill</strong> — card and Swish settle through a payment connector, so those events return early.</p><h2 id="idempotency" tabindex="-1">Idempotency <a class="header-anchor" href="#idempotency" aria-label="Permalink to &quot;Idempotency&quot;">​</a></h2><p>Consumers are delivered <strong>at-least-once</strong> and run under a system actor. Three things keep the handlers idempotent:</p><ol><li>find-or-create on the underlag,</li><li>the kernel&#39;s delivery journal,</li><li>a <strong>source-id guard</strong> — before inserting anything, each consumer asks whether lines for that <code>source_id</code> already exist and returns if they do.</li></ol><p>The source order is the dedup key, so a replayed completion adds nothing rather than billing the customer twice.</p><p>Dispatch is <strong>post-commit</strong>, and each consumer runs in <strong>its own transaction</strong>. A consumer that throws does not roll back the producer — it rolls back only its own work, and the delivery is <strong>dead-lettered</strong> (journalled with the error) so one poison event can&#39;t wedge the loop. That&#39;s why a malformed payload leaves no half-built underlag, and why <code>await</code> on the producing operation tells you nothing about whether the consumer succeeded.</p><h2 id="one-document-one-currency" tabindex="-1">One document, one currency <a class="header-anchor" href="#one-document-one-currency" aria-label="Permalink to &quot;One document, one currency&quot;">​</a></h2><p>Totals use the sanctioned <a href="/concepts/money.html">exact decimal arithmetic</a> — and <code>addMoney</code>, not <code>addDecimal</code>, precisely because it is <strong>currency-aware</strong>. Summing 100 SEK and 100 EUR into <code>200</code> is not a rounding error; it is a financial artifact stating a number that means nothing, so the engine refuses instead.</p><p>A delivery whose lines disagree with each other, or with the lines already on the open underlag, is rejected <strong>at write time</strong> and dead-lettered — so a mixed-currency event never creates a document that can&#39;t be read or exported. Rejecting at read time instead would leave a document that can never be listed or exported again.</p><div class="warning custom-block"><p class="custom-block-title">Currency lives on the line, not the document</p><p>Currency is carried per line today, so an underlag with no lines has no currency to report and falls back to <code>SEK</code> — attributing a currency to an empty document is exactly the guess this engine shouldn&#39;t make. Hoisting currency onto the underlag itself is the honest fix; it needs a migration and therefore a human review checkpoint.</p></div></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/invoicing/model.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const model = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  model as default
};
