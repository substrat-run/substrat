import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Composing & extending","description":"Registering the absence engine, the subject boundary that keeps it free of any employee directory, and why accrual, carryover and the calendar stay in your vertical.","frontmatter":{"description":"Registering the absence engine, the subject boundary that keeps it free of any employee directory, and why accrual, carryover and the calendar stay in your vertical."},"headers":[],"relativePath":"engines/absence/composing.md","filePath":"engines/absence/composing.md","lastUpdated":1787063543000}');
const _sfc_main = { name: "engines/absence/composing.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="composing-extending" tabindex="-1">Composing &amp; extending <a class="header-anchor" href="#composing-extending" aria-label="Permalink to &quot;Composing &amp; extending&quot;">​</a></h1><h2 id="using-it-as-is" tabindex="-1">Using it as-is <a class="header-anchor" href="#using-it-as-is" aria-label="Permalink to &quot;Using it as-is&quot;">​</a></h2><p>Register it and the default bindings work:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { absenceModule } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/engine-absence&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">registerModule</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(absenceModule);</span></span></code></pre></div><p>That gets you leave-type registration, the request → approve flow, balances, and availability under the engine&#39;s own permission keys. Most verticals wrap <code>request</code> and the reads instead, because that is where their directory and their vocabulary live.</p><h2 id="the-subject-boundary" tabindex="-1">The subject boundary <a class="header-anchor" href="#the-subject-boundary" aria-label="Permalink to &quot;The subject boundary&quot;">​</a></h2><p><strong>The engine never owns a directory.</strong> Every write takes <code>subject: { ref, dataSubjectId }</code>, and the engine stores the pair without ever dereferencing it. That one line is what lets the same engine serve very different verticals:</p><ul><li>an HR product binds <code>{ entityType: &#39;employee&#39;, entityId }</code> from its own employee table, with the employee id as the erasure key;</li><li>a field-service planner binds <code>{ entityType: &#39;resource&#39;, entityId }</code> from its resource register — where a <em>resource</em> is a plannable unit, not an identity: one human can be two resources, and a resource with no login still gets sick.</li></ul><p>Two consequences you own:</p><ol><li><strong>Existence checks are yours.</strong> The engine accepts any ref; verify the employee/resource exists in <em>your</em> operation, before the engine call.</li><li><strong>The <code>dataSubjectId</code> must actually map to the person.</strong> It keys crypto-shredding on every event the write emits; choose the id your erasure path already shreds on.</li></ol><h2 id="the-vocabulary-split" tabindex="-1">The vocabulary split <a class="header-anchor" href="#the-vocabulary-split" aria-label="Permalink to &quot;The vocabulary split&quot;">​</a></h2><p>The engine&#39;s <code>absence_leave_types</code> row is deliberately skeletal: <strong>key, floor, active</strong>. Everything a human sees lives in <em>your</em> table, keyed by the same string:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// Your operation, one transaction: your vocabulary row + the engine&#39;s policy row.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">defineOperation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;hr/define-leave-type&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">input</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  assertAllowed</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">check</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">PERM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.configure));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  upsertMyLeaveTypeVocabulary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, input);          </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// label, kind, statutory days — yours</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  configureLeaveType</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { key: input.key });       </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// key + floor — the engine&#39;s</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> …;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p>Per-country divergence is data, not forks: a Swedish scope registers <code>vacation</code> with 25 statutory days in its vocabulary and <em>VAB</em> as a type; a Spanish scope registers 22 and <em>baja</em> — same engine, same keys where they overlap, different vocabulary rows per scope.</p><p><strong>The floor is the one policy knob the engine holds.</strong> <code>floor: &#39;0&#39;</code> (the default) refuses overdraft; <code>floor: &#39;-25&#39;</code> is <em>förskottssemester</em> — advance vacation up to 25 days — enforced at decision time with no vertical code.</p><h2 id="accrual-carryover-and-the-calendar-—-yours" tabindex="-1">Accrual, carryover, and the calendar — yours <a class="header-anchor" href="#accrual-carryover-and-the-calendar-—-yours" aria-label="Permalink to &quot;Accrual, carryover, and the calendar — yours&quot;">​</a></h2><p>The engine records entries; it never generates them. The compositions:</p><ul><li><strong>Accrual rules</strong> — your schedule or your operation calls <code>recordEntry(…, entryKind: &#39;accrual&#39;)</code> per your formula (annual grant, monthly fraction, tenure tiers).</li><li><strong>Year-end carryover</strong> — a vertical-declared schedule that folds each subject&#39;s balance and writes <code>carryover</code> entries per your caps. The trigger seam (a manifest schedule) is the same one the engine itself uses for expiry.</li><li><strong>Day counting</strong> — the <code>days</code> on a request is yours to compute (weekends, red days, half-days), which is also why the engine never checks it against the date range.</li><li><strong>The availability verdict is half of the planner&#39;s answer.</strong><code>availability(ctx, { subject, from, to })</code> returns dates covered by approved absence; compose it with your holiday calendar for &quot;is Hugo actually out Monday&quot;:</li></ul><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">days</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> availability</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { subject: </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">resourceRef</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(id), from, to });</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> out</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Set</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(days.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">map</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">((</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> d.date));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> holidays</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> myRedDays</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, from, to);          </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// your scope&#39;s calendar</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> dates.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">((</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> !</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">out.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">has</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(d) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&amp;&amp;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> !</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">holidays.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">has</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(d));</span></span></code></pre></div><h2 id="composing-the-window-reads" tabindex="-1">Composing the window reads <a class="header-anchor" href="#composing-the-window-reads" aria-label="Permalink to &quot;Composing the window reads&quot;">​</a></h2><p><code>entriesInWindow(ctx, { from, to, entryKind: &#39;booking&#39; })</code> is in-scope only — no operation binding — because its two known callers are vertical compositions with their own gates: a <strong>payroll export</strong> collecting the period&#39;s booked absence next to approved expenses, and a <strong>planner</strong> sweeping a route window across every resource. Wrap it behind your own permission; the engine deliberately does not decide who may see everyone at once.</p><h2 id="what-extension-is-not" tabindex="-1">What extension is <em>not</em> <a class="header-anchor" href="#what-extension-is-not" aria-label="Permalink to &quot;What extension is *not*&quot;">​</a></h2><p>The engine registers one frozen constant — no options object, no config field. When you need different behaviour, you compose (your operation calling in-scope functions), store (vocabulary as scope data), or gate (the entitlement flag). If you find yourself wanting to fork it — a sixth entry kind, an approval step the machine doesn&#39;t have — that is a boundary finding worth filing, not patching around: the state machine and the entry-kind mint are exactly the invariants the engine exists to keep identical everywhere.</p><h2 id="the-extraction-handoff-if-you-were-vertical-first" tabindex="-1">The extraction handoff, if you were vertical-first <a class="header-anchor" href="#the-extraction-handoff-if-you-were-vertical-first" aria-label="Permalink to &quot;The extraction handoff, if you were vertical-first&quot;">​</a></h2><p>If you already run an absence ledger as vertical tables, adopt the engine the way the seeding HR vertical did:</p><ol><li>register <code>absenceModule</code> <strong>before</strong> your module (registration order = migration order, so the engine&#39;s tables exist when your journal runs);</li><li>a one-time extraction-handoff migration (<code>boundary-lint-allow R5</code> — the one sanctioned write to another module&#39;s schema) copies your ledger and requests into <code>absence_*</code>, mapping your subject id into the (<code>subject_type</code>, <code>subject_id</code>, <code>data_subject_id</code>) triple, and registers your leave-type keys;</li><li>your operations become compositions of the in-scope functions — behind your unchanged HTTP surface, if you keep a thin mapping layer.</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/absence/composing.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const composing = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  composing as default
};
