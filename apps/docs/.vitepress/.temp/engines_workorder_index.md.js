import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Work-order engine","description":"","frontmatter":{},"headers":[],"relativePath":"engines/workorder/index.md","filePath":"engines/workorder/index.md","lastUpdated":1784293491000}');
const _sfc_main = { name: "engines/workorder/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="work-order-engine" tabindex="-1">Work-order engine <a class="header-anchor" href="#work-order-engine" aria-label="Permalink to &quot;Work-order engine&quot;">​</a></h1><p><code>@substrat-run/engine-workorder</code> — one engine covering <strong>work orders, time reporting, and material reporting</strong>. One state machine, append-only reporting, and a billable snapshot frozen at completion. It deliberately knows nothing about pricing (the vertical&#39;s job) or invoicing (a sibling engine, reached only via events).</p><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>@substrat-run/engine-workorder</code></td></tr><tr><td><strong>Entitlement key</strong></td><td><code>workorder</code></td></tr><tr><td><strong>Owns</strong></td><td>the order state machine, append-only time &amp; material reporting, the billable snapshot</td></tr><tr><td><strong>Emits</strong></td><td>7 events, <code>workorder.created</code> → <code>workorder.closed</code> (<a href="./events.html">events</a>)</td></tr><tr><td><strong>Consumes</strong></td><td>nothing — it is a source, not a sink</td></tr><tr><td><strong>Permissions</strong></td><td>6 (<code>workorder:create</code> · <code>read</code> · <code>assign</code> · <code>report</code> · <code>complete</code> · <code>close</code>)</td></tr><tr><td><strong>Status</strong></td><td>product seed (0.x) — surfaces change until the first vertical ships</td></tr></tbody></table><h2 id="what-it-owns" tabindex="-1">What it owns <a class="header-anchor" href="#what-it-owns" aria-label="Permalink to &quot;What it owns&quot;">​</a></h2><ul><li><strong>The state machine cannot skip states.</strong> <code>planned → in_progress → completed → closed</code>; invalid transitions throw. No caller can complete a planned order or restart a completed one.</li><li><strong>Time and material are append-only.</strong> No update, no delete. Corrections are compensating entries, never silent edits — this is what makes the reporting trail trustworthy.</li><li><strong>Attribution comes from the ambient principal</strong>, never from the input.</li><li><strong>Every mutation emits a fat event.</strong> <code>workorder.completed</code> carries the full billable snapshot, so downstream consumers never query back.</li></ul><p>Details and the tables behind them: <a href="./model.html">Domain model &amp; invariants</a>.</p><h2 id="what-it-will-not-do" tabindex="-1">What it will not do <a class="header-anchor" href="#what-it-will-not-do" aria-label="Permalink to &quot;What it will not do&quot;">​</a></h2><ul><li><strong>Pricing</strong> — no price lists, no rates, no ROT/tax logic. Verticals price; the engine records quantities and freezes what it&#39;s handed.</li><li><strong>Invoicing</strong> — it emits <code>workorder.completed</code>; what happens next is the <a href="/engines/invoicing/">invoicing engine</a>&#39;s business.</li><li><strong>Scheduling</strong> — assignment is a field, not a calendar. Dispatch/scheduling is a future sibling engine.</li></ul><h2 id="is-this-a-good-match" tabindex="-1">Is this a good match? <a class="header-anchor" href="#is-this-a-good-match" aria-label="Permalink to &quot;Is this a good match?&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Reach for it when</th><th>Look elsewhere when</th></tr></thead><tbody><tr><td>Work is dispatched as discrete jobs that move through fixed states</td><td>Work is continuous or has no meaningful lifecycle</td></tr><tr><td>You need a defensible record of <em>who did what, when</em></td><td>You only need a task list — a to-do table is cheaper</td></tr><tr><td>Time and/or material get reported against the job</td><td>The billable unit isn&#39;t labour or parts</td></tr><tr><td>The job&#39;s completion is a <strong>business event</strong> others react to</td><td>Nothing downstream cares that work finished</td></tr><tr><td>Your pricing is yours and shouldn&#39;t live in shared machinery</td><td>You want the engine to price for you — it won&#39;t, by design</td></tr></tbody></table><p>The clarifying question: <strong>does your domain have a moment where work is finished and priced, and does anything care?</strong> If yes, this engine owns that moment. If your answer is &quot;we just track status&quot;, you want a table, not an engine.</p><p>Note the scope call: work orders, time, and material are <strong>one</strong> engine, not three. Time entries have no meaning outside their work order, and two engines needing chatty synchronous talk are one engine drawn wrong.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/workorder/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
