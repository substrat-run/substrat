import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Events","description":"The three events the metering engine emits, why a deduped replay emits nothing at all — leaving the stream itself dedupe-clean — and the period-closed billing hand-off. Consumes nothing.","frontmatter":{"description":"The three events the metering engine emits, why a deduped replay emits nothing at all — leaving the stream itself dedupe-clean — and the period-closed billing hand-off. Consumes nothing."},"headers":[],"relativePath":"engines/metering/events.md","filePath":"engines/metering/events.md","lastUpdated":1787063543000}');
const _sfc_main = { name: "engines/metering/events.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h1><h2 id="emitted" tabindex="-1">Emitted <a class="header-anchor" href="#emitted" aria-label="Permalink to &quot;Emitted&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Event</th><th>When</th></tr></thead><tbody><tr><td><code>metering.meter-configured</code></td><td>a meter registered, or its description/active updated</td></tr><tr><td><code>metering.usage-recorded</code></td><td>a <strong>new</strong> usage entry lands — a deduped replay emits nothing</td></tr><tr><td><code>metering.period-closed</code></td><td>a window is frozen into lines; the billing hand-off</td></tr></tbody></table><p>Consumes nothing.</p><h2 id="payload-contracts" tabindex="-1">Payload contracts <a class="header-anchor" href="#payload-contracts" aria-label="Permalink to &quot;Payload contracts&quot;">​</a></h2><p>All payloads are <strong>fat</strong>: a consumer never reads back across the module boundary.</p><ul><li><code>metering.meter-configured</code> — <code>{ key, kind, unit, active }</code>.</li><li><code>metering.usage-recorded</code> — <code>{ entryId, meterKey, kind, unit, qty, subject, occurredAt, dedupeKey }</code>. Because a deduped replay emits nothing, <strong>the event stream is itself dedupe-clean</strong>: a consumer summing <code>qty</code> per meter sees each observation exactly once (modulo the platform&#39;s at-least-once delivery — dedupe on <code>entryId</code>).</li><li><code>metering.period-closed</code> — <code>{ periodId, from, to, lines }</code>, where each line is <code>{ meterKey, kind, unit, qty, entryCount }</code>. <strong>Quantities, never prices</strong>: the vertical maps meter keys to rates and feeds <a href="/engines/invoicing/">invoicing</a>, exactly like the timesheet close hand-off (<a href="./composing.html">composing</a>). Dedupe on <code>periodId</code>.</li></ul><h2 id="pii" tabindex="-1">PII <a class="header-anchor" href="#pii" aria-label="Permalink to &quot;PII&quot;">​</a></h2><p>Every event is <code>piiClass: &#39;none&#39;</code>, and — unlike the absence engine — writes take <strong>no <code>DataSubjectId</code></strong>: meters count machines and bytes, not people. The optional <code>subject</code> ref is an attribution noun (a project, a message), not an identity. A vertical that finds itself metering <em>people</em> per-person should stop and think — and if it proceeds, hang its own PII-classified side table off the entry id rather than widening this engine&#39;s events.</p><h2 id="versioning" tabindex="-1">Versioning <a class="header-anchor" href="#versioning" aria-label="Permalink to &quot;Versioning&quot;">​</a></h2><p>Every event is emitted at <code>schemaVersion: 1</code>. Payload fields are <strong>frozen once shipped</strong>: additions are fine, renames/removals/retypes mean a <code>schemaVersion</code> bump with a dual-emit deprecation window — the platform-wide rule, restated because billing events are exactly the kind external consumers (a warehouse drain, a Stripe usage connector) quietly grow to depend on.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/metering/events.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const events = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  events as default
};
