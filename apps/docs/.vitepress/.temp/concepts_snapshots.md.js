import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderSuspense, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Snapshots & test copies","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/snapshots.md","filePath":"concepts/snapshots.md","lastUpdated":1786047367000}');
const _sfc_main = { name: "concepts/snapshots.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="snapshots-test-copies" tabindex="-1">Snapshots &amp; test copies <a class="header-anchor" href="#snapshots-test-copies" aria-label="Permalink to &quot;Snapshots &amp; test copies&quot;">​</a></h1><p>You&#39;re about to upgrade <em>Acme HR</em> to a new version, and the new version changes the database schema. If the migration mangles a table, &quot;undo&quot; is not a button — the data has moved. What you want is what you&#39;d do with a spreadsheet before a risky edit: <strong>save a copy first</strong>.</p><p>A <strong>snapshot</strong> is that copy, for a whole app: the app&#39;s entire database, duplicated at a moment in time, kept as its own independent thing. The original keeps running; the copy doesn&#39;t change when the original does; you can look at it, run the app against it, or throw it away.</p><p>Three everyday problems, one primitive:</p><table tabindex="0"><thead><tr><th>You want</th><th>You get</th></tr></thead><tbody><tr><td>&quot;Let me try this on real data without touching the live app&quot;</td><td>a <strong>test copy</strong> — fork it, poke at it, delete it</td></tr><tr><td>&quot;Will this upgrade survive our real data?&quot;</td><td><strong>snapshot-before-update</strong> — a copy taken automatically just before a risky upgrade</td></tr><tr><td>&quot;Give me real-shaped data on my laptop&quot;</td><td><a href="/reference/cli.html#scope-pull"><code>substrat scope pull</code></a> — a governed, masked export to a local SQLite file</td></tr></tbody></table><h2 id="the-picture" tabindex="-1">The picture <a class="header-anchor" href="#the-picture" aria-label="Permalink to &quot;The picture&quot;">​</a></h2><p>Code moves along one line (versions); each app&#39;s data moves along another. A snapshot branches the data line — and, unlike a git branch, it never merges back:</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-56",
        class: "mermaid",
        graph: "gitGraph%0A%20%20commit%20id%3A%20%22v1%20installed%22%0A%20%20commit%20id%3A%20%22daily%20use%E2%80%A6%22%0A%20%20branch%20snapshot-jul-25%0A%20%20checkout%20snapshot-jul-25%0A%20%20commit%20id%3A%20%22frozen%20copy%22%20type%3A%20HIGHLIGHT%0A%20%20checkout%20main%0A%20%20commit%20id%3A%20%22upgrade%20to%20v2%22%0A%20%20commit%20id%3A%20%22daily%20use%20continues%22%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<p>The live app moves on; the copy stays exactly as it was. If v2 turns out bad, the copy — still runnable, still on v1&#39;s schema — is the rollback point.</p><h2 id="why-a-copy-is-enough-no-database-branching-magic" tabindex="-1">Why a copy is enough (no database-branching magic) <a class="header-anchor" href="#why-a-copy-is-enough-no-database-branching-magic" aria-label="Permalink to &quot;Why a copy is enough (no database-branching magic)&quot;">​</a></h2><p>Neon and PlanetScale branch databases with copy-on-write storage engines. Substrat doesn&#39;t need one: a <a href="/concepts/tenancy.html">scope</a> is <strong>one tenant × one app</strong> — small by construction — so a full copy takes seconds. And because the copy <em>is</em> a scope, it isn&#39;t just data: the <strong>same app code runs against it</strong>. A database branch elsewhere gives you rows; a snapshot here gives you the running app as it was.</p><h2 id="the-one-rule-everything-follows" tabindex="-1">The one rule everything follows <a class="header-anchor" href="#the-one-rule-everything-follows" aria-label="Permalink to &quot;The one rule everything follows&quot;">​</a></h2><p>Module migrations are append-only — never edited, never un-run (<a href="/concepts/modules.html">modules</a>). Once data has moved to a new schema, there is no moving it back. Every snapshot behavior is that rule, cashed out:</p><ul><li>A copy is taken <strong>at the source&#39;s current version</strong> — so it runs as-is, no migration needed.</li><li>A copy can move <strong>forward</strong> (bind a newer version; its migrations run on the copy) — which is how you rehearse an upgrade on real-shaped data before the live app takes it.</li><li>A copy never moves <strong>backward</strong>, and neither does the live app — &quot;rollback&quot; means <em>restore the pre-upgrade copy</em>, accepting that writes made since are discarded. Because that price is real, the platform pays for the insurance automatically:</li></ul><div class="tip custom-block"><p class="custom-block-title">Snapshot-before-update</p><p>When an app is updated to a version whose <strong>migrations changed</strong>, the platform takes a snapshot first — that&#39;s the checkbox (on by default) next to &quot;Update to latest&quot; in the Dashboard. A code-only update takes no snapshot: rebinding the previous version already undoes it.</p></div><h2 id="copies-expire-on-purpose" tabindex="-1">Copies expire (on purpose) <a class="header-anchor" href="#copies-expire-on-purpose" aria-label="Permalink to &quot;Copies expire (on purpose)&quot;">​</a></h2><p>A snapshot is real customer data occupying real storage:</p><ul><li><strong>TTL</strong> — pick 1/7/30 days when creating one; the platform&#39;s scheduled sweep deletes expired copies — storage wiped, the deletion audited. An auto-deleting copy is also a smaller privacy liability than one that lingers.</li><li><strong>Keep</strong> — a copy without a TTL stays until someone deletes it.</li><li>This delete is the platform&#39;s <strong>only hard delete</strong>, and it refuses anything that isn&#39;t a copy. Live apps only ever archive; an ephemeral copy is the one thing allowed to truly disappear.</li></ul><h2 id="where-the-data-goes-and-doesn-t" tabindex="-1">Where the data goes (and doesn&#39;t) <a class="header-anchor" href="#where-the-data-goes-and-doesn-t" aria-label="Permalink to &quot;Where the data goes (and doesn&#39;t)&quot;">​</a></h2><p>An app&#39;s data lives in its own deployment, not in the control plane (<a href="/platform/control-plane.html">control plane</a>). Making or deleting a snapshot is an instruction — <em>&quot;copy A into B&quot;</em>, <em>&quot;wipe C&quot;</em> — carried out <strong>inside the app&#39;s own deployment</strong>. The platform keeps only the catalog card: where the copy came from, when, and when it expires. <strong>No app data crosses to the platform</strong> for any of it.</p><p>The one deliberate exception is <a href="/reference/cli.html#scope-pull"><code>scope pull</code></a>, whose whole point is moving data out — so it is staff-gated, audited, <strong>masked by default</strong> (PII columns and payload fields are redacted; <code>--full</code> is an explicit break-glass), and refused entirely for data pinned to a jurisdiction.</p><h2 id="what-a-snapshot-is-not" tabindex="-1">What a snapshot is <em>not</em> <a class="header-anchor" href="#what-a-snapshot-is-not" aria-label="Permalink to &quot;What a snapshot is *not*&quot;">​</a></h2><ul><li><strong>Not backup/PITR.</strong> Durable Object storage already has ~30-day point-in-time recovery — a destructive rewind of the live app. A snapshot is the opposite: a non-destructive copy that leaves the live app alone. Recovery rewinds; snapshots preview. What backs up <em>the platform&#39;s own directory</em> — the one thing PITR cannot cover, since it is a single Durable Object with nothing underneath to rebuild it from — is <a href="/platform/control-plane.html#backup-and-recovery">the control plane&#39;s scheduled directory backup</a>.</li><li><strong>Not a sync.</strong> The copy diverges the moment it&#39;s taken, and never merges back.</li><li><strong>Not a second production.</strong> A copy receives no traffic, runs no integrations or scheduled work, and expires. It contains real data, so the same access rules apply — but nothing you do in it touches the live app.</li></ul><h2 id="where-you-meet-it" tabindex="-1">Where you meet it <a class="header-anchor" href="#where-you-meet-it" aria-label="Permalink to &quot;Where you meet it&quot;">​</a></h2><ul><li><strong>Dashboard → app → Snapshots</strong> — create a test copy with a TTL, watch the expiry countdown, delete (<a href="/platform/dashboard.html#snapshots">dashboard</a>).</li><li><strong>Dashboard → app → Deployments</strong> — &quot;Update to latest&quot; with <strong>Snapshot data first</strong>.</li><li><strong>CLI</strong> — <a href="/reference/cli.html#scope-pull"><code>substrat scope pull</code></a> for the local inner loop.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/snapshots.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const snapshots = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  snapshots as default
};
