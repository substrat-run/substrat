import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"The deploy model","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/deploying.md","filePath":"concepts/deploying.md","lastUpdated":1785948498000}');
const _sfc_main = { name: "concepts/deploying.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="the-deploy-model" tabindex="-1">The deploy model <a class="header-anchor" href="#the-deploy-model" aria-label="Permalink to &quot;The deploy model&quot;">​</a></h1><p>A vertical you build on Substrat runs as a <strong>hosted app</strong>: one Durable Object per tenant scope, its own SQLite, served by code you pushed. This page is the model behind getting that code from your laptop into production and keeping it there safely — the concepts the <a href="/guide/deploying.html">deploy how-to</a> and the <a href="/platform/dashboard.html">dashboard</a> both assume. The mechanics of the <code>substrat</code> CLI live in the how-to; the <em>shape</em> lives here.</p><h2 id="push-vs-deploy" tabindex="-1">Push vs deploy <a class="header-anchor" href="#push-vs-deploy" aria-label="Permalink to &quot;Push vs deploy&quot;">​</a></h2><p>Two distinct acts, deliberately not fused:</p><ul><li>A <strong>push</strong> uploads a version. The CLI builds your worker locally, POSTs the bundle plus a manifest to the control plane, and the control plane records an immutable version (a <code>deploymentRef</code>, a permission digest, a migration digest). A push does <strong>not</strong> serve anything.</li><li>A <strong>promotion</strong> points a <strong>channel</strong> at a version and makes scopes run it. This is the deploy.</li></ul><p>The split exists because &quot;here is a new build&quot; and &quot;run this build against real data&quot; are different decisions with different blast radii. A push is cheap and reversible — it just adds a version to the registry. A promotion changes what a live tenant sees.</p><h2 id="admission-may-this-code-run-here" tabindex="-1">Admission: may this code run here? <a class="header-anchor" href="#admission-may-this-code-run-here" aria-label="Permalink to &quot;Admission: may this code run here?&quot;">​</a></h2><p>Before a version can be promoted it must be <strong>admitted</strong> — the answer to <em>&quot;may this code run on our infrastructure at all?&quot;</em> Admission is answered <strong>mechanically</strong>, by the sandbox contract: the bundle may declare only its own durable stores (a positive binding allowlist — no <code>CONTROL_PLANE</code>, no platform secret), it runs inside a Workers-for-Platforms isolate, and it is held to quotas. If the bundle satisfies the contract, nothing else is in question.</p><p><strong>Reaching the outside world is not a binding — it is a granted capability.</strong> The allowlist deliberately excludes egress-shaped bindings (<code>send_email</code>, <code>ai</code>, <code>browser</code>, …): a hosted vertical never talks to a third party by declaring a binding. When a vertical genuinely needs a platform capability — sending transactional email, provisioning tenants — it <em>declares a request</em> in its manifest (<code>substrat.sendsEmail</code>, <code>substrat.provisions</code>) and a <strong>staff grant</strong> turns it on (<code>setVerticalEmailSender</code>, <code>setVerticalTenantProvisioner</code>). The request is refreshed on every push and grants nothing by itself; the grant is a directory flag a push can never set or keep, so pushing new code can never acquire authority. At runtime the platform provides the capability behind a credential the vertical never holds — for email, a <code>POST /internal/email/send</code> relay on the control plane that sends on the vertical&#39;s behalf and checks the grant on every call. This is why &quot;how a vertical gets a dependency&quot; is: declare the request, get it granted, call the platform seam — never bind the raw resource.</p><p>Who has to <em>vouch</em> for a version depends on who will be exposed to it (decision D-36):</p><ul><li><strong>Private vertical</strong> (you own it, it is not listed on the marketplace). The only tenant that can run the code is the workspace that wrote it — you. There is no third party to protect, so the sandbox contract is the entire gate and a push lands <strong>admitted automatically</strong>. An auto-admission note records that no human vouched, so the platform can still tell an auto-admitted version from one a human reviewed.</li><li><strong>Listed vertical</strong> (published to the marketplace, so <em>other</em> tenants can install it against <em>their</em> data). Now a second question appears — <em>&quot;may other people run this code against their data?&quot;</em> — and that is a <strong>human</strong> decision. A listed vertical&#39;s pushes land <strong>pending</strong> for staff admission, and its production promotion is a staff decision too.</li></ul><p>The human gate did not disappear; it moved to the boundary where it means something. The place a private vertical becomes listed — <code>substrat publish</code>, the <code>setVerticalListed</code> decision — is where a human vouches. Publishing is the checkpoint, not every push.</p><h2 id="the-one-channel-prod" tabindex="-1">The one channel: <code>prod</code> <a class="header-anchor" href="#the-one-channel-prod" aria-label="Permalink to &quot;The one channel: \`prod\`&quot;">​</a></h2><p>A vertical has exactly <strong>one channel — <code>prod</code></strong>: the pointer at the version its scopes serve. There is no <code>dev</code> or <code>staging</code>. Those existed once and were <em>write-only</em> — nothing ever read or served them (#509) — because a channel names a <em>pointer at code</em>, and an immutable version id already does that. What a real non-production environment needs is not a second pointer but a second <strong>scope with data</strong>, which is a <a href="#previews-run-a-version-against-a-copy-of-the-data">preview</a>. The full argument, and how to run test / canary / release-candidate environments on previews, is <a href="/guide/environments-and-previews.html">Environments &amp; previews</a>.</p><p><code>prod</code> stays a <em>moving target</em> — a <a href="/guide/deploying.html#promote-to-prod">promote</a> re-points the prod scope at a new version — but it is a <strong>rebind</strong>, not a rename: the same scope, the same data, serving new code. For a vertical you own privately you self-serve it, so <code>substrat push --promote prod</code> is a complete deploy and a merge-to-main workflow can be the deploy. A prod promote re-points your live scopes in the same act — there are no <em>other</em> tenants who would be forced into lockstep (that concern, from decision D-30, is a shared vertical&#39;s many tenants, which a private vertical cannot have). For a shared vertical, that cascade is exactly why prod is a fleet-wide rebind, and why bringing <em>one</em> tenant forward first is a <a href="/guide/environments-and-previews.html#canary-pinned-tenants-per-scope-rollout">per-scope bind</a>, not a channel.</p><p>Two things a promotion always respects:</p><ul><li><strong>The surface checkpoint.</strong> A promotion whose <strong>permission</strong> or <strong>migration</strong> digest differs from what is live is refused until the diff is acknowledged. This is the same two-human- checkpoint discipline the kernel applies to <a href="/concepts/modules.html#two-human-checkpoints">modules</a>, applied at the deploy boundary — with the owner as the human at their own checkpoint.</li><li><strong>Channel history.</strong> Every promotion appends a row: what went live, what it replaced, who did it, and exactly when. That history is the dashboard&#39;s rollback picker, and each timestamp is the instant a point-in-time rewind would restore the data to.</li></ul><h2 id="in-place-updates-data-carries-forward" tabindex="-1">In-place updates: data carries forward <a class="header-anchor" href="#in-place-updates-data-carries-forward" aria-label="Permalink to &quot;In-place updates: data carries forward&quot;">​</a></h2><p>The property that makes &quot;promote prod from a laptop&quot; safe is that a vertical serves <strong>in place</strong> (decision D-37). A version update is <em>not</em> a rebind to fresh, empty storage:</p><ul><li>A prod promote re-uploads the promoted bundle onto the vertical&#39;s <strong>one stable serving script</strong>. The scope&#39;s Durable Object and its SQLite <strong>stay put</strong> — data does not move.</li><li>Because the data is still there, the kernel&#39;s append-only <strong>migrations run forward against production data</strong>, exactly as designed. This is the mechanism the whole migration model is built around; before in-place serving it never actually ran in production.</li><li>Secrets survive the deploy.</li></ul><p>A version is badged <strong>code-only</strong> or <strong>schema-change</strong> at publish, so you know whether a promote touches the schema. A code-only update just re-points; a schema-change update runs migrations, so it wants its migration diff acknowledged first.</p><h2 id="previews-run-a-version-against-a-copy-of-the-data" tabindex="-1">Previews: run a version against a copy of the data <a class="header-anchor" href="#previews-run-a-version-against-a-copy-of-the-data" aria-label="Permalink to &quot;Previews: run a version against a copy of the data&quot;">​</a></h2><p>Before a promotion whose <em>migrations</em> changed, you often want to see the new version run against real-shaped data <strong>without</strong> touching production. Substrat can do this because the scope-host contract already runs identical module code on two adapters — so a <strong>preview</strong> is a <strong>fork</strong> of a scope&#39;s data bound to the new version, reachable at its own URL.</p><p>The governing law is that <a href="/concepts/snapshots.html">migrations are forward-only</a>: a snapshot taken today (at prod&#39;s migration frontier) bound to <em>today&#39;s or a later</em> version rolls its migrations forward on the copy — rehearse the new version against real-shaped data, throw the fork away if it breaks, and prod never saw it. The inverse — pointing <em>old</em> code at <em>today&#39;s</em> data across a schema change — is invalid, because you cannot un-run a migration. So the rollback strategy is <strong>fork-before-you-promote</strong>: snapshot right before binding the new version, and a bad version leaves the prior one still runnable at its correct frontier. A fork is a governed dead end — the export copies the kernel spine too, so no connector, cron, or billing consumes from a preview — and preview URLs are non-public, gated code running against real-shaped data. The everyday shape of all this — test copies, fearless upgrades, real data pulled to a laptop — is <a href="/concepts/snapshots.html">snapshots &amp; test copies</a>.</p><h2 id="backup-restore-and-backout" tabindex="-1">Backup, restore, and backout <a class="header-anchor" href="#backup-restore-and-backout" aria-label="Permalink to &quot;Backup, restore, and backout&quot;">​</a></h2><p>Because every app is one scope with its own database, recovery is a per-tenant primitive, not an environment-wide runbook — one tenant&#39;s rewind is a clean, self-contained blast radius.</p><ul><li><strong>PITR rewind — the first-hours backout.</strong> Right before an upgrade migrates, the scope DO bookmarks the instant (Durable-Object point-in-time recovery, ~30 days of history). If a promotion goes wrong, an <strong>audited rewind</strong> rolls the scope&#39;s data back to that bookmark, time-boxed to <strong>~24h</strong> unless forced. A rewind is a <em>destructive, in-place</em> rollback of that one DO — the opposite lever to a fork, which is a non-destructive copy. It rewinds the whole database, so a stale bookmark is refused rather than silently skipped. One sharp edge: data and version binding live in different DOs, so rewinding <em>past a bad migration</em> means also rebinding the version — two coordinated rewinds, no atomicity between them.</li><li><strong>Backup / restore — the considered path.</strong> <code>substrat scope restore</code> loads a backup into an existing hosted scope, replacing its data — a pulled <code>.sqlite</code>, a local adapter-sqlite scope file, or a dump. This is the deliberate recovery when a time-boxed rewind is not the right tool.</li><li><strong>Legacy adoption.</strong> A scope that predates the stable serving script hops onto it once with <code>adopt-serving</code> (export → restore → flip, data-first), so future promotes stop stranding its data.</li></ul><h2 id="the-whole-shape" tabindex="-1">The whole shape <a class="header-anchor" href="#the-whole-shape" aria-label="Permalink to &quot;The whole shape&quot;">​</a></h2><p>Push uploads a version; admission asks <em>may this run here</em> (automatic when you are the only tenant exposed, staff when you list it); promotion points a channel and serves <strong>in place</strong> so data carries forward across every update; previews rehearse a risky version against a fork of real data; and PITR rewind plus backup/restore are the backout. The staff human gate lives at <strong>publish</strong> — the moment other tenants can run your code against their data — and nowhere else in the loop.</p><p>See also: <a href="/guide/deploying.html">Deploying a vertical</a> (the how-to), <a href="/concepts/snapshots.html">Snapshots &amp; test copies</a>, and <a href="/concepts/platform.html">The platform layer</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/deploying.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const deploying = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  deploying as default
};
