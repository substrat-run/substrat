import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"What is a connector?","description":"","frontmatter":{},"headers":[],"relativePath":"connectors/index.md","filePath":"connectors/index.md","lastUpdated":1787416740000}');
const _sfc_main = { name: "connectors/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_ConnectorLoop = resolveComponent("ConnectorLoop");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="what-is-a-connector" tabindex="-1">What is a connector? <a class="header-anchor" href="#what-is-a-connector" aria-label="Permalink to &quot;What is a connector?&quot;">​</a></h1><p>A <strong>connector</strong> is the platform&#39;s bridge to a third-party service a <em>tenant</em> uses — Scrive for e-signing, Fortnox for accounting, a BankID provider for identity. It is the one place in Substrat allowed to talk to the outside world.</p><p>Connectors are <strong>host code, never module code.</strong> A vertical cannot call an external API: the <a href="/concepts/modules.html">boundary lint</a> bans <code>fetch</code> outright, and there is nowhere for a vertical to keep a credential. That prohibition is the whole point — it means &quot;this tenant&#39;s Scrive account&quot; lives in one audited place, and a vertical sees only the capability, never the secret.</p><h2 id="where-a-connector-sits" tabindex="-1">Where a connector sits <a class="header-anchor" href="#where-a-connector-sits" aria-label="Permalink to &quot;Where a connector sits&quot;">​</a></h2><p><a href="https://github.com/substrat-run/substrat/blob/main/docs/master-plan.md" target="_blank" rel="noreferrer">Decision 18</a>&#39;s triage rule sorts every outside dependency into three buckets, and a connector is the third:</p><table tabindex="0"><thead><tr><th>Bucket</th><th>What</th><th>Example</th></tr></thead><tbody><tr><td><strong>Kernel-owned</strong></td><td>enforcement inputs and contracts</td><td>tenancy, permissions, the event spine, <strong>the integrations hub itself</strong></td></tr><tr><td><strong>Adapter</strong></td><td>infrastructure the kernel consumes, swappable behind a pure interface</td><td>the SQLite/Cloudflare scope host, the <code>SecretBox</code> that seals credentials</td></tr><tr><td><strong>Connector</strong></td><td>a third-party capability tenants use, in the hub</td><td><strong>Scrive, Fortnox, BankID, Swish, Peppol, Kivra, EDI</strong></td></tr></tbody></table><p>The <em>hub</em> — the connection store, the runtime, the authority seam — is kernel-owned. Individual connectors are not: they accrete one at a time, as a vertical needs one, and live in <code>connectors/*</code> outside the kernel.</p><h2 id="the-seam-a-connector-plugs-into" tabindex="-1">The seam a connector plugs into <a class="header-anchor" href="#the-seam-a-connector-plugs-into" aria-label="Permalink to &quot;The seam a connector plugs into&quot;">​</a></h2><p>A connector never invents its own machinery. Four kernel-owned pieces do the load-bearing work, and a connector is small because they exist:</p><ol><li><p><strong>The connection store.</strong> A tenant&#39;s authorization for one provider, held by one vertical, keyed <strong>(tenant, vertical, provider)</strong>. Credentials are sealed at rest by a <code>SecretBox</code> adapter — the metadata is readable, the secret never is. See <a href="/concepts/permissions.html">Permissions</a> for how a connection becomes a subject.</p></li><li><p><strong>The connector runtime.</strong> <code>registerConnector(id, eventType, handler)</code> binds a handler to an event. When a module emits that event, the runtime hands the handler an opened credential and a <strong><code>fetch</code> bound to the connection</strong> — so a timeout, egress policy, and per-connection health recording come for free, and module code still cannot reach any of it.</p></li><li><p><strong>At-least-once delivery with retry.</strong> The handler rides the same journal, backoff and dead-letter as every executor. A provider being briefly down is ordinary; the runtime retries with backoff and surfaces a dead letter rather than dropping the effect.</p></li><li><p><strong>The inbound authority seam.</strong> A provider&#39;s callback is not a person, so it cannot hold a <code>PrincipalId</code>. Instead <strong>a connection is a subject</strong>: <code>getConnectorScope(connectionId, scopeId)</code> opens a scope stub whose authority is the connection&#39;s own grants, narrowed by construction to that tenant and vertical. Events it causes are stamped <code>{ connection }</code> on the spine — the audit trail says &quot;Scrive did this&quot; without naming a human who did not.</p></li></ol>`);
  _push(ssrRenderComponent(_component_ConnectorLoop, null, null, _parent));
  _push(`<h2 id="what-a-connector-is-not" tabindex="-1">What a connector is <em>not</em> <a class="header-anchor" href="#what-a-connector-is-not" aria-label="Permalink to &quot;What a connector is *not*&quot;">​</a></h2><ul><li><strong>Not an <a href="/engines/">engine</a>.</strong> An engine owns invariants, operations, events and domain state inside a scope. A connector owns none of those — it <em>consumes</em> an event and <em>effects</em> something outside. It has no tables, no permissions of its own, no domain model.</li><li><strong>Not swappable infrastructure.</strong> The KMS behind <code>SecretBox</code> is an <em>adapter</em> (bucket 2). A connector is a capability a tenant deliberately connects, not plumbing the kernel picks.</li><li><strong>Not a way around the module rules.</strong> A connector cannot read a vertical&#39;s tables. If a connector needs a vertical&#39;s data, the vertical puts it in the event payload — the connector works from that and nothing else.</li></ul><h2 id="available-connectors" tabindex="-1">Available connectors <a class="header-anchor" href="#available-connectors" aria-label="Permalink to &quot;Available connectors&quot;">​</a></h2><p>Connectors accrete per vertical need, so this list is short by design and grows one entry at a time. <strong>Status</strong> is honest: a connector can be documented and half-built, because the seam it needs may not exist yet.</p><table tabindex="0"><thead><tr><th>Connector</th><th>Category</th><th>Status</th><th>Provider</th></tr></thead><tbody><tr><td><a href="/connectors/scrive.html">Scrive</a></td><td>E-signing &amp; identity</td><td><strong>Published <code>0.1.15</code></strong> — both halves built; two caveats: the vertical schedules the poll, and BankID is off on the testbed</td><td>Scrive eSign (Swedish BankID)</td></tr></tbody></table><p>Categories, as they will fill in (from the master plan&#39;s build list):</p><ul><li><strong>E-signing &amp; identity</strong> — Scrive, BankID, Kivra</li><li><strong>Accounting</strong> — Fortnox, Visma</li><li><strong>Payments</strong> — Swish</li><li><strong>E-invoicing &amp; EDI</strong> — Peppol, Ahlsell / Rexel / Sonepar</li></ul><h2 id="how-these-pages-are-organized" tabindex="-1">How these pages are organized <a class="header-anchor" href="#how-these-pages-are-organized" aria-label="Permalink to &quot;How these pages are organized&quot;">​</a></h2><p>Every connector documents itself the same way — a different shape from an engine&#39;s five pages, because a connector is a different thing. One page, these sections:</p><table tabindex="0"><thead><tr><th>Section</th><th>Answers</th></tr></thead><tbody><tr><td><strong>At a glance</strong></td><td>provider, category, status, the npm package</td></tr><tr><td><strong>What it consumes</strong></td><td>the event that triggers it, and what the payload must carry</td></tr><tr><td><strong>The credential</strong></td><td>what the connection stores, and what must <em>never</em> be stored</td></tr><tr><td><strong>The flow</strong></td><td>what it does at the provider, step by step</td></tr><tr><td><strong>What&#39;s missing</strong></td><td>the seams it still needs — stated plainly, because an incomplete connector that pretends otherwise is worse than none</td></tr></tbody></table><p>If a connector cannot fill &quot;What&#39;s missing&quot; with an empty list, it is not done, and the page says so.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("connectors/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
