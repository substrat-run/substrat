import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/cli","description":"","frontmatter":{},"headers":[],"relativePath":"reference/cli.md","filePath":"reference/cli.md","lastUpdated":1786000367000}');
const _sfc_main = { name: "reference/cli.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-cli" tabindex="-1">@substrat-run/cli <a class="header-anchor" href="#substrat-run-cli" aria-label="Permalink to &quot;@substrat-run/cli&quot;">​</a></h1><p>The <code>substrat</code> CLI — authenticated deploy tooling. You <strong>push</strong> a vertical to the platform, then manage its versions and channels. It is the client half of the <a href="/guide/deploying.html">self-serve deploy</a> flow: it builds your worker locally and POSTs a bundle; the <a href="/platform/control-plane.html">control plane</a> holds the Cloudflare credential and does the upload, so <strong>you never hold a platform token</strong> (decision D-34).</p><p>Published to npm as <a href="https://www.npmjs.com/package/@substrat-run/cli" target="_blank" rel="noreferrer"><code>@substrat-run/cli</code></a>, Apache-2.0. For the narrative walkthrough — including <em>why</em> push is not deploy — see <a href="/guide/deploying.html">Deploying a vertical</a>.</p><h2 id="install" tabindex="-1">Install <a class="header-anchor" href="#install" aria-label="Permalink to &quot;Install&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -g</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/cli</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # or pnpm add -g @substrat-run/cli</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --help</span></span></code></pre></div><p>The package has no runtime dependencies and ships web-standard + <code>node:*</code> only. It needs Node ≥ 20 and, for <code>push</code>, <code>npx wrangler</code> available in the vertical&#39;s directory (it builds with <code>wrangler deploy --dry-run</code>).</p><h2 id="commands" tabindex="-1">Commands <a class="header-anchor" href="#commands" aria-label="Permalink to &quot;Commands&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Command</th><th>What it does</th></tr></thead><tbody><tr><td><code>substrat init --ci github</code></td><td>Write <code>.github/workflows/substrat-deploy.yml</code> — the deploy + preview workflow, generated rather than re-derived.</td></tr><tr><td><code>substrat login</code></td><td>Sign in via the browser (per-human), or store a CI service token with <code>--token</code>.</td></tr><tr><td><code>substrat whoami</code></td><td>Print who you are and the workspaces you can build for.</td></tr><tr><td><code>substrat push [dir]</code></td><td>Build the vertical and push a version — <strong>admitted</strong> for a private vertical, <strong>pending</strong> for a listed one. No flags needed from inside the project.</td></tr><tr><td><code>substrat promote &lt;slug&gt; --version &lt;id&gt;</code></td><td>Point <strong><code>prod</code></strong> (the only channel) at a version. <code>--channel</code> defaults to <code>prod</code>.</td></tr><tr><td><code>substrat versions &lt;slug&gt;</code></td><td>List a vertical&#39;s versions + whether <code>prod</code> points at each.</td></tr><tr><td><code>substrat preview create [dir] --tag &lt;t&gt;</code></td><td>Run a version against a fork of prod (or <code>--empty</code> clean room) at its own URL — the non-prod environment primitive.</td></tr><tr><td><code>substrat scope bind &lt;scopeId&gt; --version &lt;id&gt;</code></td><td>Pin <strong>one</strong> scope to a version of the same vertical — the per-scope rollout primitive (canary, test env).</td></tr><tr><td><code>substrat scope domain &lt;scopeId&gt; --domain &lt;d&gt;</code></td><td>Bind a custom domain to <strong>any</strong> owned scope (a prod app, a preview, a test env).</td></tr><tr><td><code>substrat publish &lt;slug&gt;</code></td><td>Request a listing on the public marketplace (a staff operator reviews).</td></tr><tr><td><code>substrat unpublish &lt;slug&gt;</code></td><td>Remove a vertical from the public marketplace (staff).</td></tr><tr><td><code>substrat scope pull &lt;scopeId&gt;</code></td><td>Pull a scope&#39;s data to a local SQLite file — masked by default, <code>--full</code> is break-glass.</td></tr></tbody></table><p>Options on any command: <code>--cp &lt;url&gt;</code> (control-plane API base), <code>--token &lt;tok&gt;</code> (a service credential), <code>--tenant &lt;id-or-slug&gt;</code> (which workspace to act for).</p><h3 id="init" tabindex="-1"><code>init</code> <a class="header-anchor" href="#init" aria-label="Permalink to &quot;\`init\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> init</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ci</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> github</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                        # → .github/workflows/substrat-deploy.yml</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> init</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ci</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> github</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --release</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> changesets</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # the repo owns its version</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> init</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ci</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> github</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ./apps/helpdesk</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --branch</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --force</span></span></code></pre></div><p>Writes the deploy workflow: a merge to the deploy branch releases, a PR gets its <a href="#preview">preview</a> with the URL commented back, and closing the PR reaps it. It is the same file the dashboard&#39;s <a href="/guide/deploying.html#deploy-from-ci">one-click CI setup</a> commits — one generator, so the two paths cannot drift — and it is for the case the one-click path does not cover: you own your CI, or you want the release-train shape.</p><p><code>--release</code> picks what a merge means:</p><table tabindex="0"><thead><tr><th>Mode</th><th>A merge to the deploy branch</th></tr></thead><tbody><tr><td><code>trunk</code> <em>(default)</em></td><td><strong>releases.</strong> The push carries no <code>--version</code>, so the registry patch-bumps and <code>--promote prod</code> points prod at it in the same run.</td></tr><tr><td><code>changesets</code></td><td><strong>releases only when <code>package.json</code> version moved.</strong> The repo owns the version, so the merge that lands a changeset just moves the test env; prod moves when the version PR lands.</td></tr></tbody></table><p>Two behaviours are opt-in through <strong>repository variables</strong>, so enabling them never means regenerating the file:</p><table tabindex="0"><thead><tr><th>Variable</th><th>Effect</th></tr></thead><tbody><tr><td><code>SUBSTRAT_TEST_SCOPE_ID</code></td><td>Every merge rebinds that scope to the just-built version — the <a href="/guide/environments-and-previews.html#a-long-lived-test-environment">long-lived test environment</a>.</td></tr><tr><td><code>SUBSTRAT_PER_BUILD_PREVIEW</code></td><td>Set to <code>1</code> and each PR push also mints a frozen <a href="/guide/environments-and-previews.html#sticky-per-pr-and-per-build-urls">per-build URL</a> alongside the sticky one.</td></tr></tbody></table><p>The command is <strong>offline</strong> — it never authenticates and never calls the control plane — so it works in a fresh repo before the vertical exists. The slug comes from <code>package.json</code>, the branch from <code>.git/HEAD</code>; <code>--slug</code> / <code>--branch</code> override, <code>--out</code> relocates the file, and an existing file is never replaced without <code>--force</code>. You still add the <a href="/guide/deploying.html#deploy-from-ci">push token</a> as the <code>SUBSTRAT_SERVICE_TOKEN</code> Actions secret yourself; the command prints the steps.</p><h3 id="login" tabindex="-1"><code>login</code> <a class="header-anchor" href="#login" aria-label="Permalink to &quot;\`login\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> login</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                     # browser loopback (PKCE) — a per-human session</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> login</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --token</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">SERVICE_TOKE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">N</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # CI: store a service-actor credential</span></span></code></pre></div><p>The browser flow starts a one-shot <code>127.0.0.1</code> server, opens the platform&#39;s CLI broker, signs you in through <a href="/concepts/identity.html">AuthHero</a>, and exchanges a PKCE-bound <code>code</code> for a session token — the token never transits a URL. On success the CLI also resolves your workspace(s) and stores a default (prompting if you belong to several) — used by <code>promote</code> and <code>scope pull</code>; <code>push</code> pins its workspace <a href="#push">per project</a> instead. Everything lands in <code>~/.substrat/config.json</code> (mode <code>0600</code>).</p><h3 id="whoami" tabindex="-1"><code>whoami</code> <a class="header-anchor" href="#whoami" aria-label="Permalink to &quot;\`whoami\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> whoami</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># signed in as you@acme.com</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#   acme-co   (Acme Co)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#   side-org  (Side Org)</span></span></code></pre></div><p>Prints the signed-in identity and every workspace you can build for — useful before a push if you&#39;re unsure which workspace a project will act as.</p><h3 id="push" tabindex="-1"><code>push</code> <a class="header-anchor" href="#push" aria-label="Permalink to &quot;\`push\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> my-vertical</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;&amp; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> push</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      # no flags — everything defaults from the project</span></span></code></pre></div><p>Run from the vertical&#39;s directory (the one with <code>wrangler.jsonc</code> + <code>package.json</code>) and every input defaults from the project; flags override each:</p><table tabindex="0"><thead><tr><th>Input</th><th>Default</th><th>Override</th></tr></thead><tbody><tr><td>slug / name</td><td><code>&quot;substrat&quot;: { &quot;slug&quot;, &quot;name&quot; }</code> in <code>package.json</code>, else derived from the package name</td><td><code>--slug</code>, <code>--name</code></td></tr><tr><td>version</td><td>the registry&#39;s latest for the slug, patch-bumped (seeded from <code>package.json</code> <code>version</code> on the very first push)</td><td><code>--version</code></td></tr><tr><td>workspace</td><td><code>&quot;substrat&quot;: { &quot;tenant&quot; }</code> in <code>package.json</code> — the <strong>pin</strong></td><td><code>--tenant</code>, <code>SUBSTRAT_TENANT</code></td></tr></tbody></table><p>The push builds the bundle (<code>wrangler deploy --dry-run</code>, running your own <code>build.command</code>), reads the declared surface (your own DO classes, D1 databases, compatibility date/flags, entry module), computes the three digests, and POSTs to <code>{cp}/verticals/{slug}/deploy</code>. The slug is <strong>bare</strong> — the control plane forms the registry id <code>&lt;workspace&gt;/&lt;slug&gt;</code> from the workspace you push as (see <a href="/guide/deploying.html#the-workspace-prefix">the prefix</a>). The version lands <strong>pending</strong>; admission is a separate, human step.</p><p><strong>The first push of a project</strong> has no pin yet, so the CLI asks — once — and offers to write the answer into <code>package.json</code>, where it&#39;s reviewable and shared with every teammate and CI:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ substrat push</span></span>
<span class="line"><span>this project has no pinned workspace. you belong to:</span></span>
<span class="line"><span>  1. acme-co   (Acme Co)</span></span>
<span class="line"><span>  2. side-org  (Side Org)</span></span>
<span class="line"><span>push as [1-2, enter = 1]: 1</span></span>
<span class="line"><span>pin &#39;acme-co&#39; in package.json so this project always pushes there? [Y/n]: y</span></span>
<span class="line"><span>✓ pinned — package.json now carries &quot;substrat&quot;: { &quot;tenant&quot;: &quot;acme-co&quot; }</span></span>
<span class="line"><span>authenticating with browser session (workspace acme-co)</span></span>
<span class="line"><span>pushing acme-co/helpdesk@0.1.0 (Helpdesk) …</span></span></code></pre></div><p>It never falls back to the workspace stored at login: the first push of a slug <strong>claims</strong> it for a workspace, so a machine-wide default silently pointing at the wrong one would claim it for the wrong owner. A non-interactive push (CI) with no pin refuses instead of guessing.</p><h3 id="promote" tabindex="-1"><code>promote</code> <a class="header-anchor" href="#promote" aria-label="Permalink to &quot;\`promote\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> promote</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> helpdesk</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --version</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> 01J…</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ack-migrations</span></span></code></pre></div><p>Points <strong><code>prod</code></strong> at an <strong>admitted</strong> version — a rebind of the live scope(s) to new code, same data. <code>prod</code> is the <em>only</em> channel, so <code>--channel</code> defaults to it; a promote to anything else is refused with a pointer at <a href="#preview"><code>preview</code></a>. For a vertical you own privately this is self-serve, prod included; a <strong>listed</strong> vertical&#39;s prod promotion is a staff decision (the marketplace gate). A promote whose permission or migration digest differs from what is live is refused until you acknowledge the diff (<code>--ack-permissions</code> / <code>--ack-migrations</code>).</p><p>For a non-production environment — test, canary, a PR preview — you do <strong>not</strong> promote a second channel; you run the version against a scope with data. See <a href="#preview"><code>preview</code></a>, <a href="#scope-bind"><code>scope bind</code></a>, and the <a href="/guide/environments-and-previews.html">Environments &amp; previews</a> guide.</p><h3 id="versions" tabindex="-1"><code>versions</code> <a class="header-anchor" href="#versions" aria-label="Permalink to &quot;\`versions\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> versions</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> helpdesk</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># VERSION  ADMISSION  CHANNELS      ID</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 0.2.0    admitted   prod          01J…</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 0.1.0    admitted                 01J…</span></span></code></pre></div><p>Lists a vertical&#39;s versions (newest first), each one&#39;s admission state, and whether <code>prod</code> points at it. The same view is in the dashboard&#39;s <strong>Deployments</strong> tab.</p><h3 id="preview" tabindex="-1"><code>preview</code> <a class="header-anchor" href="#preview" aria-label="Permalink to &quot;\`preview\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pr-42</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">          # push this tree, fork prod, serve the pair</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> test</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ttl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> none</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --empty</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # a pinned, source-less environment</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ls</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> delete</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pr-42</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">            # reap it (idempotent)</span></span></code></pre></div><p>Creates a <strong>preview</strong> — a scope with data bound to a version, at its own <code>--&lt;tag&gt;</code> hostname. This is the non-production environment primitive: <code>create</code> pushes the working tree, forks the vertical&#39;s prod scope (or provisions an <code>--empty</code> clean-room scope), binds the pushed version, and mints the URL. Re-running the same <code>--tag</code> <strong>rebinds</strong> onto the same fork (migrations roll forward on one copy) and <strong>renews</strong> its TTL; <code>--refresh</code> starts from a clean fork. <code>--ttl</code> defaults to <code>72h</code>; <code>--ttl none</code><strong>pins</strong> the preview until you delete it. Default preview pushes use a semver <em>prerelease</em> label, so they never advance the release version your repo owns. Full workflow — sticky-per-PR + per-build URLs, a long-lived test environment, the release candidate — in <a href="/guide/environments-and-previews.html">Environments &amp; previews</a>.</p><h3 id="scope-bind" tabindex="-1"><code>scope bind</code> <a class="header-anchor" href="#scope-bind" aria-label="Permalink to &quot;\`scope bind\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> scope</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">scopeI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --version</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">versionI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [--snapshot]</span></span></code></pre></div><p>Pins <strong>one</strong> scope to a version of the same vertical — the per-scope rollout primitive. A prod promote cascades every tenant scope; <code>scope bind</code> moves a single one, which is how you canary <em>tenant A first</em> or advance a long-lived <a href="/guide/environments-and-previews.html#a-long-lived-test-environment">test environment</a> on each merge. <code>--snapshot</code> forks the scope&#39;s data before a migration-crossing bind (the rollback point). A pending (unadmitted) version is refused unless the scope is a preview.</p><h3 id="scope-domain" tabindex="-1"><code>scope domain</code> <a class="header-anchor" href="#scope-domain" aria-label="Permalink to &quot;\`scope domain\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> scope</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> domain</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">scopeI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --domain</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> crm-test.ahero.se</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [--surface </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">app]</span></span></code></pre></div><p>Binds a <strong>custom domain</strong> to any scope you own — not just a prod app, but a preview or a long-lived test environment. The hostname resolves to the <em>scope</em>, and the router serves whatever version that scope is bound to, so a custom domain on a <a href="/guide/environments-and-previews.html#a-long-lived-test-environment">pinned test preview</a> gives it a stable address like <code>crm-test.ahero.se</code>. It walks the same DNS-validation + certificate issuance as a prod domain (<code>verifying</code> → <code>active</code>); publish the CNAME/TXT it prints, then <a href="#hostnames"><code>substrat hostnames verify &lt;hostname&gt;</code></a> re-polls. A scope carrying a bound domain is protected from reaping while it resolves.</p><h3 id="hostnames" tabindex="-1"><code>hostnames</code> <a class="header-anchor" href="#hostnames" aria-label="Permalink to &quot;\`hostnames\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hostnames</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> helpdesk</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                    # list an install&#39;s hostname bindings</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hostnames</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bind</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> helpdesk</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --surface</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> app</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [--domain </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">d]</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [--scope </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">i</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hostnames</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> verify</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> crm-test.ahero.se</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # re-poll a custom domain&#39;s DNS/cert issuance</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hostnames</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> unbind</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> crm-test.ahero.se</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # remove a binding</span></span></code></pre></div><p>The install-addressed view of the hostname map. <code>hostnames bind</code> attaches a hostname to a named install (by vertical slug, <code>--scope</code> to disambiguate); <code>verify</code> and <code>unbind</code> are hostname-addressed and work for a hostname on <strong>any</strong> scope, including one attached with <a href="#scope-domain"><code>scope domain</code></a>. A platform hostname (<code>--surface</code> with no <code>--domain</code>) is live immediately on the wildcard cert; a <code>--domain</code> custom hostname walks DNS validation. To attach a custom domain to a bare scope id (a preview, a test env), reach for <a href="#scope-domain"><code>scope domain</code></a> instead.</p><h3 id="publish-unpublish" tabindex="-1"><code>publish</code> / <code>unpublish</code> <a class="header-anchor" href="#publish-unpublish" aria-label="Permalink to &quot;\`publish\` / \`unpublish\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> publish</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> helpdesk</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      # request a public-marketplace listing — staff reviews</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> unpublish</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> helpdesk</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # remove the listing (staff)</span></span></code></pre></div><p><code>publish</code> is a <strong>request</strong>: any owner may ask, and a Substrat operator reviews before the vertical is listed — once listed, every tenant can install it from the catalog. Flipping the listing itself is staff-only, so <code>unpublish</code> works for staff and is refused for builders.</p><h3 id="scope-pull" tabindex="-1"><code>scope pull</code> <a class="header-anchor" href="#scope-pull" aria-label="Permalink to &quot;\`scope pull\`&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> scope</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pull</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">scopeI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [--full] [--out </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">dir</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] [--tenant </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">id-or-slug</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span></code></pre></div><p>Brings a scope&#39;s data to your local inner loop (<a href="/concepts/snapshots.html">snapshots</a>): downloads the scope&#39;s dump from the control plane and writes a <strong>real SQLite file</strong> — <code>.substrat/&lt;tenant&gt;__&lt;scope&gt;.sqlite</code>, the exact file shape <a href="/reference/adapter-sqlite.html"><code>adapter-sqlite</code></a> stores scopes in, so a local harness runs the identical vertical against it. On node &lt; 22.13 (no <code>node:sqlite</code>) the dump lands as JSON with a notice instead.</p><p>The pull crosses the platform&#39;s trust boundary on purpose, so the server side is the gate:</p><ul><li><strong>Masked by default</strong> — PII-named columns and payload fields are redacted; ids and numbers pass through, which is what keeps the copy debuggable. <code>--full</code> is the explicit break-glass for full fidelity, and the CLI prints a treat-as-production warning.</li><li><strong>Audited</strong> — every pull writes an access-log entry against your actor.</li><li><strong>Jurisdiction-checked</strong> — a scope pinned tighter than <code>global</code> is refused: pulling would move its data outside the pin.</li></ul><p>One-way by design: you pull and diverge; nothing syncs back.</p><h2 id="auth-resolution" tabindex="-1">Auth resolution <a class="header-anchor" href="#auth-resolution" aria-label="Permalink to &quot;Auth resolution&quot;">​</a></h2><p>At request time the CLI resolves, in order:</p><ul><li><strong>Credential</strong> — explicit <code>--token</code> / <code>SUBSTRAT_SERVICE_TOKEN</code> (a service actor, for CI) → a stored browser session (sent as <code>Authorization: Bearer</code>) → a stored service token.</li><li><strong>Control-plane URL</strong> — <code>--cp</code> → <code>SUBSTRAT_CP_URL</code> → the stored config (default <code>https://console.substrat.net/api</code>).</li><li><strong>Workspace</strong> (browser session only) — sent as <code>x-substrat-tenant</code>. For <strong>push</strong> it is the <em>project&#39;s</em> choice: <code>--tenant</code> → <code>SUBSTRAT_TENANT</code> → <code>&quot;substrat&quot;: { &quot;tenant&quot; }</code> in <code>package.json</code> — never the stored login default, because the first push of a slug <strong>claims</strong><code>&lt;workspace&gt;/&lt;slug&gt;</code> for whatever workspace resolved. With no pin, an interactive push lists your workspaces and offers to write the pin; a non-TTY push refuses. Other commands (<code>promote</code>, <code>scope pull</code>) fall back to the workspace stored at login.</li></ul><p>You are always authenticated <strong>as yourself</strong> — a push is attributable to the human or service that ran it, never a hand-picked actor.</p><h2 id="config-file" tabindex="-1">Config file <a class="header-anchor" href="#config-file" aria-label="Permalink to &quot;Config file&quot;">​</a></h2><p><code>~/.substrat/config.json</code>, written <code>0600</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;controlPlaneUrl&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;https://console.substrat.net/api&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;bearerToken&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;…&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,        </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// a browser session (per-human)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;serviceToken&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;…&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,       </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// a machine credential (CI)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;defaultTenant&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;acme-co&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> // the workspace promote/scope act for (push pins one per project)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>It is a home-dir file, never committed to a repo.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/cli.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const cli = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  cli as default
};
