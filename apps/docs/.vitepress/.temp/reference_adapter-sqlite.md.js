import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/adapter-sqlite","description":"","frontmatter":{},"headers":[],"relativePath":"reference/adapter-sqlite.md","filePath":"reference/adapter-sqlite.md","lastUpdated":1787048747000}');
const _sfc_main = { name: "reference/adapter-sqlite.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-adapter-sqlite" tabindex="-1">@substrat-run/adapter-sqlite <a class="header-anchor" href="#substrat-run-adapter-sqlite" aria-label="Permalink to &quot;@substrat-run/adapter-sqlite&quot;">​</a></h1><p>The <strong>pure-SQLite scope host</strong> — real kernel semantics with no Cloudflare dependency. One SQLite file per scope, a per-scope actor for strict serialization, a directory database for fail-closed addressing, and a kernel-stamped event outbox.</p><p>It is <strong>not a mock</strong>: it is the adapter local development and CI run on, it passes the <a href="/reference/contract-tests.html">full conformance suite</a> that the production adapter passes, and it is the reason the self-host and escrow story is literally true rather than aspirational.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/adapter-sqlite</span></span></code></pre></div><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { SqliteScopeHost } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/adapter-sqlite&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { UNSAFE_allowAllChecker, webCryptoSecretBox } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/kernel&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> host</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> SqliteScopeHost</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  dir: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;./data&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,                    </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// one .sqlite file per scope + _directory.sqlite</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  checker: UNSAFE_allowAllChecker,  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// omit for the default tuple checker (deny by default)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  secretBox: </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">webCryptoSecretBox</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;k&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, key), </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// omit if you use no connectors — see below</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  fetch: myFetch,                   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// omit for the runtime&#39;s fetch</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">registerModule</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(myModule);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.admin.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">createTenant</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(actor, { id: tenantId, slug, name });                </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// tenant first</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">provisionScope</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(actor, { tenantId, scopeId, jurisdiction: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;eu&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// idempotent</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> stub</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">getScope</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(principal, tenantId, scopeId);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> stub.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">invoke</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder/create&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, input);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">close</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span></code></pre></div><h3 id="the-four-options" tabindex="-1">The four options <a class="header-anchor" href="#the-four-options" aria-label="Permalink to &quot;The four options&quot;">​</a></h3><table tabindex="0"><thead><tr><th>option</th><th>default</th><th>why it is shaped that way</th></tr></thead><tbody><tr><td><code>dir</code></td><td>required</td><td>holds <code>_directory.sqlite</code> plus one <code>&lt;tenantId&gt;__&lt;scopeId&gt;.sqlite</code> per scope, and <code>tstore__…</code> files for <a href="#per-tenant-relational-stores">per-tenant stores</a></td></tr><tr><td><code>checker</code></td><td>the built-in tuple checker</td><td>deny-by-default on empty tuples. The permissive one is called <code>UNSAFE_allowAllChecker</code> because the name is the warning</td></tr><tr><td><code>secretBox</code></td><td><em>unconfigured</em></td><td>seals per-tenant credentials at rest. Omitted, the host <strong>refuses to store a credential at all</strong> rather than storing it in the clear — every other surface keeps working, so a deployment using no connectors needs no key</td></tr><tr><td><code>fetch</code></td><td>the runtime&#39;s <code>fetch</code></td><td>egress for connectors, injectable so a test or a dev server can stand a provider up in memory. It is the only way to exercise a connector end to end before real credentials exist, and it stays the way to test failure paths a real provider will not produce on demand</td></tr></tbody></table><h2 id="how-the-semantics-map" tabindex="-1">How the semantics map <a class="header-anchor" href="#how-the-semantics-map" aria-label="Permalink to &quot;How the semantics map&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Contract guarantee</th><th>Implementation here</th><th>In production (<a href="/reference/adapter-cloudflare.html">the Cloudflare adapter</a>)</th></tr></thead><tbody><tr><td>strict serialization per scope</td><td>in-process actor, one queue per scope</td><td>Durable Object single-threaded execution</td></tr><tr><td>scope storage isolation</td><td>one SQLite file per scope</td><td>one DO (SQLite-backed) per scope</td></tr><tr><td>fail-closed addressing</td><td><code>_directory.sqlite</code> cross-check</td><td>directory + DO addressing</td></tr><tr><td>structured-clone boundary</td><td>explicit <code>structuredClone</code> both directions</td><td>the RPC boundary itself</td></tr><tr><td>stamped event envelopes</td><td>outbox table written in the same transaction</td><td>same, drained to the event spine</td></tr><tr><td>per-tenant relational store</td><td>a separate <code>tstore__*.sqlite</code> file</td><td>a minted per-tenant D1</td></tr></tbody></table><h2 id="what-lives-in-the-spine" tabindex="-1">What lives in the spine <a class="header-anchor" href="#what-lives-in-the-spine" aria-label="Permalink to &quot;What lives in the spine&quot;">​</a></h2><p>Every scope database carries the kernel&#39;s own tables alongside your module&#39;s. Module code may <strong>read</strong> them for projections like a timeline; writing to them is a <a href="/reference/boundary-lint.html">boundary-lint</a> error, because a forged spine row is indistinguishable from a real one.</p><table tabindex="0"><thead><tr><th>table</th><th>what it holds</th></tr></thead><tbody><tr><td><code>_substrat_outbox</code></td><td>stamped domain events, with the checks that authorized them (K-34) and a <code>drained_at</code> marker</td></tr><tr><td><code>_substrat_denials</code></td><td>refused permission checks — recorded <em>outside</em> the transaction the denial rolled back (K-35)</td></tr><tr><td><code>_substrat_migrations</code></td><td>the applied journal, per <code>(module, version)</code></td></tr><tr><td><code>_substrat_tuples</code></td><td>scope-local permission tuples</td></tr><tr><td><code>_substrat_deliveries</code></td><td>executor delivery attempts, retries, and dead letters</td></tr><tr><td><code>_substrat_attachments</code></td><td>attachment records against your entities</td></tr><tr><td><code>_substrat_platform_requests</code></td><td>durable <a href="/concepts/platform.html">platform intents</a> a vertical enqueued for the platform to execute</td></tr><tr><td><code>_substrat_schedule_state</code></td><td>the last-fired cadence per registered schedule</td></tr></tbody></table><p>The directory database holds the other half — tenants, scopes, hostnames, verticals and their versions and channels, orgs, roles, entitlements, identity pools and links, connections and their sealed secrets, subject keys, and the admin, access and ops-failure logs.</p><h2 id="the-host-surface" tabindex="-1">The host surface <a class="header-anchor" href="#the-host-surface" aria-label="Permalink to &quot;The host surface&quot;">​</a></h2><p>Beyond <code>getScope</code>, the host implements the admin-side contract the platform drives:</p><ul><li><strong>lifecycle</strong> — <code>provisionScope</code>, <code>migrateScope</code>, <code>migrationFrontier</code>, <code>close</code></li><li><strong>copies</strong> — <code>snapshotScope</code>, <code>importScope</code>, <code>restoreScope</code>, <code>deleteSnapshot</code> (<a href="/concepts/snapshots.html">snapshots &amp; test copies</a>)</li><li><strong>stores</strong> — <code>provisionTenantStore</code>, <code>openTenantStore</code>, <code>provisionBlobStore</code></li><li><strong>delivery</strong> — <code>registerExecutor</code>, <code>drainDue</code>, <code>executorDeadLetters</code></li><li><strong>connectors</strong> — <code>registerConnector</code>, <code>dispatchConnector</code>, <code>getConnectorScope</code>, <code>getConnectorAttachments</code></li><li><strong>schedules</strong> — <code>registeredSchedules</code>, <code>runDueSchedules</code>, <code>getSystemScope</code></li><li><strong>platform intents</strong> — <code>listPlatformRequests</code>, <code>listPlatformRequestHistory</code>, <code>settlePlatformRequest</code></li><li><strong>attachments</strong> — <code>attachments</code></li></ul><p>…plus <code>host.admin</code>, the <a href="/reference/kernel.html"><code>HostAdmin</code></a> surface every control-plane operation goes through.</p><h3 id="per-tenant-relational-stores" tabindex="-1">Per-tenant relational stores <a class="header-anchor" href="#per-tenant-relational-stores" aria-label="Permalink to &quot;Per-tenant relational stores&quot;">​</a></h3><p>A vertical that needs one database per <em>tenant</em> rather than per scope gets a handle, never a path: <code>provisionTenantStore</code> returns an opaque <code>ref</code> — here a <code>tstore__…​.sqlite</code> filename, in production a minted D1 id — and <code>openTenantStore</code> turns it back into a store. The prefix exists so a store file can never collide with a scope database.</p><h2 id="debugging-is-opening-a-file" tabindex="-1">Debugging is opening a file <a class="header-anchor" href="#debugging-is-opening-a-file" aria-label="Permalink to &quot;Debugging is opening a file&quot;">​</a></h2><p>Scope databases run in WAL mode and can be opened read-only with any SQLite tool, which is the single biggest practical reason to develop against this adapter:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sqlite3</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ./data/</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">tenantI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">__</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">scopeI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">.sqlite</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;.tables&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sqlite3</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ./data/</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">tenantI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">__</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">scopeI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">.sqlite</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;SELECT type, entity_id, actor FROM _substrat_outbox ORDER BY occurred_at DESC LIMIT 20;&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sqlite3</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ./data/_directory.sqlite</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;SELECT * FROM scopes;&#39;</span></span></code></pre></div><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><ul><li>Uses <a href="https://www.npmjs.com/package/better-sqlite3" target="_blank" rel="noreferrer">better-sqlite3</a> (a native module). With pnpm 10+, allow its build script via <code>pnpm.onlyBuiltDependencies</code>.</li><li><strong>Single-node by design.</strong> It preserves the serialization <em>semantics</em>, not the scale-out; production scale-out is the Cloudflare adapter&#39;s job. That is a deliberate split, not a gap — the semantics are what the contract tests pin, and they are identical.</li><li>The directory schema is re-applied on every open <strong>and after a restore</strong>, because a dump taken before a directory migration carries the old shape and replaying it verbatim would roll the schema backwards.</li><li>Pre-release: interfaces change without notice until the first vertical ships.</li></ul><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="/concepts/scope-host.html">Operations &amp; the scope host</a> — the contract in prose</li><li><a href="/reference/contract-tests.html"><code>@substrat-run/contract-tests</code></a> — what both adapters must pass</li><li><a href="/guide/running-locally.html">Running locally</a> — this adapter as a development loop</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/adapter-sqlite.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const adapterSqlite = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  adapterSqlite as default
};
