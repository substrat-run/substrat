import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Dashboard","description":"","frontmatter":{},"headers":[],"relativePath":"platform/dashboard.md","filePath":"platform/dashboard.md","lastUpdated":1787127415000}');
const _sfc_main = { name: "platform/dashboard.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="dashboard" tabindex="-1">Dashboard <a class="header-anchor" href="#dashboard" aria-label="Permalink to &quot;Dashboard&quot;">​</a></h1><p><em>&quot;Vercel, but for Substrat.&quot;</em> The <strong>tenant-facing self-service surface</strong> — where a customer&#39;s admin runs <strong>their own</strong> org. Sign up, get a tenant; provision apps (vertical instances) into it; manage members, domains, connections, and the plan. Seeing only their own tenant, gated by customer sign-up, not staff SSO.</p><p>It is the counterpart to the <a href="/platform/console.html">Console</a>: the Console is the operator&#39;s back office (all tenants, run the platform); the Dashboard is the customer&#39;s home (one tenant, run my org). Same platform, opposite audience and blast radius.</p><h2 id="the-bet-the-dashboard-is-itself-a-substrat-vertical" tabindex="-1">The bet: the Dashboard is itself a Substrat vertical <a class="header-anchor" href="#the-bet-the-dashboard-is-itself-a-substrat-vertical" aria-label="Permalink to &quot;The bet: the Dashboard is itself a Substrat vertical&quot;">​</a></h2><p>The load-bearing decision is that the Dashboard is <strong>built as a Substrat vertical</strong> — the platform, dogfooded on itself — which is what makes the hard part (authorization) fall out of the kernel instead of being re-invented. The Vercel analogy maps almost one-to-one:</p><table tabindex="0"><thead><tr><th>Vercel</th><th>Substrat</th></tr></thead><tbody><tr><td>Team / account</td><td><strong>Tenant</strong></td></tr><tr><td>Project</td><td><strong>Vertical instance</strong> — &quot;Acme HR&quot; is a Meridian instance</td></tr><tr><td>Deployment / version</td><td>a registered <strong>vertical version</strong> bound to a scope</td></tr><tr><td>Environment</td><td><strong>Scope</strong> — a tenant holds several</td></tr><tr><td>Team members</td><td><strong>principals + role assignments</strong></td></tr><tr><td>Domains</td><td><strong>hostname bindings</strong></td></tr><tr><td>Integrations</td><td><strong>connections</strong> (Scrive, Fortnox)</td></tr><tr><td>&quot;New Project from a template&quot;</td><td><strong>create instance</strong> (catalog → provision)</td></tr><tr><td>Preview deployment</td><td><strong><a href="/guide/environments-and-previews.html">preview</a></strong> — a version bound to a scope with data, at its own URL</td></tr><tr><td>Preview / staging <em>environment</em></td><td>a <strong>pinned preview + custom domain</strong> — there is no <code>staging</code> channel, <a href="/concepts/deploying.html#the-one-channel-prod">only <code>prod</code></a></td></tr></tbody></table><p>Concretely: a customer <em>is</em> a tenant; sign-up bootstraps that tenant, one <strong>dashboard scope</strong> (the customer&#39;s home), and the signer as its <strong>owner</strong>. A customer&#39;s apps are <strong>scopes</strong> in that same tenant. The dashboard scope&#39;s own operations are the account actions — every self-service affordance in the UI is a real vertical operation whose first line is a permission check. They fall into a handful of groups:</p><ul><li><strong>App lifecycle</strong> — <code>provision-app</code>, <code>mark-app-active</code>, <code>mark-app-failed</code>, <code>list-apps</code>, <code>app-events</code>, <code>update-app</code>, <code>delete-app</code>.</li><li><strong>Data &amp; previews</strong> — <code>snapshot-app</code>, <code>delete-app-snapshot</code>, <code>export-app-data</code>, <code>restore-app-data</code>.</li><li><strong>Hostnames</strong> — <code>bind-app-hostname</code>, <code>unbind-app-hostname</code>.</li><li><strong>Environment</strong> — <code>set-app-env</code>, <code>list-app-env</code>, <code>delete-app-env</code>.</li><li><strong>Identity</strong> — <code>set-app-auth</code>, <code>get-app-auth</code>.</li><li><strong>Members &amp; invites</strong> — <code>init-team</code>, <code>invite-member</code>, <code>accept-invite</code>, <code>preview-invite</code>, <code>resend-invite</code>, <code>revoke-invite</code>, <code>list-members</code>, <code>remove-member</code>, <code>leave-self</code>, <code>delete-team</code>.</li><li><strong>Connections</strong> — <code>begin-connection</code>.</li></ul><p>Provisioning an app is <code>assertAllowed(ctx.check(&#39;dashboard:provision-app&#39;))</code> then a <strong>tenant-narrowed</strong> <code>provisionScope</code> into the caller&#39;s own tenant: the kernel refuses a caller without the key before anything is created, and cannot provision into someone else&#39;s tenant by construction.</p><h2 id="auth" tabindex="-1">Auth <a class="header-anchor" href="#auth" aria-label="Permalink to &quot;Auth&quot;">​</a></h2><p>Login is <a href="/concepts/identity.html#two-real-choices-made-differently">AuthHero OIDC</a> through the shared <a href="/reference/oidc-rp.html"><code>@substrat-run/oidc-rp</code></a> relying party. Unlike the Console&#39;s staff-roster gate, the Dashboard does a <strong>JIT tenant bootstrap</strong>: a new user&#39;s first sign-in provisions their own tenant and dashboard scope, and makes them its owner.</p><h2 id="the-app-detail-five-tabs" tabindex="-1">The app detail: five tabs <a class="header-anchor" href="#the-app-detail-five-tabs" aria-label="Permalink to &quot;The app detail: five tabs&quot;">​</a></h2><p>Opening an app lands on its detail page, whose tab bar is five real nouns — <strong>Overview · Data · Deployments · Previews · Settings</strong>. The day-to-day configuration surfaces (Environment, Domains, Integrations) live as sections <em>inside</em> Settings rather than as top-level tabs, so the bar stays short. Old <code>snapshots</code> / <code>env</code> / <code>domains</code> / <code>integrations</code> tab URLs are aliased to their new homes, so bookmarks and in-flight links keep working.</p><h3 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h3><p>Real fields from the app row: the vertical, the <strong>running version</strong> (the version the app&#39;s scope is actually pinned to — what the router serves, not a hardcoded label), status, created-by, scope id, and a live activity timeline read from the app&#39;s own audit trail. A <strong>Production</strong> card lists one public URL per surface the vertical fronts (K-26), each with a copy button and a <em>Visit</em>. Beside it an <strong>API card</strong> surfaces the app&#39;s OpenAPI document (<code>/openapi.json</code>) and an <em>API docs</em> link into the app&#39;s <a href="https://scalar.com" target="_blank" rel="noreferrer">Scalar</a> reference — which rides the app&#39;s own session, so you sign in to the app first.</p><h3 id="data" tabindex="-1">Data <a class="header-anchor" href="#data" aria-label="Permalink to &quot;Data&quot;">​</a></h3><p>A read-only browser of the app&#39;s <strong>own</strong> database: the vertical&#39;s tables on one side (with the <code>_substrat_*</code> spine grouped apart), a paged view of the selected table on the other, and a collapsible <strong>SQL console</strong> for the one read-only <code>SELECT</code> the table browser can&#39;t express. Read-only is enforced below the seam — raw writes would bypass the event log and forge invariants — and every read is audited. Below it sits <strong>Export &amp; import</strong>: <em>Export</em> downloads the app&#39;s data as a <code>.dump.json</code> the CLI accepts (personal data redacted unless it&#39;s a staff/CLI full-fidelity export); <em>Import</em> replaces the app&#39;s data with an uploaded dump, behind a danger dialog and always after the platform forks a safety preview first.</p><h3 id="deployments" tabindex="-1">Deployments <a class="header-anchor" href="#deployments" aria-label="Permalink to &quot;Deployments&quot;">​</a></h3><p>For a customer who <em>builds</em> a vertical (not just instantiates one from the catalog), the <strong>Deployments</strong> tab is the builder-facing mirror of the staff <a href="/platform/console.html">console</a>&#39;s Verticals view — narrowed to the verticals <strong>this workspace owns</strong> (the ones it <a href="/guide/deploying.html">pushed with the CLI</a>). Per vertical: each version&#39;s admission state, and whether <strong><code>prod</code></strong> — the one channel (<a href="/concepts/deploying.html#the-one-channel-prod">dev/staging retired</a>) — points at it. &quot;Running&quot; is the version this app&#39;s scope is pinned to (what the router dispatches on), not the vertical&#39;s prod channel — they diverge when prod moves after install. A builder self-serves <code>prod</code> promotion right here for an <strong>owned, private</strong> vertical; a <strong>listed</strong> vertical&#39;s prod promotion is a platform decision (the marketplace gate). A non-production environment is not a second channel to promote — it is a <a href="#previews-environments">preview</a> below. Every read and promotion is checked to be one of the caller&#39;s own verticals — the dashboard&#39;s shared-plane credential can&#39;t be turned into a lever on another tenant&#39;s deployment. Beside promotion, a <strong>Bind version</strong> action pins <em>this</em> scope to any admitted version of its vertical — the per-scope catch-up (and the primitive behind a canary or a test environment); it carries the same <em>Snapshot data first</em> safety copy as an update.</p><p>When prod points somewhere other than where this app is pinned, <strong>Update to latest</strong> rebinds it. Two safety affordances sit alongside it. A <strong>Snapshot data first</strong> checkbox (on by default) takes a copy before a migration-crossing update, so a bad upgrade has a rollback point — a code-only update snapshots nothing, because the platform compares migration digests, not the checkbox. And after a migration, a time-boxed <strong>Back out</strong> offer appears while the pre-migration bookmark is inside the window the platform honors (~24h): rewinding the whole database to just before the migration — <em>discarding everything written since</em> — an honest first-hours backout, not a merge. For anything older, restore a preview instead.</p><h3 id="previews-environments" tabindex="-1">Previews &amp; environments <a class="header-anchor" href="#previews-environments" aria-label="Permalink to &quot;Previews &amp; environments {#previews-environments}&quot;">​</a></h3><p>There are two preview surfaces, because a preview does two jobs — a <strong>data</strong> test copy of one install, and a <strong>non-production environment</strong> for a vertical you build.</p><p><strong>Per-app previews</strong> (an install&#39;s <strong>Previews</strong> tab) are <a href="/concepts/snapshots.html">test copies</a> of <em>that app&#39;s</em> data. <strong>Create preview</strong> forks the app&#39;s entire database into an independent copy with a retention choice (1/7/30 days, or keep until deleted); the list shows each copy&#39;s provenance, its own URL, and a live expiry countdown; expired copies are reaped by the platform&#39;s scheduled sweep. A preview is unmistakably <em>not</em> the live app: it receives no traffic, integrations are off, and deleting it is safe by construction — the platform refuses to hard-delete anything that isn&#39;t a copy. The same machinery backs the <em>Snapshot data first</em> checkbox in Deployments (above) and the safety copy taken before a data import. Authorization is the same key that manages apps (<code>dashboard:provision-app</code>), checked in-scope before any platform effect; the fork itself runs inside the app&#39;s own deployment, so <a href="/concepts/snapshots.html#where-the-data-goes-and-doesn-t">no app data crosses to the platform</a>.</p><p><strong>Builder environments</strong> (on the <strong>Deployments</strong> side, for a vertical you <em>push</em>) are the same primitive turned outward: a preview is a version bound to a scope with data, at its own URL, and it is how you run every non-production environment now that <a href="/concepts/deploying.html#the-one-channel-prod">there is only a <code>prod</code> channel</a>. From here a builder can, for a vertical they own (private <strong>or</strong> listed — publishing widens who may <em>install</em>, not who may preview their own code):</p><ul><li><strong>Create a preview</strong> of any pushed version — a fork of prod, or an <code>--empty</code> clean room for a vertical with no prod scope yet. The <a href="/guide/deploying.html#deploy-from-ci">PR-preview CI</a> creates and updates these automatically; the list mirrors what the <a href="/reference/cli.html#preview"><code>substrat preview</code></a> CLI shows.</li><li><strong>Pin a preview</strong> (no expiry) and <strong>attach a custom domain</strong> to it — turning it into a long-lived <strong>test environment</strong> at a stable address like <code>crm-test.ahero.se</code>. The domain binds to the scope (Settings → Domains works on a preview scope too), and a merge-to-main job rebinds that scope to the head of <code>main</code> so the environment always runs the latest code.</li></ul><p>The full workflow — sticky-per-PR + per-build URLs, the pinned test environment, canary rollout, and the release candidate — is <a href="/guide/environments-and-previews.html">Environments &amp; previews</a>.</p><h3 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to &quot;Settings&quot;">​</a></h3><p>Configuration, not daily-driver surfaces — gathered under one tab with its own sections, each keeping its own URL (<code>settings/environment</code> …) so deep links survive:</p><ul><li><strong>General</strong> — rename the app, read its kind, and the danger zone (deleting an app deprovisions its scope for real; the audit history is retained). It also carries the <strong>Identity</strong> card: the <code>substrat:auth</code> choice made at install (which issuer the app trusts), readable and editable after the fact — the client secret is write-only.</li><li><strong>Environment</strong> — the former <em>Env</em> tab. Not a free-form key/value editor, but a form <strong>generated from the vertical&#39;s declared <a href="/concepts/modules.html#declared-environment-envspec"><code>envSpec</code></a></strong>: each field carries the manifest&#39;s label, description, placeholder, and <code>required</code>/<code>secret</code> flags. The spec is read from the <strong>registry</strong> (where <code>registerVertical</code> stored it), so a pushed builder vertical gets a config form exactly like a builtin, without the dashboard bundling its code. Values are per app; secret values are <strong>write-only</strong> — masked, never returned by the API, left blank to keep. Delivery follows the app&#39;s shape: a hosted vertical reads its per-tenant config at runtime rather than through per-app worker secrets.</li><li><strong>Domains</strong> — custom-hostname binding (K-26 multi-surface). One scope can front several surfaces, and the hostname decides which surface the vertical serves. A <strong>platform</strong> hostname is minted from the app&#39;s label and is live immediately (it rides the wildcard cert); a <strong>custom</strong> domain lands <code>pending</code> and walks DNS validation + certificate issuance, going live only when <code>active</code>. The default hostname can&#39;t be removed here — deleting the app retires it. Backed by <code>dashboard/bind-app-hostname</code> / <code>unbind-app-hostname</code>.</li><li><strong>Integrations</strong> — third-party <strong>connections</strong> (Scrive, Fortnox), begun with <code>dashboard/begin-connection</code> and its signed OAuth handshake.</li></ul><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Built and connected — self-service sign-up bootstraps a tenant, the catalog offers a real <a href="/verticals/callout.html">Callout</a> entry, and provisioning runs through the tenant-narrowed control-plane seam (an app becomes a live scope; deleting one deprovisions it for real). Most of the designed surface is now shipped: the app lifecycle, builder <strong>Deployments</strong>, a read-only <strong>Data</strong> browser with export/import, <strong>Previews</strong>, per-app <strong>Environment</strong>, custom <strong>Domains</strong>, team <strong>members</strong> (invite / accept / remove / leave, with a Team view), and third-party <strong>connections</strong> (an Integrations view). Billing and the plan are the main pieces still on the roadmap the <a href="https://github.com/substrat-run/substrat/blob/main/docs/architecture/dashboard.md" target="_blank" rel="noreferrer">design note</a> lays out. It is served as a React SPA bundled into its worker; the account menu lives in the sidebar footer.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("platform/dashboard.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const dashboard = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  dashboard as default
};
