import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"What is Substrat?","description":"","frontmatter":{},"headers":[],"relativePath":"guide/what-is-substrat.md","filePath":"guide/what-is-substrat.md","lastUpdated":1786737227000}');
const _sfc_main = { name: "guide/what-is-substrat.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="what-is-substrat" tabindex="-1">What is Substrat? <a class="header-anchor" href="#what-is-substrat" aria-label="Permalink to &quot;What is Substrat?&quot;">​</a></h1><p>Substrat is a substrate for building <strong>vertical B2B SaaS</strong> — the software a property manager, an installation firm, or a point-of-sale chain actually runs their business on.</p><p>AI has made building that kind of software fast and cheap — except for the parts that were never about writing code: multi-tenancy, identity, permissions, integrations, data integrity, audit, and GDPR. Those are exactly the parts that are catastrophic when wrong, and the parts LLM-generated code gets wrong most often.</p><p>Substrat owns those hard parts and enforces them <strong>at runtime</strong>, so small teams — including non-engineers wielding AI tools — can build production-grade products on top, at AI speed, without the speed being fatal.</p><blockquote><p>We build the substrate. You build the verticals.</p></blockquote><h2 id="the-three-layers" tabindex="-1">The three layers <a class="header-anchor" href="#the-three-layers" aria-label="Permalink to &quot;The three layers&quot;">​</a></h2><p>Everything in Substrat hangs off one decomposition:</p><h3 id="_1-kernel" tabindex="-1">1. Kernel <a class="header-anchor" href="#_1-kernel" aria-label="Permalink to &quot;1. Kernel&quot;">​</a></h3><p>Everything that is true of <em>every</em> B2B SaaS, and nothing that is true of any particular one: identity, nested tenancy, permissions, events/audit, the module system, GDPR machinery.</p><p>The kernel owns <strong>no domain entities</strong> — there is no customer table and no work-order table in the kernel. Instead it offers <em>attachment contracts</em> (events, permissions, documents, relations) that bind to opaque <code>(entityType, entityId)</code> references your code defines. Your domain stays yours; the guarantees stay the kernel&#39;s.</p><h3 id="_2-engines" tabindex="-1">2. Engines <a class="header-anchor" href="#_2-engines" aria-label="Permalink to &quot;2. Engines&quot;">​</a></h3><p>Domain machinery shared across verticals but too domain-shaped for the kernel: work orders, invoicing, scheduling, ticketing. Engines are headless, versioned npm packages that register into the kernel like any other module.</p><p>Engines own <strong>invariants</strong>: state machines can&#39;t skip states, time entries are append-only, exported invoices are immutable, every mutation emits an event, every access passes the permission check. Verticals own everything with a user&#39;s fingerprints on it: vocabulary, extra fields, triggers, pricing logic, UI.</p><p>The design test for the boundary: <em>if a vertical ever needs to fork an engine, the engine drew its line wrong.</em></p><p>See <a href="/engines/">What is an engine?</a> for the composition rules and the engines that exist today.</p><h3 id="_3-verticals" tabindex="-1">3. Verticals <a class="header-anchor" href="#_3-verticals" aria-label="Permalink to &quot;3. Verticals&quot;">​</a></h3><p>The actual products — your code. A vertical composes the kernel and one or more engines, adds its own module (schema, operations, screens), and ships. Verticals are where AI tools do their best work, because the layer where LLMs are strongest (screens, forms, workflows, reports) is the layer where mistakes are cosmetic — the catastrophic layer sits below the API surface, enforced by the kernel.</p><h2 id="what-enforced-at-runtime-means-concretely" tabindex="-1">What &quot;enforced at runtime&quot; means concretely <a class="header-anchor" href="#what-enforced-at-runtime-means-concretely" aria-label="Permalink to &quot;What &quot;enforced at runtime&quot; means concretely&quot;">​</a></h2><p>Code built on Substrat <strong>cannot</strong>:</p><ul><li><strong>reach another tenant&#39;s data</strong> — data access only exists as capability-scoped operations invoked through a stub minted for one <code>(tenant, scope)</code> pair; a mismatched pair fails closed rather than resolving to someone else&#39;s data;</li><li><strong>skip the audit log</strong> — events are stamped with tenant, scope, actor, and timestamp <em>below</em> the API surface; calling code cannot forge, suppress, or mislabel them;</li><li><strong>emit unclassified PII</strong> — every event carries a mandatory <code>piiClass</code>, and a PII-classed event without a data-subject key fails validation, so GDPR erasure is always possible;</li><li><strong>bypass the permission model</strong> — operations run inside the scope&#39;s execution domain and check permissions there, and every allow carries the proof path that granted it.</li></ul><p>None of this depends on the discipline of the code above it — which is the point, because increasingly that code is written by an agent.</p><h2 id="current-status" tabindex="-1">Current status <a class="header-anchor" href="#current-status" aria-label="Permalink to &quot;Current status&quot;">​</a></h2><p>Substrat is pre-release (0.x). What exists today:</p><table tabindex="0"><thead><tr><th>Piece</th><th>Package</th><th>Status</th></tr></thead><tbody><tr><td>Contract schemas (Zod, source of truth)</td><td><a href="/reference/contracts.html"><code>@substrat-run/contracts</code></a></td><td>Working</td></tr><tr><td>Kernel interfaces (scope host, permission checker)</td><td><a href="/reference/kernel.html"><code>@substrat-run/kernel</code></a></td><td>Working</td></tr><tr><td>Pure-SQLite scope host (local dev, CI, self-host)</td><td><a href="/reference/adapter-sqlite.html"><code>@substrat-run/adapter-sqlite</code></a></td><td>Working</td></tr><tr><td>Adapter conformance suite</td><td><a href="/reference/contract-tests.html"><code>@substrat-run/contract-tests</code></a></td><td>Working</td></tr><tr><td>Cloudflare scope host (Durable Objects, production)</td><td><a href="/reference/adapter-cloudflare.html"><code>@substrat-run/adapter-cloudflare</code></a></td><td>Working — same suite, real workerd; scope-local permissions + the router landed</td></tr><tr><td>Work-order engine</td><td><a href="/engines/workorder/"><code>@substrat-run/engine-workorder</code></a></td><td>Seed</td></tr><tr><td>Booking engine</td><td><a href="/engines/booking/"><code>@substrat-run/engine-booking</code></a></td><td>Seed</td></tr><tr><td>Invoicing engine</td><td><a href="/engines/invoicing/"><code>@substrat-run/engine-invoicing</code></a></td><td>Seed</td></tr><tr><td>Protocol / checklist engine</td><td><a href="/engines/protocol/"><code>@substrat-run/engine-protocol</code></a></td><td>Seed</td></tr><tr><td>Invites engine</td><td><a href="/engines/invites/"><code>@substrat-run/engine-invites</code></a></td><td>Seed</td></tr><tr><td>Absence engine</td><td><a href="/engines/absence/"><code>@substrat-run/engine-absence</code></a></td><td>Seed</td></tr><tr><td>Metering engine</td><td><a href="/engines/metering/"><code>@substrat-run/engine-metering</code></a></td><td>Seed</td></tr><tr><td>Scrive connector (e-signing, BankID)</td><td><a href="/connectors/scrive.html"><code>@substrat-run/connector-scrive</code></a></td><td>Published <code>0.1.15</code></td></tr><tr><td>The <code>substrat</code> CLI — authenticated deploy (<code>login</code>, <code>push</code>)</td><td><a href="/guide/deploying.html"><code>@substrat-run/cli</code></a></td><td>Working</td></tr><tr><td>Platform surfaces — <a href="/platform/control-plane.html">control plane</a>, <a href="/platform/console.html">console</a>, <a href="/platform/router.html">router</a>, <a href="/platform/dashboard.html">dashboard</a></td><td>private deployments</td><td>Working (self-serve deploy foundation)</td></tr><tr><td>Callout (field service) — the canonical composition; first CP-less pushable vertical</td><td><a href="/verticals/callout.html"><code>demos/callout</code></a></td><td>Working</td></tr><tr><td>Handlebar (bike workshop) — engine reuse under new vocabulary</td><td><a href="/verticals/handlebar.html"><code>demos/handlebar</code></a></td><td>Working</td></tr><tr><td>RallyPoint (padel club) — the booking invariant shape</td><td><a href="/verticals/rallypoint.html"><code>demos/rally</code></a></td><td>Working</td></tr><tr><td>Kallkälla (coffee shop) — two audiences, one API; commerce</td><td><a href="/verticals/shop.html"><code>demos/shop</code></a></td><td>Working</td></tr><tr><td>Meridian (HR) — leave/absence, time, expenses, onboarding; one role-adaptive app</td><td><a href="/verticals/meridian.html"><code>demos/meridian</code></a></td><td>Working</td></tr><tr><td>Manyfold (headless CMS) — multi-scope, <em>site = scope</em>; the editorial lifecycle as the invariant</td><td><a href="/verticals/manyfold.html"><code>demos/manyfold</code></a></td><td>Working</td></tr></tbody></table><p>Interfaces change without notice until the first vertical ships.</p><p><strong>Meridian</strong> is the deliberate shape-breaker: a domain with <em>no ready-made engine</em>, so leave/absence, time reporting, and expenses are all vertical code — proof the kernel&#39;s guarantees (nested tenancy, permissions, audit, GDPR) hold with zero engine support, and that its value isn&#39;t borrowed from the work-order state machine. It reuses only the protocol engine (onboarding) and is the vertical that surfaces the <a href="/engines/#engines-today">absence / entry-ledger engine</a> candidate. It also exercises what Callout doesn&#39;t: two country scopes (Sweden 25 days + saved days, Spain 22 + <em>registro de jornada</em>) diverging from one codebase, and a single app that adapts to the person — an employee sees only their own record; a team lead who is <em>also</em> an employee gets a <strong>Manage</strong> section beside their own <strong>My work</strong>, all behind the same permission checks.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/what-is-substrat.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const whatIsSubstrat = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  whatIsSubstrat as default
};
