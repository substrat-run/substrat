import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Operations, functions & permissions","description":"","frontmatter":{},"headers":[],"relativePath":"engines/absence/surface.md","filePath":"engines/absence/surface.md","lastUpdated":1786727111000}');
const _sfc_main = { name: "engines/absence/surface.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="operations-functions-permissions" tabindex="-1">Operations, functions &amp; permissions <a class="header-anchor" href="#operations-functions-permissions" aria-label="Permalink to &quot;Operations, functions &amp; permissions&quot;">​</a></h1><h2 id="operations" tabindex="-1">Operations <a class="header-anchor" href="#operations" aria-label="Permalink to &quot;Operations&quot;">​</a></h2><p>Registered bindings, each one a permission check plus a call into the in-scope function below.</p><table tabindex="0"><thead><tr><th>Operation</th><th>Permission</th><th>Does</th></tr></thead><tbody><tr><td><code>absence/configure-leave-type</code></td><td><code>absence:configure</code></td><td>register/update a leave type&#39;s key, floor, active flag</td></tr><tr><td><code>absence/list-leave-types</code></td><td><code>absence:read</code></td><td>list registered types</td></tr><tr><td><code>absence/record-entry</code></td><td><code>absence:configure</code></td><td>append an <code>accrual</code> / <code>correction</code> / <code>carryover</code> entry</td></tr><tr><td><code>absence/request</code></td><td><code>absence:request</code> <em>(per subject)</em></td><td>file a request for a subject</td></tr><tr><td><code>absence/decide</code></td><td><code>absence:approve</code></td><td>approve (books, floor-checked) or reject</td></tr><tr><td><code>absence/cancel</code></td><td><code>absence:approve</code>, <strong>or</strong> <code>absence:request</code> <em>(per subject)</em> for one&#39;s own still-<code>requested</code> row</td><td>withdraw or cancel; an approved cancel writes the reversal</td></tr><tr><td><code>absence/expire-stale</code></td><td><code>absence:approve</code></td><td>cancel requests past their start date — the declared schedule&#39;s target</td></tr><tr><td><code>absence/balance</code></td><td><code>absence:read</code> <em>(per subject)</em></td><td>balance-as-of fold</td></tr><tr><td><code>absence/availability</code></td><td><code>absence:read</code> <em>(per subject)</em></td><td>per-date coverage over a range</td></tr><tr><td><code>absence/list-requests</code></td><td><code>absence:read</code> <em>(per subject when filtered, node otherwise)</em></td><td>requests, by subject and/or status</td></tr><tr><td><code>absence/list-entries</code></td><td><code>absence:read</code> <em>(per subject)</em></td><td>the raw ledger for a subject</td></tr></tbody></table><p>Checks marked <em>(per subject)</em> pass the subject&#39;s <code>EntityRef</code>, so a principal holding an <strong>entity-narrowed grant</strong> on their own ref — an employee, a fältarbetare with a login — reaches their own balance, requests, and withdrawals while holding no role at all. Node holders (managers, planners, HR) pass the same checks unnarrowed.</p><h2 id="in-scope-functions" tabindex="-1">In-scope functions <a class="header-anchor" href="#in-scope-functions" aria-label="Permalink to &quot;In-scope functions&quot;">​</a></h2><p>The composable surface. A vertical calls these <strong>inside its own operation and its own permission check</strong>, in one transaction — this is how you extend the engine without forking it.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">configureLeaveType</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { key, floor?, active? })                → LeaveType</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">listLeaveTypes</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx)                                              → LeaveType[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">recordEntry</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { subject, leaveTypeKey, entryKind,             </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// accrual|correction|carryover</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">                   delta, effectiveDate, note? })                → AbsenceEntry</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">requestAbsence</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { subject, leaveTypeKey,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">                      startDate, endDate, days, note? })         → AbsenceRequest</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">decideAbsence</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { requestId, decision, note? })               → { request, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">booking</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: AbsenceEntry </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">|</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cancelAbsence</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { requestId, reason? })                       → { request, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">reversal</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: AbsenceEntry </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">|</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">expireStaleRequests</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx)                                         → { </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">expired</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: number }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">balanceAsOf</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { subject, leaveTypeKey, asOf? })               → string          </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// pure fold</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">availability</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { subject, from, to })                         → { days, requests }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">listRequests</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { subject?, status? })                         → AbsenceRequest[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">listEntries</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { subject, leaveTypeKey? })                     → AbsenceEntry[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">entriesInWindow</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { from, to, entryKind? })                   → AbsenceEntry[]  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// in-scope only, no binding</span></span></code></pre></div><p>Every <strong>write</strong> takes a <code>subject</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">subject</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  ref</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: EntityRef,               </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// your noun — { entityType: &#39;employee&#39; | &#39;resource&#39; | …, entityId }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  dataSubjectId</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: DataSubjectId, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// keys erasure on every event this write emits</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><strong>Reads</strong> take the bare <code>ref</code> — no erasure key is needed to look.</p><p>Notes worth knowing:</p><ul><li><code>decideAbsence(approve)</code> <strong>re-folds at decision time</strong> and enforces the leave type&#39;s floor then — the request may be days old and the world may have moved.</li><li><code>cancelAbsence</code> on an <code>approved</code> request writes the compensating <code>reversal</code> in the same transaction as the status change; on a <code>requested</code> row it touches no ledger.</li><li><code>entriesInWindow</code> has <strong>no operation binding</strong> — it exists for vertical compositions (a payroll export collecting the period&#39;s bookings; a planner sweeping a route window) that gate it behind their own permission.</li><li><code>availability</code> clamps to the queried range and reports <strong>approved</strong> coverage only — a merely-requested absence never shows as covered.</li></ul><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><p><code>absence:read</code> · <code>absence:request</code> · <code>absence:approve</code> · <code>absence:configure</code></p><ul><li><strong><code>absence:request</code></strong> is the self-service key: granted entity-narrowed on a subject&#39;s own ref, it lets that person file — and withdraw — their own requests, and nothing else.</li><li><strong><code>absence:approve</code></strong> covers deciding, cancelling an approved absence, and the expiry sweep — it is also the single permission the declared schedule&#39;s system principal is granted, which is exactly what appears in the permission diff.</li><li><strong><code>absence:configure</code></strong> covers leave-type policy and direct ledger writes (<code>recordEntry</code>) — the administrator&#39;s escape hatch, deliberately floor-unchecked.</li><li><strong><code>absence:read</code></strong> unnarrowed is the planner&#39;s and manager&#39;s view; narrowed, it is &quot;my balance&quot;.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/absence/surface.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const surface = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  surface as default
};
