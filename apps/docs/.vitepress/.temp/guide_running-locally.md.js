import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderSuspense, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Running the whole stack locally","description":"","frontmatter":{},"headers":[],"relativePath":"guide/running-locally.md","filePath":"guide/running-locally.md","lastUpdated":1787410694000}');
const _sfc_main = { name: "guide/running-locally.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="running-the-whole-stack-locally" tabindex="-1">Running the whole stack locally <a class="header-anchor" href="#running-the-whole-stack-locally" aria-label="Permalink to &quot;Running the whole stack locally&quot;">​</a></h1><p><a href="/guide/getting-started.html">Getting started</a> builds one host in one script. This page is the other end: the <strong>entire</strong> flow — a vertical, the shared control plane, and the admin console — running together on your machine with <code>pnpm dev</code> and nothing but local SQLite. No cloud account, no Docker, no second datastore.</p><div class="tip custom-block"><p class="custom-block-title">This is a monorepo command</p><p><code>pnpm dev</code> at the root of the <a href="https://github.com/substrat-run/substrat" target="_blank" rel="noreferrer">substrat monorepo</a> runs the Callout demo vertical (<code>demos/callout</code>) wired to the console (<code>apps/console</code>). It is the reference for what a local stack looks like, not a published tool.</p></div><h2 id="one-command" tabindex="-1">One command <a class="header-anchor" href="#one-command" aria-label="Permalink to &quot;One command&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span></span></code></pre></div><p>It ends on a banner telling you where to go:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  substrat · local stack — one process, one SQLite dir</span></span>
<span class="line"><span>  ────────────────────────────────────────────────────</span></span>
<span class="line"><span>    ▶ Console (open this)   http://localhost:5272</span></span>
<span class="line"><span>    ▶ Portal — Callout    http://localhost:5271</span></span>
<span class="line"><span></span></span>
<span class="line"><span>      vertical API          http://localhost:8871</span></span>
<span class="line"><span>      control plane API     http://localhost:8788</span></span>
<span class="line"><span>  ────────────────────────────────────────────────────</span></span>
<span class="line"><span>    data   …/demos/callout/.data</span></span></code></pre></div><p>Open the <strong>console</strong> to act as the platform operator (tenants, scopes, entitlements, suspend). Open the <strong>portal</strong> to act as a tenant&#39;s user (log in as a persona, do work).</p><h2 id="what-is-actually-running" tabindex="-1">What is actually running <a class="header-anchor" href="#what-is-actually-running" aria-label="Permalink to &quot;What is actually running&quot;">​</a></h2><p>The surprising part — and the thing that makes the flow real rather than mocked — is that there is <strong>one backend process</strong>. The vertical API and the control plane are two HTTP listeners over the <strong>same <code>SqliteScopeHost</code></strong>, which owns both the directory and every scope&#39;s data. Two browser apps (Vite dev servers) sit in front, each proxying <code>/api</code> to its own listener.</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-28",
        class: "mermaid",
        graph: "flowchart%20TB%0A%20%20subgraph%20browser%5B%22Your%20browser%22%5D%0A%20%20%20%20C%5B%22Console%20UI%3Cbr%2F%3Elocalhost%3A5272%22%5D%0A%20%20%20%20A%5B%22Callout%20app%20(portal)%3Cbr%2F%3Elocalhost%3A5271%22%5D%0A%20%20end%0A%0A%20%20subgraph%20proc%5B%22One%20Node%20process%20%E2%80%94%20pnpm%20dev%22%5D%0A%20%20%20%20direction%20TB%0A%20%20%20%20CP%5B%22control-plane%20router%3Cbr%2F%3E%3A8788%22%5D%0A%20%20%20%20V%5B%22vertical%20API%20(Hono)%3Cbr%2F%3E%3A8871%22%5D%0A%20%20%20%20H%5B%22SqliteScopeHost%3Cbr%2F%3E%3Ci%3Ethe%20shared%20host%3C%2Fi%3E%22%5D%0A%20%20%20%20CP%20--%3E%20H%0A%20%20%20%20V%20--%3E%20H%0A%20%20end%0A%0A%20%20subgraph%20data%5B%22demos%2Fcallout%2F.data%20%E2%80%94%20one%20SQLite%20directory%22%5D%0A%20%20%20%20D%5B(%22_directory.sqlite%3Cbr%2F%3Etenants%20%C2%B7%20scopes%20%C2%B7%20roles%20%C2%B7%20entitlements%20%C2%B7%20audit%22)%5D%0A%20%20%20%20S%5B(%22%26lt%3Btenant%26gt%3B__%26lt%3Bscope%26gt%3B.sqlite%3Cbr%2F%3Eone%20file%20per%20scope%20%E2%80%94%20its%20data%20%2B%20outbox%22)%5D%0A%20%20%20%20B%5B(%22better-auth.sqlite%3Cbr%2F%3Elogins%20%C2%B7%20sessions%22)%5D%0A%20%20end%0A%0A%20%20C%20--%3E%7C%22%2Fapi%20%E2%86%92%20%3A8788%22%7C%20CP%0A%20%20A%20--%3E%7C%22%2Fapi%20%E2%86%92%20%3A8871%22%7C%20V%0A%20%20H%20--%3E%20D%0A%20%20H%20--%3E%20S%0A%20%20V%20-.-%3E%7Cidentity%7C%20B%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<p>Because the control plane and the vertical share <strong>one host</strong>, they share <strong>one directory</strong>. That is why a suspend in the console immediately fails the portal&#39;s next action closed: <code>getScope</code> reads the scope&#39;s status from <code>_directory.sqlite</code> on every call, and the console just wrote to that same row. There is no sync, no second copy — it is one <code>UPDATE</code> and one <code>SELECT</code> against the same file.</p><h3 id="the-processes" tabindex="-1">The processes <a class="header-anchor" href="#the-processes" aria-label="Permalink to &quot;The processes&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Port</th><th>Process</th><th>What it is</th></tr></thead><tbody><tr><td><code>5272</code></td><td>Vite dev server</td><td>The <strong>console</strong> — the platform operator&#39;s admin UI</td></tr><tr><td><code>5271</code></td><td>Vite dev server</td><td>The <strong>portal</strong> — Callout&#39;s tenant-facing app</td></tr><tr><td><code>8871</code></td><td>Node (Hono)</td><td>The <strong>vertical API</strong> — resolves a user, <code>getScope</code>, invokes operations</td></tr><tr><td><code>8788</code></td><td>Node (Hono)</td><td>The <strong>control plane</strong> — the audited directory surface the console drives</td></tr></tbody></table><p>The two Node listeners are the <em>same process</em> sharing one host; the two Vite servers are separate. All four are launched and torn down together by <code>pnpm dev</code>.</p><h3 id="the-databases" tabindex="-1">The databases <a class="header-anchor" href="#the-databases" aria-label="Permalink to &quot;The databases&quot;">​</a></h3><p>Everything lives under <code>demos/callout/.data</code> as plain SQLite files (WAL mode — the <code>-wal</code> / <code>-shm</code> siblings are SQLite&#39;s, not yours to touch):</p><table tabindex="0"><thead><tr><th>File</th><th>Owned by</th><th>Holds</th></tr></thead><tbody><tr><td><code>_directory.sqlite</code></td><td>the shared host</td><td>The directory: tenant registry, scope records + lifecycle status, roles, entitlements, tenant-level permission tuples, and the admin audit log</td></tr><tr><td><code>&lt;tenantId&gt;__&lt;scopeId&gt;.sqlite</code></td><td>the shared host</td><td>One per scope — that scope&#39;s own tables, permission tuples, and event outbox. Isolated: a scope is its own database and consistency domain</td></tr><tr><td><code>better-auth.sqlite</code></td><td>the vertical&#39;s auth</td><td>Identities, credentials, and sessions for the portal&#39;s logins</td></tr></tbody></table><p>Debugging is opening a file:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sqlite3</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> demos/callout/.data/_directory.sqlite</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;SELECT slug, status FROM scopes;&#39;</span></span></code></pre></div><p>Delete the <code>.data</code> directory to reset the world; it re-seeds on the next boot.</p><h2 id="letting-an-agent-run-it" tabindex="-1">Letting an agent run it <a class="header-anchor" href="#letting-an-agent-run-it" aria-label="Permalink to &quot;Letting an agent run it&quot;">​</a></h2><p>Every demo and every scaffolded project ships a <code>.claude/launch.json</code>, so <a href="https://code.claude.com/docs/en/desktop" target="_blank" rel="noreferrer">Claude Desktop</a> starts the dev servers itself, opens the web app in the Browser pane, and — with <code>autoVerify</code> on — screenshots and checks for errors after each edit it makes.</p><p>This is worth more here than in most projects. A demo&#39;s scenario test composes the host directly and <strong>never boots <code>src/server.ts</code></strong>, so a green suite says nothing at all about the HTTP layer. The Browser pane is the reliable way to drive the part the tests skip.</p><p>Each process gets its <strong>own</strong> entry rather than the single <code>concurrently</code> pair <code>pnpm dev</code> runs, which is the point: Claude can attach the Browser to the web port while reading the API&#39;s log independently. Callout&#39;s:</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;version&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;0.0.1&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;configurations&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;name&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;api&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;runtimeExecutable&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;pnpm&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;runtimeArgs&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;run&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;server&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;port&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">8871</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;env&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;ALLOW_DEV_HEADER&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;true&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;autoPort&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;name&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;web&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;runtimeExecutable&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;pnpm&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;runtimeArgs&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;--dir&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;app&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;dev&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;port&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">5271</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;autoPort&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><h3 id="these-files-are-emitted-—-don-t-hand-edit-them" tabindex="-1">These files are emitted — don&#39;t hand-edit them <a class="header-anchor" href="#these-files-are-emitted-—-don-t-hand-edit-them" aria-label="Permalink to &quot;These files are emitted — don&#39;t hand-edit them&quot;">​</a></h3><p>The topology is declared once in the <code>substrat.devServers</code> block of each project&#39;s <code>package.json</code>, and the launch file is generated from it by <code>pnpm lint:launch</code> (CI runs <code>--check</code> and fails on drift). A declaration names the env var that moves a port and the file that binds it; the <strong>number is read out of that file</strong>, so moving a port means editing <code>src/server.ts</code> or <code>app/vite.config.ts</code> and re-running the emitter — never editing the JSON.</p><p>The same block carries <code>requires</code> — the keys a process cannot start <strong>without</strong> locally, checked by <code>tools/env-preflight.mjs</code> before the server boots. It is deliberately <em>not</em> emitted into <code>launch.json</code>: a launch file starts a server, and what a server needs in order to start is not something a client-specific adapter should hold. Note this is a different question from <code>envSpec</code>&#39;s <code>required</code>, which means required to <strong>deploy</strong> — a hosted install receives most of its config through per-scope delivery, so keys that are optional there can still be mandatory on your machine, where no such delivery exists.</p><h3 id="gotchas" tabindex="-1">Gotchas <a class="header-anchor" href="#gotchas" aria-label="Permalink to &quot;Gotchas&quot;">​</a></h3><ul><li><strong>Open the session in <code>demos/&lt;name&gt;</code>, not the monorepo root.</strong> Preview servers use the selected folder as the working directory and do not scan subfolders, so a session opened at the root finds no configuration.</li><li><strong><code>autoPort: false</code> everywhere, deliberately.</strong> The OIDC-only demos redirect to a fixed callback and the shop&#39;s Better Auth trusts two fixed origins, so a silently reassigned port would break the <em>login</em>, not the boot — a much worse failure to debug. The cost is that a genuine clash is fatal: <code>demos/rally</code> and <code>demos/auth-server</code> both sit on <code>:8877</code> and <code>:5277</code>, so they cannot run at the same time without <code>PORT=… WEB_PORT=…</code>.</li><li><strong>A process can bind more than the port it declares.</strong> Callout&#39;s <code>api</code> entry starts the vertical API on <code>:8871</code> <em>and</em> the co-located control plane on <code>:8788</code>; only the first is declared, because only the first is the one to attach a browser to. If <code>:8788</code> is taken — by another project, or a stray <code>pnpm dev:connected</code> — the entry dies on <code>EADDRINUSE</code> for a port Claude is not watching, which reads as a server that simply never came up. <code>CP_PORT=…</code> moves it.</li><li><strong>No secrets in <code>launch.json</code></strong> — it is committed. Desktop also does not inherit your full shell environment, and <code>env</code> in <code>~/.claude/settings.json</code> reaches <em>sessions</em> but not dev servers. Put values in <strong><code>.dev.vars</code></strong> in the project directory instead: it is gitignored, <code>wrangler dev</code> already reads it, and the <code>server</code> script loads it with <code>--env-file-if-exists</code>, so one file serves every way of starting the vertical. A shell variable still wins over it — but only in a terminal, which is exactly the gap that makes the file the reliable answer for Desktop.</li><li><strong>Claude curling its own API from Bash may fail.</strong> The sandboxed Bash tool still blocks outbound TCP to <code>localhost</code> (<a href="https://github.com/anthropics/claude-code/issues/28018" target="_blank" rel="noreferrer">claude-code#28018</a>). The Browser-pane path is separate and works; wiring up Bash-side <code>curl</code> is a deliberate <code>excludedCommands</code> entry, not something to discover mid-session.</li></ul><h2 id="two-audiences-one-directory" tabindex="-1">Two audiences, one directory <a class="header-anchor" href="#two-audiences-one-directory" aria-label="Permalink to &quot;Two audiences, one directory&quot;">​</a></h2><p>The console and the portal are not two views of the same app — they are two <strong>audiences</strong>:</p><ul><li>The <strong>console</strong> is the platform operator. It reaches every tenant, and its actions (suspend a tenant, grant an entitlement) are cross-tenant. This is the surface <a href="/concepts/platform.html">the platform layer</a> describes.</li><li>The <strong>portal</strong> is one tenant&#39;s user, confined to their scope by the identity they logged in with. Anna sees ElMontage; Mallory sees a different tenant entirely.</li></ul><p>From a scope&#39;s row in the console you can click <strong>Portal ↗</strong> to jump to that scope&#39;s app — the local stand-in for the production hostname router that maps a domain to <code>(tenant, scope, vertical)</code>.</p><h2 id="adding-tenants-and-scopes" tabindex="-1">Adding tenants and scopes <a class="header-anchor" href="#adding-tenants-and-scopes" aria-label="Permalink to &quot;Adding tenants and scopes&quot;">​</a></h2><p>Everything the console can do, it does against the running directory — so it is the fastest way to change the local world:</p><ul><li><strong>New tenant:</strong> the console&#39;s Tenants view has a <em>Create tenant</em> dialog. It mints a ULID and calls the same audited <code>createTenant</code> the platform uses.</li><li><strong>Grant/revoke entitlements, suspend, archive:</strong> all live in the console and take effect immediately, because they write the shared directory the portal reads.</li><li><strong>New scope:</strong> provisioning a scope is on the control-plane API (<code>POST /scopes</code>) but does not yet have a console button — the demo seeds its scopes in <code>demos/callout/src/seed.ts</code>. Add one there, or <code>curl</code> the API with a platform-actor header.</li></ul><h2 id="adding-another-application" tabindex="-1">Adding another application <a class="header-anchor" href="#adding-another-application" aria-label="Permalink to &quot;Adding another application&quot;">​</a></h2><p>A &quot;new application&quot; is a new <strong>vertical</strong>. Today each of the six demo verticals (<code>demos/{callout,handlebar,manyfold,meridian,rally,shop}</code>) ships its own dev server — Callout&#39;s is <code>demos/callout/src/server.ts</code> — and each composes its engines + module into a host. (A seventh directory, <code>demos/auth-server</code>, is a shared OIDC provider, not a business vertical.) To scaffold one, follow <a href="/guide/getting-started.html">Getting started</a> with the engines you need, and <a href="/guide/deploying.html">Deploying a vertical</a> when it&#39;s ready to ship.</p><p>What is <strong>not</strong> wired yet is running several verticals against <strong>one</strong> shared console locally — that needs each vertical to register into a <em>separate</em> control-plane process over HTTP, rather than co-locating the directory in its own host as the demo does for convenience. Until that seam exists, the local stack is one vertical + the console; the console&#39;s fleet view is designed for the many-vertical world it will grow into.</p><h2 id="the-faithful-topology-pnpm-dev-connected" tabindex="-1">The faithful topology: <code>pnpm dev:connected</code> <a class="header-anchor" href="#the-faithful-topology-pnpm-dev-connected" aria-label="Permalink to &quot;The faithful topology: \`pnpm dev:connected\`&quot;">​</a></h2><p><code>pnpm dev</code> co-locates the control plane and the vertical in one process for speed. To run the shape production actually uses — a <strong>separate</strong> control plane that the vertical <em>registers into</em> and is <em>gated by</em> — use:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev:connected</span></span></code></pre></div><p>This starts three things: a standalone control plane (its own process, on <code>:8788</code>), the Callout vertical in <strong>connected mode</strong>, and the console pointed at that control plane. On boot the vertical registers its tenants and scopes into the control plane over HTTP; before every request it asks the control plane &quot;is this scope still active?&quot; So when you suspend a scope in the console, the vertical&#39;s next action fails closed — the same outcome as the co-located stack, but now crossing a real process boundary, exactly as it would cross a deployment boundary in production.</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-277",
        class: "mermaid",
        graph: "flowchart%20LR%0A%20%20CO%5B%22Console%3Cbr%2F%3E%3A5272%22%5D%20--%3E%7Cwrites%7C%20CP%5B%22Control%20plane%3Cbr%2F%3E%3A8788%20%E2%80%94%20own%20process%22%5D%0A%20%20V%5B%22Callout%20vertical%3Cbr%2F%3E%3A8871%20%E2%80%94%20own%20process%22%5D%20--%3E%7C%22register%20%2B%20gate%20(HTTP)%22%7C%20CP%0A%20%20V%20--%3E%7Clocal%20execution%7C%20DB%5B(%22scope%20SQLite%22)%5D%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<p>The seam is <code>ControlPlaneClient</code> from <code>@substrat-run/control-plane-api</code>: <code>createTenant</code> / <code>provisionScope</code> / <code>grantEntitlement</code> to register, and <code>assertScopeActive</code> to gate. One deliberate limit — the control-plane HTTP surface exposes lifecycle and entitlements but <strong>not role or grant writes</strong> (those are the permission-diff human checkpoint), so a connected vertical keeps its permission model local while the shared plane is authoritative for tenant/scope lifecycle and entitlements.</p><p>Unlike the quick <code>pnpm dev</code> (which trusts a dev-actor header), the connected control plane runs <strong>real staff auth</strong> — the console shows a sign-in screen. Sign in with the seeded operator:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>markus@substrat.run / substrat123</span></span></code></pre></div><p>Auth is Better Auth behind a provider-agnostic seam (<code>sessionPlatformAuth</code> + a staff allowlist), so who authenticates staff can change without touching the console or the router. The vertical registering its scopes is a <em>service</em>, not staff — locally it uses the dev-actor header as a stand-in for a real service credential.</p><h2 id="how-production-differs" tabindex="-1">How production differs <a class="header-anchor" href="#how-production-differs" aria-label="Permalink to &quot;How production differs&quot;">​</a></h2><p>Co-location is a local convenience, not the topology; <code>pnpm dev:connected</code> above is the faithful shape on one machine. In production the control plane is its <strong>own deployment</strong> and each vertical is a <strong>separate deployment</strong>, all reaching one durable directory — the same surfaces you see here, split across processes and hosts. The SQLite adapter you run locally and the Cloudflare adapter you deploy on are the same kernel above <a href="/concepts/scope-host.html">the scope-host contract</a>; only the composition root changes.</p><p>Getting a vertical from this laptop to that production topology is its own step — the <code>substrat</code> CLI pushes a bundle, and an admission in the console lets a scope serve it. See <a href="/guide/deploying.html">Deploying a vertical</a>.</p><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><a href="/concepts/tenancy.html">Tenants &amp; scopes</a> — the tenancy tree the directory records.</li><li><a href="/concepts/platform.html">The platform layer</a> — what the console is a thin client over.</li><li><a href="/concepts/scope-host.html">Operations &amp; the scope host</a> — the seam that makes SQLite-local and Cloudflare-deployed the same code.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/running-locally.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const runningLocally = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  runningLocally as default
};
