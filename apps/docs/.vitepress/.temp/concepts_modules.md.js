import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Modules & the manifest","description":"Engines and verticals join a scope host the same way — as modules bundling a manifest, migrations, operations and consumers. Covers envSpec, schedules, and attachment contracts.","frontmatter":{"description":"Engines and verticals join a scope host the same way — as modules bundling a manifest, migrations, operations and consumers. Covers envSpec, schedules, and attachment contracts."},"headers":[],"relativePath":"concepts/modules.md","filePath":"concepts/modules.md","lastUpdated":1787230840000}');
const _sfc_main = { name: "concepts/modules.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="modules-the-manifest" tabindex="-1">Modules &amp; the manifest <a class="header-anchor" href="#modules-the-manifest" aria-label="Permalink to &quot;Modules &amp; the manifest&quot;">​</a></h1><p>Engines and verticals join a scope host the same way: as <strong>modules</strong>. A module is one registration object bundling a manifest, migrations, operations, and event consumers:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> type</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { ModuleRegistration } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/kernel&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> registration</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ModuleRegistration</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  manifest,      </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// self-describing metadata (validated Zod document)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  migrations,    </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// ordered SQL, journaled per module, applied lazily per scope</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  operations,    </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// &#39;workorder/create&#39; → handler</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  consumers,     </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// &#39;workorder.completed&#39; → handler</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">};</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">registerModule</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(registration);</span></span></code></pre></div><h2 id="the-manifest" tabindex="-1">The manifest <a class="header-anchor" href="#the-manifest" aria-label="Permalink to &quot;The manifest&quot;">​</a></h2><p>The manifest is what makes a module <strong>self-describing</strong> — to the kernel that loads it, to the app shell that renders it, and to the agents that build on it. It&#39;s a Zod-validated document (<code>moduleManifest</code> in <code>@substrat-run/contracts</code>):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> workorderManifest</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> moduleManifest.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">parse</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  id: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;@substrat-run/engine-workorder&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  version: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;0.0.1&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  kernelContract: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;^0.0.1&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,          </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// semver range of the kernel API it targets</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  permissions: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    { key: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder:create&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, description: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Create work orders&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // ...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  events: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    emits: [{ type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder.created&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, schemaVersion: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> } </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* ... */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    consumes: [],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  migrations: { journalDir: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;./migrations&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, compatibleFrom: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;0.0.1&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  attachmentTargets: [{ entityType: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, readPermission: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder:read&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  entityRelations: [{ entityType: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, parentType: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;facility&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  entitlementKey: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  envSpec: [                         </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// optional: config the deployment must provide</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    { key: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;WEBHOOK_SECRET&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, description: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Signing secret for inbound webhooks&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, secret: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ui: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* routes, nav, entityViews, widgets */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p>Field by field:</p><table tabindex="0"><thead><tr><th>Field</th><th>What it declares</th><th>Who consumes it</th></tr></thead><tbody><tr><td><code>id</code>, <code>version</code>, <code>kernelContract</code></td><td>identity + the kernel API range this module targets</td><td>host, tooling</td></tr><tr><td><code>permissions</code></td><td>every key the module checks, with a description</td><td>permission review (the human-readable diff), admin UI, agents</td></tr><tr><td><code>events.emits</code> / <code>events.consumes</code></td><td>the module&#39;s event contract, schema-versioned</td><td>host wiring, compatibility checks, agents</td></tr><tr><td><code>migrations</code></td><td>journal location and <code>compatibleFrom</code> — the oldest schema this code tolerates (the skew window)</td><td>migration runner</td></tr><tr><td><code>attachmentTargets</code></td><td>entity types that accept attachments (documents, comments), and which permission gates reading them</td><td>kernel attachment services</td></tr><tr><td><code>entityRelations</code></td><td>parent edges (<code>workorder → facility</code>) that permission flows along</td><td>the <a href="/concepts/permissions.html">permission evaluator</a></td></tr><tr><td><code>entitlementKey</code></td><td>the SKU flag that gates loading this module for a tenant</td><td>entitlements / billing</td></tr><tr><td><code>ownerGrants</code></td><td>permissions a fresh install&#39;s owner holds on day one (marketplace install); scope-provisioning verticals only</td><td>dashboard install, registry</td></tr><tr><td><code>entitlements</code></td><td>the full entitlement set an install grants (own + composed); absent ⇒ derive from <code>entitlementKey</code></td><td>entitlements, registry</td></tr><tr><td><code>provides</code> / <code>requires</code></td><td>named capabilities this vertical offers (<code>oidc-issuer</code>) or delegates to — wired tenant-side via the connection store</td><td>capability wiring, registry</td></tr><tr><td><code>envSpec</code></td><td>declared environment variables (label, description, placeholder, <code>required</code>, <code>secret</code>) a deployment must provide</td><td>host/console config forms — carried on the registry (see below)</td></tr><tr><td><code>guards</code></td><td>manifest-declared operation pre-conditions: a named predicate the kernel runs inside the operation&#39;s transaction, before the handler (a throw blocks it)</td><td>kernel</td></tr><tr><td><code>schedules</code></td><td>recurring work — operations the platform invokes on every live scope of this vertical, on a cadence, under a system actor (a date-triggered business rule)</td><td>the platform sweep (<a href="#recurring-work-schedules">recurring work</a>)</td></tr><tr><td><code>withdraws</code></td><td>operation names whose default binding this module suppresses — the name stops resolving, so a vertical can re-offer the transition behind its own guarded operation</td><td>kernel operation resolver</td></tr><tr><td><code>searchables</code></td><td>entity types and fields to index for search — the kernel derives a per-scope FTS5 index and the triggers that maintain it</td><td>kernel (<a href="/concepts/reads.html#finding-a-row-by-what-someone-typed">Reads &amp; scaling</a>)</td></tr><tr><td><code>api</code></td><td>path to the emitted OpenAPI for the module&#39;s HTTP surface, if any</td><td>tooling / SDK generation</td></tr><tr><td><code>ui</code></td><td>routes, nav items, entity views, widgets — permission-keyed, composed into the vertical&#39;s app at build time</td><td>app shell</td></tr><tr><td><code>ui.settingsPanels</code></td><td>permission-keyed settings screens the shell mounts for the app</td><td>app shell</td></tr></tbody></table><p>Every field past <code>entitlementKey</code> is <strong>optional and additive</strong> (decision 28): a manifest that omits them still parses, and adding one never breaks an existing module.</p><h2 id="two-manifests-authored-in-typescript-transported-as-json" tabindex="-1">Two manifests: authored in TypeScript, transported as JSON <a class="header-anchor" href="#two-manifests-authored-in-typescript-transported-as-json" aria-label="Permalink to &quot;Two manifests: authored in TypeScript, transported as JSON&quot;">​</a></h2><p>Worth being precise, because &quot;manifest&quot; names two things. The <strong>module manifest</strong> above is <strong>TypeScript</strong> — a typed object literal that <code>moduleManifest.parse(...)</code> validates. That <code>parse</code> is a correctness gate (a malformed manifest throws at load), <em>not</em> a sign you author JSON: you get full types, autocomplete, and refactoring across every field, and the keys in <code>permissions</code> are the same branded <code>PermissionKey</code>s your operations check.</p><p>JSON appears in exactly one place — the <strong>deploy manifest</strong>, the envelope <code>substrat push</code> assembles and POSTs to the control plane. It&#39;s JSON because it crosses a process and trust boundary (the builder&#39;s CLI → the platform), and the same <code>deployManifest</code> schema re-parses it on the far side. So the shape you <em>edit</em> is typed TypeScript; the &quot;one big JSON that gets parsed&quot; is only the wire form, and only at the boundary where a wire form is unavoidable.</p><p><strong>The permission surface travels in that envelope.</strong> The keys and descriptions, role templates, and entity-grant <em>shapes</em> a vertical declares are carried in the deploy manifest as a <code>registry</code>, content-hashed as <code>digests.permission</code> (D-39). It&#39;s derived from the same declared surface the host registers (<code>MODULES</code> + <code>ROLES</code> + <code>ENTITY_GRANTS</code>), so it cannot drift from what is enforced, and <code>PERMISSIONS.md</code> is the human-readable render of that same surface — the review artifact for the permission checkpoint. Admission consumes it as a real permission diff between two versions; any tenant-facing permissions view reads it back. The runtime grant table is deliberately <em>not</em> in it: minted capability grants are scope-local tuples, reached only through the admin-query RPC.</p><p>Two fields deserve special mention:</p><ul><li><strong><code>ui.entityViews</code></strong> is the cross-engine rendering mechanism: a module that stores an opaque <code>EntityRef</code> can render the entity&#39;s card by looking up the view registered for its <code>entityType</code> — no imports between engines.</li><li><strong><code>entitlementKey</code></strong> is how the module system turns commercial: enabling an engine for a tenant is flipping an entitlement, and the kernel refuses to load what isn&#39;t entitled.</li></ul><h2 id="declared-environment-envspec" tabindex="-1">Declared environment (<code>envSpec</code>) <a class="header-anchor" href="#declared-environment-envspec" aria-label="Permalink to &quot;Declared environment (\`envSpec\`)&quot;">​</a></h2><p>A vertical <strong>opts in</strong> to a configuration surface by declaring <code>envSpec</code> — the environment variables a deployment must provide, each self-describing:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">envSpec</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  { key: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;PUBLIC_ORIGIN&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, label: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Issuer origin&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, description: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;The public URL of this app.&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    placeholder: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;https://app.example.com&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, required: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, secret: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, group: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;General&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  { key: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;ADMIN_PASSWORD&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, label: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Admin password&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, description: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Bootstrap admin password.&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    placeholder: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;at least 8 characters&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, required: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, secret: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, group: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Bootstrap&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span></code></pre></div><p>It&#39;s optional and additive — a vertical that declares nothing has no config surface. <code>secret: true</code> marks a value that is <strong>write-only</strong> in any UI (masked, never echoed back) and delivered as a secret; <code>group</code> sections the form; <code>required</code> is validated before deploy.</p><p>The spec <strong>rides the registry</strong>: when a vertical is registered (<code>registerVertical</code>), its <code>envSpec</code> is stored alongside its slug. So a host or console can render a config form for <strong>any</strong> registered vertical — a bundled builtin or a pushed builder vertical — without loading its code. That is what makes opt-in a single edit in the manifest: declare <code>envSpec</code>, and the <a href="/platform/dashboard.html">Dashboard</a> shows a settings form for the app automatically. It evolves with the manifest — re-registering a vertical refreshes its spec.</p><p>For a <strong>pushed</strong> vertical, <code>substrat push</code> reads <code>envSpec</code> from the vertical&#39;s <code>package.json</code><code>substrat</code> block (the same static, code-free source it reads <code>slug</code>/<code>name</code> from) and carries it in the deploy manifest — the CLI never loads the built module, so the declaration must be readable as data at push time.</p><div class="tip custom-block"><p class="custom-block-title">Delivery depends on the app&#39;s shape</p><p>A <strong>standalone</strong> app (its own worker script) receives these as worker secrets/vars at deploy. A <strong>hosted</strong> vertical (one script serving many tenants&#39; scopes) can&#39;t use per-app worker secrets — all its scopes share one script — so it takes per-tenant values through the per-scope config it reads at runtime.</p></div><h2 id="recurring-work-schedules" tabindex="-1">Recurring work (<code>schedules</code>) <a class="header-anchor" href="#recurring-work-schedules" aria-label="Permalink to &quot;Recurring work (\`schedules\`)&quot;">​</a></h2><p>Some business rules have no caller but the passage of time: a contract that becomes active on its start date, a leave request that can no longer be approved once it has already begun. The operation is ordinary — idempotent, permission-checked — it just needs something to invoke it on a cadence. A vertical declares that in <code>schedules</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">schedules</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    operation: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;hr/expire-stale-requests&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// a registered module/verb operation</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    cadence: { everyMinutes: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1440</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },          </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// once a day</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    permissions: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;absence:approve&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],         </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// what the operation checks</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // input: { … }</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                           // optional static input, re-parsed by the op</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span></code></pre></div><p>The <a href="/concepts/platform.html#scheduled-work">platform sweep</a> enumerates every live scope of the vertical and invokes the operation on each due one. Two things make it safe to run unattended:</p><ul><li><strong>It is attributed to the schedule, never a person.</strong> The invocation runs under a <strong>system actor</strong> — the emitted events read as <code>{ system: &#39;@your/module&#39; }</code>, not as whichever admin happened to be the last human to touch the data. Modelling a nightly job as a signed-in user is exactly the audit-trail laundering this exists to avoid.</li><li><strong><code>ctx.check</code> is still the only gate.</strong> The <code>permissions</code> a schedule declares are granted to the module&#39;s system principal when a scope is provisioned, so the operation&#39;s own <code>assertAllowed(await ctx.check(…))</code> resolves the same way it does for any caller — a schedule can do exactly what it declares and no more. Those permissions appear in the vertical&#39;s <a href="/concepts/permissions.html#from-declaration-to-enforcement"><code>PERMISSIONS.md</code></a> under a <em>Scheduled work</em> section, so widening what a schedule may do lands in the reviewed diff. Revoking that grant for one tenant turns the schedule off for them — no special &quot;disabled&quot; flag, the operation just fails its own check closed.</li></ul><p><code>cadence</code> is a floor, not a guarantee of exact timing: a schedule fires no more often than <code>everyMinutes</code>, and the sweep is what actually runs it (typically every couple of minutes), so sub-sweep cadences round up. It is optional and additive like every field past <code>entitlementKey</code> — a vertical that declares none has no recurring work.</p><div class="tip custom-block"><p class="custom-block-title">Where it runs</p><p>The sweep must run in the vertical&#39;s <strong>own</strong> runtime, where its modules and scope data live — a node server calls <code>startPlatformSweeper</code> at boot, a Cloudflare deployment arms a <code>PlatformSweeperDO</code> alarm. See <a href="/concepts/platform.html#scheduled-work">the platform sweep</a>.</p></div><h2 id="migrations" tabindex="-1">Migrations <a class="header-anchor" href="#migrations" aria-label="Permalink to &quot;Migrations&quot;">​</a></h2><p>Migrations are plain SQL, ordered and uniquely versioned per module:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">migrations</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  { version: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;0001-init&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, sql: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">\`CREATE TABLE workorder_orders ( ... );\`</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span></code></pre></div><p>Semantics:</p><ul><li><strong>Applied lazily per scope</strong>, inside the scope&#39;s serialization domain, journaled in <code>_substrat_migrations</code> per (module, version). No global migration step; a scope migrates when it wakes.</li><li><strong>Skew is a normal state.</strong> With thousands of scopes, some will run the old schema for a window. <code>compatibleFrom</code> declares the oldest schema version the module&#39;s code tolerates; a reconciliation sweep wakes stragglers before the window closes.</li><li><strong>Migrations are a human checkpoint.</strong> They&#39;re deliberately plain SQL so review is review, not archaeology.</li></ul><h2 id="operations-consumers-and-in-scope-functions" tabindex="-1">Operations, consumers, and in-scope functions <a class="header-anchor" href="#operations-consumers-and-in-scope-functions" aria-label="Permalink to &quot;Operations, consumers, and in-scope functions&quot;">​</a></h2><ul><li><strong><code>operations</code></strong> — the module&#39;s invokable surface, namespaced (<code>&#39;workorder/create&#39;</code>). Each default binding starts with its own permission check.</li><li><strong><code>consumers</code></strong> — event handlers keyed by event type; the types must appear in <code>manifest.events.consumes</code>. Idempotency required (at-least-once delivery).</li><li><strong>In-scope functions</strong> — plain exports (not registered anywhere) that a vertical&#39;s own operations can call to compose engine behavior in the same transaction. See <a href="/concepts/scope-host.html#in-scope-functions-vs-registered-operations">Operations &amp; the scope host</a>.</li></ul><h2 id="attachment-contracts-and-opaque-refs" tabindex="-1">Attachment contracts and opaque refs <a class="header-anchor" href="#attachment-contracts-and-opaque-refs" aria-label="Permalink to &quot;Attachment contracts and opaque refs&quot;">​</a></h2><p>The kernel owns no entities, so everything generic binds to an opaque reference:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> EntityRef</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">entityType</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">entityId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> };</span></span></code></pre></div><p>Attachment contracts (documents, comments, activity, custom fields) attach to an <code>EntityRef</code>; <code>attachmentTargets</code> declares which types accept them and which permission gates access; <code>visibility</code> (<code>&#39;internal&#39; | &#39;customer&#39;</code>) classifies every attachment item so customer-portal exposure is a total, mandatory decision — like <code>piiClass</code>, a classification only works if it was never optional.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("concepts/modules.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const modules = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  modules as default
};
