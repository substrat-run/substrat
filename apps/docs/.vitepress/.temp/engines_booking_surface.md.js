import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Operations, functions & permissions","description":"","frontmatter":{},"headers":[],"relativePath":"engines/booking/surface.md","filePath":"engines/booking/surface.md","lastUpdated":1784804439000}');
const _sfc_main = { name: "engines/booking/surface.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="operations-functions-permissions" tabindex="-1">Operations, functions &amp; permissions <a class="header-anchor" href="#operations-functions-permissions" aria-label="Permalink to &quot;Operations, functions &amp; permissions&quot;">​</a></h1><h2 id="operations" tabindex="-1">Operations <a class="header-anchor" href="#operations" aria-label="Permalink to &quot;Operations&quot;">​</a></h2><p>Registered bindings, each one a permission check plus a call into the in-scope function below.</p><table tabindex="0"><thead><tr><th>Operation</th><th>Permission</th><th>Does</th></tr></thead><tbody><tr><td><code>booking/create-resource</code></td><td><code>booking:manage-resources</code></td><td>add a bookable resource</td></tr><tr><td><code>booking/set-resource-active</code></td><td><code>booking:manage-resources</code></td><td>take one out of service</td></tr><tr><td><code>booking/list-resources</code></td><td><code>booking:read</code></td><td>list resources</td></tr><tr><td><code>booking/hold</code></td><td><code>booking:hold</code></td><td>tentative hold; throws <code>SlotUnavailable</code></td></tr><tr><td><code>booking/confirm</code></td><td><code>booking:confirm</code> <em>(per reservation)</em></td><td>held → confirmed, re-checking capacity</td></tr><tr><td><code>booking/expire</code></td><td><code>booking:confirm</code></td><td>surface a lapsed hold as <code>expired</code></td></tr><tr><td><code>booking/join</code></td><td><code>booking:create</code> <em>(per reservation)</em></td><td>add a participant; auto-confirms at <code>fillTarget</code></td></tr><tr><td><code>booking/leave</code></td><td><code>booking:cancel</code> <em>(per reservation)</em></td><td>soft-leave</td></tr><tr><td><code>booking/cancel</code></td><td><code>booking:cancel</code> <em>(per reservation)</em></td><td>held/confirmed → cancelled</td></tr><tr><td><code>booking/move</code></td><td><code>booking:move</code> <em>(per reservation)</em></td><td>reschedule; throws <code>SlotUnavailable</code></td></tr><tr><td><code>booking/open</code></td><td><code>booking:confirm</code> <em>(per reservation)</em></td><td>put places on offer on an existing reservation, or close it again</td></tr><tr><td><code>booking/start</code> · <code>complete</code> · <code>no-show</code></td><td><code>booking:complete</code></td><td>service transitions</td></tr><tr><td><code>booking/get</code> · <code>list</code> · <code>availability</code></td><td><code>booking:read</code></td><td>reads</td></tr></tbody></table><p>Checks marked <em>(per reservation)</em> pass an <code>EntityRef</code>, so a consumer holding an entity-narrowed grant reaches their own booking and no one else&#39;s.</p><h2 id="in-scope-functions" tabindex="-1">In-scope functions <a class="header-anchor" href="#in-scope-functions" aria-label="Permalink to &quot;In-scope functions&quot;">​</a></h2><p>The composable surface. A vertical calls these <strong>inside its own operation and its own permission check</strong>, in one transaction — this is how you extend the engine without forking it.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">createResource</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { kind, name, capacity? })            → Resource</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">setResourceActive</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { resourceId, active })            → Resource</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">listResources</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)                                 → Resource[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">holdReservation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { resourceId, startsAt, endsAt,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">                       expiresAt, quantity?, fillTarget? }) → Reservation  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// throws SlotUnavailable</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">confirmReservation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { reservationId })                → Reservation</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">expireReservation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { reservationId })                 → Reservation</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">joinReservation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { reservationId, partyRef, share? }) → { participant, reservation }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">leaveReservation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { reservationId, participantId })   → Reservation</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">moveReservation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { reservationId, resourceId?,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">                       startsAt?, endsAt? })              → Reservation  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// throws SlotUnavailable</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">openReservation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { reservationId, fillTarget })       → Reservation  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// null closes it again</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">cancelReservation · startReservation · completeReservation · markNoShow</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">getReservation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, reservationId, now</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)  → { reservation, participants }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">listReservations</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { resourceId?, from?, to? }) → Reservation[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">availability</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, { resourceId, from, to })        → FreeInterval[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">effectiveStateOf</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(state, expiresAt, now)            → ReservationState</span></span></code></pre></div><h3 id="move-not-update" tabindex="-1"><code>move</code>, not <code>update</code> <a class="header-anchor" href="#move-not-update" aria-label="Permalink to &quot;\`move\`, not \`update\`&quot;">​</a></h3><p><code>moveReservation</code> changes resource and/or interval, re-runs the allocation check <strong>excluding itself</strong> (so nudging a booking that overlaps its own old slot is legal), and keeps the reservation&#39;s identity, roster and payments. Passing <code>startsAt</code> alone <em>shifts</em> it, preserving duration — what dragging a calendar cell means.</p><p>It is explicitly not cancel-then-rebook, which would lose all three. And there is no generic <code>updateReservation</code>: engines model named transitions, participants are an append-only log rather than a patchable field, and <code>booking.moved</code> carrying from/to is worth more to a consumer than a permanent diff blob.</p><h3 id="open-—-a-reservation-can-be-put-on-offer-after-the-fact" tabindex="-1"><code>open</code> — a reservation can be put on offer after the fact <a class="header-anchor" href="#open-—-a-reservation-can-be-put-on-offer-after-the-fact" aria-label="Permalink to &quot;\`open\` — a reservation can be put on offer after the fact&quot;">​</a></h3><p><code>fillTarget</code> is engine state: it drives the auto-confirm in <code>joinReservation</code>. So a booking cannot be opened to others by a vertical keeping its own counter beside the engine&#39;s and hoping the two stay in step — <code>openReservation</code> sets it, and <code>null</code> closes it again into a private booking.</p><p>Additive: reservations made without a target are unaffected. A target below the people already on the reservation is refused rather than silently stranding one of them.</p><p>This is what lets the three shapes of &quot;open game&quot; share one mechanism: a club opening a court with nobody on it, a player opening one they are on, and a player opening a booking they already hold.</p><h3 id="availability-returns-intervals-not-slots" tabindex="-1"><code>availability</code> returns intervals, not slots <a class="header-anchor" href="#availability-returns-intervals-not-slots" aria-label="Permalink to &quot;\`availability\` returns intervals, not slots&quot;">​</a></h3><p><code>FreeInterval[]</code> — <code>{ startsAt, endsAt, available }</code>, merged, with <code>available</code> a <strong>number</strong> because capacity may exceed 1. It reports raw gaps between reservations and knows nothing of opening hours: left alone it will happily call 03:00 free. Intersecting with the venue&#39;s bookable window is the vertical&#39;s job.</p><p>This shape is deliberate. With mixed durations there is no canonical slot list — &quot;does 90 fit at 19:00? does 120?&quot; is a question about gaps, and a fixed slot list cannot answer it.</p><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Key</th><th>Grants</th></tr></thead><tbody><tr><td><code>booking:create</code></td><td>add participants to a reservation</td></tr><tr><td><code>booking:read</code></td><td>resources, reservations, availability</td></tr><tr><td><code>booking:hold</code></td><td>place a tentative hold</td></tr><tr><td><code>booking:confirm</code></td><td>confirm a held reservation</td></tr><tr><td><code>booking:cancel</code></td><td>cancel a reservation, or leave one</td></tr><tr><td><code>booking:move</code></td><td>reschedule to another slot or resource</td></tr><tr><td><code>booking:complete</code></td><td>start service, complete, or mark a no-show</td></tr><tr><td><code>booking:manage-resources</code></td><td>create, edit and deactivate resources</td></tr></tbody></table><p><code>booking:move</code> is separate from <code>booking:cancel</code> on purpose: staff who may reschedule a customer are not necessarily staff who may cancel and refund one. <code>booking/open</code> is guarded by <code>booking:confirm</code> rather than a key of its own: whoever may confirm a reservation may decide whether it is on offer.</p><h2 id="entitlement" tabindex="-1">Entitlement <a class="header-anchor" href="#entitlement" aria-label="Permalink to &quot;Entitlement&quot;">​</a></h2><p><code>entitlementKey: &#39;booking&#39;</code>. Default-deny — grant it to a tenant before its operations resolve.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/booking/surface.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const surface = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  surface as default
};
