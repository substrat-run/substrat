import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Operations, functions & permissions","description":"","frontmatter":{},"headers":[],"relativePath":"engines/invoicing/surface.md","filePath":"engines/invoicing/surface.md","lastUpdated":1784462441000}');
const _sfc_main = { name: "engines/invoicing/surface.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="operations-functions-permissions" tabindex="-1">Operations, functions &amp; permissions <a class="header-anchor" href="#operations-functions-permissions" aria-label="Permalink to &quot;Operations, functions &amp; permissions&quot;">​</a></h1><p>An engine has <strong>no endpoints</strong>. It exposes operations (invoked through a scope stub) and in-scope functions (called by a vertical inside its own transaction). HTTP, where it exists, is a generated artifact pointed at by the manifest&#39;s <code>api</code> field.</p><p>Most of this engine&#39;s surface, though, isn&#39;t either one — it&#39;s the <strong>consumers</strong>. Lines arrive by event, not by call. See <a href="./events.html">Events</a>.</p><h2 id="operations" tabindex="-1">Operations <a class="header-anchor" href="#operations" aria-label="Permalink to &quot;Operations&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Operation</th><th>Permission</th><th>Does</th></tr></thead><tbody><tr><td><code>invoicing/list</code></td><td><code>invoicing:read</code></td><td>list underlag (optionally by status), each with its computed total</td></tr><tr><td><code>invoicing/get</code></td><td><code>invoicing:read</code></td><td>one underlag with all lines and total</td></tr><tr><td><code>invoicing/export</code></td><td><code>invoicing:export</code></td><td>flip to <code>exported</code> — the point of no return</td></tr></tbody></table><p>There is no <code>create</code> and no <code>add-line</code>: an underlag is never authored, only accumulated. The only way to put a line on one is to emit an event this engine consumes. That is the design — a basis nobody can hand-write is a basis nobody can forge.</p><h2 id="in-scope-functions" tabindex="-1">In-scope functions <a class="header-anchor" href="#in-scope-functions" aria-label="Permalink to &quot;In-scope functions&quot;">​</a></h2><p><strong>This engine exports none.</strong> All three operations carry their logic inline.</p><div class="warning custom-block"><p class="custom-block-title">No composable surface — an artifact, not a decision</p><p>The convention is that engine operations are thin (a permission check plus one exported in-scope function) so verticals extend by composition rather than forking. This engine doesn&#39;t meet it: <code>list</code>, <code>get</code>, and <code>export</code> have no exported function behind them.</p><p>The practical consequence: a vertical <strong>cannot</strong> export an underlag and touch its own tables in the same transaction, and cannot wrap export in its own vocabulary without re-implementing it. Compare the <a href="/engines/workorder/surface.html#in-scope-functions">work-order engine</a>, which exports <code>createWorkOrder</code>, <code>completeWorkOrder</code>, and friends.</p><p>Unlike the work-order engine&#39;s deliberately missing <code>create</code>, there is no design reason for this gap. Extracting <code>exportUnderlag(ctx, …)</code> would be a purely additive change.</p></div><p>What <em>is</em> exported today: <code>INVOICING_PERM</code>, <code>invoicingManifest</code>, <code>invoicingMigrations</code>, the <code>UnderlagRow</code> / <code>UnderlagLine</code> row types, and <code>invoicingModule</code>.</p><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Key</th><th>Description</th></tr></thead><tbody><tr><td><code>invoicing:read</code></td><td>Read invoice basis</td></tr><tr><td><code>invoicing:export</code></td><td>Export an invoice basis (makes it immutable)</td></tr></tbody></table><p>Two keys, and the split is the point: reading a basis is routine, and <strong>export is irreversible</strong>. Keep <code>invoicing:export</code> on a back-office role, not on whoever can see totals.</p><h2 id="entitlement" tabindex="-1">Entitlement <a class="header-anchor" href="#entitlement" aria-label="Permalink to &quot;Entitlement&quot;">​</a></h2><p><code>entitlementKey: &#39;invoicing&#39;</code>. This engine is priceable independently of any other — a tenant can hold <code>workorder</code> without <code>invoicing</code>. The gate is checked per invoke and fails closed.</p><div class="warning custom-block"><p class="custom-block-title">The entitlement gate is on operations, not consumers</p><p><code>dispatch</code> iterates every registered module&#39;s consumers with <strong>no entitlement check</strong> — only <code>invoke</code> consults <code>operationEntitlement</code>. So if the invoicing module is registered on the host, an unentitled tenant&#39;s <code>workorder.completed</code> events still build underlag rows in their scope; they simply can&#39;t <code>list</code>, <code>get</code>, or <code>export</code> them.</p><p>&quot;Not entitled&quot; therefore means <em>invisible</em>, not <em>inert</em> — the accumulation happens either way. Grant the entitlement later and the history is already there, which is convenient, but it is not what &quot;a module loads for a tenant only if the tenant holds its SKU flag&quot; implies. Worth knowing before you price this engine as off.</p></div></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/invoicing/surface.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const surface = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  surface as default
};
