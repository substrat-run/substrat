import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"guide/architecture.md","filePath":"guide/architecture.md","lastUpdated":1787416740000}');
const _sfc_main = { name: "guide/architecture.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_LayerStack = resolveComponent("LayerStack");
  const _component_ScopeTopology = resolveComponent("ScopeTopology");
  const _component_RuntimeTopology = resolveComponent("RuntimeTopology");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h1><p>Substrat is one decomposition seen two ways: a stack of <strong>layers</strong> (what the code <em>is</em>) and a request path across <strong>isolated databases</strong> (how it <em>runs</em>). Both pictures are below.</p><h2 id="the-three-layers" tabindex="-1">The three layers <a class="header-anchor" href="#the-three-layers" aria-label="Permalink to &quot;The three layers&quot;">​</a></h2><p>Everything hangs off one split — humans and hard guarantees below the line, AI velocity above it. Every band is a real, shipping package.</p>`);
  _push(ssrRenderComponent(_component_LayerStack, null, null, _parent));
  _push(`<h2 id="topology" tabindex="-1">Topology <a class="header-anchor" href="#topology" aria-label="Permalink to &quot;Topology&quot;">​</a></h2>`);
  _push(ssrRenderComponent(_component_ScopeTopology, null, null, _parent));
  _push(`<p>The invariants this picture encodes:</p><ul><li>The <strong>only</strong> data path from vertical code to operational data is the scope stub. Holding a stub <em>is</em> the authorization to talk to that scope — and the scope still re-validates every call against its own ACL.</li><li><strong>Events are emitted below the API surface.</strong> Vertical code cannot emit on another scope&#39;s behalf, suppress an audit event, or edit its envelope.</li><li><strong>Ambient tenancy.</strong> After obtaining a stub, vertical code never passes tenant or scope IDs again — context rides inside the stub and the operation context. There is no ID parameter to get wrong.</li></ul><h2 id="the-scope-unit-of-isolation-and-consistency" tabindex="-1">The scope: unit of isolation and consistency <a class="header-anchor" href="#the-scope-unit-of-isolation-and-consistency" aria-label="Permalink to &quot;The scope: unit of isolation and consistency&quot;">​</a></h2><p>A <strong>scope</strong> is one isolation domain — a housing association, a branch office, a client company, a brand. Each scope has:</p><ul><li><strong>its own database</strong> (one SQLite file locally; a SQLite-backed Durable Object on the Cloudflare adapter),</li><li><strong>strict serialization</strong> — one operation at a time, run to completion. No interleaved read-modify-writes, no lost updates, no need for row locking in module code,</li><li><strong>a structured-clone boundary</strong> — inputs and results are cloned even in-process, so code can never share mutable state with a scope.</li></ul><p>Operations run <em>inside</em> the scope&#39;s execution domain: one hop to reach the scope, then local, synchronous SQL. This is what makes engine invariants enforceable — the handler sees <code>sql</code>, <code>emit</code>, and <code>check</code>; the caller sees only <code>invoke()</code>.</p><p>See <a href="/concepts/tenancy.html">Tenants &amp; scopes</a> and <a href="/concepts/scope-host.html">Operations &amp; the scope host</a>.</p><h2 id="contracts-first-adapters-below" tabindex="-1">Contracts first, adapters below <a class="header-anchor" href="#contracts-first-adapters-below" aria-label="Permalink to &quot;Contracts first, adapters below&quot;">​</a></h2><p>Every boundary-crossing data shape is a <a href="https://zod.dev" target="_blank" rel="noreferrer">Zod</a> schema in <a href="/reference/contracts.html"><code>@substrat-run/contracts</code></a> — the reviewed artifact <em>is</em> the runtime validator (&quot;parse, don&#39;t trust&quot;). The kernel&#39;s behavioral seams are pure TypeScript interfaces in <a href="/reference/kernel.html"><code>@substrat-run/kernel</code></a> that import no platform APIs.</p><p>Platform specifics live only in <strong>adapters</strong>:</p><table tabindex="0"><thead><tr><th>Adapter</th><th>Backing</th><th>Use</th></tr></thead><tbody><tr><td><a href="/reference/adapter-sqlite.html"><code>@substrat-run/adapter-sqlite</code></a></td><td>one SQLite file per scope, per-scope actor</td><td>local dev, CI, self-host</td></tr><tr><td><a href="/reference/adapter-cloudflare.html"><code>@substrat-run/adapter-cloudflare</code></a></td><td>SQLite-backed Durable Object per scope + a durable control-plane DO</td><td>production</td></tr></tbody></table><p>The rule is testable and non-negotiable: <strong>a module&#39;s contract tests must pass unchanged on both adapters</strong> — and they do: the shared <a href="/reference/contract-tests.html">conformance suite</a> runs green on the pure-SQLite adapter in Node <em>and</em> on the Cloudflare adapter in real <code>workerd</code> against Durable Objects. Neither is a mock; both implement the same semantics (serialization, clone boundary, fail-closed addressing, stamped envelopes). This is what makes local development deterministic, CI cloud-free, and the self-host/escrow story literally true — and it&#39;s how a vertical moves from laptop to Cloudflare with no code change.</p><h2 id="the-hosted-runtime" tabindex="-1">The hosted runtime <a class="header-anchor" href="#the-hosted-runtime" aria-label="Permalink to &quot;The hosted runtime&quot;">​</a></h2><p>The topology above is adapter-neutral. Here is what it becomes on the <strong>Cloudflare adapter</strong> — the production shape. The one thing to hold onto: <strong>every box is a Durable Object with its own SQLite.</strong> There is no shared cluster; a tenant&#39;s data sits in its own isolated database, and the router&#39;s only job is to find the right door.</p>`);
  _push(ssrRenderComponent(_component_RuntimeTopology, null, null, _parent));
  _push(`<h2 id="modules-how-everything-joins" tabindex="-1">Modules: how everything joins <a class="header-anchor" href="#modules-how-everything-joins" aria-label="Permalink to &quot;Modules: how everything joins&quot;">​</a></h2><p>Engines and verticals join a host the same way — as <strong>modules</strong>. A module registration bundles:</p><ul><li>a <strong>manifest</strong> — self-describing metadata: permissions (with human-readable descriptions), events emitted and consumed, migrations, attachment targets, entity relations, an entitlement key, and optional UI contributions;</li><li><strong>migrations</strong> — plain SQL, journaled per module, applied lazily per scope inside the scope&#39;s serialization domain;</li><li><strong>operations</strong> — named handlers (<code>&#39;workorder/create&#39;</code>) invoked through scope stubs;</li><li><strong>event consumers</strong> — handlers for event types other modules emit;</li><li><strong>schedules</strong> — recurring work, fired by the deployment&#39;s own sweeper under a system actor rather than by a cron the module holds.</li></ul><p>A vertical increasingly does not <em>write</em> most of that. It declares its entities, operations and permissions once in <a href="/concepts/model.html">the model</a>, and the manifest&#39;s entity fragments, permission list, event list and DDL are <strong>derived</strong> from that declaration — with CI failing on drift between the declaration and what is checked in.</p><p>See <a href="/concepts/modules.html">Modules &amp; the manifest</a>.</p><h2 id="composition-star-topology" tabindex="-1">Composition: star topology <a class="header-anchor" href="#composition-star-topology" aria-label="Permalink to &quot;Composition: star topology&quot;">​</a></h2><p>Engines talk to the kernel, <strong>never to each other</strong>. No engine imports or calls a sibling. Composition happens through three kernel-mediated channels:</p><ol><li><strong>Opaque refs</strong> — attachment contracts bind to <code>(entityType, entityId)</code> without knowing what the entity is.</li><li><strong>Events</strong> — an engine reacts to another&#39;s schema-versioned events. A contract, not a call: the <a href="/engines/invoicing/">invoicing engine</a> consumes <code>workorder.completed</code> <em>and</em><code>commerce.order-placed</code> — events from two different domains — without importing a single type from either producer.</li><li><strong>Vertical-owned orchestration</strong> — synchronous flows that need two engines are wired in the vertical, where the glue is visible and editable.</li></ol><p>This keeps compatibility at <em>N</em> kernel contracts instead of <em>N²</em> engine pairs, and keeps each engine independently versioned. The corollary test: <em>if two engines need chatty synchronous communication, they are one engine drawn wrong</em> — which is why &quot;work orders + time reporting&quot; is one engine, not two.</p><h2 id="language-typescript-end-to-end" tabindex="-1">Language: TypeScript end-to-end <a class="header-anchor" href="#language-typescript-end-to-end" aria-label="Permalink to &quot;Language: TypeScript end-to-end&quot;">​</a></h2><p>Verticals are TypeScript regardless of what the kernel is written in — React UIs, prompt-to-app tools, and coding agents all emit it. Keeping the kernel in TypeScript means one source of truth at the most important interface in the system: &quot;invalid states unrepresentable&quot; materializes directly at the SDK boundary (branded ID types, discriminated unions, literal types) rather than through generated bindings.</p><p>Types erase at runtime, so they are never the enforcement: every trust boundary validates at runtime with the same Zod schemas, and the guarantees that matter are structural — the scope boundary, capability stubs, kernel-side stamping — not type-level. Types are ergonomics, especially for agents; the enforcement doesn&#39;t depend on the compiler.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/architecture.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const architecture = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  architecture as default
};
