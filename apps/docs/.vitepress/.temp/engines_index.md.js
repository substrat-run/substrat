import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"What is an engine?","description":"","frontmatter":{},"headers":[],"relativePath":"engines/index.md","filePath":"engines/index.md","lastUpdated":1786737227000}');
const _sfc_main = { name: "engines/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="what-is-an-engine" tabindex="-1">What is an engine? <a class="header-anchor" href="#what-is-an-engine" aria-label="Permalink to &quot;What is an engine?&quot;">​</a></h1><p>An <strong>engine</strong> is domain machinery shared across verticals but too domain-shaped for the kernel: work orders, invoicing, scheduling, ticketing, protocols/checklists. Engines are headless, versioned npm packages that register into a scope host as <a href="/concepts/modules.html">modules</a> — no UI framework, no HTTP server, no storage of their own beyond the tables their migrations create inside each scope.</p><h2 id="the-division-of-labor" tabindex="-1">The division of labor <a class="header-anchor" href="#the-division-of-labor" aria-label="Permalink to &quot;The division of labor&quot;">​</a></h2><p><strong>Engines own invariants.</strong> State machines can&#39;t skip states. Time entries are append-only. An exported invoice basis is immutable. Every mutation emits an event. Every access passes a permission check.</p><p><strong>Verticals own everything with a user&#39;s fingerprints on it.</strong> Vocabulary, extra states and fields, triggers, pricing logic, screens, reports, industry content.</p><p>The design test for the boundary: <strong>if a vertical ever needs to fork an engine, the engine drew its line wrong.</strong> A concrete example from the work-order engine: it has no price list and no pricing logic — pricing differs per business, so the <em>vertical</em> computes billable lines and hands them to <code>complete</code>. The engine enforces what must always hold (status transitions, append-only reporting, event emission), not what any particular business decides.</p><h2 id="star-topology-engines-never-talk-to-each-other" tabindex="-1">Star topology: engines never talk to each other <a class="header-anchor" href="#star-topology-engines-never-talk-to-each-other" aria-label="Permalink to &quot;Star topology: engines never talk to each other&quot;">​</a></h2><p>No engine imports or calls a sibling — enforced by review and by design. Engines compose through three kernel-mediated channels:</p><ol><li><strong>Opaque refs</strong> — an engine stores <code>(entityType, entityId)</code> without knowing what it is. The work-order engine binds orders to a <code>facility</code> ref it never dereferences.</li><li><strong>Events</strong> — schema-versioned contracts on the spine. The invoicing engine consumes <code>workorder.completed</code> with <strong>zero imports</strong> from the work-order engine; it parses its own Zod view of the payload.</li><li><strong>Vertical-owned orchestration</strong> — synchronous flows needing two engines are wired in the vertical, where the glue is visible and editable.</li></ol><p>Why: with <em>N</em> engines talking to the kernel there are <em>N</em> contracts to keep compatible; with engines talking to each other there are <em>N²</em>. Star topology keeps every engine independently versionable, licensable, and replaceable.</p><p>The corollary: <strong>if two engines need chatty synchronous communication, they are one engine drawn wrong.</strong> That&#39;s why &quot;work orders + time + material&quot; is one engine, not three — time entries have no meaning outside their work order.</p><h2 id="what-an-engine-is-not" tabindex="-1">What an engine is <em>not</em> <a class="header-anchor" href="#what-an-engine-is-not" aria-label="Permalink to &quot;What an engine is *not*&quot;">​</a></h2><p>&quot;Engine&quot; is a borrowed word, and every reader arrives with a different picture of it. Four contrasts to clear it up:</p><ul><li><strong>Not an Odoo app</strong> (or any &quot;app on a platform&quot;). An Odoo app bundles the whole vertical slice — ORM models, logic, views, vocabulary — and extends its siblings by inheritance (<code>_inherit</code>, cross-app foreign keys). An engine is the inverse: headless, no UI, no vocabulary, owning only invariants, and forbidden from importing a sibling. What Odoo puts in one app, Substrat splits across an engine (the invariants) and a vertical (everything with a user&#39;s fingerprints on it). If you ever want to <em>subclass</em> an engine, it drew its line wrong.</li><li><strong>Its real cousin is a <a href="https://docs.medusajs.com/learn/fundamentals/modules/isolation" target="_blank" rel="noreferrer">Medusa v2</a> module</strong> — and that&#39;s no coincidence. Strict isolation, cross-module links instead of foreign keys, per-module migrations: Medusa converged on the same shape from e-commerce. Know Medusa modules and you already know engines — Substrat wraps them in nested multi-tenancy and runtime enforcement.</li><li><strong>Not a Rails engine or a plugin.</strong> Those are code-organization conventions; nothing at runtime stops a plugin from reaching into the host&#39;s tables. An engine&#39;s boundaries are <em>enforced</em> — another module&#39;s tables are private, reachable only through its exported functions and events, and the <code>boundary-lint</code> check fails the build when you cross the line.</li><li><strong>Not a microservice.</strong> No network hop, no separate deployment, no eventual consistency between engine and vertical. An engine runs in-process inside the scope, and a vertical composes its in-scope functions in the <em>same transaction</em> — <code>createWorkOrder(ctx, …)</code> and your own table write commit together or not at all. The isolation is architectural, not physical.</li><li><strong>Not a <a href="/connectors/">connector</a>.</strong> An engine owns invariants and domain state <em>inside</em> a scope; a connector reaches a third-party service <em>outside</em> one. An engine never calls the network — that is precisely a connector&#39;s job. The two are different layers, and the clearest tell is that an engine has tables and permissions while a connector has neither.</li></ul><h2 id="anatomy-of-an-engine-package" tabindex="-1">Anatomy of an engine package <a class="header-anchor" href="#anatomy-of-an-engine-package" aria-label="Permalink to &quot;Anatomy of an engine package&quot;">​</a></h2><p>Every engine exports the same shape:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> PERM</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* parsed permission keys */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> };</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> engineManifest</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> moduleManifest.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">parse</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({ </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* self-description */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> });</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> engineMigrations</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [ </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* ordered SQL */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ];</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// In-scope functions — composable from vertical operations, same transaction.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> createWorkOrder</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">input</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) { </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* ... */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// The full registration: manifest + migrations + default operation bindings</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> engineModule</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ModuleRegistration</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* ... */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> };</span></span></code></pre></div><p>Using an engine as-is is one line:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">registerModule</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(workorderModule);</span></span></code></pre></div><p>Wrapping it with vertical logic means writing your own operation and calling the engine&#39;s exported in-scope functions — same transaction, same serialization domain, and the permission check becomes your responsibility:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">defineOperation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;acme/create-order&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">input</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  assertAllowed</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">check</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">PERM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.create));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> order</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> createWorkOrder</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(ctx, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mapToEngineInput</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(input));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  // your own tables, in the same transaction</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> order;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><h2 id="engines-today" tabindex="-1">Engines today <a class="header-anchor" href="#engines-today" aria-label="Permalink to &quot;Engines today&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Engine</th><th>Package</th><th>What it owns</th></tr></thead><tbody><tr><td><a href="/engines/workorder/">Work orders</a></td><td><code>@substrat-run/engine-workorder</code></td><td>the order state machine, append-only time &amp; material reporting</td></tr><tr><td><a href="/engines/booking/">Bookings</a></td><td><code>@substrat-run/engine-booking</code></td><td>allocation against capacity over an interval — no double-booking, holds and their expiry</td></tr><tr><td><a href="/engines/invoicing/">Invoicing</a></td><td><code>@substrat-run/engine-invoicing</code></td><td>invoice-basis accumulation from billable events, immutability on export</td></tr><tr><td><a href="/engines/protocol/">Protocols</a></td><td><code>@substrat-run/engine-protocol</code></td><td>protocols/checklists with the sign → immutable invariant, verifiable content hash</td></tr><tr><td><a href="/engines/invites/">Invites</a></td><td><code>@substrat-run/engine-invites</code></td><td>single-use invitations to join an org with a role — invited → accepted; the plaintext identifier is never stored, the per-scope salted hash is</td></tr><tr><td><a href="/engines/absence/">Absence</a></td><td><code>@substrat-run/engine-absence</code></td><td>the append-only absence ledger over an opaque subject — balance as a fold, approval as the only mint for bookings, per-type floors</td></tr><tr><td><a href="/engines/metering/">Metering</a></td><td><code>@substrat-run/engine-metering</code></td><td>the billable-usage ledger — idempotent ingest, counters vs gauges, period close as frozen billing evidence; quantities, never prices</td></tr></tbody></table><p>All seven are <strong>product seeds</strong> — small deliberately, hardened as real verticals consume them. Six were extracted from the demo verticals; metering is the one exception, noted below.</p><p>The booking engine is the one <strong>second invariant shape</strong>: where a work order is a <em>state machine</em>, a reservation is <em>allocation against capacity over an interval</em>. It is also the clearest demonstration that the DO-per-scope choice buys something concrete — the engine contains no locking code at all, because a scope is a single writer. The protocol engine is the extraction proof itself: it was forced out of vertical code only when a <em>second</em> vertical (a bike shop&#39;s per-bike condition report) needed the same sign-immutability invariant in a different shape — and a <em>third</em>, <a href="/guide/what-is-substrat.html#current-status">an HR vertical</a>, reuses it again for employee <strong>onboarding checklists</strong>, bound to an <code>employee</code> ref instead of a work order. Same engine, three shapes; that is the reuse thesis holding.</p><p>The absence engine is the newest extraction, and the second run of the protocol engine&#39;s proof: its <em>append-only, a-correction-is-a-new-entry, current-value-is-a-fold</em> shape was written vertical-first inside the HR demo, and extracted only when a second consumer with a different shape — field-crew resource planning, where the subject is a plannable <code>resource</code> rather than an employee — forced the line. (The question of whether the generic entries-against-a-ref core belongs in the kernel was considered and settled: it stays engine-private until a non-absence consumer wants the raw ledger shape.)</p><p>The metering engine is the honest exception to the extraction rule: it was built engine-first, against a named first consumer (the platform&#39;s own builder portal, whose AI turns must be billed) with the second — vertical AI features — in sight rather than in hand. The decision log carries that caveat rather than glossing it; the discipline it follows instead is a design document reviewed before code, and the same one-invariant-set smallness as its extracted siblings.</p><p>Planned next, in the order verticals force them: <strong>scheduling/dispatch</strong> and <strong>ticketing</strong> (ärende).</p><h2 id="how-these-pages-are-organized" tabindex="-1">How these pages are organized <a class="header-anchor" href="#how-these-pages-are-organized" aria-label="Permalink to &quot;How these pages are organized&quot;">​</a></h2><p>Every engine documents itself the same way, in the same five pages. The shape is a contract: a page that has nothing to say is a finding, not an omission.</p><table tabindex="0"><thead><tr><th>Page</th><th>Answers</th></tr></thead><tbody><tr><td><strong>index</strong></td><td><em>Is this a good match?</em> — what it owns, what it won&#39;t do, when to reach for it</td></tr><tr><td><strong>Domain model &amp; invariants</strong></td><td>the tables, and the rules the engine will not let you break</td></tr><tr><td><strong>Operations, functions &amp; permissions</strong></td><td>the callable surface, and which parts are composable</td></tr><tr><td><strong>Events</strong></td><td>what it emits and consumes, payload contracts, versioning</td></tr><tr><td><strong>Composing &amp; extending</strong></td><td>using it from a vertical, configuration, the connector seam</td></tr></tbody></table><p>Two conventions worth knowing before you read:</p><p><strong>Engines have no endpoints.</strong> They expose <em>operations</em> (named handlers invoked through a scope stub, each doing its own permission check) and <em>in-scope functions</em> (plain exports a vertical calls inside its own transaction, where the caller owns the check). Any HTTP surface is generated from the manifest&#39;s <code>api</code> field, never hand-written. The split between those two surfaces <strong>is</strong> the extension model, which is why every engine has a page for it.</p><p><strong>Engines take no configuration.</strong> <code>registerModule</code> accepts one frozen constant; there is no options object, no factory, and no <code>config</code> field on the manifest. When behaviour must vary, you compose (your own operation calling in-scope functions), declare (a guard predicate with its own kernel-opaque config), store (tenant content as data in the scope), or gate (the entitlement flag). <em>Configuration is dynamic; composition is code.</em> The Composing page of each engine says which of those applies, and admits where the engine doesn&#39;t yet allow it.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
