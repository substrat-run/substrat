import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Where AI mistakes stop","description":"Nothing here claims an agent cannot write a bug — only that one class of bug is unreachable rather than discouraged. The six guards a change passes through, in the order they fire.","frontmatter":{"description":"Nothing here claims an agent cannot write a bug — only that one class of bug is unreachable rather than discouraged. The six guards a change passes through, in the order they fire."},"headers":[],"relativePath":"guide/ai-guardrails.md","filePath":"guide/ai-guardrails.md","lastUpdated":1787679261000}');
const _sfc_main = { name: "guide/ai-guardrails.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_BlastRadius = resolveComponent("BlastRadius");
  const _component_GuardPath = resolveComponent("GuardPath");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="where-ai-mistakes-stop" tabindex="-1">Where AI mistakes stop <a class="header-anchor" href="#where-ai-mistakes-stop" aria-label="Permalink to &quot;Where AI mistakes stop&quot;">​</a></h1><p>Nothing on this page claims an agent cannot write a bug. It claims that one <strong>class</strong> of bug — the catastrophic, silent, cross-tenant kind — is unreachable rather than discouraged, and that a second class — the small, confident, inconsistent kind — has something independent watching for it.</p><p>This is the enforcement half of building on Substrat with an agent. The ergonomic half — bring-your-own-model, the markdown docs slice, the manifests an agent reads — is on <a href="/guide/ai-agents.html">Building for AI agents</a>.</p><h2 id="the-structural-insight" tabindex="-1">The structural insight <a class="header-anchor" href="#the-structural-insight" aria-label="Permalink to &quot;The structural insight&quot;">​</a></h2>`);
  _push(ssrRenderComponent(_component_BlastRadius, null, null, _parent));
  _push(`<p>Analyses of AI-generated apps keep finding the same list: missing row-level security, broken auth boundaries, no tenant isolation, no audit trail. Prompting a model to &quot;be secure&quot; measurably does not fix it, which is why the split above is architectural rather than instructional. The long form of that argument is <a href="/guide/why-substrat.html">Why runtime enforcement?</a>.</p><div class="tip custom-block"><p class="custom-block-title">Defaults, not configuration</p><p>The subtler failure than <em>no enforcement</em> is enforcement you can misconfigure. There is no API that returns another scope&#39;s data with the wrong flag set — the API for reaching a scope <em>is</em> the isolation mechanism. There is no &quot;remember to log this&quot; — the event envelope is stamped kernel-side on every <code>emit</code>, and tenant, scope, actor and time are not parameters. The permission checker&#39;s secure default is deny; the permissive one is exported as <code>UNSAFE_allowAllChecker</code>, where the name is the warning.</p></div><h2 id="six-guards-in-the-order-they-fire" tabindex="-1">Six guards, in the order they fire <a class="header-anchor" href="#six-guards-in-the-order-they-fire" aria-label="Permalink to &quot;Six guards, in the order they fire&quot;">​</a></h2><p>These are not layers of a diagram. They are stages a commit passes through in time, each one able to stop it, and the point of the sequence is that a mistake surviving one guard meets the next.</p>`);
  _push(ssrRenderComponent(_component_GuardPath, null, null, _parent));
  _push(`<p>Guard 01 is the figure above and needs no more room. The rest of this page is the five that do.</p><h2 id="guard-02-the-layer-rules" tabindex="-1">Guard 02: the layer rules <a class="header-anchor" href="#guard-02-the-layer-rules" aria-label="Permalink to &quot;Guard 02: the layer rules&quot;">​</a></h2><p>Every other guardrail in Substrat fails <strong>loud</strong>: branded IDs at compile time, Zod at the boundary, <code>getScope</code> closed on a mismatched <code>(tenant, scope)</code>. The layer rules are the ones that fail <strong>silently</strong>, which is why they need a linter rather than a convention.</p><p><strong>Module code</strong> is everything reachable from a <code>ModuleRegistration</code> — operations and consumers. Composition roots (<code>server.ts</code>, <code>seed.ts</code>, <code>worker.ts</code>) are harness and exempt.</p><table tabindex="0"><thead><tr><th>Rule</th><th>What it enforces</th></tr></thead><tbody><tr><td><strong>R1</strong> star topology</td><td>an engine never imports another <code>@substrat-run/engine-*</code></td></tr><tr><td><strong>R2</strong> no raw access</td><td>no <code>better-sqlite3</code>, no adapters, no <code>node:*</code>, no <code>cloudflare:workers</code> — data access is <code>ctx.sql</code> only</td></tr><tr><td><strong>R3</strong> no network</td><td>module code never calls <code>fetch()</code> or imports an HTTP client</td></tr><tr><td><strong>R4</strong> spine is sacred</td><td>module code never <em>writes</em> <code>_substrat_*</code> tables (reads are fine — timelines are projections)</td></tr><tr><td><strong>R5</strong> tables private</td><td>module code never references another module&#39;s tables in SQL</td></tr><tr><td><strong>R6</strong> one clock</td><td>time comes from <code>ctx.now()</code>, stable for the whole invocation</td></tr><tr><td><strong>R7</strong> no bare catch</td><td>an engine error is caught only inside <code>ctx.atomic</code> — a bare <code>catch</code> commits the engine&#39;s partial writes</td></tr></tbody></table><p>Table ownership is <strong>derived, never declared</strong> — a table belongs to whichever module&#39;s <code>CREATE TABLE</code> migration created it. There is deliberately no manifest field for it, because a second source of truth would drift and wave a real violation through.</p><p>R5 and R6 have an explicit, reviewable opt-out (<code>boundary-lint-allow R5</code> … <code>boundary-lint-end R5</code>) for a one-time extraction handoff and for code that must read the real clock. There is no escape hatch for R1–R4, and deliberately none for R7 — a hatch there would only ever be used to silence the rule. The full reference is <a href="/reference/boundary-lint.html"><code>boundary-lint</code></a>.</p><div class="warning custom-block"><p class="custom-block-title">A green light it had not earned</p><p>Exit codes are load-bearing: <code>0</code> clean, <code>1</code> violations, and <strong><code>2</code> the linter could not do its job</strong> — no module code found, or no engines resolved, so an R5 pass would trivially succeed. A linter that passes by finding nothing is worse than no linter, so it fails loudly instead.</p></div><h2 id="guard-03-three-marks-or-it-is-not-generated" tabindex="-1">Guard 03: three marks, or it is not generated <a class="header-anchor" href="#guard-03-three-marks-or-it-is-not-generated" aria-label="Permalink to &quot;Guard 03: three marks, or it is not generated&quot;">​</a></h2><p>A derived file is worth nothing if a reader cannot tell it apart from one a person maintains — and worth less than nothing if it <em>says</em> it is generated while nothing re-emits it. So a generated file carries all three of:</p><ol><li><strong>The filename</strong> — a <code>.generated.ts</code> suffix (or, for a document, a <code>&lt;!-- GENERATED … --&gt;</code> first line). This is the signal a reviewer reads in a diff without opening the file.</li><li><strong>A header naming the producer and the source</strong> — so &quot;where does this come from&quot; is answered in the file itself.</li><li><strong>A <code>--check</code> re-emit in CI.</strong> This is the only one that enforces anything. &quot;Do not edit&quot; is a request; the gate is what makes it true.</li></ol><p><code>src/migrations.generated.ts</code> carried the comment for a year with no gate, and a hand-edit to shipped SQL passed every check in the repo. The gates that exist today: <code>lint:model</code>, <code>lint:permissions</code>, <code>lint:migrations</code>, <code>lint:api</code>, <code>lint:client</code>, <code>lint:conformance</code>, <code>lint:tests</code>, <code>lint:boundaries</code>, <code>lint:decisions</code>, <code>lint:playbook</code>, <code>lint:docs</code>, <code>lint:llms</code>, <code>lint:agent-rules</code>, <code>lint:launch</code>, <code>lint:plugin</code>, <code>lint:pins</code>, <code>lint:connector-grants</code>, <code>lint:deps</code> and <code>lint:scaffold</code>.</p><p><strong>The one exception, stated rather than hidden:</strong> a file generated from a <em>remote</em> source cannot be re-emitted hermetically in CI, so it gets marks 1 and 2 plus a <code>GENERATED_AT</code> stamp instead of mark 3. An in-repo source with no gate is a defect, not a style.</p><h2 id="guard-04-two-descriptions-that-are-allowed-to-disagree" tabindex="-1">Guard 04: two descriptions that are allowed to disagree <a class="header-anchor" href="#guard-04-two-descriptions-that-are-allowed-to-disagree" aria-label="Permalink to &quot;Guard 04: two descriptions that are allowed to disagree&quot;">​</a></h2><p>This is the quiet one, and it may be the most valuable thing here. Models are fantastic and <em>inconsistent</em>: they do not fail loudly, they make many small mistakes. You cannot prompt that away, so the architecture has to catch it.</p><p><strong>Every defect worth catching is two descriptions disagreeing.</strong> Once the implementation is derived from the model, the code is a <em>function of</em> the model and can no longer contradict it. So the second description has to be the tests — and they are only a second description if they were written from somewhere else. That makes the direction load-bearing:</p><blockquote><p><strong>Code comes from the model. Tests come from the concept.</strong></p></blockquote><p>The concept is the human-approved prose (<code>spec/concept.md</code>); the model is the typed declaration (<code>spec/model.ts</code>). Two independent derivations of the same intent, and the disagreement between them is the entire product.</p><p>A suite written <em>after</em> the handlers can only agree with whatever got built. It will pass, it will look thorough, and it will ratify a wrong model perfectly and forever. That is the failure mode every AI-generated test suite has, and almost nobody names it.</p><p>The mechanical rule that keeps it honest: <strong>literal inputs, literal outputs, import nothing from <code>spec/</code></strong>. A test that builds its input from the schema it is meant to judge <em>cannot</em> disagree with that schema — it is the mirror again, one level down. <code>pnpm lint:tests</code> enforces it.</p><p>Two consequences worth stating plainly:</p><ul><li><strong>The build may not edit its own oracle.</strong> Tests are written and approved first; the build&#39;s job is to make them pass.</li><li><strong>If the concept doesn&#39;t say what should happen, that is a gap in the concept.</strong> The agent says so and stops, rather than inventing an answer and thereby making it agreed.</li></ul><h2 id="guard-05-the-two-human-checkpoints" tabindex="-1">Guard 05: the two human checkpoints <a class="header-anchor" href="#guard-05-the-two-human-checkpoints" aria-label="Permalink to &quot;Guard 05: the two human checkpoints&quot;">​</a></h2><p>Two things stay under human review even in a fully agent-driven shop:</p><ol><li><strong>Schema migrations.</strong> The blast radius of a bad migration is data, not pixels. Migrations are plain, reviewable SQL, journaled per module and append-only — a shipped version is never edited. The agent writes them; a person approves them.</li><li><strong>Permission definitions.</strong> Permissions are declared with human-readable descriptions, and every decision carries a proof path, which makes &quot;who gains what, where&quot; a diff rather than an archaeology project. <code>pnpm lint:permissions</code> renders each vertical&#39;s <code>PERMISSIONS.md</code> from the same <code>MODULES</code> and <code>ROLES</code> the seed exports, and CI re-emits with <code>--check</code>, so a widened role cannot merge without appearing in the diff.</li></ol><p>Both are still a <em>human</em> reading a diff. CI going red is what makes the reading unskippable; it is not itself the approval.</p><p>Everything else — screens, workflows, operations, reports — iterates at agent speed with contained blast radius: the worst a bad operation can do is fail inside its own scope, audited.</p><h2 id="guard-06-a-running-copy-of-production-to-be-wrong-in" tabindex="-1">Guard 06: a running copy of production to be wrong in <a class="header-anchor" href="#guard-06-a-running-copy-of-production-to-be-wrong-in" aria-label="Permalink to &quot;Guard 06: a running copy of production to be wrong in&quot;">​</a></h2><p>Reviewing a migration diff is a human checkpoint. Clicking a URL on the pull request and <em>watching that migration run against a copy of the real data</em> is what makes the checkpoint honest.</p><p>Open a PR and the platform forks the production scope, binds the version that PR just pushed, gives it its own hostname, and posts the URL on the PR; closing the PR reaps it. Successive pushes to the same PR roll their migrations <strong>forward on one copy</strong>, exactly as a real upgrade would. When a bind crosses a migration-digest boundary the pre-migration data is snapshotted first — and the digest comparison is the gate, not a flag someone remembers to pass, so the safety net is on precisely when it matters and absent on a code-only rebind.</p><p><em>Version 2 landing on version 1&#39;s live data is the single most common way a generated app dies.</em> This is the machinery that makes it survivable. See <a href="/guide/environments-and-previews.html">Environments &amp; previews</a>.</p><h2 id="the-receipt-the-guards-leave-behind" tabindex="-1">The receipt the guards leave behind <a class="header-anchor" href="#the-receipt-the-guards-leave-behind" aria-label="Permalink to &quot;The receipt the guards leave behind&quot;">​</a></h2><p>The six guards above are a commit&#39;s problem. This one is a reader&#39;s: every guard above is invisible from the outside, and none of them leaves anything a person who does not have the repo can look at. A scope <em>is</em> a Durable Object with its own SQLite, so another tenant&#39;s row is not absent-by-predicate but absent-by-construction — the strongest claim here, and the hardest one to show anybody. A buyer cannot see an absence, and an auditor cannot file one.</p><p><code>pnpm lint:conformance</code> renders a <code>CONFORMANCE.md</code> beside each package&#39;s <code>PERMISSIONS.md</code>. Where the permission snapshot is a statement of intent, this is what is asserted against it, and the two halves are deliberately not mixed:</p><ol><li><strong>Kernel-enforced properties</strong>, cited <strong>once</strong> — cross-tenant addressing failing closed, the envelope stamped kernel-side, a narrowed grant that does not widen — each naming the contract-suite test that verifies it against <em>both</em> adapters.</li><li><strong>That app&#39;s own entity checks</strong>, covered and uncovered <strong>by name</strong>. An operation that should check per-entity but checks at the node passes for anyone holding the key anywhere in the scope, with every test green; only a behavioural pair separates the two, and the pair is generated from the declaration rather than written by hand.</li></ol><p>The count discipline is the design. The tempting version multiplies every endpoint by every tenant and reports a four-figure assertion count — but cross-tenant isolation is one kernel fact, and restating it per endpoint measures the same thing repeatedly. An auditor who notices that discounts the whole document, which leaves it worth less than the absence it replaced. So the kernel section carries no per-app number at all.</p><p>It is a statement of what is asserted, not a record that it passed. CI going red is what makes it true — the same standing <code>PERMISSIONS.md</code> has.</p><h2 id="where-this-is-honest-about-itself" tabindex="-1">Where this is honest about itself <a class="header-anchor" href="#where-this-is-honest-about-itself" aria-label="Permalink to &quot;Where this is honest about itself&quot;">​</a></h2><ul><li><strong>An agent can still write a bug.</strong> The guards make one class unreachable and give a second class something independent watching. They do not make generated code correct.</li><li><strong>The egress sandbox has a documented hole.</strong> Outbound traffic is bounded by a <a href="/concepts/platform.html">declared per-version allowlist</a> enforced at the egress seam, but Durable Object subrequests are a known gap. Don&#39;t call it airtight; it isn&#39;t yet.</li><li><strong>A red build is not an approval.</strong> Both checkpoints are still a human reading a diff. CI going red is what makes that reading unskippable — it is not itself the sign-off.</li><li><strong>Enforcement is a slow argument.</strong> It asks a buyer to follow a claim about runtime architecture before they can price it. Guard 07 makes the claim legible and citable; it does not make it fast. An inherited certification is the version a procurement officer prices in one sentence, and it isn&#39;t there yet.</li><li><strong>A receipt is not an audit.</strong> <code>CONFORMANCE.md</code> is generated from the repo&#39;s own declarations and verified by the repo&#39;s own tests. That is exactly as much independence as a self-signed statement has — which is more than nothing, and less than a third party.</li></ul><p>The rest of what is missing is on <a href="/guide/what-substrat-lacks.html">What Substrat doesn&#39;t have (yet)</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/ai-guardrails.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const aiGuardrails = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  aiGuardrails as default
};
