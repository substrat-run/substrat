import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/control-plane-api","description":"","frontmatter":{},"headers":[],"relativePath":"reference/control-plane-api.md","filePath":"reference/control-plane-api.md","lastUpdated":1787601143000}');
const _sfc_main = { name: "reference/control-plane-api.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-control-plane-api" tabindex="-1">@substrat-run/control-plane-api <a class="header-anchor" href="#substrat-run-control-plane-api" aria-label="Permalink to &quot;@substrat-run/control-plane-api&quot;">​</a></h1><p>The <strong>HTTP surface over <a href="/reference/kernel.html"><code>HostAdmin</code></a></strong> — the audited control-plane transport (control-plane.md §4.5). It is the seam between the platform&#39;s admin operations (provision a scope, bind a hostname, grant a role, deploy a vertical) and whatever runs them: the CLI, the Console, and the Dashboard all call this one surface.</p><p>It is a <strong>transport, not a source of truth</strong>. Every route lands on a <code>HostAdmin</code> method, every mutation is audited there, and the same API runs over any scope host — the <a href="/reference/adapter-sqlite.html">pure-SQLite adapter</a> in CI, <a href="/reference/adapter-cloudflare.html">Durable Objects</a> in production.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/control-plane-api</span></span></code></pre></div><h2 id="createcontrolplaneapi" tabindex="-1"><code>createControlPlaneApi</code> <a class="header-anchor" href="#createcontrolplaneapi" aria-label="Permalink to &quot;\`createControlPlaneApi\`&quot;">​</a></h2><p>A <a href="https://hono.dev" target="_blank" rel="noreferrer">Hono</a> app that exposes <code>HostAdmin</code> over HTTP, with authentication and the audit trail wired in. It takes a <code>ScopeHost</code> (whose <code>.admin</code> is the <code>HostAdmin</code> it projects) plus the auth, deploy, and observability seams, and returns a plain Hono app you serve on Node, Workers, or in tests:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { createControlPlaneApi } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/control-plane-api&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> app</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> createControlPlaneApi</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({ host </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* ScopeHost */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* auth, deploy, … */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> });</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> default</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> app;</span></span></code></pre></div><p>Every request is authenticated to a <strong><code>PlatformActorId</code></strong> (staff SSO/session) or a <strong>builder</strong> principal (a scoped push token), and that actor is the same one <code>HostAdmin</code> stamps into every audit row — the transport never invents authority, it carries it.</p><h2 id="the-surface" tabindex="-1">The surface <a class="header-anchor" href="#the-surface" aria-label="Permalink to &quot;The surface&quot;">​</a></h2><p>Route groups map one-to-one onto the <code>HostAdmin</code> capability groups:</p><ul><li><strong>Tenants</strong> — <code>/tenants</code> (+ <code>/status</code>, <code>/reap</code>, <code>/identities</code>, <code>/entitlements</code>): the tenant registry, lifecycle, identity links, and per-tenant SKU grants.</li><li><strong>Scopes</strong> — <code>/scopes</code>, <code>/tenants/:t/scopes/:s</code> and its lifecycle verbs (<code>configure</code>, <code>version</code>, <code>snapshots</code>, <code>restore</code>, <code>rewind</code>, <code>bookmarks</code>, <code>reap</code>), the read-only data window (<code>tables</code>, <code>tables/:table</code>, <code>query</code>, <code>export</code>, <code>health</code>), and <code>/fleet/migrations</code>.</li><li><strong>Verticals</strong> — <code>/verticals</code> (+ <code>versions</code>, <code>versions/:id/admit</code>|<code>reject</code>, <code>channels</code>, <code>channels/:c/promote</code>, <code>deploy</code>, <code>instances</code>, <code>listing</code>, <code>publish-request</code>, <code>install-block</code>): the registry, admission, promotion, and the deploy path. A vertical has one channel — <code>prod</code> (<a href="/concepts/deploying.html#the-one-channel-prod">dev/staging retired</a>); <code>channels/:c/promote</code> refuses a non-<code>prod</code> channel with a <code>400</code>.</li><li><strong>Hostnames</strong> — <code>/hostnames</code> (+ <code>/status</code>, <code>/verify</code>): the K-26 hostname map and its Cloudflare-for-SaaS issuance/verification.</li><li><strong>Connections</strong> — <code>/tenants/:t/connections</code> (+ <code>/verify</code>, <code>/credential</code>, <code>/activity</code>, <code>/connection-grants</code>): the integrations hub. A credential is <em>verified against the provider</em> when it is connected rather than merely stored, <code>/credential</code> says which one is loaded without revealing it, and <code>/activity</code> reads what the connection actually did.</li><li><strong>Copies and erasure</strong> — <code>/directory/backups</code> and <code>/directory/restore</code> for the directory itself, <code>/scopes/:s/backups</code> and <code>/snapshots</code> for a scope, and <code>/scopes/:s/subjects/:id/shred</code>, which redacts the spine payloads and destroys the sealing key in one audited, idempotent call — returning <strong>a receipt the DSAR response is written from</strong>. Deliberately staff-only: a builder forwards the request, the platform executes it.</li><li><strong>Platform grants</strong> — <code>/verticals/:slug/email-sender</code> and <code>/verticals/:slug/tenant-provisioner</code>: the capabilities a vertical must be <em>granted</em> rather than configure for itself.</li><li><strong>Roles</strong>, <strong>admin-log</strong>, <strong>ops-failures</strong>, <strong>meters</strong>, and <strong>observability</strong> (<code>/observability/logs</code>, <code>/observability/metrics</code>) — the permission surface, the append-only audit history, operational failures, billable readings, and fleet telemetry.</li><li><strong>Denials</strong> — <code>/tenants/:t/scopes/:s/denials</code> and <code>…/denials/summary</code>: the refusals a scope recorded (K-35). The third log beside the two above — the admin log holds staff <em>mutations</em>, the K-24 access log staff <em>reads</em>, and this one the operations that were refused. Scope-local rather than directory-side, because a denial rolls its own operation back and so must be written where that operation ran; the summary is bucketed per (actor, permission) so a prober&#39;s volume cannot hide a quiet actor.</li><li><strong><code>/push-tokens</code></strong> — mints the scoped builder tokens a <code>substrat push</code> authenticates with.</li></ul><p>Routes are the shape only; enumerate the <a href="/reference/contracts.html">OpenAPI</a> document for the exact request/response schemas. The package also ships the typed clients that consume this surface — <code>ControlPlaneClient</code> (Console/CLI) and the narrowed, tenant-scoped <code>VerticalClient</code> (an app provisioning itself) — plus the deploy helpers (<code>deployManifest</code>, <code>createWfpUploader</code>) that validate a bundle against the sandbox contract and upload it to Workers-for-Platforms.</p><h2 id="the-audited-transport-role" tabindex="-1">The audited-transport role <a class="header-anchor" href="#the-audited-transport-role" aria-label="Permalink to &quot;The audited-transport role&quot;">​</a></h2><p>The one property that must not be retrofitted (K-20): a surface that can act on the directory without a durable record of <em>who</em> acted is worse than none. This package is that guarantee at the network edge — it authenticates the actor, forwards to <code>HostAdmin</code>, and lets the kernel write the append-only row. Staff auth (SSO, MFA) gates <em>exposing</em> the surface; the audit property is built into it. See <a href="/platform/control-plane.html">The control plane</a>.</p><h2 id="versioning" tabindex="-1">Versioning <a class="header-anchor" href="#versioning" aria-label="Permalink to &quot;Versioning&quot;">​</a></h2><p><code>0.25.0</code>, AGPL-3.0. Pre-release (0.x): the surface changes without notice until the platform GAs.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/control-plane-api.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const controlPlaneApi = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  controlPlaneApi as default
};
