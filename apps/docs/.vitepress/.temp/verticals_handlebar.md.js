import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Handlebar (bike workshop)","description":"","frontmatter":{},"headers":[],"relativePath":"verticals/handlebar.md","filePath":"verticals/handlebar.md","lastUpdated":1784804439000}');
const _sfc_main = { name: "verticals/handlebar.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="handlebar-bike-workshop" tabindex="-1">Handlebar (bike workshop) <a class="header-anchor" href="#handlebar-bike-workshop" aria-label="Permalink to &quot;Handlebar (bike workshop)&quot;">​</a></h1><p><code>demos/handlebar</code> — a one-workshop bike-repair shop (Kedja &amp; Kugghjul Cykelverkstad AB, Stockholm): customers bring bikes in, mechanics repair them, the shop prices and invoices the job.</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Handlebar is the <strong>engine-reuse proof</strong>. It is deliberately the <em>v2 skin</em> — the <strong>same engines as <a href="/verticals/callout.html">Callout</a>, under different vocabulary</strong> — so &quot;two verticals on shared engines&quot; is demonstrated rather than claimed. The vertical owns only vocabulary, extra fields, its price list, roles, and screens; nothing in any engine&#39;s state machine is touched.</p><p>A repair <em>is</em> a <a href="/engines/workorder/">work order</a>: <code>reparation</code>, on a <code>cykel</code> (<code>facility</code>), worked by a <code>mekaniker</code> (<code>assigned_to</code>), priced and invoiced through the same <a href="/engines/invoicing/">invoicing</a> path Callout uses. The interesting part is the third engine.</p><h3 id="it-forced-the-protocol-engine-out" tabindex="-1">It forced the protocol engine out <a class="header-anchor" href="#it-forced-the-protocol-engine-out" aria-label="Permalink to &quot;It forced the protocol engine out&quot;">​</a></h3><p>Handlebar is the vertical that made <a href="/engines/protocol/"><code>protocol</code></a> an engine. Its <code>tillståndsrapport</code> — a per-bike condition report — needed the same <strong>sign → immutable</strong> invariant Callout&#39;s checklists have, but in a <em>different shape</em>:</p><ul><li>the <strong>workshop signs</strong> it (freezing the content forever), and the <strong>customer counter-signs the same frozen content at pickup</strong> — a second signature on already-frozen content;</li><li>fills are <strong>append-only</strong>;</li><li>filling and signing are <strong>different roles</strong> (the mechanic fills; the workshop lead signs).</li></ul><p>A second shape of the same invariant is exactly what proves an invariant belongs in an engine and not in one vertical&#39;s code. So it was extracted — and Handlebar keeps only the template&#39;s vocabulary; every invariant (sign-freeze, counter-sign on frozen content, append-only responses, verifiable hash) lives in <code>@substrat-run/engine-protocol</code>.</p><p>That extraction also lets Handlebar lean on <strong>manifest-declared</strong> mechanisms where Callout uses code: a declared guard <code>protocol/all-signed</code> (<code>countersigned: true</code>) sits <code>before</code><code>bike-shop/close-repair</code>, and the vertical <code>withdraws</code> the engine&#39;s own <code>workorder/close</code> so the <em>only</em> door to <code>closed</code> is the guarded operation. Callout composes the equivalent guard as conditional vertical glue; Handlebar declares it. Two poles of the same seam.</p><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>@substrat-run/demo-handlebar</code></td></tr><tr><td><strong>Engines composed</strong></td><td><a href="/engines/workorder/"><code>workorder</code></a> · <a href="/engines/invoicing/"><code>invoicing</code></a> · <a href="/engines/protocol/"><code>protocol</code></a></td></tr><tr><td><strong>Own tables</strong></td><td><code>bike_shop_customers</code> · <code>bike_shop_bikes</code> · <code>bike_shop_price_list</code></td></tr><tr><td><strong>Roles</strong></td><td><code>workshop-admin</code> (15 keys) · <code>mechanic</code> (4: <code>protocol:fill</code>/<code>read</code>, <code>workorder:read</code>/<code>report</code>) — portal customers hold no role, only entity-narrowed <code>protocol:countersign</code>/<code>read</code> + <code>workorder:read</code> per customer</td></tr><tr><td><strong>Permission surface</strong></td><td><a href="https://github.com/substrat-run/substrat/blob/main/demos/handlebar/PERMISSIONS.md" target="_blank" rel="noreferrer"><code>PERMISSIONS.md</code></a> — 19 keys, 4 modules, 2 roles</td></tr><tr><td><strong>Apps</strong></td><td>node API (<code>:8872</code>) + React SPA (<code>:5272</code>) — runs side by side with Callout</td></tr><tr><td><strong>Status</strong></td><td>Working — demo seed</td></tr></tbody></table><h2 id="the-cast-what-s-denied" tabindex="-1">The cast &amp; what&#39;s denied <a class="header-anchor" href="#the-cast-what-s-denied" aria-label="Permalink to &quot;The cast &amp; what&#39;s denied&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Who</th><th>Holds</th><th>Cannot</th></tr></thead><tbody><tr><td><strong>Greta</strong> — verkstadschef</td><td><code>workshop-admin</code>, tenant level</td><td>— (the whole lifecycle: customers, bikes, prices, repairs, invoicing)</td></tr><tr><td><strong>Måns</strong> — mekaniker</td><td><code>mechanic</code> at the work scope</td><td><strong>assign</strong>, complete, or invoice — only start repairs and report time &amp; parts</td></tr><tr><td><strong>Lisbeth</strong> — portal customer (Crescent owner)</td><td>no role; entity-narrowed grants on her own customer</td><td>see Otto&#39;s repairs, write anything, or invoice — she sees her own repairs + timeline and <strong>counter-signs</strong> her report</td></tr><tr><td><strong>Otto</strong> — portal customer (Bianchi owner)</td><td>the same, for his customer</td><td>see Lisbeth&#39;s repairs</td></tr><tr><td><strong>Rutger</strong> — admin of rival tenant <em>Trampolin Cykel AB</em></td><td><code>workshop-admin</code> in his own tenant</td><td>anything here — <code>unknown scope</code> on a forged pair, <code>permission denied</code> on every operation</td></tr></tbody></table><p>Provisioning yields <strong>only</strong> what a customer gets — one tenant, one scope, roles, and an owner with <code>workshop-admin</code>. The named cast and the rival tenant are demo fixtures, structurally unreachable from provisioning (<code>provisionHandlebar</code> returns a shape with no room for a cast).</p><h2 id="run-it" tabindex="-1">Run it <a class="header-anchor" href="#run-it" aria-label="Permalink to &quot;Run it&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/demo-handlebar</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># API  http://localhost:8872</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># web  http://localhost:5272</span></span></code></pre></div><p>Two suites: <code>test/provision.test.ts</code> (one tenant/one scope, only the owner gets a role, idempotent) and <code>test/scenario.test.ts</code> — the 13-step scenario covering the priced completion, the star-topology invoicing event, portal isolation, immutable export, the withdrawn <code>workorder/close</code>, append-only fill with the fill/sign split, sign-freeze + customer counter-sign on the same content, and the manifest guard gating pickup.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("verticals/handlebar.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const handlebar = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  handlebar as default
};
