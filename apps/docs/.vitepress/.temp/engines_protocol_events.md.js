import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Events","description":"","frontmatter":{},"headers":[],"relativePath":"engines/protocol/events.md","filePath":"engines/protocol/events.md","lastUpdated":1785339849000}');
const _sfc_main = { name: "engines/protocol/events.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h1><p>The engine <strong>emits 9 and consumes none</strong> — protocols are driven by operations, not by upstream facts.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">events</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  emits</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [ </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* the 9 below */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  consumes</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><h2 id="emitted" tabindex="-1">Emitted <a class="header-anchor" href="#emitted" aria-label="Permalink to &quot;Emitted&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Event</th><th>v</th><th>piiClass</th><th>Payload</th></tr></thead><tbody><tr><td><code>protocol.instantiated</code></td><td>1</td><td>none</td><td>instance id, template <code>(key, version)</code>, entity</td></tr><tr><td><code>protocol.response-recorded</code></td><td>1</td><td>pseudonymous</td><td>instance id, response id, item key, value</td></tr><tr><td><code>protocol.content-bound</code></td><td>1</td><td>none</td><td>instance id, document type, content ref, bound hash</td></tr><tr><td><code>protocol.signatures-requested</code></td><td>1</td><td>none</td><td>content hash, method, parties (label, kind, request id)</td></tr><tr><td><code>protocol.signature-declined</code></td><td>1</td><td>none</td><td>request id, party label, outcome, reason</td></tr><tr><td><code>protocol.signatures-cancelled</code></td><td>1</td><td>none</td><td>instance id, how many were withdrawn, reason</td></tr><tr><td><code>protocol.signed</code></td><td>1</td><td>pseudonymous</td><td>signatory, content hash, <strong>frozen answers</strong></td></tr><tr><td><code>protocol.countersigned</code></td><td>1</td><td>pseudonymous</td><td>signatory, counter-signatory, hash, <strong>frozen answers</strong></td></tr><tr><td><code>protocol.voided</code></td><td>1</td><td>pseudonymous</td><td>instance id, entity, previous status, reason</td></tr></tbody></table><p><code>protocol.signatures-requested</code> is the connector&#39;s dispatch order: it carries the hash, the parties and the content ref, so an executor never needs a read back into the scope to know what to send where.</p><h2 id="the-signature-events-carry-the-whole-document" tabindex="-1">The signature events carry the whole document <a class="header-anchor" href="#the-signature-events-carry-the-whole-document" aria-label="Permalink to &quot;The signature events carry the whole document&quot;">​</a></h2><p><code>protocol.signed</code> and <code>protocol.countersigned</code> are <strong>fat events</strong>: the frozen answers travel in the payload alongside the content hash. A downstream consumer — an archival connector, a compliance export — never queries back.</p><p>This matters more here than elsewhere. The point of the engine is that a signed protocol can be produced <em>unaltered, years later</em>. An event carrying the hash but not the answers would force the consumer to re-read tables that may have moved on, which is exactly the join the snapshot discipline exists to prevent. The event <strong>is</strong> the archival record.</p><h2 id="pii-and-erasure" tabindex="-1">PII and erasure <a class="header-anchor" href="#pii-and-erasure" aria-label="Permalink to &quot;PII and erasure&quot;">​</a></h2><p>Events referencing a person carry <code>piiClass: &#39;pseudonymous&#39;</code> and that person&#39;s <code>subjectId</code>. A GDPR erasure crypto-shreds the person while the fact that a signed protocol exists survives.</p><p><strong><code>subjectId</code> is not always the acting principal.</strong> On a signature recorded from an external provider it is the <em>signatory</em> — a <code>DataSubjectId</code> for someone with no account in the system at all. That is what makes an external signature shreddable, and it is why a personnummer must never be used as the signatory reference: it would be <code>direct</code> PII written into a row that immutability makes permanent. The provider&#39;s own party identifier goes in <code>evidence_ref</code>.</p><p>That split is deliberate and slightly subtle: the <em>compliance</em> claim (&quot;this protocol was signed, and here is its hash&quot;) outlives the <em>personal</em> claim (&quot;by this named individual&quot;). The artifact stays verifiable; the person becomes unidentifiable.</p><h2 id="evolution-rules" tabindex="-1">Evolution rules <a class="header-anchor" href="#evolution-rules" aria-label="Permalink to &quot;Evolution rules&quot;">​</a></h2><p>Payload fields are <strong>frozen once shipped</strong>. New fields may be added; renaming, removing, or retyping one means a <code>schemaVersion</code> bump.</p><p>Before bumping, read the <a href="/engines/invoicing/events.html#versioning">invoicing engine&#39;s <code>underlag-exported</code> v2 note</a>: consumer dispatch keys on event <strong>type alone</strong>, so dual-emitting two versions delivers <em>both</em> to every consumer of that type. The &quot;deprecation window&quot; the conventions describe isn&#39;t actually available until version routing exists.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/protocol/events.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const events = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  events as default
};
