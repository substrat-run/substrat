import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Domain model & invariants","description":"Templates, instances, responses and signature requests across five tables: immutable template versions, the checklist and document content kinds, and provider-agnostic signature evidence.","frontmatter":{"description":"Templates, instances, responses and signature requests across five tables: immutable template versions, the checklist and document content kinds, and provider-agnostic signature evidence."},"headers":[],"relativePath":"engines/protocol/model.md","filePath":"engines/protocol/model.md","lastUpdated":1787424503000}');
const _sfc_main = { name: "engines/protocol/model.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_StateMachine = resolveComponent("StateMachine");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="domain-model-invariants" tabindex="-1">Domain model &amp; invariants <a class="header-anchor" href="#domain-model-invariants" aria-label="Permalink to &quot;Domain model &amp; invariants&quot;">​</a></h1><h2 id="templates-instances-responses-signatures" tabindex="-1">Templates, instances, responses, signatures <a class="header-anchor" href="#templates-instances-responses-signatures" aria-label="Permalink to &quot;Templates, instances, responses, signatures&quot;">​</a></h2><p>Five concepts, mirrored by five tables (migrations <code>0001-init</code> and <code>0002-signature-requests</code>, inside each scope&#39;s own database):</p><ul><li><strong><code>protocol_templates</code></strong> — <code>key</code>, <code>version</code>, <code>title</code>, and <code>content_json</code>. Immutable per <code>(key, version)</code>: new content is a <strong>new version</strong>, never an edit. Content comes in two <strong>kinds</strong> (below): a <code>checklist</code> of sections → items, or an opaque <code>document</code>.</li><li><strong><code>protocol_instances</code></strong> — a template pinned at <code>(key, version)</code> bound to an <code>entity_type</code>/<code>entity_id</code>, with <code>status</code> <code>open</code> → <code>pending_signature</code> → <code>signed</code>, or <code>voided</code>. Carries the <code>frozen_hash</code> once content freezes, plus the <code>content_ref</code>/<code>bound_hash</code> a document instance binds.</li><li><strong><code>protocol_signature_requests</code></strong> — one row per party a document was sent to for signature: <code>party_label</code>, <code>party_kind</code>, <code>method</code>, <code>status</code>, <code>external_ref</code>. The noun that makes asynchronous, multi-party signing expressible.</li><li><strong><code>protocol_responses</code></strong> — append-only fill entries: <code>item_key</code>, <code>value_json</code>, <code>note</code>, <code>responded_by</code>, <code>responded_at</code>. <strong>Latest-per-item wins</strong>, ordered by append (<code>rowid</code>, same-millisecond safe) — the history <em>is</em> audit material (&quot;value changed from 4.2 to 5.1 before signing&quot;).</li><li><strong><code>protocol_signatures</code></strong> — <code>signed_by</code> (the signatory&#39;s reference), <code>signatory_kind</code> (<code>principal</code>/<code>external</code>), <code>kind</code> (<code>primary</code>/<code>counter</code>), <code>method</code>, <code>content_hash</code>, optional <code>evidence_ref</code> and <code>request_id</code>. Exactly one <code>primary</code> per signed instance.</li></ul><p>The template pins at instantiation <strong>forever</strong>: editing a template tomorrow never rewrites what a signed document referred to.</p><h2 id="the-invariants" tabindex="-1">The invariants <a class="header-anchor" href="#the-invariants" aria-label="Permalink to &quot;The invariants&quot;">​</a></h2><ol><li><p><strong>Freeze freezes.</strong> Content is writable only while an instance is <code>open</code>. Freezing happens either at in-app signature or at <code>requestSignatures</code>, and from that moment fills and rebinds fail at the engine.</p></li><li><p><strong>Verifiable content hash.</strong> At freeze time the engine computes a SHA-256 and stores it as the instance&#39;s <code>frozen_hash</code>. Every signature must match it. The recipe is the contract — anyone can replay it against the stored rows and compare. One recipe per content kind:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>checklist: &#39;&lt;key&gt;@&lt;version&gt;\\n&lt;content_json&gt;\\n&#39; + &#39;item=value_json\\n&#39; per item, sorted by key</span></span>
<span class="line"><span>document:  &#39;&lt;key&gt;@&lt;version&gt;\\n&lt;content_json&gt;\\ndocument:&lt;boundHash&gt;\\n&#39;</span></span></code></pre></div><p>The checklist recipe is byte-identical to the one that shipped originally, so signatures made before the document kind existed still verify. The hash is Web Crypto (<code>globalThis.crypto</code>), so the same code runs in Node, Workers, and the browser.</p></li><li><p><strong>Counter-signature on frozen content.</strong> A counter-signature (the customer at pickup) is a <em>second signature row</em> on the <strong>same</strong> frozen content — the engine recomputes the hash and it must equal the primary&#39;s, so a counter-sign can never silently attach to changed content. A principal never counter-signs what they primary-signed, and never counter-signs twice.</p></li><li><p><strong>Append-only responses</strong>, bound to an open instance.</p></li><li><p><strong>Every mutation emits a fat event</strong> (<a href="./events.html">events</a>) — a consumer never needs a cross-module read.</p></li><li><p><strong>Void, not delete.</strong> A superseded protocol is <code>voided</code> (with a reason and an event), never mutated or removed; a replacement is a new instance.</p></li></ol><h2 id="two-content-kinds" tabindex="-1">Two content kinds <a class="header-anchor" href="#two-content-kinds" aria-label="Permalink to &quot;Two content kinds&quot;">​</a></h2><p>The attestation half of this engine — the hash, the freeze, the signatures, the guards — was always content-agnostic. Only <code>fill</code> and the template shape were checklist-specific. That seam is exposed as a discriminated <code>kind</code>:</p><ul><li><strong><code>checklist</code></strong> — sections → items, filled response-by-response. Item types are <code>check</code> (boolean), <code>value</code> (a measurement — decimal string, with an optional <code>unit</code> like <code>MΩ</code>), and <code>text</code>. Templates written before the discriminant existed carry no <code>kind</code> and parse as <code>checklist</code>; their stored <code>content_json</code> is never rewritten, because the hash covers that string verbatim.</li><li><strong><code>document</code></strong> — content the engine <strong>never sees</strong>. A priced avtal, a styrelserapport, a PDF: the vertical owns the rows, computes their hash, and binds <code>(contentRef, contentHash)</code> to the instance with <code>bindDocument</code>. Rebinding is allowed while open — an avtal&#39;s price moves during negotiation — and each rebind moves what a signature will attest to.</li></ul><div class="tip custom-block"><p class="custom-block-title">Why not model a contract as a one-item checklist?</p><p>Because the engine would then attest to the sentence &quot;I accept this contract&quot; and nothing else. The hash covers template content plus responses; if the real content lives in your vertical, a degenerate checklist produces a signature that looks like evidence and is not. The <code>document</code> kind says out loud what is actually true: the signature is over a hash the vertical computed. <code>documentContent.hashRecipe</code> is a <strong>required</strong> field for exactly that reason — a signature over an unreproducible hash is worth nothing, so the method for reproducing it has to be written where an auditor will find it.</p><p>What the engine proves for a document: this signature was made over exactly this hash, at this time, by this signatory, and the hash has not changed since. What it cannot prove: that your rows still hash to it. That check is yours — see Meridian&#39;s <code>hr/verify-contract</code> for what owning it looks like.</p></div><h2 id="the-signature-model-provider-agnostic-evidence" tabindex="-1">The signature model: provider-agnostic evidence <a class="header-anchor" href="#the-signature-model-provider-agnostic-evidence" aria-label="Permalink to &quot;The signature model: provider-agnostic evidence&quot;">​</a></h2><p>A signature records its signatory + <code>method</code> + <code>content_hash</code> + optional <code>evidence_ref</code>.</p><p>The <strong><code>in-app</code></strong> method is the everyday field case: the authenticated principal taps sign, and integrity comes from the engine itself — the hash, the immutability, the spine event. No third party, works self-hosted. Freezing and signing coincide, which is sound precisely <em>because</em> it is synchronous: there is no window between what the signer saw and what was hashed.</p><p><strong>External providers (BankID via Scrive, …) are a different operation shape, not the same one with better evidence.</strong> Three things that <code>signProtocol</code> takes from ambient context are not available:</p><table tabindex="0"><thead><tr><th></th><th>in-app</th><th>external provider</th></tr></thead><tbody><tr><td>who signs</td><td><code>ctx.principal</code></td><td>a person with <strong>no account</strong></td></tr><tr><td>when</td><td>now</td><td>days later, at the provider&#39;s timestamp</td></tr><tr><td>freeze</td><td>at signature</td><td>at <strong>dispatch</strong> — or the document drifts while it is out</td></tr></tbody></table><p>So the external path is <code>requestSignatures</code> → <em>(days)</em> → <code>recordSignature</code>, and the signatory is data rather than context:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">signatory</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;principal&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ref</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: PrincipalId }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;external&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ref</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: DataSubjectId }   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// opaque, shreddable</span></span></code></pre></div><div class="danger custom-block"><p class="custom-block-title">Never put a personnummer in <code>ref</code></p><p>It is <code>direct</code> PII, and <code>subjectId</code> on the emitted event is what crypto-shredding keys the erasure on. A <code>DataSubjectId</code> is shreddable; a personnummer written into a signature row is a GDPR liability that immutability makes permanent. The provider&#39;s own party identifier belongs in <code>evidence_ref</code>. This follows <code>engines/booking</code>&#39;s <code>partyRef</code> for the same reason.</p></div><p>eIDAS advanced/qualified levels <em>are</em> an evidence-quality upgrade — but only once the flow that produces them exists. The engine never imports a vendor SDK. (See <a href="./composing.html#reaching-the-outside-world">Composing</a> for what is and is not built.)</p><h2 id="the-lifecycle" tabindex="-1">The lifecycle <a class="header-anchor" href="#the-lifecycle" aria-label="Permalink to &quot;The lifecycle&quot;">​</a></h2>`);
  _push(ssrRenderComponent(_component_StateMachine, { engine: "protocol" }, null, _parent));
  _push(`<p><code>pending_signature</code> is frozen: no fill, no rebind. It is the state that closes the drift window — with freeze welded to signing, an instance sitting at a provider for days stayed <code>open</code> and writable, so the document the customer saw could differ from the one that was hashed, undetected.</p><p>An instance reaches <code>signed</code> only when <strong>every</strong> requested party has signed. A declined or expired request is not &quot;pending&quot;, but it is not a signature either — treating it as completion would mark an avtal fully executed that a party refused. An unresolved refusal holds the instance frozen until someone explicitly withdraws the request set, which is a separate, permissioned, audited act.</p><p>Cancelling <strong>thaws</strong>: status returns to <code>open</code> and <code>frozen_hash</code> clears, so the next request freezes fresh content at a fresh hash. Signatures already collected are kept — they are append-only history attesting to content that really was frozen — but they were taken over the old hash and can never satisfy the new one. A party who signed v1 has not signed v2.</p><h2 id="sign-is-separate-from-fill" tabindex="-1">Sign is separate from fill <a class="header-anchor" href="#sign-is-separate-from-fill" aria-label="Permalink to &quot;Sign is separate from fill&quot;">​</a></h2><p><code>protocol:fill</code> and <code>protocol:sign</code> are deliberately distinct permissions. The field reality: the technician fills the protocol, but only the <em>arbetsledare</em> signs it.</p><p>Filling records the responder as <code>ctx.principal</code>; signing records the signer. Attribution comes from the ambient context, never from the input — you cannot sign as someone else by passing their id.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/protocol/model.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const model = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  model as default
};
