import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/model-emit","description":"","frontmatter":{},"headers":[],"relativePath":"reference/model-emit.md","filePath":"reference/model-emit.md","lastUpdated":1787175850000}');
const _sfc_main = { name: "reference/model-emit.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-model-emit" tabindex="-1">@substrat-run/model-emit <a class="header-anchor" href="#substrat-run-model-emit" aria-label="Permalink to &quot;@substrat-run/model-emit&quot;">​</a></h1><p>Build-time tooling over a vertical&#39;s <a href="/concepts/model.html">model</a>: the DDL your entities already describe, the reader that checks a migration journal against them, and the planner that says what one new journal entry would have to contain.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -D</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/model-emit</span></span></code></pre></div><p>It is a <strong>dev dependency</strong>. Nothing here runs inside a scope — it produces text you commit.</p><h2 id="why-it-exists" tabindex="-1">Why it exists <a class="header-anchor" href="#why-it-exists" aria-label="Permalink to &quot;Why it exists&quot;">​</a></h2><p>A vertical declares its entities once:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> entities</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> defineEntities</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  customer: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    table: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;acme_customers&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    fields: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">object</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({ id: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(), number: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(), name: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() }),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    key: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;number&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p>…and then, without this package, writes the same thing again by hand as SQL. Two descriptions of one schema, nothing holding them together — so they drift, and the drift is invisible until a query returns <code>undefined</code> for a column somebody renamed on one side.</p><p><code>emitTables</code> derives the second from the first.</p><h2 id="emittables-—-the-ddl" tabindex="-1"><code>emitTables</code> — the DDL <a class="header-anchor" href="#emittables-—-the-ddl" aria-label="Permalink to &quot;\`emitTables\` — the DDL&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { emitTables } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/model-emit&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { entities } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;./spec/model.js&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> sql</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> emitTables</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(entities);                     </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// CREATE TABLE …</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> sql</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> emitTables</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(entities, { ifNotExists: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// CREATE TABLE IF NOT EXISTS …</span></span></code></pre></div><table tabindex="0"><thead><tr><th>declared</th><th>emitted</th></tr></thead><tbody><tr><td><code>id</code></td><td><code>id TEXT PRIMARY KEY NOT NULL</code></td></tr><tr><td><code>z.string()</code> / <code>.nullable()</code></td><td><code>TEXT NOT NULL</code> / <code>TEXT</code></td></tr><tr><td><code>z.number()</code></td><td><code>INTEGER</code></td></tr><tr><td><code>z.boolean()</code></td><td><strong>refused</strong> — SQLite stores 0/1, so declare <code>z.number()</code> and keep the row type honest. <code>z.boolean()</code> stays right for an operation&#39;s <em>input</em></td></tr><tr><td><code>z.enum([&#39;a&#39;,&#39;b&#39;])</code></td><td><code>TEXT NOT NULL CHECK (col IN (&#39;a&#39;,&#39;b&#39;))</code></td></tr><tr><td><code>key: [&#39;number&#39;]</code></td><td><code>UNIQUE (number)</code>; composite over all its fields — <code>key: [&#39;a&#39;,&#39;b&#39;]</code> is one <code>UNIQUE (a, b)</code></td></tr><tr><td><code>parents: [&#39;customer&#39;]</code></td><td><code>REFERENCES acme_customers(id)</code> on the matching <code>customer_id</code></td></tr><tr><td><code>jsonColumn(&#39;because…&#39;)</code></td><td><code>TEXT</code></td></tr></tbody></table><p><code>columnsOf(entities)</code> and <code>uniqueConstraints(name, entity)</code> expose the same derivation piecewise, for a tool that wants the parts rather than the statement.</p><h3 id="it-is-stricter-than-a-hand-written-schema-in-exactly-one-way" tabindex="-1">It is stricter than a hand-written schema, in exactly one way <a class="header-anchor" href="#it-is-stricter-than-a-hand-written-schema-in-exactly-one-way" aria-label="Permalink to &quot;It is stricter than a hand-written schema, in exactly one way&quot;">​</a></h3><p>An <code>id</code> becomes <code>TEXT PRIMARY KEY **NOT NULL**</code>. In SQLite a non-INTEGER primary key does <em>not</em> imply <code>NOT NULL</code>, so a hand-written <code>id TEXT PRIMARY KEY</code> accepts a NULL id — and every hand-written <code>vertical_*</code> table in this repo had that hole. The emitter cannot produce it.</p><h3 id="it-refuses-rather-than-guesses" tabindex="-1">It refuses rather than guesses <a class="header-anchor" href="#it-refuses-rather-than-guesses" aria-label="Permalink to &quot;It refuses rather than guesses&quot;">​</a></h3><p>A Zod shape it cannot map to a column throws, naming the field:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>emit-sql: cannot map thing.blob (zod kind &#39;array&#39;) to a column —</span></span>
<span class="line"><span>  map it explicitly, or model the field as one this understands</span></span></code></pre></div><p>This is deliberate. A production vertical once shipped 18 events carrying <code>entityId: undefined</code> because its emitter <em>defaulted</em> instead of refusing — applied uniformly, silently, eighteen times. For anything reaching a migration, absent has to be loud.</p><p>A column that genuinely holds a document is declared, with a reason:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">fields</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">object</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  id: z.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  geometry: </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">jsonColumn</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;a route geometry — modelling its interior says nothing useful&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p><code>jsonColumn</code> lives in <a href="/reference/contracts.html"><code>@substrat-run/contracts</code></a>, because you <em>write</em> it in your model. A bare <code>z.unknown()</code> is still an error: deliberately opaque and not-yet-modelled have to stay distinguishable, or the first becomes cover for the second.</p><h2 id="journalcolumns-journaluniques-—-the-other-half" tabindex="-1"><code>journalColumns</code> / <code>journalUniques</code> — the other half <a class="header-anchor" href="#journalcolumns-journaluniques-—-the-other-half" aria-label="Permalink to &quot;\`journalColumns\` / \`journalUniques\` — the other half&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { journalColumns } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/model-emit&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> journal</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> journalColumns</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(migrations.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">map</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">((</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">m</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> m.sql).</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">join</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">\\n</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">journal.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;acme_customers&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// Set { &#39;id&#39;, &#39;number&#39;, &#39;name&#39; }</span></span></code></pre></div><p>Columns per table, replayed from a migration journal: <code>CREATE TABLE</code>, <code>ADD COLUMN</code>, <code>DROP TABLE</code>, and <code>RENAME TO</code>. That last one matters — append-only journals rebuild a table by creating a <code>_new</code>, copying, dropping the original and renaming onto its name, and a reader that misses that reports the pre-rebuild columns forever. <code>journalUniques</code> does the same for unique constraints and <code>journalPrimaryKeys</code> for the key.</p><p><strong>Your journal is not parsed — it is replayed.</strong> The readers run it into a throwaway in-memory SQLite and read the schema back through <code>PRAGMA table_info</code> / <code>index_list</code> / <code>index_info</code>. So the answer is the one your production database would give, and every SQL spelling works because SQLite is the thing reading it: several columns on one line, a <code>CREATE TABLE</code> on one line, a <code>) STRICT;</code> suffix, a quoted <code>&quot;order&quot;</code> identifier, a wrapped <code>PRIMARY KEY (</code> list, a comma inside a literal, a <code>--</code> or <code>/* */</code> comment. Reformatting a journal never changes an answer, which matters because history is the one thing an append-only journal may not rewrite.</p><p>Only <strong>schema</strong> statements are replayed. Your <code>INSERT</code>s do not change your schema, and skipping them is what lets a vertical&#39;s journal be read on its own when it hands data to an engine whose tables live in a different journal. Foreign keys stay off, so a <code>REFERENCES</code> pointing at another module&#39;s table is created rather than refused.</p><p>A journal whose schema statements <strong>do not apply</strong> now throws, naming the statement. That is deliberate: a journal that cannot be replayed will not apply to a scope either, and the old parser answered anyway.</p><div class="warning custom-block"><p class="custom-block-title">Node only</p><p>The readers use <code>node:sqlite</code>, so they need <strong>Node 22.5+</strong>. That is not a new restriction in practice — this package is a build-time devDependency and nothing in it runs inside a scope.</p></div><p><code>journalUniques</code> reads all three spellings of a uniqueness rule, because real journals use all three:</p><table tabindex="0"><thead><tr><th>in the journal</th><th>read as</th></tr></thead><tbody><tr><td><code>UNIQUE (a, b)</code> — table-level</td><td><code>a, b</code></td></tr><tr><td><code>b TEXT NOT NULL UNIQUE</code> — column-level</td><td><code>b</code></td></tr><tr><td><code>CREATE UNIQUE INDEX ux ON t (b)</code></td><td><code>b</code></td></tr><tr><td><code>CREATE UNIQUE INDEX ux ON t (b) WHERE deleted_at IS NULL</code></td><td><strong>nothing</strong> — a partial index constrains a subset of the rows, so reading it as a key would claim a guarantee the database does not make</td></tr></tbody></table><p>It ships with the emitter because the two are one claim: the emitter says <em>what the database ends up with</em>, and this is how that gets checked. Until your migrations are derived, use it to hold your registry and your journal to each other:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">it</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;the registry agrees with the journal&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> journal</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> journalColumns</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(migrations.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">map</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">((</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">m</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> m.sql).</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">join</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">\\n</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  for</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> entity</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> of</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> Object.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">values</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(entities)) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    expect</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(Object.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">keys</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(entity.fields.shape).</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sort</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">())</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      .</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">toEqual</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">([</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">...</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(journal.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(entity.table) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">??</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [])].</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sort</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">());</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><h2 id="planmigration-—-nobody-writes-the-version-number" tabindex="-1"><code>planMigration</code> — nobody writes the version number <a class="header-anchor" href="#planmigration-—-nobody-writes-the-version-number" aria-label="Permalink to &quot;\`planMigration\` — nobody writes the version number&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { planMigration, parseJournal } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/model-emit&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> plan</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> planMigration</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(entities, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">parseJournal</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">JSON</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">parse</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(raw)));</span></span></code></pre></div><p>The model states the current shape; the journal states what has already been applied. <code>planMigration</code> reconstructs the second, diffs against the first, and returns one of three things:</p><table tabindex="0"><thead><tr><th>result</th><th>meaning</th></tr></thead><tbody><tr><td><code>{ kind: &#39;up-to-date&#39; }</code></td><td>the journal already describes the model</td></tr><tr><td><code>{ kind: &#39;append&#39;, entry }</code></td><td>exactly one new entry, with a <strong>derived</strong> monotonic <code>version</code> (<code>0003</code>) and a slug from what changed</td></tr><tr><td><code>{ kind: &#39;refused&#39;, reasons }</code></td><td>the diff is not a diff — see below</td></tr></tbody></table><p>It is pure: same model + same journal → same plan, every time. It reads no clock and mints no id, which is what lets the result be committed and reviewed as a diff.</p><p><strong>Declaring a version is declaring a fact a diff already knows</strong> — and hand-numbering has failed in practice: a production journal in a real app ships two entries numbered <code>0010</code>, because two people numbered by hand in two branches. Two branches both deriving <code>0003</code> collide in <code>journal.json</code>, which is the right signal on an ordered append-only list, and the resolution is mechanical: merge the model, re-run, it renumbers.</p><h3 id="what-it-refuses" tabindex="-1">What it refuses <a class="header-anchor" href="#what-it-refuses" aria-label="Permalink to &quot;What it refuses&quot;">​</a></h3><p>Anything that would rewrite history or lose data: a dropped table or column, a retyped column, or a <strong>required</strong> column added to a table that may already hold rows. Those are real decisions — expand/contract, a backfill, a <a href="/concepts/model.html"><code>renamedFrom</code></a> declaration — and a generator that guessed at them would be guessing with somebody&#39;s data. A refusal names each reason in the vocabulary of your model, not in raw table names.</p><h2 id="it-reads-the-typescript-never-model-json" tabindex="-1">It reads the TypeScript, never <code>model.json</code> <a class="header-anchor" href="#it-reads-the-typescript-never-model-json" aria-label="Permalink to &quot;It reads the TypeScript, never \`model.json\`&quot;">​</a></h2><p><code>z.toJSONSchema</code> keeps the declarative constraints (<code>.min</code>, <code>.regex</code>, <code>.enum</code>, <code>.nullable</code>, <code>.default</code>) and <strong>silently drops the programmatic ones</strong> — <code>.refine()</code> and <code>.brand()</code> both emit as a bare <code>{&quot;type&quot;:&quot;string&quot;}</code>. An emitter reading the JSON would produce a schema weaker than your model declares.</p><p><code>model.json</code> is for consumers that must <em>not</em> execute your code (a hosted console drawing your model) or that want diffability rather than validators (a breaking-change classifier). Those are different jobs, and the <a href="/concepts/model.html#model-json">model page</a> keeps them apart.</p><h2 id="what-it-is-not" tabindex="-1">What it is not <a class="header-anchor" href="#what-it-is-not" aria-label="Permalink to &quot;What it is not&quot;">​</a></h2><p>It emits a <strong>schema, not a migration history</strong>. Freezing released entries and running expand/contract across a live scope are the <a href="/concepts/deploying.html">deploy model&#39;s</a> problem, and this does not pretend to solve them. Use <code>emitTables</code> for a scope that has never run, and <code>journalColumns</code> + <code>planMigration</code> to hold an existing journal to your registry.</p><h2 id="licence" tabindex="-1">Licence <a class="header-anchor" href="#licence" aria-label="Permalink to &quot;Licence&quot;">​</a></h2><p>Apache-2.0, unlike the AGPL runtime packages. Substrat&#39;s line is whether a package is the substrate you <em>run to serve</em> (kernel, adapters, engines) or something you <em>build with</em> (contracts, templates, the CLI). A generator is the second.</p><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="/concepts/model.html">The model</a> — what a vertical declares, and what the compiler checks</li><li><a href="/reference/contracts.html"><code>@substrat-run/contracts</code></a> — <code>defineEntities</code>, <code>jsonColumn</code>, <code>emitModel</code></li><li><a href="/concepts/deploying.html">The deploy model</a> — where migrations actually run</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/model-emit.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const modelEmit = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  modelEmit as default
};
