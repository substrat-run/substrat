import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Invoicing engine","description":"","frontmatter":{},"headers":[],"relativePath":"engines/invoicing/index.md","filePath":"engines/invoicing/index.md","lastUpdated":1784462441000}');
const _sfc_main = { name: "engines/invoicing/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="invoicing-engine" tabindex="-1">Invoicing engine <a class="header-anchor" href="#invoicing-engine" aria-label="Permalink to &quot;Invoicing engine&quot;">​</a></h1><p><code>@substrat-run/engine-invoicing</code> — accumulates <strong>invoice bases</strong> (Swedish: <em>invoice basis</em>) from billable events and makes them <strong>immutable once exported</strong>.</p><p>It is the reference example of star-topology composition <em>and</em> cross-domain reuse: it builds invoice bases from events in more than one domain — work-order completions and retail orders — with zero imports from either producer.</p><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>@substrat-run/engine-invoicing</code></td></tr><tr><td><strong>Entitlement key</strong></td><td><code>invoicing</code></td></tr><tr><td><strong>Owns</strong></td><td>invoice-basis accumulation, one-document-one-currency, immutability on export</td></tr><tr><td><strong>Emits</strong></td><td><code>invoicing.underlag-updated</code> (v1), <code>invoicing.underlag-exported</code> (<strong>v2</strong>)</td></tr><tr><td><strong>Consumes</strong></td><td><code>workorder.completed</code>, <code>commerce.order-placed</code> — with zero imports from either</td></tr><tr><td><strong>Permissions</strong></td><td>2 (<code>invoicing:read</code> · <code>invoicing:export</code>)</td></tr><tr><td><strong>Extends by composition</strong></td><td>not yet — <a href="./surface.html#in-scope-functions">no in-scope functions</a></td></tr><tr><td><strong>Status</strong></td><td>product seed (0.x) — surfaces change until the first vertical ships</td></tr></tbody></table><h2 id="what-it-owns" tabindex="-1">What it owns <a class="header-anchor" href="#what-it-owns" aria-label="Permalink to &quot;What it owns&quot;">​</a></h2><ul><li><strong>Exported means immutable.</strong> Once a basis is exported, nothing appends, nothing edits, and exporting again throws.</li><li><strong>One document, one currency.</strong> A delivery whose lines disagree on currency is rejected at write time rather than producing a total that means nothing.</li><li><strong>Snapshot, not join.</strong> Prices and quantities are frozen from the event payload; provenance is kept as <code>EntityRef</code> columns.</li><li><strong>Idempotent on replay.</strong> The source order is the dedup key, so a redelivered completion adds nothing rather than billing twice.</li></ul><p>Details: <a href="./model.html">Domain model &amp; invariants</a>.</p><h2 id="what-it-will-not-do" tabindex="-1">What it will not do <a class="header-anchor" href="#what-it-will-not-do" aria-label="Permalink to &quot;What it will not do&quot;">​</a></h2><ul><li><strong>Issue invoices.</strong> It produces the <em>basis</em> — billable lines with frozen prices and provenance, ready to hand to whatever actually invoices.</li><li><strong>Bookkeeping.</strong> Not a ledger, not accounts receivable, not payment tracking. That territory belongs to accounting systems, reached through connectors.</li><li><strong>Pricing.</strong> Prices arrive priced, in the event payload. The engine sums; it never rates.</li></ul><h2 id="is-this-a-good-match" tabindex="-1">Is this a good match? <a class="header-anchor" href="#is-this-a-good-match" aria-label="Permalink to &quot;Is this a good match?&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Reach for it when</th><th>Look elsewhere when</th></tr></thead><tbody><tr><td>Billable facts arrive as <strong>events</strong> from elsewhere</td><td>You want to build an invoice by hand in a form — nothing consumes to</td></tr><tr><td>You need a frozen, auditable basis handed to an accounting system</td><td>You need the invoice itself, with numbering and VAT logic</td></tr><tr><td>&quot;What was this charge based on, and where did it come from?&quot; must be answerable years later</td><td>Nobody will ever ask</td></tr><tr><td>You want prices frozen at the moment of the fact, not at read time</td><td>Re-pricing history is acceptable</td></tr><tr><td>Your bookkeeping lives in Fortnox/Visma-class software</td><td>You intend to <em>be</em> the ledger</td></tr></tbody></table><p>The clarifying question: <strong>do you need the artifact that justifies an invoice, or the invoice?</strong> This engine owns the first and deliberately stops at the second.</p><p>Cross-domain reuse is proven here, not theoretical: the same immutability and snapshot machinery serves a field-service firm (<code>workorder.completed</code>) and a coffee roaster (<code>commerce.order-placed</code>), and adding the second was a purely additive change.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/invoicing/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
