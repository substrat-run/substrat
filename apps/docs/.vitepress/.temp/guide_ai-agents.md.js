import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Building for AI agents","description":"What makes the platform legible to a coding agent: bring your own model, docs published as markdown, a project that announces its own rules, and self-describing module manifests.","frontmatter":{"description":"What makes the platform legible to a coding agent: bring your own model, docs published as markdown, a project that announces its own rules, and self-describing module manifests."},"headers":[],"relativePath":"guide/ai-agents.md","filePath":"guide/ai-agents.md","lastUpdated":1787486158000}');
const _sfc_main = { name: "guide/ai-agents.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="building-for-ai-agents" tabindex="-1">Building for AI agents <a class="header-anchor" href="#building-for-ai-agents" aria-label="Permalink to &quot;Building for AI agents&quot;">​</a></h1><p>Substrat treats coding agents as primary users of the platform, and that shapes the API more than any other single requirement. If you&#39;re pointing Claude Code (or any agent) at a Substrat vertical, this page explains what the platform does to make itself <strong>legible</strong> to one — the surface, the docs, and the rules a project announces about itself.</p><h2 id="the-one-paragraph-version" tabindex="-1">The one-paragraph version <a class="header-anchor" href="#the-one-paragraph-version" aria-label="Permalink to &quot;The one-paragraph version&quot;">​</a></h2><p>The layer where LLMs are weakest — tenancy, auth, migrations, integrations, compliance — is the layer where mistakes are catastrophic. The layer where they are strongest — screens, forms, workflows, reports — is the layer where mistakes are cosmetic. Substrat puts hard guarantees under that line and agent velocity above it.</p><h2 id="what-stops-a-mistake" tabindex="-1">What stops a mistake <a class="header-anchor" href="#what-stops-a-mistake" aria-label="Permalink to &quot;What stops a mistake&quot;">​</a></h2><p>That half of the story — the six guards a change passes through, the layer rules, the oracle the build is not allowed to edit, and the two things an agent never self-approves — has its own page: <strong><a href="/guide/ai-guardrails.html">Where AI mistakes stop</a></strong>. Read it first if the question you have is <em>&quot;what happens when the agent gets it wrong&quot;</em>.</p><p>What follows here is the other half: what makes the platform readable to an agent in the first place.</p><h2 id="bring-your-own-model-bring-your-own-agent" tabindex="-1">Bring your own model, bring your own agent <a class="header-anchor" href="#bring-your-own-model-bring-your-own-agent" aria-label="Permalink to &quot;Bring your own model, bring your own agent&quot;">​</a></h2><p>You are not locked into someone&#39;s prompt box. Design and build run in <em>your</em> agent against repo skills — <code>npm create substrat</code> writes <code>AGENTS.md</code>, <code>.substrat/playbook.md</code>, and command stubs for Claude Code, Cursor and opencode alike, so the project announces its own rules and build flow to whatever you open it in.</p><p>And the code is on your disk, running locally: not &quot;export a zip that dies without our SDK&quot;, but a repo that boots against SQLite with no platform in the loop.</p><h2 id="these-docs-as-markdown" tabindex="-1">These docs, as markdown <a class="header-anchor" href="#these-docs-as-markdown" aria-label="Permalink to &quot;These docs, as markdown&quot;">​</a></h2><p>An agent that reads this site as HTML spends its context on navigation and theme markup. Every page here is also published as raw markdown at the same path plus <code>.md</code> — <code>/concepts/model</code> is also <a href="/concepts/model.html"><code>/concepts/model.md</code></a> — and two files index the whole set:</p><table tabindex="0"><thead><tr><th>File</th><th>What it is</th></tr></thead><tbody><tr><td><a href="/llms.txt"><code>/llms.txt</code></a></td><td>The index: every page, grouped by section, one line of description each. Start here and fetch only the pages you need.</td></tr><tr><td><a href="/llms-full.txt"><code>/llms-full.txt</code></a></td><td>Every page concatenated, for one-shot ingestion. Large — the index exists so this is the fallback, not the default.</td></tr></tbody></table><p>Both state the <code>@substrat-run/kernel</code> version they describe, and <code>/llms.txt</code> is also served at <code>/llms-&lt;version&gt;.txt</code>. That second URL is the one to pin: Substrat is 0.x and interfaces change without notice, so an agent holding pages from two minors ago is a real failure mode. Fetching <code>/llms-&lt;the version you have installed&gt;.txt</code> returns 200 only while the published docs still describe your kernel — a 404 means they have moved on, and the answer is to re-read <code>/llms.txt</code> rather than trust the cache.</p><div class="tip custom-block"><p class="custom-block-title">Fetch the markdown, not the page</p><p>Point your agent at the <code>.md</code> URLs. Links <em>inside</em> those files already point at other <code>.md</code> files, so an agent that follows a reference stays in markdown instead of bouncing back into HTML.</p></div><h2 id="the-project-announces-itself" tabindex="-1">The project announces itself <a class="header-anchor" href="#the-project-announces-itself" aria-label="Permalink to &quot;The project announces itself&quot;">​</a></h2><p>You should not have to remember any of the above. A scaffolded vertical carries <code>.substrat/hooks/session-start.mjs</code>, which runs when an agent session opens and hands it three things it would otherwise have to discover: that this is a Substrat vertical, where the rules and the build flow live, and the URL of the docs slice describing <strong>the kernel this project actually has installed</strong> — read from <code>node_modules</code>, not from the range in <code>package.json</code>, because a caret on <code>0.x</code> pins the minor and the resolved version is what your code compiles against.</p><p>It also remembers which version it last announced, so the first session after an upgrade opens by stating the jump rather than letting an agent discover it by being wrong.</p><p>The script sits in <code>.substrat/</code> — the tool-neutral home, beside <code>playbook.md</code> — and <code>.claude/settings.json</code> is a three-line adapter that runs it. Any other client that grows a session hook binds to the same script.</p><p>It is silent unless <code>package.json</code> has a <code>substrat</code> block, so it is inert in any other project; it makes no network request, so it costs nothing at startup and works offline; and it never fails a session — any unexpected error exits quietly. Opt out entirely by creating <code>.substrat/no-session-context</code>.</p><div class="tip custom-block"><p class="custom-block-title">Why not just check whether the docs match?</p><p>Because the check is already mechanical for whoever actually fetches. <code>/llms-&lt;version&gt;.txt</code> returns 200 only while the published docs still describe that kernel, so a 404 <em>is</em> the mismatch warning — and putting an HTTP request on the critical path of every session start, to compute an answer the next fetch computes for free, is a poor trade.</p></div><h2 id="self-describing-modules" tabindex="-1">Self-describing modules <a class="header-anchor" href="#self-describing-modules" aria-label="Permalink to &quot;Self-describing modules&quot;">​</a></h2><p>The <a href="/concepts/modules.html">module manifest</a> is what makes a Substrat system legible to an agent without reading its implementation: every module declares its permissions (with descriptions), the events it emits and consumes (with schema versions), its entity relations, its migrations and compatibility window, and its UI contributions. An agent scaffolding a new vertical can discover the whole surface of the installed engines from their manifests.</p><h2 id="local-loop" tabindex="-1">Local loop <a class="header-anchor" href="#local-loop" aria-label="Permalink to &quot;Local loop&quot;">​</a></h2><p>The pure-SQLite adapter is the agent&#39;s development loop: real serialization semantics, real isolation, real stamped events, in-process, deterministic, fast. An agent can build a module, run the contract tests, inspect the resulting <code>.sqlite</code> files, and iterate — with no credentials and no shared environment to damage.</p><h2 id="where-this-is-honest-about-itself" tabindex="-1">Where this is honest about itself <a class="header-anchor" href="#where-this-is-honest-about-itself" aria-label="Permalink to &quot;Where this is honest about itself&quot;">​</a></h2><p>Nothing above claims an agent cannot write a bug. What the platform does about that — and what it still doesn&#39;t — is on <a href="/guide/ai-guardrails.html#where-this-is-honest-about-itself">Where AI mistakes stop</a> and <a href="/guide/what-substrat-lacks.html">What Substrat doesn&#39;t have (yet)</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/ai-agents.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const aiAgents = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  aiAgents as default
};
