import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/boundary-lint","description":"","frontmatter":{},"headers":[],"relativePath":"reference/boundary-lint.md","filePath":"reference/boundary-lint.md","lastUpdated":1787679261000}');
const _sfc_main = { name: "reference/boundary-lint.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-boundary-lint" tabindex="-1">@substrat-run/boundary-lint <a class="header-anchor" href="#substrat-run-boundary-lint" aria-label="Permalink to &quot;@substrat-run/boundary-lint&quot;">​</a></h1><p>The <strong>Substrat layer rules, enforced mechanically</strong> — the static analysis behind the <a href="/concepts/modules.html">three-layer rule</a> and the module-code rules, runnable in this monorepo or against a standalone vertical. It ships a <code>substrat-boundary-lint</code> bin.</p><p>Every other guardrail in Substrat fails <strong>loud</strong>: branded IDs at compile time, Zod at the boundary, <code>getScope</code> closed on a mismatched <code>(tenant, scope)</code>. The layer rules are the ones that fail <strong>silently</strong> — a raw <code>SELECT</code> against an engine&#39;s private table returns the right rows, the test passes, and nothing tells you the engine can now never ship a migration. This linter is the only thing that can tell the shortcut from the sanctioned path.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npx</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/boundary-lint</span></span></code></pre></div><h2 id="what-it-checks" tabindex="-1">What it checks <a class="header-anchor" href="#what-it-checks" aria-label="Permalink to &quot;What it checks&quot;">​</a></h2><p><strong>Module code</strong> is everything reachable from a <code>ModuleRegistration</code> — operations and consumers. Composition roots (<code>server.ts</code>, <code>seed.ts</code>, <code>worker.ts</code>, …) are harness and exempt.</p><table tabindex="0"><thead><tr><th>Rule</th><th>What it enforces</th></tr></thead><tbody><tr><td><strong>R1</strong> star topology</td><td>an engine never imports another <code>@substrat-run/engine-*</code></td></tr><tr><td><strong>R2</strong> no raw access</td><td>module code imports no <code>better-sqlite3</code>, no adapters, no <code>node:*</code> — data access is <code>ctx.sql</code> only</td></tr><tr><td><strong>R3</strong> no network</td><td>module code never calls <code>fetch()</code> or imports an HTTP client</td></tr><tr><td><strong>R4</strong> spine is sacred</td><td>module code never <em>writes</em> <code>_substrat_*</code> tables (reads are fine — timelines are projections)</td></tr><tr><td><strong>R5</strong> tables private</td><td>module code never references another module&#39;s tables in SQL</td></tr><tr><td><strong>R6</strong> no clock</td><td>module code never reads the wall clock (<code>new Date()</code>, <code>Date.now()</code>) — the operation&#39;s instant is <code>ctx.now()</code></td></tr><tr><td><strong>R7</strong> no bare catch</td><td>module code never catches an engine error outside <a href="/concepts/modules.html"><code>ctx.atomic</code></a> — a <code>catch</code> around a raw engine call commits its partial writes</td></tr></tbody></table><p>R7 allows the two shapes that do not swallow: <code>try</code>/<code>finally</code> with no <code>catch</code>, and a catch that always rethrows (<code>catch (e) { log(e); throw e }</code>) — the operation still fails, so the whole transaction rolls back either way.</p><p>Table ownership is <strong>derived, never declared</strong>: a table belongs to whichever module&#39;s <code>CREATE TABLE</code> migration created it, and that SQL survives compilation into <code>dist/</code>, so ownership resolves identically from a workspace checkout or from <code>node_modules</code> (keyed on the npm package name). There is deliberately no manifest field for it — a second source of truth would drift and wave a real violation through.</p><h3 id="the-escape-hatch" tabindex="-1">The escape hatch <a class="header-anchor" href="#the-escape-hatch" aria-label="Permalink to &quot;The escape hatch&quot;">​</a></h3><p>R5 and R6 have an explicit, reviewable opt-out — R5 for a one-time extraction handoff (decision 27), R6 for code that must read the <em>real</em> clock (a JWT whose <code>exp</code> a remote server judges):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// boundary-lint-allow R5 — one-time extraction handoff, removed after the cutover</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> legacy</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ctx.sql.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">query</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;SELECT * FROM workorder_time_entries&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// boundary-lint-end R5</span></span></code></pre></div><p>There is no escape hatch for R1–R4, and deliberately none for R7: unlike a data handoff or a real-clock JWT, there is no legitimate reason to swallow an engine error unprotected, so a hatch would only ever be used to silence the rule.</p><h2 id="running-it" tabindex="-1">Running it <a class="header-anchor" href="#running-it" aria-label="Permalink to &quot;Running it&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">node</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> tools/boundary-lint.mjs</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      # the monorepo entry point (what CI runs)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npx</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/boundary-lint</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # a standalone vertical (zero config)</span></span></code></pre></div><p>Both call the same code — the monorepo lints itself with the exact linter a from-scratch vertical runs, so the rules can never drift between the two. Zero config in the two shapes that matter (a standalone vertical with module code in <code>src/</code>; the monorepo&#39;s <code>engines/*</code>/<code>demos/*</code>); otherwise a <code>boundary-lint.config.json</code> or a <code>substrat.boundaryLint</code> key in <code>package.json</code> names the local packages and any third-party engines. It is also usable programmatically via <code>lint()</code> / <code>formatViolations()</code>.</p><p>Exit codes are load-bearing: <code>0</code> clean, <code>1</code> violations, and <strong><code>2</code> the linter could not do its job</strong> — no module code found, or no engines resolved, so an R5 pass would trivially succeed. A green light it had not earned is worse than no linter, so it fails loudly instead.</p><p>CI runs <code>node tools/boundary-lint.mjs</code> on every change (see the project&#39;s <code>Commands</code>), so a boundary violation cannot merge.</p><h2 id="versioning" tabindex="-1">Versioning <a class="header-anchor" href="#versioning" aria-label="Permalink to &quot;Versioning&quot;">​</a></h2><p><code>0.0.5</code>, Apache-2.0 — the same permissive license as <a href="/reference/contracts.html"><code>@substrat-run/contracts</code></a>, because the rules are part of the public module contract, not the proprietary runtime.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/boundary-lint.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const boundaryLint = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  boundaryLint as default
};
