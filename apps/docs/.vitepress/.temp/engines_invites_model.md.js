import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderComponent, ssrRenderStyle } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Invites: domain model & invariants","description":"","frontmatter":{},"headers":[],"relativePath":"engines/invites/model.md","filePath":"engines/invites/model.md","lastUpdated":1787424503000}');
const _sfc_main = { name: "engines/invites/model.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_StateMachine = resolveComponent("StateMachine");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="invites-domain-model-invariants" tabindex="-1">Invites: domain model &amp; invariants <a class="header-anchor" href="#invites-domain-model-invariants" aria-label="Permalink to &quot;Invites: domain model &amp; invariants&quot;">​</a></h1><h2 id="the-state-machine" tabindex="-1">The state machine <a class="header-anchor" href="#the-state-machine" aria-label="Permalink to &quot;The state machine&quot;">​</a></h2>`);
  _push(ssrRenderComponent(_component_StateMachine, { engine: "invites" }, null, _parent));
  _push(`<p>Terminal states are terminal. An accepted invitation cannot be revoked, a revoked one cannot be accepted, and an expired one cannot be revived — a new invitation is a new row.</p><h2 id="one-table" tabindex="-1">One table <a class="header-anchor" href="#one-table" aria-label="Permalink to &quot;One table&quot;">​</a></h2><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">invites_invitation (</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  id, org_id, identifier_hash, role_key, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">state</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  invited_by, accepted_by, created_at, expires_at, settled_at</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre></div><p><code>accepted_by</code> is null until acceptance, because until then the platform genuinely does not know who the recipient is. That is the point: an invitation is addressed to an identifier, not to a principal.</p><h2 id="the-identifier-is-hashed-and-salted-per-scope" tabindex="-1">The identifier is hashed, and salted per scope <a class="header-anchor" href="#the-identifier-is-hashed-and-salted-per-scope" aria-label="Permalink to &quot;The identifier is hashed, and salted per scope&quot;">​</a></h2><p>The plaintext address is never persisted. An invite table full of addresses is a mailing list, and a breach of it is a breach of everyone who was ever invited anywhere.</p><p>The salt is the <strong>scope</strong>, not a global constant. A global salt would make one address produce one hash everywhere, so anyone holding the table could correlate a person across tenants — reintroducing exactly what per-tenant identity pools exist to prevent. Identifiers are normalised before hashing, so <code>A@b.com</code> and <code>a@b.com </code> are one person.</p><h2 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h2><p><strong>Re-inviting is indistinguishable from inviting.</strong> Sending to someone who already has an open invitation returns the existing id, in the same shape as a first send. Saying &quot;already invited&quot; would let a sender probe membership one address at a time.</p><p><strong>Every failed accept returns one identical error.</strong> Wrong identifier, unknown invitation, already settled, expired — all the same message. Distinguishing them would make the endpoint an oracle.</p><p><strong>Expiry is applied on read and on transition</strong>, never by a background sweep. An expired invitation must never be acceptable, and the only moments that matters are when someone looks or someone acts. A sweep would be a second source of truth for the same fact.</p><p><strong>Open invitations are bounded</strong> — 25 per sender per organization, 14 days by default. The limit counts <em>open</em> invitations rather than lifetime sends, or a busy admin would eventually be locked out forever.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/invites/model.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const model = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  model as default
};
