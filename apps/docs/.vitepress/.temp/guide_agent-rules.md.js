import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Agent rules","description":"The always-on contract for building a Substrat vertical: the three layers, the ten non-negotiable module-code rules, the gates to run, and the two checkpoints an agent may never self-approve.","frontmatter":{"description":"The always-on contract for building a Substrat vertical: the three layers, the ten non-negotiable module-code rules, the gates to run, and the two checkpoints an agent may never self-approve."},"headers":[],"relativePath":"guide/agent-rules.md","filePath":"guide/agent-rules.md","lastUpdated":1787478522000}');
const _sfc_main = { name: "guide/agent-rules.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="agent-rules" tabindex="-1">Agent rules <a class="header-anchor" href="#agent-rules" aria-label="Permalink to &quot;Agent rules&quot;">​</a></h1><p><strong>If you are an agent building on Substrat, this is the page to read first.</strong> It is the always-on contract — the rules that hold no matter what you touch — and most of what a generated vertical gets wrong is on this page.</p><p>This is not a summary written for the website. It is the exact file <a href="/reference/create-substrat.html"><code>create-substrat</code></a> writes into every scaffolded project as <code>AGENTS.md</code>, published here so an agent that has not scaffolded anything can still read it. The two are kept identical mechanically; a rule cannot say one thing in your project and another thing here.</p><p>What this page is <em>not</em> is the build flow. Interviewing for a domain, mapping it onto the engines, and landing a design document a human approves before any code is a <strong>playbook</strong>, invoked when you start a vertical — <code>/substrat</code> in Claude Code, the <code>new-vertical</code> command in Cursor and opencode. Scaffold first with <a href="/guide/getting-started.html">Getting started</a>; this page is what a session already mid-build must never violate.</p><h2 id="the-mental-model" tabindex="-1">The mental model <a class="header-anchor" href="#the-mental-model" aria-label="Permalink to &quot;The mental model&quot;">​</a></h2><p>Three layers. You only own the third.</p><ol><li><strong>Kernel — free, always.</strong> Tenancy (one scope = one isolated database; there is no cross-tenant API), permissions (roles, grants, and a proof path for every decision), events + audit (every mutation emits a kernel-stamped event you cannot mislabel), migrations (journaled per module, applied lazily per scope).</li><li><strong>Engines — compose or feed.</strong> Headless, own invariants that cannot be violated (state machines that can&#39;t skip states, append-only entries). You either <strong>compose</strong> an engine (import it; its in-scope functions run in <em>your</em> transaction) or <strong>feed</strong> it (emit a fat event; it consumes — no import). Engines never import each other. Read an engine&#39;s real surface from <code>node_modules/@substrat-run/engine-*/dist/index.d.ts</code> — never guess at it.</li><li><strong>Your vertical — everything a user touches.</strong> Vocabulary, price list, extra fields, roles, screens. If your core noun isn&#39;t something an engine already owns, this is most of the app — a normal, supported outcome.</li></ol><h2 id="project-layout" tabindex="-1">Project layout <a class="header-anchor" href="#project-layout" aria-label="Permalink to &quot;Project layout&quot;">​</a></h2><p>The linter and tests expect this shape. <code>manifest</code>/<code>migrations</code>/<code>module</code> are <strong>module code</strong> (the rules below bind them); <code>seed</code>/<code>server</code> are <strong>harness</strong> (exempt).</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/manifest.ts        moduleManifest.parse({…}) + PERM consts   ← module code</span></span>
<span class="line"><span>src/migrations.ts      the SqlMigration[]                         ← module code</span></span>
<span class="line"><span>src/module.ts          imports both; operations + registration    ← module code</span></span>
<span class="line"><span>src/provision.ts       MODULES, ROLES, grant shapes — node-free    ← module code</span></span>
<span class="line"><span>src/seed.ts            host, tenants, demo cast, seed world        ← harness</span></span>
<span class="line"><span>src/routes.ts          the HTTP route table — BOTH hosts mount it   ← harness</span></span>
<span class="line"><span>src/server.ts          the dev entrypoint (node + persona picker)   ← harness</span></span>
<span class="line"><span>src/worker.ts          the deployable Cloudflare worker             ← harness</span></span>
<span class="line"><span>src/config-do.ts       per-instance config store (Cloudflare only)  ← harness</span></span>
<span class="line"><span>test/scenario.test.ts  the scenario — including the denials</span></span></code></pre></div><p><strong>A new route goes in <code>src/routes.ts</code>, never in an entrypoint.</strong> Both <code>server.ts</code> and <code>worker.ts</code> mount that one table, so a route added there is live on both — and a route added to only one is a surface that works in dev and 404s in production (or the reverse), which nothing catches until you deploy: the scenario tests call operations directly and never boot either host. What an entrypoint may still own is only what is genuinely its own — building a host, resolving a caller, and its own auth-shaped route (<code>/api/cast</code> in dev, <code>/api/me</code> in the worker).</p><p><code>provision.ts</code> is deliberately node-free: both hosts register from it (the dev server&#39;s SQLite host and the worker&#39;s <code>ScopeDO</code>), and <code>substrat push</code> reads the permission registry from it (package.json <code>substrat.permissions</code>). Roles or modules defined anywhere else will run locally and silently not deploy. <code>worker.ts</code> <strong>mounts</strong> the platform&#39;s <code>/internal/*</code> management contract via <code>mountPlatformSurface</code> from <code>@substrat-run/vertical-host</code> (one call — the routes and the <code>{ error }</code> envelope are authored there, not here, so they can&#39;t drift or ship half-done). What <code>worker.ts</code> still owns is <strong>the auth seam</strong> — it resolves nobody until you wire real auth there, and every <code>/api/*</code> call is 401 until you do. That is deliberate: there is no dev header to forget to turn off. <code>src/server.ts</code> shows the shape, signing in against the local OIDC issuer <code>pnpm dev</code> starts.</p><p><strong>A UI ships as declared assets.</strong> If <code>app/</code> exists, <code>substrat.runtimeNeeds.assets</code> must point at its build output and <code>runtimeNeeds.build</code> must produce it — otherwise the deployed vertical serves the API and 404s on <code>/</code>, with every local gate green. Declare it in the same change that creates <code>app/</code>, not at deploy time. (<code>substrat push</code> refuses an undeclared UI, but by then you are already deploying.)</p><p>Among the hooks it passes, <strong><code>onConfigure</code> is the one you must not drop.</strong> It is how per-instance settings reach the running app: the dashboard&#39;s Settings → Env and Identity tabs POST to <code>/internal/configure</code>, and a vertical that supplies no hook answers <strong>501</strong> to that call for its whole life — the setting is saved, the dashboard reports <code>delivered: false</code>, and the app never sees it. That includes the <code>substrat:auth</code> issuer choice, i.e. the difference between a working login and 401-on-everything. The starter stores deliveries in <code>config-do.ts</code> and reads them back through <code>resolveScopedEnvSpec</code> (<code>instanceConfig</code>). Read settings that way and never off <code>env</code> directly: an <code>envSpec</code> default rides as a worker binding shared by every install of one serving script, so <code>env.FOO</code> is the same string for every tenant no matter what any of them saved. Declare a setting in <strong>both</strong><code>src/manifest.ts</code> (<code>SHOP_ENV</code>) and package.json <code>substrat.envSpec</code> — <code>substrat push</code> reads the JSON, not the TypeScript.</p><h2 id="the-rules-non-negotiable" tabindex="-1">The rules (non-negotiable) <a class="header-anchor" href="#the-rules-non-negotiable" aria-label="Permalink to &quot;The rules (non-negotiable)&quot;">​</a></h2><p><strong>Module code</strong> = everything reachable from a <code>ModuleRegistration</code> (operations, consumers). Rules 1–5 are enforced mechanically by <code>boundary-lint</code>.</p><ol><li><strong>Data access is <code>ctx.sql</code> only.</strong> Never import <code>better-sqlite3</code>, an adapter, <code>node:*</code>, or <code>cloudflare:workers</code> in module code. That last one is not a style rule: it exports an ambient <code>env</code>, so a single import hands module code every binding and secret your worker declares — including its own <code>SCOPE</code> Durable Object namespace, which reaches <em>another scope&#39;s</em> data. <code>ctx.sql</code> is closed over one scope and cannot. Capabilities arrive on <code>ctx</code>; <code>DurableObject</code> is imported in harness code (<code>worker.ts</code>, <code>*-do.ts</code>), never here.</li><li><strong>No <code>fetch</code> / network in module code.</strong> It would hold the scope&#39;s transaction open on a third party. The sanctioned path is a <strong>connector</strong>: emit a fat event, register a handler that runs outside the transaction. An integration is never impossible because of this rule — it has an answer.</li><li><strong>Never write <code>_substrat_*</code> tables.</strong> Reads are fine (timelines are projections); writes forge the audit spine.</li><li><strong>Another module&#39;s tables are private.</strong> Never <code>SELECT</code> from <code>workorder_*</code> etc. — use the engine&#39;s exported in-scope functions. This is the rule with no runtime equivalent: the shortcut <em>works</em> and silently welds you to an engine&#39;s private schema forever. Need extra data on an engine entity? Add <strong>your own side table keyed by the engine&#39;s id</strong> — never a column upstream.</li><li><strong>Time comes from <code>ctx.now()</code>.</strong> Module code has no other clock — <code>new Date()</code> and <code>Date.now()</code> are banned exactly like <code>node:*</code>. It is the same instant for the whole operation, so your rows and the events announcing them agree about when. Store it as ISO text, never an epoch integer. Because the host injects the clock, a scenario can test elapsed time (<code>manualClock</code> from <code>@substrat-run/kernel</code>) instead of sleeping or shrinking the window to zero — the workaround that proves nothing.</li><li><strong>Every operation checks a permission first.</strong> <code>assertAllowed(await ctx.check(PERM))</code> is the first line.</li><li><strong>Every mutation emits a fat event</strong> — a consumer must never need a cross-module read.</li><li><strong>Never fork an engine.</strong> Extend by composition. If you must fork, the engine drew its line wrong — that&#39;s design feedback, not a coding problem.</li><li><strong>IDs are <code>ulid()</code>. Money is strings</strong> via <code>@substrat-run/contracts</code> helpers (<code>moneyOf</code>, <code>mulMoney</code>, <code>addDecimal</code>, <code>compareDecimal</code>) — never floats.</li><li><strong>Web-standard APIs always</strong> — <code>globalThis.crypto</code>, <code>TextEncoder</code>, <code>URL</code>. Never hand-roll a hash to dodge an import ban.</li><li><strong>Parse, don&#39;t trust.</strong> Zod at every boundary — but import <code>z</code> from <code>@substrat-run/contracts</code>, <strong>never from <code>zod</code></strong>. Zod schemas don&#39;t compose across copies or majors; composing a contracts schema into one built from a separate <code>zod</code> fails at <em>runtime</em> (<code>expected a Zod schema</code>) with an error pointing nowhere near the cause.</li></ol><h2 id="declare-every-link-edge" tabindex="-1">Declare every link edge <a class="header-anchor" href="#declare-every-link-edge" aria-label="Permalink to &quot;Declare every link edge&quot;">​</a></h2><p><code>entityRelations</code> in the manifest must declare every edge you traverse — both your own (<code>bike → customer</code>) and the ones an engine makes on your behalf (<code>workorder → bike</code>). The adapter <strong>rejects</strong> a <code>ctx.link</code> for an undeclared edge, so a missing one fails loudly. This is also what lets a portal permission-walk reach the owner.</p><h2 id="the-gates-—-run-them-believe-them" tabindex="-1">The gates — run them, believe them <a class="header-anchor" href="#the-gates-—-run-them-believe-them" aria-label="Permalink to &quot;The gates — run them, believe them&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> test</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                        # the scenario, including the denials</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npx</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/boundary-lint</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> # the layer rules (1–5)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> typecheck</span></span></code></pre></div><p><code>boundary-lint</code> exits non-zero if it <em>couldn&#39;t do its job</em> (no module code found, no engines resolvable) — a pass that checked nothing is worse than no linter. Never wave that through; fix the setup until it can see your code.</p><p>A green scenario test does <strong>not</strong> mean the app works: the test calls operations directly and never exercises <code>server.ts</code>, its routes, or the principal picker. Before calling a vertical done, boot the server and drive the real flow over HTTP as two personas — one who should succeed and one who should be denied — and confirm the denial arrives as a denial (not a generic error).</p><h2 id="two-human-checkpoints-—-you-may-never-self-approve" tabindex="-1">Two human checkpoints — you may never self-approve <a class="header-anchor" href="#two-human-checkpoints-—-you-may-never-self-approve" aria-label="Permalink to &quot;Two human checkpoints — you may never self-approve&quot;">​</a></h2><p>Present these and stop:</p><ol><li><strong>Migration diff</strong> — every new <code>SqlMigration</code>, verbatim. Migrations are append-only forever once shipped, so this is the last cheap moment to change your mind.</li><li><strong>Permission diff</strong> — a table: key → description → which roles hold it → why. Walk the reviewer through it in their own vocabulary until they can answer <em>who can now see the money, and who can see other tenants&#39; data?</em> A permission diff nobody understands is theater — it reproduces the exact failure Substrat exists to prevent.</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/agent-rules.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const agentRules = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  agentRules as default
};
