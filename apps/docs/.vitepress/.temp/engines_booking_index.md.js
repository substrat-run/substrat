import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Booking engine","description":"","frontmatter":{},"headers":[],"relativePath":"engines/booking/index.md","filePath":"engines/booking/index.md","lastUpdated":1784804439000}');
const _sfc_main = { name: "engines/booking/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="booking-engine" tabindex="-1">Booking engine <a class="header-anchor" href="#booking-engine" aria-label="Permalink to &quot;Booking engine&quot;">​</a></h1><p><code>@substrat-run/engine-booking</code> — reservations as <strong>allocation against capacity over an interval</strong>. A resource is held for a span of time, and concurrent allocations may never exceed that resource&#39;s capacity. It deliberately knows nothing about timezones, pricing, opening hours, or who is allowed to book — all of which are the vertical&#39;s.</p><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>@substrat-run/engine-booking</code></td></tr><tr><td><strong>Entitlement key</strong></td><td><code>booking</code></td></tr><tr><td><strong>Owns</strong></td><td>the allocation invariant, the reservation state machine, holds and their expiry, append-only participants</td></tr><tr><td><strong>Emits</strong></td><td>12 events, <code>booking.held</code> → <code>booking.completed</code> (<a href="./events.html">events</a>)</td></tr><tr><td><strong>Consumes</strong></td><td>nothing — it is a source, not a sink</td></tr><tr><td><strong>Permissions</strong></td><td>8 (<code>booking:create</code> · <code>read</code> · <code>hold</code> · <code>confirm</code> · <code>cancel</code> · <code>move</code> · <code>complete</code> · <code>manage-resources</code>)</td></tr><tr><td><strong>Status</strong></td><td>product seed (0.x) — surfaces change until the first vertical ships</td></tr></tbody></table><h2 id="what-it-owns" tabindex="-1">What it owns <a class="header-anchor" href="#what-it-owns" aria-label="Permalink to &quot;What it owns&quot;">​</a></h2><ul><li><strong>No overallocation, ever.</strong> For any resource, the sum of live allocations over any instant never exceeds its capacity. Exclusive booking is capacity <code>1</code>; capacity above 1 is for genuinely fungible pools (rental equipment, general-admission slots) where nobody cares <em>which</em> unit.</li><li><strong>Intervals are half-open</strong> — <code>[startsAt, endsAt)</code>. A booking ending at 19:00 and one starting at 19:00 do not collide. Stated once, relied on everywhere.</li><li><strong>A hold is never permanent.</strong> <code>held</code> requires an <code>expiresAt</code>, and expiry is <em>lazy</em>: a lapsed hold simply stops counting, with no sweeper to run and nothing to go wrong if one never runs.</li><li><strong>The state machine cannot skip.</strong> <code>held → confirmed → in_service → completed</code>, plus <code>held → expired</code>, <code>{held | confirmed} → cancelled</code>, and <code>{confirmed | in_service} → completed | no_show</code>. Invalid transitions throw.</li><li><strong>Participants are append-only.</strong> Leaving sets <code>left_at</code>; the row survives, so the record of who was on a booking stays intact.</li></ul><h3 id="the-property-worth-understanding" tabindex="-1">The property worth understanding <a class="header-anchor" href="#the-property-worth-understanding" aria-label="Permalink to &quot;The property worth understanding&quot;">​</a></h3><p><strong>There is no locking code in this engine, and none is needed.</strong> The allocation check is a plain read-then-write. It is correct because a scope is a single Durable Object — one serialization domain, one writer — so the read and the write that follows it never interleave with another transaction.</p><p>That has a hard consequence for how you scope: <strong>a resource&#39;s entire calendar must live in one scope.</strong> Split one court&#39;s bookings across two scopes and you have silently reintroduced distributed locking, with nothing to tell you. SQLite has no exclusion constraint, so the guarantee comes from the serialization domain rather than from the database.</p><h2 id="what-it-will-not-do" tabindex="-1">What it will not do <a class="header-anchor" href="#what-it-will-not-do" aria-label="Permalink to &quot;What it will not do&quot;">​</a></h2><ul><li><strong>Timezones and calendar arithmetic.</strong> The engine takes and compares absolute instants. &quot;Every Tuesday at 19:00&quot;, opening hours, DST — all vertical. See <a href="./composing.html">composing</a>.</li><li><strong>Pricing.</strong> It never learns what a slot costs. Duration is an input, not a multiplier.</li><li><strong>Policy of any kind</strong> — cancellation windows, who may book, skill bands, membership. It knows only <em>fill target</em> and <em>deadline</em>.</li><li><strong>Field patches.</strong> Mutations are named transitions. There is no <code>updateReservation</code>; rescheduling is <a href="./surface.html"><code>move</code></a>, putting places on offer is <a href="./surface.html"><code>open</code></a>, and participants change through join/leave.</li></ul><h2 id="is-this-a-good-match" tabindex="-1">Is this a good match? <a class="header-anchor" href="#is-this-a-good-match" aria-label="Permalink to &quot;Is this a good match?&quot;">​</a></h2><p>Reach for it when something scarce is held for a span of time and double-booking is unacceptable: courts and pitches, salon chairs, workshop benches, meeting rooms, clinic slots, equipment rental, classes with a seat count.</p><p>Reach for something else when the thing you are modelling has no interval (a queue, a ticket) or when &quot;capacity&quot; is really inventory that depletes rather than a slot that frees up again afterwards.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/booking/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
