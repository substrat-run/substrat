import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Metering engine","description":"","frontmatter":{},"headers":[],"relativePath":"engines/metering/index.md","filePath":"engines/metering/index.md","lastUpdated":1787127415000}');
const _sfc_main = { name: "engines/metering/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="metering-engine" tabindex="-1">Metering engine <a class="header-anchor" href="#metering-engine" aria-label="Permalink to &quot;Metering engine&quot;">​</a></h1><p><code>@substrat-run/engine-metering</code> — billable usage as an <strong>append-only, idempotent meter ledger whose closed periods are frozen billing evidence</strong>. It counts what happened — tokens, requests, bytes stored — and deliberately knows nothing about what any of it costs: pricing is the vertical&#39;s.</p><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>@substrat-run/engine-metering</code></td></tr><tr><td><strong>Entitlement key</strong></td><td><code>metering</code></td></tr><tr><td><strong>Owns</strong></td><td>the append-only usage ledger, idempotent ingest, the counter/gauge aggregation split, the period-close journal and its horizon</td></tr><tr><td><strong>Emits</strong></td><td>3 events, <code>metering.usage-recorded</code> → <code>metering.period-closed</code> (<a href="./events.html">events</a>)</td></tr><tr><td><strong>Consumes</strong></td><td>nothing — it is a source, not a sink</td></tr><tr><td><strong>Permissions</strong></td><td>4 (<code>metering:read</code> · <code>record</code> · <code>configure</code> · <code>close</code>)</td></tr><tr><td><strong>Status</strong></td><td>product seed (0.x) — surfaces change until the first vertical ships</td></tr></tbody></table><h2 id="what-it-owns" tabindex="-1">What it owns <a class="header-anchor" href="#what-it-owns" aria-label="Permalink to &quot;What it owns&quot;">​</a></h2><ul><li><strong>The ledger is append-only, always.</strong> A usage entry is never edited or deleted; an over-recorded counter is corrected by a <strong>compensating entry</strong> with a negative delta, so the record of what was believed when survives — the same discipline as every other ledger on the platform.</li><li><strong>Ingest is idempotent by construction.</strong> Every <code>recordUsage</code> names a caller-supplied <strong>dedupe key</strong>, unique per meter. A replay with the same key and quantity returns the existing entry — no second row, no second event, no double bill. The same key with a <em>different</em> quantity throws: that is an upstream bug, and swallowing it would hide exactly the defect the key exists to catch.</li><li><strong>Counters and gauges aggregate differently, on purpose.</strong> A <em>counter</em> (tokens, requests) is a flow you <strong>sum</strong>; a <em>gauge</em> (bytes stored, seats) is a level you <strong>sample</strong> — its window aggregate is the max in-window, and a gauge with no samples in a window <strong>carries its last level forward</strong>. The kind lives on the meter definition and is frozen after creation.</li><li><strong>A closed period is evidence, not a snapshot.</strong> <code>closePeriod</code> freezes a half-open <code>[from, to)</code> window into immutable per-meter lines and emits one fat event. Closes are monotonic, and the latest closed <code>to</code> is a <strong>hard horizon</strong>: no new entry may land behind it, so a closed period&#39;s lines stay reproducible from its entries forever.</li><li><strong>Entries may carry an opaque attribution ref.</strong> An optional <code>subject</code> (<code>EntityRef</code> — a builder project, a message, whatever the vertical&#39;s noun is) tags an entry for bill-splitting and filtering. The engine never dereferences it.</li></ul><h3 id="the-property-worth-understanding" tabindex="-1">The property worth understanding <a class="header-anchor" href="#the-property-worth-understanding" aria-label="Permalink to &quot;The property worth understanding&quot;">​</a></h3><p><strong>This is the <em>billable</em> metering plane, and it is deliberately not the only one.</strong> The platform&#39;s high-volume telemetry — per-request, per-subrequest, per-egress-verdict — rides Analytics Engine datapoints: cheap, unbounded, lossy-tolerant, right for cost attribution and abuse detection, and disqualified from invoices by its sampling. This engine is the other plane: durable, transactional, in the scope&#39;s own SQL, auditable — because an invoice needs evidence a customer can dispute. If you are metering something high-volume for billing, <strong>pre-aggregate before recording</strong> (one entry per hour or day, with the bucket id as the dedupe key) rather than mirroring the firehose into the ledger (<a href="./composing.html">composing</a>).</p><h2 id="what-it-will-not-do" tabindex="-1">What it will not do <a class="header-anchor" href="#what-it-will-not-do" aria-label="Permalink to &quot;What it will not do&quot;">​</a></h2><ul><li><strong>No prices, no currency, no plans.</strong> <code>metering.period-closed</code> carries <em>unpriced</em> quantities. The vertical maps meter keys to rates and feeds <a href="/engines/invoicing/">invoicing</a> — the day this engine emits a <code>Money</code>, it has crossed into vertical vocabulary.</li><li><strong>No close schedule.</strong> <em>When</em> to close a period — monthly, weekly, on demand — is billing policy, which is vertical vocabulary. The vertical calls <code>closePeriod</code> from its own schedule or operation.</li><li><strong>No collection.</strong> The engine records what callers hand it; observing the usage (an AI turn&#39;s token count, a storage sample) is the vertical&#39;s or platform&#39;s job.</li><li><strong>No per-subject bill splitting in closed lines (v0).</strong> Lines aggregate per meter across all subjects; per-subject detail stays queryable from the entries. Grouping lines by <code>(meter, subject)</code> is a plausible additive extension when a vertical needs the <em>bill itself</em> split.</li><li><strong>No spend caps or rate limits.</strong> Those are enforcement, live on the platform plane, and must not depend on a ledger a scope owner could starve.</li></ul><h2 id="where-it-came-from" tabindex="-1">Where it came from <a class="header-anchor" href="#where-it-came-from" aria-label="Permalink to &quot;Where it came from&quot;">​</a></h2><p>Unlike its siblings, this engine was <strong>not extracted from a vertical</strong> — it was built against a named first consumer with the second in sight, and the decision log carries that honestly. The platform&#39;s billing doctrine (<em>meter everything, bill the few</em>) deferred metering &quot;until a vertical meters something&quot;; the <strong>builder portal&#39;s token economy</strong> is that trigger — AI turns whose token usage must be recorded so it can be charged for. Vertical AI features are the anticipated second consumer. The design decisions — the two planes, the dedupe contract, the horizon — are pinned in <code>docs/engines/metering.md</code> in the repository.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/metering/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
