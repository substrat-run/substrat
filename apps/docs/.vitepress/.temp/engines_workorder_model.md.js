import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderComponent, ssrRenderStyle } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Domain model & invariants","description":"","frontmatter":{},"headers":[],"relativePath":"engines/workorder/model.md","filePath":"engines/workorder/model.md","lastUpdated":1787424503000}');
const _sfc_main = { name: "engines/workorder/model.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_StateMachine = resolveComponent("StateMachine");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="domain-model-invariants" tabindex="-1">Domain model &amp; invariants <a class="header-anchor" href="#domain-model-invariants" aria-label="Permalink to &quot;Domain model &amp; invariants&quot;">​</a></h1><h2 id="the-state-machine" tabindex="-1">The state machine <a class="header-anchor" href="#the-state-machine" aria-label="Permalink to &quot;The state machine&quot;">​</a></h2><p><strong>Declared</strong>, in <code>engines/workorder/src/lifecycle.ts</code>, and emitted to <code>engines/workorder/model.json</code> — this is the first engine to adopt a <a href="/concepts/lifecycle.html">lifecycle</a>. The table below is written from that declaration; the declaration is the source, and <code>pnpm lint:model --check</code> is what keeps this page from drifting away from it.</p>`);
  _push(ssrRenderComponent(_component_StateMachine, { engine: "workorder" }, null, _parent));
  _push(`<table tabindex="0"><thead><tr><th>Operation</th><th>Legal in</th><th>Moves to</th><th>Permission</th></tr></thead><tbody><tr><td><code>workorder/create</code></td><td>—</td><td><code>planned</code> (the declared <code>initial</code>)</td><td><code>workorder:create</code></td></tr><tr><td><code>workorder/assign</code></td><td><code>planned</code></td><td>—</td><td><code>workorder:assign</code></td></tr><tr><td><code>workorder/start</code></td><td><code>planned</code></td><td><code>in_progress</code></td><td><code>workorder:report</code></td></tr><tr><td><code>workorder/report-time</code></td><td><code>planned</code>, <code>in_progress</code></td><td>—</td><td><code>workorder:report</code></td></tr><tr><td><code>workorder/report-material</code></td><td><code>planned</code>, <code>in_progress</code></td><td>—</td><td><code>workorder:report</code></td></tr><tr><td><code>workorder/complete</code></td><td><code>in_progress</code></td><td><code>completed</code></td><td><code>workorder:complete</code></td></tr><tr><td><code>workorder/close</code></td><td><code>completed</code></td><td><code>closed</code></td><td><code>workorder:close</code></td></tr></tbody></table><p>The rows with no target are <code>allow</code> entries — legal in that state, moving nothing. Assignment is a field, not a state, which is why <code>assign</code> leaves the order in <code>planned</code>.</p><p><code>in_progress</code> is the one state marked <code>extensible</code>, so a vertical may refine it with substates (<code>awaiting_parts</code>, <code>pending_customer_approval</code>). <code>completed</code> and <code>closed</code> carry billing consequences and admit none. <code>closed</code> is <code>terminal</code>.</p><p>Invalid transitions throw a <code>conflict</code> carrying <code>reason: &#39;invalid_transition&#39;</code> — a <code>completed</code> order cannot be started, a <code>planned</code> order cannot be completed. The engine owns this; no caller can skip a state.</p><h2 id="tables" tabindex="-1">Tables <a class="header-anchor" href="#tables" aria-label="Permalink to &quot;Tables&quot;">​</a></h2><p>Created by migration <code>0001-init</code>, inside each scope&#39;s own database:</p><ul><li><strong><code>workorder_orders</code></strong> — id, sequential <code>number</code> (per scope), <code>facility</code> and <code>customer</code> as opaque <code>EntityRef</code> columns, vertical-defined <code>kind</code>, title/description, status, assignment, provenance (<code>created_by</code>, <code>created_at</code>, <code>completed_at</code>).</li><li><strong><code>workorder_time_entries</code></strong> — order, technician, decimal <code>hours</code>, note, <code>reported_at</code>.</li><li><strong><code>workorder_material_lines</code></strong> — order, article, decimal <code>qty</code>, note, reporter, <code>reported_at</code>.</li></ul><p>Notice what&#39;s <em>absent</em>: no facility table, no customer table, no price columns. The engine references facilities and customers as opaque refs the vertical owns, and it records quantities, not prices — <strong>quantities are facts, prices are business decisions.</strong></p><h2 id="the-invariants" tabindex="-1">The invariants <a class="header-anchor" href="#the-invariants" aria-label="Permalink to &quot;The invariants&quot;">​</a></h2><ol><li><strong>The state machine cannot skip states</strong> (above).</li><li><strong>Time and material are append-only.</strong> There is no update or delete operation for reported entries. Corrections are a vertical-level concern (a compensating entry), never a silent edit.</li><li><strong>The reporter is the principal.</strong> Time entries record <code>ctx.principal</code> as the technician; material lines record the reporter. Attribution comes from the ambient context, not from the input — a caller cannot claim someone else did the work.</li><li><strong>Every mutation emits a fat event</strong> (<a href="./events.html">events</a>).</li></ol><h2 id="completion-the-pricing-boundary" tabindex="-1">Completion: the pricing boundary <a class="header-anchor" href="#completion-the-pricing-boundary" aria-label="Permalink to &quot;Completion: the pricing boundary&quot;">​</a></h2><p><code>complete</code> is where the engine meets money, and the boundary is precise: <strong>the vertical prices, the engine freezes.</strong></p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> stub.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">invoke</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workorder/complete&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  orderId,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  billable: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      article: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;TIME&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, description: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Servicetekniker&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, qty: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;2.5&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, unit: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;h&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      unitPrice: </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">moneyOf</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;850&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;SEK&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">), lineTotal: </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">moneyOf</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;2125&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;SEK&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      sourceType: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;time&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, sourceId: timeEntryId,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // ...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p>Each billable line carries <strong>provenance</strong> (<code>sourceType: &#39;time&#39; | &#39;material&#39;</code>, <code>sourceId</code>) back to the reported entry it prices. The engine validates the lines (Zod + <a href="/concepts/money.html">exact decimal arithmetic</a>), sums the total with <code>addMoney</code>, transitions the order, and emits <code>workorder.completed</code> with the full billable snapshot in the payload.</p><p>The engine never <em>derives</em> a price from a reported entry — it has no rates to derive from. It checks that what it was handed is arithmetically sound, then makes it permanent.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/workorder/model.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const model = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  model as default
};
