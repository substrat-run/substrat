import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/dev-issuer","description":"","frontmatter":{},"headers":[],"relativePath":"reference/dev-issuer.md","filePath":"reference/dev-issuer.md","lastUpdated":1787484668000}');
const _sfc_main = { name: "reference/dev-issuer.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-dev-issuer" tabindex="-1">@substrat-run/dev-issuer <a class="header-anchor" href="#substrat-run-dev-issuer" aria-label="Permalink to &quot;@substrat-run/dev-issuer&quot;">​</a></h1><p>A local <strong>OpenID Connect provider</strong> you sign into by picking a name — so a vertical needs no dev-only auth branch.</p><p>Point a vertical at it and the vertical is an ordinary relying party running the login its users will run. Point it at Auth0, Keycloak, the <code>auth-server</code> demo issuer or your own issuer instead, and nothing in the vertical changes but <code>OIDC_ISSUER</code>. That is the whole design: <strong>dev and production differ by configuration, not by code path.</strong></p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">substrat-dev-issuer</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --personas</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> src/personas.ts</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # :8879, or $ISSUER_PORT</span></span></code></pre></div><div class="danger custom-block"><p class="custom-block-title">Never deploy it</p><p>Its signing key is checked into the repository, deliberately — see <a href="#why-the-key-is-public">Why the key is public</a>. Nothing but a loopback relying party may trust it.</p></div><h2 id="the-problem-it-removes" tabindex="-1">The problem it removes <a class="header-anchor" href="#the-problem-it-removes" aria-label="Permalink to &quot;The problem it removes&quot;">​</a></h2><p>The obvious way to move between users locally is a header the server believes: <code>x-principal: greta</code>. It is a bad trade twice over.</p><p>It is an <strong>impersonation bypass</strong> — a caller naming whoever they like — sitting inside the thing you deploy, one environment variable from being live. And it <strong>forks the auth path</strong>: the vertical grows a second way to resolve a caller, its SPA grows a second mode to talk to it, and the login exercised all day in dev is one no deployment ever runs. The real login is then only ever tested in production, which is the same defect class as a route that exists in <code>server.ts</code> and 404s in <code>worker.ts</code>.</p><p>Moving the picker <em>out</em> of the vertical and into an issuer removes both at once. The picker still exists — it is just on the other side of a real OIDC redirect.</p><h2 id="what-it-is" tabindex="-1">What it is <a class="header-anchor" href="#what-it-is" aria-label="Permalink to &quot;What it is&quot;">​</a></h2><ul><li><strong>A real provider.</strong> Discovery, JWKS, Authorization Code + PKCE, a signed ID token with <code>nonce</code>, RP-initiated logout. The single shortcut is that <code>/authorize</code> renders a list of people instead of a password field.</li><li><strong>Stateless.</strong> The authorization code <em>is</em> a short-lived JWT carrying subject, client, redirect URI, nonce and PKCE challenge. There is no code store and no session store.</li><li><strong>Loopback-only redirects.</strong> Anything but <code>http://localhost</code> / <code>127.0.0.1</code> / <code>[::1]</code> is refused unless explicitly allowed, so a stray instance cannot serve as an open redirector.</li><li><strong>Web-standard.</strong> <code>jose</code> + Web Crypto; no <code>node:*</code> in the issuer itself.</li></ul><p>Statelessness buys two things worth naming. Restarting invalidates nothing, so a dev server and a test script restart independently of each other. And with no SSO cookie the picker appears on <strong>every</strong> <code>/authorize</code> — which is exactly what you want here: switching user is one click, and <code>prompt=select_account</code> needs no special handling because it is already the only behaviour.</p><h2 id="endpoints" tabindex="-1">Endpoints <a class="header-anchor" href="#endpoints" aria-label="Permalink to &quot;Endpoints&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Path</th><th>What it does</th></tr></thead><tbody><tr><td><code>/.well-known/openid-configuration</code></td><td>discovery; <code>issuer</code> derives from the request origin unless pinned</td></tr><tr><td><code>/authorize</code></td><td>without <code>sub</code>, the picker; with a known <code>sub</code>, issues the code</td></tr><tr><td><code>/token</code></td><td><code>authorization_code</code> only; PKCE <strong>is</strong> verified, the client secret is not</td></tr><tr><td><code>/jwks.json</code></td><td>the one public signing key</td></tr><tr><td><code>/userinfo</code></td><td>the persona&#39;s claims behind a bearer</td></tr><tr><td><code>/logout</code></td><td>RP-initiated; honours <code>post_logout_redirect_uri</code></td></tr><tr><td><code>/dev/token</code></td><td>mints tokens for a <code>sub</code> with no browser</td></tr><tr><td><code>/dev/personas</code></td><td>the cast, as JSON</td></tr></tbody></table><p>The client secret is accepted and <strong>not</strong> checked. An issuer that registers no clients has no secret to compare against, and pretending otherwise would mean inventing the registration step this exists to remove. PKCE is checked, because it is what binds an exchange to the browser that began it — and the platform&#39;s relying party always sends it.</p><h2 id="the-personas-file" tabindex="-1">The personas file <a class="header-anchor" href="#the-personas-file" aria-label="Permalink to &quot;The personas file&quot;">​</a></h2><p><code>--personas</code> names a module exporting <code>PERSONAS: DevPersona[]</code>. Point it at the vertical&#39;s own file, so the picker&#39;s cast and the identity links the seed writes are the same array and cannot drift:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> type</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { DevPersona } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/dev-issuer&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> PERSONAS</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> DevPersona</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[] </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  { sub: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;dev|greta&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,  name: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Greta&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,  email: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;greta@example.test&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,  note: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;workshop-admin&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  { sub: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;dev|mans&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,   name: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Måns&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,   email: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;mans@example.test&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,   note: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;mechanic&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  { sub: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;dev|rutger&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, name: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Rutger&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, email: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;rutger@other.test&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,   note: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;a DIFFERENT shop&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">];</span></span></code></pre></div><p><code>sub</code> is the join between the two halves. The issuer asserts it; your seed binds it to a principal with <a href="/concepts/identity.html"><code>linkIdentity</code></a>, naming the tenant and scope that persona lives in. That link is what carries Rutger into the other tenant, so the cross-tenant beat still runs with no persona table anywhere in the server.</p><p>Keep <code>sub</code> values stable and readable. They end up in a directory that outlives a restart, and someone reading <code>_substrat_identities</code> should be able to tell who <code>dev|greta</code> is.</p><p>Note what is <strong>not</strong> in that file: no roles, no employee ids, no &quot;which customer this person belongs to&quot;. Those are domain facts, and they belong to operations the vertical already has — a <code>whoami</code>, a scope&#39;s own tables. A persona cast that carries them makes them true locally and guesswork everywhere else.</p><h2 id="the-relying-party-half" tabindex="-1">The relying-party half <a class="header-anchor" href="#the-relying-party-half" aria-label="Permalink to &quot;The relying-party half&quot;">​</a></h2><p><code>devLogin</code> is the other side, for a vertical&#39;s dev server. It performs the same two steps a hosted deployment does — verify the request against the issuer, then resolve the subject through the identity directory — written once rather than per vertical, since it is security-relevant glue:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { devLogin } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/dev-issuer&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> login</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> devLogin</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({ directory: host.admin, actor: staff, provider: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;oidc:dev-issuer&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> });</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">app.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">on</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">([</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;GET&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;POST&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">], </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;/api/auth/*&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">c</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> login.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">handle</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(c.req.raw));</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> caller</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> login.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">caller</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(req.headers);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// → { principal, tenantId, scopeId, sub, display } | null</span></span></code></pre></div><p><code>caller()</code> accepts a session cookie or a bearer, so a browser and a curl script take the same path. It deliberately does <strong>not</strong> decide what &quot;nobody&quot; means: one vertical answers 401, another checks whether its owner seat is unclaimed and answers <code>needs-setup</code> instead. That is policy, so it returns <code>null</code>.</p><p><code>subject()</code> returns the issuer&#39;s answer <em>before</em> any directory lookup, for verticals whose tenant is not the directory&#39;s to choose — a multi-venue app knows which tenant to ask about.</p><div class="tip custom-block"><p class="custom-block-title">A proxy in front of the API must not rewrite Host</p><p>A dev server derives its OIDC <code>redirect_uri</code> from the request origin, which under a Vite proxy is the <em>browser&#39;s</em> origin — that is what lands the callback back on your app. Setting <code>changeOrigin</code> on that proxy sends it to the API port instead, and login breaks.</p></div><h2 id="without-a-browser" tabindex="-1">Without a browser <a class="header-anchor" href="#without-a-browser" aria-label="Permalink to &quot;Without a browser&quot;">​</a></h2><p>Tests, curl and headless verification mint tokens directly:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">curl</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -XPOST</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> localhost:8879/dev/token</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;{&quot;sub&quot;:&quot;dev|greta&quot;}&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># → { id_token, access_token, token_type: &quot;Bearer&quot;, expires_in }</span></span></code></pre></div><p>This <em>is</em> impersonation, and its address is the point. It lives in a process that binds to localhost and is never deployed, rather than behind a flag in the artifact that ships — where the same capability reaches production and only an environment variable stands between it and a cross-tenant hole.</p><h2 id="why-the-key-is-public" tabindex="-1">Why the key is public <a class="header-anchor" href="#why-the-key-is-public" aria-label="Permalink to &quot;Why the key is public&quot;">​</a></h2><p>A relying party verifies an ID token against the issuer&#39;s JWKS, so the issuer needs a keypair. Generating one at boot would work, and would invalidate every session a developer holds on every restart — along with every <code>/dev/token</code> bearer a test run is halfway through using.</p><p>So the key is <strong>fixed</strong>, and fixed means <strong>public</strong>: it is a file in a public repository, and anyone can mint a token this issuer&#39;s JWKS will validate. That is the correct posture for a process that binds to localhost, hands out logins by clicking a name, and is never deployed.</p><p>It is also precisely why no production relying party may be pointed at it. The trust anchor is <code>packages/dev-issuer/src/keys.ts</code>.</p><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Pre-release (0.x): interfaces change without notice. <code>hono</code> is a peer dependency.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/dev-issuer.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const devIssuer = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  devIssuer as default
};
