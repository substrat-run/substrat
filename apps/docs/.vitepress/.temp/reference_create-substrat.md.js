import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"create-substrat","description":"","frontmatter":{},"headers":[],"relativePath":"reference/create-substrat.md","filePath":"reference/create-substrat.md","lastUpdated":1786535775000}');
const _sfc_main = { name: "reference/create-substrat.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="create-substrat" tabindex="-1">create-substrat <a class="header-anchor" href="#create-substrat" aria-label="Permalink to &quot;create-substrat&quot;">​</a></h1><p>The initializer — scaffolds a vertical that installs, runs, and already carries the rules your AI editor needs.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">di</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">r</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> substrat</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        # scaffold into the current directory</span></span></code></pre></div><p><a href="/guide/getting-started.html">Getting started</a> builds the smallest real thing by hand, package by package, to show what the pieces are. This is the other entry point: the full project shape in one command, for when you want to build an actual vertical rather than learn the layers.</p><h2 id="what-it-writes" tabindex="-1">What it writes <a class="header-anchor" href="#what-it-writes" aria-label="Permalink to &quot;What it writes&quot;">​</a></h2><p><strong>The vertical skeleton</strong> — <code>src/manifest.ts</code>, <code>migrations.ts</code>, <code>module.ts</code>, <code>provision.ts</code>, <code>seed.ts</code>, a Node <code>server.ts</code> for local work, a <code>worker.ts</code> that mounts the platform contract through <a href="/reference/vertical-host.html"><code>@substrat-run/vertical-host</code></a>, and <code>test/scenario.test.ts</code>.</p><p><strong>The instruction layer</strong> — <code>AGENTS.md</code> (the rules) and <code>.substrat/playbook.md</code> (the build flow), plus a command stub per tool so Claude Code, Cursor and opencode all read the <em>same</em> two files. This is the point of the scaffolder: the skeleton is small, but an agent that already knows the <a href="/guide/architecture.html">three-layer rule</a> and the <a href="/concepts/modules.html">module-code rules</a> is what makes the next hour productive.</p><p><strong>The generated configs</strong> — <code>package.json</code>, <code>tsconfig.json</code>, <code>vitest.config.ts</code>, <code>.gitignore</code> and a project <code>README.md</code>. The <code>package.json</code> carries the <code>substrat</code> block that <a href="/reference/cli.html"><code>substrat push</code></a> reads — the permission surface it diffs at the promotion checkpoint, and the runtime needs it derives deploy config from. You never author wrangler config.</p><h2 id="then-the-agent-builds" tabindex="-1">Then the agent builds <a class="header-anchor" href="#then-the-agent-builds" aria-label="Permalink to &quot;Then the agent builds&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">di</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">r</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;&amp; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span></span></code></pre></div><p>Open the project in your AI editor and start the flow — <code>/substrat</code> in Claude Code, the <code>new-vertical</code> command in Cursor or opencode. See <a href="/guide/ai-agents.html">building for AI agents</a> for what that loop looks like and why the rules live in the repo rather than in a prompt.</p><h2 id="dependency-free-by-design" tabindex="-1">Dependency-free by design <a class="header-anchor" href="#dependency-free-by-design" aria-label="Permalink to &quot;Dependency-free by design&quot;">​</a></h2><p>Node built-ins only, no build step, no runtime dependencies — so the initializer can never break at install time.</p><div class="warning custom-block"><p class="custom-block-title"><code>substrat</code> on npm is not Substrat</p><p>It is an unrelated HTML5 build system published in 2013. Substrat&#39;s packages are all under the <a href="https://www.npmjs.com/org/substrat-run" target="_blank" rel="noreferrer"><code>@substrat-run</code></a> scope. Never <code>npm install substrat</code>.</p></div></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/create-substrat.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const createSubstrat = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  createSubstrat as default
};
