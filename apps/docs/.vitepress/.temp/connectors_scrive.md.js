import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Scrive (e-signing)","description":"","frontmatter":{},"headers":[],"relativePath":"connectors/scrive.md","filePath":"connectors/scrive.md","lastUpdated":1787127415000}');
const _sfc_main = { name: "connectors/scrive.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="scrive-e-signing" tabindex="-1">Scrive (e-signing) <a class="header-anchor" href="#scrive-e-signing" aria-label="Permalink to &quot;Scrive (e-signing)&quot;">​</a></h1><p>Turns a signature request from the <a href="/engines/protocol/">protocol engine</a> into a real signing flow at <strong>Scrive</strong>, authenticated with Swedish <strong>BankID</strong>.</p><div class="warning custom-block"><p class="custom-block-title">Published (0.1.15), with two honest caveats a consumer must own</p><p>Both halves work and it now ships as a public package: a request becomes a started Scrive document (<strong>outbound</strong>, verified against <code>api-testbed.scrive.com</code>), and a completed signature is recorded back into the scope (<strong>inbound</strong>, via <code>reconcileScriveDispatch</code> on the <a href="/connectors/#the-seam-a-connector-plugs-into">authority seam</a>). It is a <code>0.x</code> release, which already signals an unstable surface, and two caveats travel with it — neither in the connector code: <strong>the consuming vertical must schedule the poll on a timer</strong> (<code>sweepScriveReconciliations</code> via <code>startPlatformSweeper</code> on node, or <code>definePlatformSweeperDO</code> — a self-re-arming Durable Object alarm in <code>@substrat-run/adapter-cloudflare</code> — on Workers; see the <a href="https://github.com/substrat-run/substrat/blob/main/docs/architecture/scheduler.md" target="_blank" rel="noreferrer">scheduler</a>), and <strong>BankID-to-sign is disabled on the testbed account</strong>, so the real signing round-trip is unverified. See <a href="#what-s-missing">What&#39;s missing</a>.</p></div><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Provider</strong></td><td>Scrive eSign, <code>se_bankid</code> authentication-to-sign</td></tr><tr><td><strong>Category</strong></td><td>E-signing &amp; identity</td></tr><tr><td><strong>Status</strong></td><td>Published <code>0.1.15</code> — both halves built (outbound + return path + poll driver); two caveats: the vertical must schedule the poll, and BankID is off on the testbed</td></tr><tr><td><strong>Package</strong></td><td><code>@substrat-run/connector-scrive</code> (published, <code>0.1.15</code>, public)</td></tr><tr><td><strong>Consumes</strong></td><td><code>protocol.signatures-requested</code></td></tr><tr><td><strong>Registered with</strong></td><td><code>registerConnector(&#39;scrive&#39;, &#39;protocol.signatures-requested&#39;, …)</code></td></tr></tbody></table><h2 id="what-it-consumes" tabindex="-1">What it consumes <a class="header-anchor" href="#what-it-consumes" aria-label="Permalink to &quot;What it consumes&quot;">​</a></h2><p>The <a href="/engines/protocol/model.html">protocol engine</a> emits <code>protocol.signatures-requested</code> when a vertical freezes a document and sends it for signature. This connector only answers when the event&#39;s <code>method</code> is <code>scrive</code> — a vertical asking for BankID through another provider emits the same event, and this must not answer for it.</p><p>The payload it reads is <strong>fat by design</strong> — a connector cannot read the vertical&#39;s tables, so everything it needs travels on the event: the template key and version, the content hash, the bound document hash, and the parties (each with a label, a <code>principal</code> / <code>external</code> kind, and a <code>primary</code> / <code>counter</code> signature kind).</p><h2 id="the-credential" tabindex="-1">The credential <a class="header-anchor" href="#the-credential" aria-label="Permalink to &quot;The credential&quot;">​</a></h2><p>The connection stores Scrive&#39;s <strong>OAuth1 personal access credentials</strong> — four parts:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{ </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;clientId&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;…&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;clientSecret&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;…&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;tokenId&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;…&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;tokenSecret&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;…&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span></code></pre></div><p>sealed at rest by the host&#39;s <code>SecretBox</code>. They combine into a PLAINTEXT signature header on every call (<code>oauth_signature=&quot;&lt;clientSecret&gt;&amp;&lt;tokenSecret&gt;&quot;</code>); there is no token exchange.</p><div class="tip custom-block"><p class="custom-block-title">Verified against reality</p><p>The connector was first written for OAuth2 bearer tokens, from the docs. A live call to the testbed rejected that immediately — Scrive&#39;s UI labels &quot;Client credentials&quot; and &quot;Token credentials&quot; are two <em>halves</em> of one credential, not two schemes. This is the <a href="/connectors/">connector seam&#39;s</a> &quot;green means ready to check, never verified&quot; caveat cashing out, and why the connector carries an opt-in test that runs against <code>api-testbed.scrive.com</code> when credentials are present.</p></div><p><strong>A personnummer is never stored.</strong> When a party signs with BankID, their Swedish personal number is passed <em>through</em> to Scrive on the signing request and kept nowhere: it is <code>direct</code> PII, and the protocol engine deliberately records an opaque <code>DataSubjectId</code> as the signatory instead. The provider needs the number; our tables must not have it.</p><h2 id="the-flow" tabindex="-1">The flow <a class="header-anchor" href="#the-flow" aria-label="Permalink to &quot;The flow&quot;">​</a></h2><p>Given a <code>scrive</code> signature request, the connector:</p><ol><li><strong>Renders an attestation sheet</strong> — a one-page PDF naming the template, the parties, and the content hash the signature refers to. This is <strong>not the contract</strong>: rendering the real avtal belongs to the vertical that owns its content (see <a href="#what-s-missing">What&#39;s missing</a>). The PDF writer is dependency-free and web-standard, and its text encoder <em>throws</em> on any character it cannot represent rather than substitute silently — PDF text is WinAnsi, where an unmapped character otherwise turns an em-dash into a euro sign on a document someone is about to sign.</li><li><strong>Creates the document</strong> — <code>POST /api/v2/documents/new</code>.</li><li><strong>Attaches the file</strong> — <code>POST …/setfile</code>. Separate from creation in Scrive&#39;s own API, which is what makes the no-file creation step possible.</li><li><strong>Sets the parties</strong> — <code>POST …/update</code>, mapping each <code>external</code> party to <code>se_bankid</code> and each <code>principal</code> party to <code>standard</code>. An external signatory (a new hire on their first day, with no account) authenticates with BankID; the issuing principal need not.</li><li><strong>Sets a capability callback URL</strong> — an unguessable secret in the path, because Scrive&#39;s callbacks carry <strong>no signature to verify</strong>, so a callback can only ever be a <em>hint</em> to re-read the document, never a trusted fact.</li><li><strong>Starts it</strong> — <code>POST …/start</code>. The document is now <code>pending</code> and the parties are invited.</li><li><strong>Records the dispatch</strong> in a directory-side ledger (<code>putConnectorState</code>, keyed by the connection), carrying the document id, the frozen content hash, and each party&#39;s request id and signatory ref. Two jobs: a redelivery finds this row and <strong>skips</strong> instead of sending a second document, and the poll driver reads it to map a signed party back to the request it resolves. Directory-side because a connector runs <em>inside</em> the scope&#39;s dispatch and re-entering the scope actor deadlocks.</li></ol><p>Retry is tuned for a contract, not a directory write: <strong>8 attempts</strong>, 5-second base backoff, up to a 15-minute ceiling. Giving up on a signature after the executor default of five tries would be giving up on a contract.</p><h2 id="reaching-the-signature-back" tabindex="-1">Reaching the signature back <a class="header-anchor" href="#reaching-the-signature-back" aria-label="Permalink to &quot;Reaching the signature back&quot;">​</a></h2><p>Once a party signs, Scrive changes the document status, and the connector records that signature onto the protocol instance in the scope. Two ways to learn of the change; this connector is built for the first, and the second is a later optimization on the <em>same</em> write-back:</p><ul><li><strong>Polling</strong> — <code>GET /api/v2/documents/{id}/get</code> returns the full document and its status. This needs no ingress at all, which is why webhook transport (<a href="https://github.com/substrat-run/substrat/issues/96" target="_blank" rel="noreferrer">#96</a>) is <em>not</em> on the critical path.</li><li><strong>Webhooks</strong> — Scrive can POST on status change, but unauthenticated, so the callback URL is a capability and the body is never trusted. A webhook is an optimization over polling, not a replacement for it — even with one, the handler re-reads <code>…/get</code> and runs the same reconcile.</li></ul><p>The write-back goes through the <a href="/connectors/#the-seam-a-connector-plugs-into">inbound authority seam</a>: <code>reconcileScriveDispatch</code> opens a scope stub with <code>getConnectorScope</code> — the connection acting as itself — and records the signature by invoking <code>protocol/record-signature</code>, gated on the connection&#39;s own <code>protocol:record-signature</code> grant (visible in the permission diff). It runs as a <strong>top-level operation, outside any dispatch</strong>, so re-entering the scope is safe; it re-checks the provider-reported content hash against the frozen one and fails closed on a mismatch; and it is idempotent across polls. <code>sweepScriveReconciliations</code> is the poll driver over it — it enumerates the dispatch ledger (<code>listConnectorState</code>) and reconciles every outstanding instance.</p><p>The <strong>timer</strong> that calls the sweep now exists for both runtimes: <code>startPlatformSweeper</code> (node, a self-rescheduling interval — live in the Meridian demo server) and <code>definePlatformSweeperDO</code> (<code>@substrat-run/adapter-cloudflare</code>, a self-re-arming Durable Object alarm — chosen over a cron because a vertical pushed into a Workers-for-Platforms dispatch namespace gets no <code>triggers.crons</code>). Both drive the same <code>runPlatformSweep</code> (the kernel&#39;s <a href="https://github.com/substrat-run/substrat/blob/main/docs/architecture/scheduler.md" target="_blank" rel="noreferrer">scheduler</a> unit of work), and the control-plane worker is deliberately not the home, because its scope DO is module-less. The call site belongs in the vertical&#39;s own runtime — what remains is a <em>deployed</em> vertical wiring one up with a live connection.</p><h2 id="what-s-missing" tabindex="-1">What&#39;s missing <a class="header-anchor" href="#what-s-missing" aria-label="Permalink to &quot;What&#39;s missing&quot;">​</a></h2><p>Most of the gaps found by building it have since closed. What is done, and what is left:</p><ol><li><s><strong>Recording the provider&#39;s document id / dispatch idempotency.</strong></s> <strong>Done.</strong> A redelivery once created a <em>second</em> Scrive document — duplicate legal paperwork to real signatories, the sharp correctness problem. The connector now records each dispatch in a directory-side ledger and skips if a prior one is found.</li><li><s><strong>Recording a signature.</strong></s> <strong>Done</strong> via the <a href="https://github.com/substrat-run/substrat/issues/97" target="_blank" rel="noreferrer">authority seam (#97)</a>: <code>reconcileScriveDispatch</code> records it as the connection, through <code>getConnectorScope</code>.</li><li><s><strong>Connector state has no home.</strong></s> <strong>Done</strong> — the dispatch ledger above is that home (<code>putConnectorState</code> / <code>listConnectorState</code>).</li><li><strong>No document store.</strong> There is still no place for rendered document <em>bytes</em>, which is why this sends an attestation sheet rather than the avtal, and why the real contract&#39;s rendering waits on the vertical plus a store.</li><li><s><strong>Nothing schedules the poll.</strong></s> <strong>Done — both triggers ship.</strong> <code>startPlatformSweeper</code> drives the sweep on node (wired in the Meridian demo server), and <code>definePlatformSweeperDO</code> (<code>@substrat-run/adapter-cloudflare</code>) drives it on Workers: a singleton Durable Object whose alarm runs one <code>runPlatformSweep</code> pass and re-arms only after the pass settles — non-overlap by construction, and it works inside a Workers-for-Platforms dispatch namespace, where crons do not (<a href="https://github.com/substrat-run/substrat/issues/96" target="_blank" rel="noreferrer">#96</a>, the poll path). For a hosted CP-less vertical the platform runs the whole pass (<a href="https://github.com/substrat-run/substrat/issues/574" target="_blank" rel="noreferrer">#574</a>): the control plane&#39;s scheduled sweep enumerates ITS connection directory (phase 1), the webhook ingress terminates on the control plane (phase 2), and outbound dispatch rides platform-requests (phase 3) — the vertical registers the connector as usual, its host routes each delivery as a <code>connector:scrive</code> intent, and the platform executes the same handler with the credential and egress the vertical must never hold.</li><li><strong>BankID-to-sign is disabled on the testbed account</strong>, so <code>start</code> returns 409 for <code>se_bankid</code> and the real BankID signing round-trip (and Scrive&#39;s live <code>get</code> party shape and order) cannot be verified yet. The live test uses <code>standard</code> auth until it is enabled.</li></ol><p>Because the return path landed, the connector no longer takes a required write-back callback: the callback URL is now <strong>optional</strong> (polling is a complete strategy on its own), and where it is set, a callback is only ever a hint to re-read — never a trusted fact.</p><h2 id="testing-without-an-account" tabindex="-1">Testing without an account <a class="header-anchor" href="#testing-without-an-account" aria-label="Permalink to &quot;Testing without an account&quot;">​</a></h2><p><code>ScriveMock</code> implements the documented endpoints in memory, so the whole outbound lifecycle runs without a Scrive account. What it proves is that <em>our shape</em> works — credential resolution, egress, the document lifecycle, retry. It <strong>cannot</strong> prove our reading of Scrive&#39;s API is correct, because it <em>is</em> that reading: same author, same misunderstandings, on both sides of the call. A green suite means &quot;ready to check against <code>api-testbed.scrive.com</code>&quot;, never &quot;verified.&quot;</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("connectors/scrive.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const scrive = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  scrive as default
};
