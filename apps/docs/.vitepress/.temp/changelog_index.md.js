import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Changelog","description":"A weekly digest of everything merged into Substrat — what changed, why it matters to someone building on it, and which package versions carry it.","frontmatter":{"title":"Changelog","description":"A weekly digest of everything merged into Substrat — what changed, why it matters to someone building on it, and which package versions carry it."},"headers":[],"relativePath":"changelog/index.md","filePath":"changelog/index.md","lastUpdated":1787735456000}');
const _sfc_main = { name: "changelog/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h1><p>Substrat ships continuously. Every Monday this page gains an entry for the week just ended: the handful of changes that alter how you build on the platform, written out in full, and a complete accounting of everything else that landed.</p><p>Weeks are listed in the sidebar, newest first.</p><h2 id="what-this-is-not" tabindex="-1">What this is not <a class="header-anchor" href="#what-this-is-not" aria-label="Permalink to &quot;What this is not&quot;">​</a></h2><p><strong>It is not the package changelogs.</strong> Every published package carries its own <code>CHANGELOG.md</code>, generated by <a href="https://github.com/changesets/changesets" target="_blank" rel="noreferrer">changesets</a> from the changeset each PR declares. Those answer a precise, narrow question — <em>what changed in <code>@substrat-run/kernel</code> between 0.86.0 and 0.87.0</em> — and they answer it per package, in isolation, which is exactly what you want when a version bump breaks your build.</p><p>This answers a different one: <em>what happened, and does it change how I write a vertical?</em> A single week&#39;s work usually crosses the kernel, the contracts, two engines, the host and the scaffold at once, and no per-package changelog can show you that shape. Each entry closes with the version spans the week produced, so the two views meet.</p><p><strong>It is not release notes.</strong> Substrat is pre-1.0. Interfaces move without a deprecation window, and an entry will say so plainly when they do rather than phrase a break as a feature.</p><h2 id="how-an-entry-is-put-together" tabindex="-1">How an entry is put together <a class="header-anchor" href="#how-an-entry-is-put-together" aria-label="Permalink to &quot;How an entry is put together&quot;">​</a></h2><p>Highlights are written, not generated. A digest is an editorial act, and the repo&#39;s rule for generated files — a <code>.generated</code> suffix, a header naming the producer, and a <code>--check</code> re-emit in CI — is deliberately not claimed for prose that no producer could re-emit. A file that <em>says</em> it is generated while nothing regenerates it is worse than one that admits an author.</p><p>What <em>is</em> mechanical is completeness. <code>pnpm lint:changelog --check</code> reads each entry&#39;s declared <code>range</code>, walks <code>git log</code> for the merges inside it, and fails if any of them goes uncited. An author who drops thirty PRs out of ninety produces a page that reads as finished and is not, and the only way to catch that by hand is to redo the work.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("changelog/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
