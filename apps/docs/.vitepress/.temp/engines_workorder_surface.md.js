import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse(`{"title":"Operations, functions & permissions","description":"The work-order engine's two surfaces: operations invoked through a scope stub, and in-scope functions a vertical calls inside its own transaction — plus its permission keys and entitlement.","frontmatter":{"description":"The work-order engine's two surfaces: operations invoked through a scope stub, and in-scope functions a vertical calls inside its own transaction — plus its permission keys and entitlement."},"headers":[],"relativePath":"engines/workorder/surface.md","filePath":"engines/workorder/surface.md","lastUpdated":1787693422000}`);
const _sfc_main = { name: "engines/workorder/surface.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="operations-functions-permissions" tabindex="-1">Operations, functions &amp; permissions <a class="header-anchor" href="#operations-functions-permissions" aria-label="Permalink to &quot;Operations, functions &amp; permissions&quot;">​</a></h1><p>An engine has <strong>no endpoints</strong>. It exposes two surfaces, and the difference between them is the whole extension model:</p><ul><li><strong>Operations</strong> — named handlers invoked through a scope stub. Each is a default binding that does its own permission check.</li><li><strong>In-scope functions</strong> — plain exports a vertical calls <em>inside its own operation</em>, in the same transaction, where the vertical owns the permission check.</li></ul><p>HTTP is derived, never authored: a manifest may point at an emitted OpenAPI spec via its <code>api</code> field. Nothing in the engine speaks HTTP.</p><h2 id="operations" tabindex="-1">Operations <a class="header-anchor" href="#operations" aria-label="Permalink to &quot;Operations&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Operation</th><th>Permission</th><th>Does</th></tr></thead><tbody><tr><td><code>workorder/get</code></td><td><code>workorder:read</code> (per-entity)</td><td>one order with its time and material</td></tr><tr><td><code>workorder/list</code></td><td><code>workorder:read</code></td><td>orders, optionally by status</td></tr><tr><td><code>workorder/assign</code></td><td><code>workorder:assign</code></td><td>assign a technician (order stays <code>planned</code>)</td></tr><tr><td><code>workorder/start</code></td><td><code>workorder:report</code></td><td><code>planned</code> → <code>in_progress</code></td></tr><tr><td><code>workorder/report-time</code></td><td><code>workorder:report</code></td><td>append a time entry</td></tr><tr><td><code>workorder/report-material</code></td><td><code>workorder:report</code></td><td>append a material line</td></tr><tr><td><code>workorder/complete</code></td><td><code>workorder:complete</code></td><td>freeze billable lines, <code>in_progress</code> → <code>completed</code></td></tr><tr><td><code>workorder/close</code></td><td><code>workorder:close</code></td><td><code>completed</code> → <code>closed</code></td></tr></tbody></table><p><code>workorder/get</code> checks per-entity (<code>ctx.check(PERM.read, orderRef(id))</code>), which is what makes the customer-portal walk work — see <a href="./composing.html#portal-reads">Composing</a>.</p><div class="tip custom-block"><p class="custom-block-title">There is no <code>workorder/create</code> operation — and that is the design</p><p>The engine registers no <code>create</code>. Creation is the in-scope function <code>createWorkOrder(ctx, …)</code> only, because <strong>the vertical must price and label the order first</strong>: it arrives from a felanmälan, a booking, a ticket. <code>demos/callout</code> reaches it through <code>callout/create-workorder</code>.</p><p>The hole is deliberate and load-bearing — the engine owns the state machine, the vertical owns vocabulary and pricing, and the engine leaves a gap exactly where the vertical belongs. It&#39;s also why an engines-only scope can&#39;t do anything on its own: <em>configuration is dynamic; composition is code.</em></p></div><h2 id="in-scope-functions" tabindex="-1">In-scope functions <a class="header-anchor" href="#in-scope-functions" aria-label="Permalink to &quot;In-scope functions&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { createWorkOrder, completeWorkOrder, PERM } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/engine-workorder&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Function</th><th>Backs</th><th>Notes</th></tr></thead><tbody><tr><td><code>createWorkOrder(ctx, input)</code></td><td><em>(no operation)</em></td><td>the deliberate hole above</td></tr><tr><td><code>completeWorkOrder(ctx, input)</code></td><td><code>workorder/complete</code></td><td>validates + freezes billable lines</td></tr><tr><td><code>closeWorkOrder(ctx, input)</code></td><td><code>workorder/close</code></td><td></td></tr><tr><td><code>listOrders(ctx, status?)</code></td><td><code>workorder/list</code></td><td></td></tr><tr><td><code>getReportedLines(ctx, orderId)</code></td><td><code>workorder/get</code></td><td>time + material for an order</td></tr></tbody></table><p>None of these check permissions — that is the caller&#39;s job, by design. Calling one from your own operation without <code>assertAllowed(await ctx.check(…))</code> first is a bug the linter won&#39;t catch for you.</p><p><strong>What they return is parsed, not asserted.</strong> Every value crossing back out of this engine goes through the schema the engine publishes — <code>workOrder</code>, <code>timeEntry</code>, <code>materialLine</code> — the same schema you point your own operation&#39;s <code>output</code> at. Engine surfaces evolve additively (D-28), but that rule is held by review; the parse is what makes the failure it prevents <em>loud</em>. A vertical compiled against 0.3 and running against 0.4, whose row shape moved, used to read a field that had become <code>null</code> and render it — now the read throws at the seam. Reads name their columns for the same reason: <code>SELECT *</code> would publish whatever the physical table happens to hold.</p><div class="warning custom-block"><p class="custom-block-title">Not every operation has an in-scope function yet</p><p><code>assign</code>, <code>start</code>, <code>report-time</code>, and <code>report-material</code> carry their logic <strong>inline in the operation handler</strong>, so there is no composable export behind them. A vertical that wants to report time <em>and</em> touch its own tables in one transaction cannot currently do it through the engine — the convention (&quot;engine operations are thin: a permission check plus one exported in-scope function&quot;) is not yet met here. Unlike the missing <code>create</code>, this asymmetry is an artifact, not a decision.</p></div><h2 id="permissions" tabindex="-1">Permissions <a class="header-anchor" href="#permissions" aria-label="Permalink to &quot;Permissions&quot;">​</a></h2><p>Declared in the manifest with descriptions — fuel for the <a href="/concepts/permissions.html">permission-review diff</a>:</p><table tabindex="0"><thead><tr><th>Key</th><th>Description</th></tr></thead><tbody><tr><td><code>workorder:create</code></td><td>Create work orders</td></tr><tr><td><code>workorder:read</code></td><td>Read work orders, time and material</td></tr><tr><td><code>workorder:assign</code></td><td>Assign a technician</td></tr><tr><td><code>workorder:report</code></td><td>Start work, report time and material</td></tr><tr><td><code>workorder:complete</code></td><td>Complete a work order (with billable lines)</td></tr><tr><td><code>workorder:close</code></td><td>Close a completed work order</td></tr></tbody></table><p><code>workorder:create</code> is declared even though no operation binds it: <code>createWorkOrder</code> is composed by verticals, which check this key themselves.</p><p>Typical role shapes: a <em>technician</em> gets <code>read</code> + <code>report</code>; a <em>coordinator</em> adds <code>create</code> + <code>assign</code> + <code>complete</code>; closing (the bookkeeping-facing act) can be reserved for back-office.</p><h2 id="entitlement" tabindex="-1">Entitlement <a class="header-anchor" href="#entitlement" aria-label="Permalink to &quot;Entitlement&quot;">​</a></h2><p><code>entitlementKey: &#39;workorder&#39;</code>. A tenant that doesn&#39;t hold the flag can&#39;t resolve these operations — checked per invoke, fails closed. It&#39;s a binary SKU gate, not configuration; see <a href="./composing.html#configuration">Composing</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("engines/workorder/surface.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const surface = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  surface as default
};
