import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"@substrat-run/contract-tests","description":"","frontmatter":{},"headers":[],"relativePath":"reference/contract-tests.md","filePath":"reference/contract-tests.md","lastUpdated":1787230840000}');
const _sfc_main = { name: "reference/contract-tests.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="substrat-run-contract-tests" tabindex="-1">@substrat-run/contract-tests <a class="header-anchor" href="#substrat-run-contract-tests" aria-label="Permalink to &quot;@substrat-run/contract-tests&quot;">​</a></h1><p>The <strong>conformance suite</strong> for scope-host adapters. Every adapter — pure SQLite, Cloudflare Durable Objects, and any future one — must pass this suite <strong>unchanged, forever</strong>. If an adapter needs the suite modified, the contract changed, and that is a decision, not a patch.</p><p>This is the mechanism behind the platform&#39;s central promise: the guarantees are properties of the substrate, and here is the substrate being tested for them. It is also what makes a <a href="/guide/comparisons.html">third adapter</a> a bounded job rather than an archaeology project — the suite <em>is</em> the specification of what an adapter must do.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -D</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/contract-tests</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> vitest</span></span></code></pre></div><h2 id="the-suites" tabindex="-1">The suites <a class="header-anchor" href="#the-suites" aria-label="Permalink to &quot;The suites&quot;">​</a></h2><p>The package exports test <em>suites</em> built on <a href="https://vitest.dev" target="_blank" rel="noreferrer">Vitest</a>; it runs nothing itself. Each adapter calls them from its own <code>test/</code> folder, passing a factory that returns a fresh host.</p><table tabindex="0"><thead><tr><th>suite</th><th>what it holds the adapter to</th><th>runs against</th></tr></thead><tbody><tr><td><code>scopeHostContractSuite</code></td><td>the scope-host contract — isolation, serialization, the spine, snapshots, connectors</td><td><code>UNSAFE_allowAllChecker</code>, so a failure is never a permission failure in disguise</td></tr><tr><td><code>permissionContractSuite</code></td><td>the tuple checker — roles, grants, orgs, identity, the audit and access logs</td><td>the <strong>default</strong> checker; the whole point is the real engine</td></tr><tr><td><code>scheduleContractSuite</code></td><td>registered schedules firing under the system actor</td><td>the <strong>default</strong> checker, so the system grant resolves for real</td></tr><tr><td><code>atomicContractSuite</code></td><td><code>ctx.atomic</code> — a caught engine error discards that region&#39;s writes and nothing else</td><td>the <strong>default</strong> checker; the K-34 assertion needs a real authorization</td></tr><tr><td><code>searchContractSuite</code></td><td>the derived FTS index — triggers, tokenizers, read-after-write, and a fork that searches without a re-index</td><td><code>UNSAFE_allowAllChecker</code>; what the index answers, not who may ask</td></tr></tbody></table><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// packages/adapter-yours/test/contract.test.ts</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  connectorTestFetch,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  permissionContractSuite,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  scheduleContractSuite,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  scopeHostContractSuite,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">} </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/contract-tests&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { UNSAFE_allowAllChecker, webCryptoSecretBox } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/kernel&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { YourScopeHost } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;../src/index.js&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">scopeHostContractSuite</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;adapter-yours&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> host</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> YourScopeHost</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    checker: UNSAFE_allowAllChecker,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    secretBox: </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">webCryptoSecretBox</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;test-key&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Uint8Array</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">32</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">).</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">fill</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">7</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    fetch: connectorTestFetch, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// the in-memory third party — see below</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  });</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  return</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { host, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cleanup</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> host.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">close</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() };</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">permissionContractSuite</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;adapter-yours&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* default checker */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> });</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">scheduleContractSuite</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;adapter-yours&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* default checker */</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> });</span></span></code></pre></div><p>Today that is <strong>253 assertions</strong>, and the two shipped adapters run every one of them.</p><h2 id="what-the-suites-verify" tabindex="-1">What the suites verify <a class="header-anchor" href="#what-the-suites-verify" aria-label="Permalink to &quot;What the suites verify&quot;">​</a></h2><p>The list has grown well past the original five guarantees. Grouped by what breaks if the adapter gets it wrong:</p><p><strong>Isolation and addressing</strong></p><ul><li>a write in one scope is invisible in another;</li><li>a mismatched <code>(tenantId, scopeId)</code> pair throws rather than resolving to someone else&#39;s data — on <code>getScope</code>, and on every admin-side path that takes the pair (K-3);</li><li>provisioning is idempotent, and refuses a scope under a tenant with no record.</li></ul><p><strong>Transaction semantics</strong></p><ul><li>10 concurrent read-await-write increments land on exactly 10 (K-6);</li><li>mutating an input after <code>invoke()</code>, or mutating a returned result, never affects scope state — the structured-clone boundary is real on both directions;</li><li>a throwing handler rolls back <em>everything</em> it did, including the events it emitted and the platform intents it enqueued (K-4).</li></ul><p><strong>The spine</strong></p><ul><li>tenant, scope, actor, ULID id and timestamp are stamped kernel-side, not by the caller;</li><li>a PII-classed event without a <code>subjectId</code> is rejected at emit;</li><li>every allowed check is stamped onto the event it authorized (K-34), and every <em>refused</em> check is recorded as a denial even though its operation rolled back (K-35);</li><li>reads of the directory and of the audit trail are themselves recorded in the access log — including reads <em>of the access log</em> — and never leak into the mutation trail;</li><li>the two-tier retention split: drain to Tier 2, then prune exactly what drained, and refuse to prune anything undrained (K-24).</li></ul><p><strong>Migrations</strong></p><ul><li>module migrations apply lazily and journal per <code>(module, version)</code>;</li><li>the host reports a migration frontier a woken scope is never behind;</li><li><code>migrateScope</code> on an up-to-date scope touches nothing.</li></ul><p><strong>Delivery</strong></p><ul><li>each event reaches an executor exactly once;</li><li>a failing executor does not fail the operation that emitted the event;</li><li>one poison delivery does not block the deliveries behind it;</li><li>exhausting attempts dead-letters the delivery <em>with the evidence kept</em>, and a dead-lettered delivery is terminal.</li></ul><p><strong>Export, fork, restore, snapshot</strong> — the machinery behind <a href="/concepts/snapshots.html">snapshots and previews</a></p><ul><li>a scope dump is complete: every introspected table is in it;</li><li>an import round-trips into a new scope, is an independent copy, and records its fork provenance;</li><li>a restore rewinds a diverged scope and it still <em>runs</em> afterwards — including the awkward cases: a dump missing kernel-spine tables, a child table sorting before its parent, a target already holding FK-related rows, and scope-level grants that must be re-pointed at the destination;</li><li><code>bindScopeVersion</code> snapshots a migration-changing rebind <strong>and only that</strong> — the digest is the gate;</li><li>the GC sweep reaps exactly the expired forks, and <code>gcSnapshots: false</code> skips it.</li></ul><p><strong>Erasure</strong> (<a href="/concepts/events.html">GDPR</a>)</p><ul><li>a shred redacts the payload and <strong>keeps the envelope</strong>, for the named subject only;</li><li>it is idempotent, and re-running still reports the tombstone;</li><li>after a shred what was sealed no longer opens, and nothing may be re-sealed;</li><li>a different subject in the same scope stays fully readable;</li><li>the erasure lands in <strong>both</strong> logs — the mutation trail and the evidence-destruction one.</li></ul><p><strong>Introspection and the SQL console</strong> (§5.4, #219)</p><ul><li>the table list flags the spine as system;</li><li><code>queryScope</code> runs a read-only <code>SELECT</code> (joins included, spine readable), caps the result and <em>reports</em> truncation rather than erroring, and rejects every write shape — leaving no trace when it does.</li></ul><p><strong>Connections and connectors</strong></p><ul><li>a connection is a subject that opens the door and confers nothing: no memberships, no roles, a grant is a separate act;</li><li>it cannot reach a scope outside its own tenant or vertical, and stops working the moment it is revoked;</li><li>credentials seal at rest and never come back out.</li></ul><p><strong>Permissions</strong> (the second suite)</p><ul><li>deny by default, reporting the checked permission and node;</li><li>tenant roles inherit downward into scopes, with a proof path;</li><li>scope roles stay confined to their scope;</li><li>entity-narrowed grants resolve through declared parent edges, and expire;</li><li><code>ctx.grant</code> delegates and <strong>cannot elevate</strong> — a caller may narrow only what it holds;</li><li>revoking a membership stops it granting but keeps it readable as evidence;</li><li>identity is keyed per tenant: the same <code>externalId</code> in two pools is two people, and a tenant-bound pool refuses to enumerate tenants.</li></ul><h2 id="connectortestfetch-—-the-third-party-in-memory" tabindex="-1"><code>connectorTestFetch</code> — the third party, in memory <a class="header-anchor" href="#connectortestfetch-—-the-third-party-in-memory" aria-label="Permalink to &quot;\`connectorTestFetch\` — the third party, in memory&quot;">​</a></h2><p>Connector conformance needs an outside world, and a suite that reached a real API would be neither hermetic nor able to produce the failure paths that matter. The package ships one:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { connectorCalls, connectorTestFetch, resetConnectorCalls } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@substrat-run/contract-tests&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre></div><p><code>connectorTestFetch</code> is a <code>fetch</code> the host can be constructed with; <code>connectorCalls</code> records what was attempted, so a test can assert on the request an adapter <em>made</em> as well as the result it returned.</p><h2 id="the-fixture-modules" tabindex="-1">The fixture modules <a class="header-anchor" href="#the-fixture-modules" aria-label="Permalink to &quot;The fixture modules&quot;">​</a></h2><p>The suites need modules to run, and they bring their own rather than borrowing a demo&#39;s — so a change to Callout can never quietly change what an adapter is held to:</p><table tabindex="0"><thead><tr><th>export</th><th>what it is for</th></tr></thead><tbody><tr><td><code>contractTestModules</code></td><td>the standard registration set most of the suite runs against</td></tr><tr><td><code>contractTestInitialModules</code></td><td>the pre-migration set, for the lazy-migration assertions</td></tr><tr><td><code>contractTestBareOps</code></td><td>operations with no migrations, for the paths that must not need any</td></tr><tr><td><code>brokenMod</code></td><td>a module whose handler throws — the rollback assertions</td></tr><tr><td><code>connectorMod</code></td><td>exercises the connector dispatch path</td></tr><tr><td><code>scheduleMod</code></td><td>a registered schedule, for the schedule suite</td></tr><tr><td><code>billedMod</code></td><td>emits meter readings</td></tr></tbody></table><h2 id="why-this-matters-if-you-never-write-an-adapter" tabindex="-1">Why this matters if you never write an adapter <a class="header-anchor" href="#why-this-matters-if-you-never-write-an-adapter" aria-label="Permalink to &quot;Why this matters if you never write an adapter&quot;">​</a></h2><p>This package is why your local test run means something. The pure-SQLite host your CI uses and the Cloudflare host your customers hit are held to identical, executable semantics — so &quot;works locally&quot; and &quot;holds in production&quot; are the same claim, and it is tested on every commit rather than asserted in a README.</p><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="/reference/adapter-sqlite.html"><code>@substrat-run/adapter-sqlite</code></a> — the local/CI/self-host host</li><li><a href="/reference/adapter-cloudflare.html"><code>@substrat-run/adapter-cloudflare</code></a> — the production one</li><li><a href="/concepts/scope-host.html">Operations &amp; the scope host</a> — the contract in prose</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/contract-tests.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const contractTests = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  contractTests as default
};
