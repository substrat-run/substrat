import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Manyfold (headless CMS)","description":"","frontmatter":{},"headers":[],"relativePath":"verticals/manyfold.md","filePath":"verticals/manyfold.md","lastUpdated":1785339849000}');
const _sfc_main = { name: "verticals/manyfold.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="manyfold-headless-cms" tabindex="-1">Manyfold (headless CMS) <a class="header-anchor" href="#manyfold-headless-cms" aria-label="Permalink to &quot;Manyfold (headless CMS)&quot;">​</a></h1><p><code>demos/manyfold</code> — a <strong>multi-scope headless CMS</strong>: content types authored as data, a draft → review → publish editorial lifecycle, append-only revisions, and a frozen published projection served by (type, slug) — one tenant running <strong>many sites, each its own scope</strong>, in a single content-studio app over one API.</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Manyfold is the demo that puts two ideas in one place: a <strong>content lifecycle</strong> and <strong>multi-scope tenancy where the scope <em>is</em> the site</strong>. A publishing studio runs several client sites from one account; each site is a scope, and the same login is a different principal — with a different role — in each. Café Nordlys, Padel Nordic and a law firm share one codebase and one tenant, and nothing published on one is visible from another.</p><p>Like <a href="/verticals/meridian.html">Meridian</a>, it is a <strong>shape-breaker: its core domain composes no engine at all.</strong> There is no CMS engine to lean on — the editorial state machine, append-only revisions, the freeze-on-publish hash, and reference resolution are all <strong>vertical code on the kernel</strong>. That is deliberate (decision 27): the engine extraction waits for a <em>second</em> content vertical to force it, the same &quot;second consumer, different shape&quot; signal every other engine was extracted on. So the invariants are written cleanly in the vertical now — a state machine that can&#39;t skip, revisions that only ever append, a projection kept consistent inside the publish transaction — positioned so the later extraction is mechanical rather than a rewrite. Until then, Manyfold is the proof that the kernel&#39;s guarantees (nested tenancy, permissions, the event spine, per-scope isolation) carry a whole domain with <strong>zero engine support</strong>.</p><p>It is interesting for reasons the field-service demos can&#39;t show:</p><ul><li><strong>Site = scope.</strong> One tenant, many scopes, and the multi-tenancy is <em>within</em> a single customer rather than across them. Provisioning creates one tenant and N sites; a login is granted a role per site, and reachability — not a <code>WHERE site = ?</code> filter someone remembered — is what keeps one site&#39;s content out of another.</li><li><strong>A lifecycle that freezes.</strong> Publishing takes a content hash over the exact revision and marks it immutable; any later edit is a <em>new</em> revision, never a mutation of history. The published projection serves that frozen body, so what a reader sees can&#39;t drift out from under the hash.</li><li><strong>Content types are data, not code.</strong> The four default types are seeded on first use; an admin authors new ones at runtime, and each save bumps the type&#39;s version. A <code>ref</code> field is resolved <strong>at delivery</strong> against the published projection — a link to a draft comes back as an explicit <em>unresolved</em> marker, a broken link shown honestly rather than hidden.</li></ul><h2 id="at-a-glance" tabindex="-1">At a glance <a class="header-anchor" href="#at-a-glance" aria-label="Permalink to &quot;At a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Package</strong></td><td><code>demos/manyfold</code> (private)</td></tr><tr><td><strong>Tenancy shape</strong></td><td>1 tenant — Nordlys Studio · <strong>3 sites, each its own scope</strong>: Café Nordlys · Padel Nordic · Lindqvist &amp; Ruiz</td></tr><tr><td><strong>Engines composed</strong></td><td><em>none</em> — the editorial lifecycle is vertical code on the kernel alone (decision 27; the CMS-engine extraction waits for a second content vertical)</td></tr><tr><td><strong>Own tables</strong></td><td><code>manyfold_entry</code> · <code>manyfold_revision</code> (append-only) · <code>manyfold_status_log</code> (append-only) · <code>manyfold_delivery</code> (published read model) · <code>manyfold_content_type</code> (types are data)</td></tr><tr><td><strong>Roles</strong></td><td><code>admin</code> (owner, tenant-wide) · <code>publisher</code> / <code>editor</code> / <code>author</code> / <code>viewer</code> (per site) — a role ladder, no entity-narrowed grants</td></tr><tr><td><strong>Permission surface</strong></td><td><a href="https://github.com/substrat-run/substrat/blob/main/demos/manyfold/PERMISSIONS.md" target="_blank" rel="noreferrer"><code>PERMISSIONS.md</code></a> — 5 keys · 5 roles</td></tr><tr><td><strong>App</strong></td><td>one content-studio React app over one API, with a dev persona + <strong>site</strong> picker (<code>x-principal</code> / <code>x-site</code>)</td></tr><tr><td><strong>Status</strong></td><td>Working — a 10-case scenario green on the pure-SQLite adapter</td></tr></tbody></table><h2 id="no-engine-and-why-that-s-the-point" tabindex="-1">No engine, and why that&#39;s the point <a class="header-anchor" href="#no-engine-and-why-that-s-the-point" aria-label="Permalink to &quot;No engine, and why that&#39;s the point&quot;">​</a></h2><p>Every other demo borrows an invariant from an engine — a work-order state machine, an allocation arbiter, an invoice basis. Manyfold&#39;s central invariant is the <strong>editorial lifecycle</strong>, and there is no engine that owns it. So the vertical does, and it does so under exactly the discipline an engine would enforce:</p><ul><li>a <strong>state machine</strong> that rejects any transition not on the allowed graph;</li><li><strong>append-only</strong> revisions and an append-only status log — a correction is a new row;</li><li><strong>immutable-after-publish</strong>: the published revision is frozen with a content hash;</li><li><strong>every mutation emits a fat event</strong> on the spine.</li></ul><p>That the same four properties keep reappearing — here, in Callout&#39;s work orders, in Meridian&#39;s ledgers — is the extraction signal itself. The line Manyfold draws for a future <code>content</code> / editorial engine is visible in <code>module.ts</code>: the lifecycle helpers (<code>transition</code>, the <code>ALLOWED</code> graph, <code>contentHash</code>, the delivery projection) are the piece that would move; the content-type <em>modelling</em> and the vocabulary would stay in the vertical. It isn&#39;t extracted yet <strong>on purpose</strong> — a single consumer is a guess, and D-27 is the rule against designing an engine off one demo&#39;s wishlist.</p><h2 id="the-editorial-state-machine" tabindex="-1">The editorial state machine <a class="header-anchor" href="#the-editorial-state-machine" aria-label="Permalink to &quot;The editorial state machine&quot;">​</a></h2><p>An entry&#39;s <code>status</code> moves along a fixed graph; <code>transition</code> refuses anything off it and writes an append-only <code>manyfold_status_log</code> row for every hop:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>draft ──▶ in_review ──▶ approved ──▶ published ──▶ unpublished</span></span>
<span class="line"><span>  │           │            │             │              │</span></span>
<span class="line"><span>  │           ▼            ▼             ▼              ▼</span></span>
<span class="line"><span>  └────────▶ archived (terminal) ◀───────────────────────</span></span>
<span class="line"><span>        (in_review can bounce back to draft; unpublished re-enters at in_review)</span></span></code></pre></div><p>The guards <strong>move with state</strong>, which is the property the scenario pins:</p><ul><li><code>publish</code> requires an entry to be <strong><code>approved</code></strong> — a fresh draft can&#39;t skip straight to published, and neither can one merely submitted for review.</li><li>Only a <code>draft</code> or <code>unpublished</code> entry takes a new revision; editing a <code>published</code> one is refused, because its revision is frozen.</li><li><code>approve</code> / <code>reject</code> are <code>content:review</code> acts; a rejection <strong>requires a note</strong>, which lands on the <code>draft</code> transition it causes.</li></ul><h3 id="freeze-on-publish" tabindex="-1">Freeze on publish <a class="header-anchor" href="#freeze-on-publish" aria-label="Permalink to &quot;Freeze on publish&quot;">​</a></h3><p><code>publish</code> computes a SHA-256 over <code>(typeKey, revNo, canonical body)</code> — Web Crypto, never <code>node:crypto</code> — flips the revision&#39;s <code>frozen</code> flag, records the hash, and upserts the <strong>delivery</strong> projection in the <em>same transaction</em>. From then on the published body is immutable; any change authors a new revision against a new number. <code>unpublish</code> and <code>archive</code> remove the entry from delivery. The read model can never show content that disagrees with the frozen hash, because it&#39;s only ever written from the freeze.</p><h2 id="content-types-are-data" tabindex="-1">Content types are data <a class="header-anchor" href="#content-types-are-data" aria-label="Permalink to &quot;Content types are data&quot;">​</a></h2><p>The four defaults — <code>author</code>, <code>page</code>, <code>post</code>, <code>snippet</code> — are seeded lazily on first use, and a <code>content:admin</code> may author more at runtime (<code>manyfold/save-type</code>). A type names its fields, its title field, an optional slug field, and <code>ref</code> / <code>refMany</code> fields that target other types. Every save <strong>bumps the type&#39;s version</strong> (schema evolution is a new version); an entry body is validated against its type&#39;s generated Zod schema at the boundary, so an unknown field is rejected. Milestone A persists bodies as JSON, so adding a field is free — the typed-table migration each type <em>compiles to</em> (<code>compileTypeToSql</code>) is kept as the reviewable artifact, not run as a live <code>ALTER</code>.</p><h3 id="delivery-and-references-resolved-late" tabindex="-1">Delivery, and references resolved late <a class="header-anchor" href="#delivery-and-references-resolved-late" aria-label="Permalink to &quot;Delivery, and references resolved late&quot;">​</a></h3><p><code>manyfold_delivery</code> is the public read model: <strong>only published, frozen content</strong>, keyed by <code>(type, slug)</code>. <code>manyfold/deliver</code> reads it and resolves each reference field against the projection — a target that is published comes back as <code>{ $ref, type, slug, title }</code>; a target still in draft (or archived) comes back as <code>{ $unresolved: true, reason: &#39;not_published&#39; }</code>. A broken link is shown honestly rather than silently dropped, and a reference &quot;heals&quot; the moment its target is published, without touching the referring entry.</p><h2 id="roles-permissions" tabindex="-1">Roles &amp; permissions <a class="header-anchor" href="#roles-permissions" aria-label="Permalink to &quot;Roles &amp; permissions&quot;">​</a></h2><p>Authority is a <strong>role ladder</strong>, held per site (K-22: the same login is a different principal, with a different role, in each scope). There are <strong>no entity-narrowed grants</strong> — this vertical&#39;s access is node-level, not per-entity, which is itself a contrast with RallyPoint and Meridian:</p><table tabindex="0"><thead><tr><th>Role</th><th>Holds</th><th>Can</th></tr></thead><tbody><tr><td><code>viewer</code></td><td><code>content:read</code></td><td>read entries, revisions, models</td></tr><tr><td><code>author</code></td><td>+ <code>content:author</code></td><td>create/edit drafts, submit for review, restore revisions</td></tr><tr><td><code>editor</code></td><td>+ <code>content:review</code></td><td>approve or reject entries in review</td></tr><tr><td><code>publisher</code></td><td>+ <code>content:publish</code></td><td>publish, unpublish, archive</td></tr><tr><td><code>admin</code></td><td>+ <code>content:admin</code></td><td>manage members, roles, and content <strong>models</strong></td></tr></tbody></table><p>The five keys are <code>content:read</code> · <code>content:author</code> · <code>content:review</code> · <code>content:publish</code> · <code>content:admin</code>. The installing owner holds <code>admin</code> tenant-wide (<code>scopeId: null</code>) — admin of every site from day one; everyone else is assigned a role <strong>per site</strong>. The full surface — every key and which role holds it — is the checked-in <a href="https://github.com/substrat-run/substrat/blob/main/demos/manyfold/PERMISSIONS.md" target="_blank" rel="noreferrer"><code>PERMISSIONS.md</code></a>, re-emitted by CI so it can&#39;t drift from what runs.</p><h2 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h2><p>Every lifecycle transition emits a fat event and consumes none: <code>content.submitted</code> · <code>content.approved</code> · <code>content.rejected</code> · <code>content.published</code> · <code>content.unpublished</code> · <code>content.archived</code>. <code>content.published</code> is the fat event a <strong>webhook connector</strong> would consume to purge a CDN or trigger a static rebuild — that consumer is host code, the documented next step, not module code.</p><h2 id="the-app" tabindex="-1">The app <a class="header-anchor" href="#the-app" aria-label="Permalink to &quot;The app&quot;">​</a></h2><p>One content-studio React app, composed over the vertical&#39;s single API. The twist over the other demos is <strong>multi-scope</strong>: the dev picker sends <code>x-principal</code> (who you are) <strong>and <code>x-site</code></strong> (which of the tenant&#39;s sites you&#39;re working in), and the server maps both to a scope stub. The app gates its own chrome on <code>manyfold/whoami</code>, which reports what the current principal may do <em>in this site</em> — so the same login sees author tools on one site and read-only on another. The seeded personas make that concrete:</p><table tabindex="0"><thead><tr><th>Persona</th><th>cafe</th><th>padel</th><th>law</th></tr></thead><tbody><tr><td><strong>Maja Lindqvist</strong> (owner)</td><td>admin</td><td>admin</td><td>admin</td></tr><tr><td><strong>Emil Berg</strong></td><td>publisher</td><td>author</td><td>viewer</td></tr><tr><td><strong>Sofia Ruiz</strong></td><td>author</td><td>—</td><td>—</td></tr></tbody></table><p>Switching Emil between sites is the demo: the same human, a different role — and on a site where he holds nothing, denied even to read.</p><h2 id="run-it" tabindex="-1">Run it <a class="header-anchor" href="#run-it" aria-label="Permalink to &quot;Run it&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/demo-manyfold</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span></span></code></pre></div><p>Starts the API on <code>:8876</code> and the app on <code>:5276</code> (both in the private <code>887x</code> / <code>527x</code> block; override with <code>PORT=… WEB_PORT=…</code>). Switch persona <strong>and</strong> site to watch the same login&#39;s authority change scope by scope.</p><p>The executable spec is the scenario test — ten cases replayed headlessly on the pure-SQLite adapter:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @substrat-run/demo-manyfold</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> test</span></span></code></pre></div><p>It asserts the module journal applied per scope; that an admin can model a new content type (with a reference) and it drives <code>create-entry</code> immediately while an author cannot model; append-only revisions and a restore that copies an old body into a <em>new</em> revision without mutating history; the workflow denials (author can&#39;t approve or publish, a viewer can&#39;t write, a login with no role on a site is denied even to read); publish freezing a revision with a verifiable hash and filling the delivery projection; immutability and the no-state-machine-skips guards; references resolving late (draft = unresolved, then resolved once the target publishes); scope isolation (publishing on one site leaves the others with no delivered content); and that every mutation landed on the spine, in order.</p><p>The <code>PERMISSIONS.md</code> and the append-only migrations (<code>0001-init</code>, <code>0002-content-types</code>) are the human checkpoints the platform makes unskippable.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("verticals/manyfold.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const manyfold = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  manyfold as default
};
