{
  "name": "@substrat-run/router",
  "version": "0.1.13",
  "private": true,
  "description": "The environment-wide router — hostname to (tenant, scope, surface), then dispatch (K-26)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "cf:deploy": "wrangler deploy",
    "cf:deploy:test": "wrangler deploy --env test",
    "dry-run": "wrangler deploy --dry-run --outdir dist-worker"
  },
  "dependencies": {
    "@substrat-run/adapter-cloudflare": "workspace:^",
    "@substrat-run/contracts": "workspace:^"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^7.0.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.110.0"
  }
}
