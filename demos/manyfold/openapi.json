{
  "openapi": "3.1.0",
  "info": {
    "title": "Manyfold CMS API",
    "version": "0.0.1",
    "description": "The Manyfold demo vertical — a multi-scope headless CMS (draft→review→publish lifecycle, append-only revisions, freeze-on-publish, data-defined content types) on the Substrat kernel. Every operation checks a permission for the calling principal."
  },
  "paths": {
    "/api/op/manyfold/create-entry": {
      "post": {
        "operationId": "manyfold-create-entry",
        "summary": "Create an entry (draft revision 1).",
        "description": "Requires `content:author`. `body` is validated against the content type’s own field schema.",
        "tags": [
          "Authoring"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "typeKey": {
                    "type": "string",
                    "minLength": 1
                  },
                  "body": {}
                },
                "required": [
                  "typeKey",
                  "body"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/save-draft": {
      "post": {
        "operationId": "manyfold-save-draft",
        "summary": "Save a new draft revision (append-only — an edit is a new revision).",
        "description": "Requires `content:author`. Only `draft` or `unpublished` entries take new revisions.",
        "tags": [
          "Authoring"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "entryId": {
                    "type": "string",
                    "minLength": 1
                  },
                  "body": {}
                },
                "required": [
                  "entryId",
                  "body"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/restore-revision": {
      "post": {
        "operationId": "manyfold-restore-revision",
        "summary": "Restore an old revision — as a new revision, never a mutation of history.",
        "description": "Requires `content:author`.",
        "tags": [
          "Authoring"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "entryId": {
                    "type": "string",
                    "minLength": 1
                  },
                  "revNo": {
                    "type": "integer",
                    "exclusiveMinimum": 0,
                    "maximum": 9007199254740991
                  }
                },
                "required": [
                  "entryId",
                  "revNo"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/submit-for-review": {
      "post": {
        "operationId": "manyfold-submit-for-review",
        "summary": "Submit a draft for review.",
        "description": "Requires `content:author`. The state machine cannot skip: draft → in_review.",
        "tags": [
          "Lifecycle"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "entryId": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "entryId"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/approve": {
      "post": {
        "operationId": "manyfold-approve",
        "summary": "Approve an entry in review.",
        "description": "Requires `content:review`.",
        "tags": [
          "Lifecycle"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "entryId": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "entryId"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/reject": {
      "post": {
        "operationId": "manyfold-reject",
        "summary": "Reject an entry in review, back to draft — a rejection needs a note.",
        "description": "Requires `content:review`.",
        "tags": [
          "Lifecycle"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "entryId": {
                    "type": "string",
                    "minLength": 1
                  },
                  "note": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "entryId",
                  "note"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/publish": {
      "post": {
        "operationId": "manyfold-publish",
        "summary": "Publish an approved entry: freeze the revision with a content hash, project to delivery.",
        "description": "Requires `content:publish`. A frozen revision is immutable; later edits are new revisions.",
        "tags": [
          "Lifecycle"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "entryId": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "entryId"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/unpublish": {
      "post": {
        "operationId": "manyfold-unpublish",
        "summary": "Unpublish an entry and remove it from delivery.",
        "description": "Requires `content:publish`.",
        "tags": [
          "Lifecycle"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "entryId": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "entryId"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/archive": {
      "post": {
        "operationId": "manyfold-archive",
        "summary": "Archive an entry (terminal).",
        "description": "Requires `content:publish`.",
        "tags": [
          "Lifecycle"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "entryId": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "entryId"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/list-entries": {
      "post": {
        "operationId": "manyfold-list-entries",
        "summary": "List entries, optionally by type and/or status.",
        "description": "Requires `content:read`.",
        "tags": [
          "Read"
        ],
        "requestBody": {
          "required": false,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "typeKey": {
                    "type": "string",
                    "minLength": 1
                  },
                  "status": {
                    "type": "string",
                    "enum": [
                      "draft",
                      "in_review",
                      "approved",
                      "published",
                      "unpublished",
                      "archived"
                    ]
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/review-queue": {
      "post": {
        "operationId": "manyfold-review-queue",
        "summary": "Entries waiting in review.",
        "description": "Requires `content:review`.",
        "tags": [
          "Read"
        ],
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/get-entry": {
      "post": {
        "operationId": "manyfold-get-entry",
        "summary": "One entry: current draft body + revision history.",
        "description": "Requires `content:read`.",
        "tags": [
          "Read"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "entryId": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "entryId"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/list-types": {
      "post": {
        "operationId": "manyfold-list-types",
        "summary": "The content types (data, not code) and the typed-table SQL each compiles to.",
        "description": "Requires `content:read`. This is where a client discovers what an entry `body` must look like.",
        "tags": [
          "Modelling"
        ],
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/save-type": {
      "post": {
        "operationId": "manyfold-save-type",
        "summary": "Create or update a content type — every save bumps its version.",
        "description": "Requires `content:admin`.",
        "tags": [
          "Modelling"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "key": {
                    "type": "string",
                    "pattern": "^[a-z][a-z0-9_]*$"
                  },
                  "title": {
                    "type": "string",
                    "minLength": 1
                  },
                  "titleField": {
                    "type": "string",
                    "minLength": 1
                  },
                  "slugField": {
                    "type": "string"
                  },
                  "fields": {
                    "type": "object",
                    "propertyNames": {
                      "type": "string",
                      "pattern": "^[a-z][a-zA-Z0-9]*$"
                    },
                    "additionalProperties": {
                      "type": "object",
                      "properties": {
                        "type": {
                          "type": "string",
                          "enum": [
                            "text",
                            "richText",
                            "slug",
                            "bool",
                            "int",
                            "date",
                            "enum",
                            "textArray",
                            "assetRef",
                            "assetRefMany",
                            "ref",
                            "refMany"
                          ]
                        },
                        "required": {
                          "type": "boolean"
                        },
                        "index": {
                          "type": "boolean"
                        },
                        "options": {
                          "type": "array",
                          "items": {
                            "type": "string"
                          }
                        },
                        "target": {
                          "type": "string"
                        },
                        "source": {
                          "type": "string"
                        },
                        "maxLen": {
                          "type": "integer",
                          "exclusiveMinimum": 0,
                          "maximum": 9007199254740991
                        }
                      },
                      "required": [
                        "type"
                      ]
                    }
                  }
                },
                "required": [
                  "key",
                  "title",
                  "titleField",
                  "fields"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/delete-type": {
      "post": {
        "operationId": "manyfold-delete-type",
        "summary": "Delete a content type no entries use.",
        "description": "Requires `content:admin`.",
        "tags": [
          "Modelling"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "key": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "key"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/request-site": {
      "post": {
        "operationId": "manyfold-request-site",
        "summary": "Request a new site — provisions a new scope for the tenant.",
        "description": "Requires `content:manage-sites`. The vertical cannot provision itself, so this enqueues a platform intent the control plane drains; returns the request id to poll.",
        "tags": [
          "Sites"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "slug": {
                    "type": "string",
                    "minLength": 1
                  },
                  "name": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "slug",
                  "name"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/deliver": {
      "post": {
        "operationId": "manyfold-deliver",
        "summary": "One published document by (type, slug), references resolved against the published projection.",
        "description": "Requires `content:read`. Only published, frozen content — an unpublished reference comes back as an explicit unresolved marker.",
        "tags": [
          "Delivery"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "typeKey": {
                    "type": "string",
                    "minLength": 1
                  },
                  "slug": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "typeKey",
                  "slug"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/list-delivery": {
      "post": {
        "operationId": "manyfold-list-delivery",
        "summary": "The published projection, optionally by type.",
        "description": "Requires `content:read`.",
        "tags": [
          "Delivery"
        ],
        "requestBody": {
          "required": false,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "typeKey": {
                    "type": "string",
                    "minLength": 1
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/whoami": {
      "post": {
        "operationId": "manyfold-whoami",
        "summary": "The caller’s principal and capability flags — the app gates its chrome on this.",
        "description": "Requires `content:read`. The kernel still enforces the real permission on every operation regardless.",
        "tags": [
          "Read"
        ],
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    },
    "/api/op/manyfold/timeline": {
      "post": {
        "operationId": "manyfold-timeline",
        "summary": "The event-spine timeline for one entity.",
        "description": "Requires `content:read`.",
        "tags": [
          "Read"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "entityType": {
                    "type": "string",
                    "minLength": 1
                  },
                  "entityId": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "entityType",
                  "entityId"
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The operation result."
          },
          "400": {
            "description": "Validation failed — the input did not parse against the operation schema."
          },
          "401": {
            "description": "No session — sign in (or present a bearer token) first."
          },
          "403": {
            "description": "Permission denied — the caller lacks the permission this operation checks."
          },
          "404": {
            "description": "Unknown operation, or an entity named in the input does not exist."
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "session": {
        "type": "apiKey",
        "in": "cookie",
        "name": "sb_session",
        "description": "The vertical's session cookie. Instances on the Better-Auth provider use its cookie instead; same-origin requests carry either automatically."
      },
      "bearer": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    }
  },
  "security": [
    {
      "session": []
    },
    {
      "bearer": []
    }
  ]
}
