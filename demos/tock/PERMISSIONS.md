<!-- GENERATED by tools/permission-diff.mts — do not edit by hand.
     Regenerate: pnpm lint:permissions
     HUMAN CHECKPOINT (CLAUDE.md): an agent may never self-approve a change to this
     file. Reviewer: §3 is the risk surface — a key gaining a role. -->

# Permission snapshot — @substrat-run/demo-tock

4 keys · 1 modules · 4 roles

## 1. Registry — every key a registered manifest declares

| Key | Description | Declared by |
| --- | --- | --- |
| `report:read` | Read counts, schemas, runs and findings | `@substrat-run/demo-tock` |
| `row:read` | Read the mapped rows of a run, and download the file it came from | `@substrat-run/demo-tock` |
| `run:manage` | Upload a file and take a run through profiling, mapping and counting | `@substrat-run/demo-tock` |
| `schema:manage` | Declare a source and write new versions of its shape | `@substrat-run/demo-tock` |

## 2. Roles — as defined by this vertical's provisioning code

Identical in every tenant. Per-tenant customisation is a runtime concern.

| Role | Permissions |
| --- | --- |
| `admin` | `report:read`, `row:read`, `run:manage`, `schema:manage` |
| `analyst` | `report:read`, `row:read`, `run:manage` |
| `modeller` | `report:read`, `row:read`, `run:manage`, `schema:manage` |
| `viewer` | `report:read` |

## 3. Coverage — which roles hold each key

| Key | Held by |
| --- | --- |
| `report:read` | `admin`, `analyst`, `modeller`, `viewer` |
| `row:read` | `admin`, `analyst`, `modeller` |
| `run:manage` | `admin`, `analyst`, `modeller` |
| `schema:manage` | `admin`, `modeller` |

## 4. Not covered by this artifact

- **The grants themselves** — per-principal, per-entity, minted at runtime with
  random ULIDs. Only their shapes above are representable deterministically.
- **Operator-defined roles** — roles created against a live deployment. This
  artifact covers provisioning code only.
