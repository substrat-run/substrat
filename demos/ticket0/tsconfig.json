{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "types": [
      "node"
    ]
  },
  "include": [
    "spec",
    "src",
    "harness",
    "test",
    "tools",
    "vitest.workers.config.ts"
  ],
  "exclude": [
    "src/worker.ts",
    // Runs inside workerd, not node — its own project (tsconfig.workerd-test.json).
    "test/workerd"
  ]
}
