{
	"name": "external-vertical",
	"main": "src/worker.ts",
	"compatibility_date": "2025-01-01",
	"durable_objects": {
		"bindings": [
			{ "name": "SCOPE", "class_name": "ScopeDO" },
			{ "name": "CONTROL_PLANE", "class_name": "ControlPlaneDO" }
		]
	},
	"migrations": [
		{
			"tag": "v1",
			"new_sqlite_classes": ["ScopeDO", "ControlPlaneDO"]
		}
	]
	// No `vars`: the OIDC settings are deliberately absent so a real `wrangler deploy`
	// authenticates nobody until it is given an issuer. `npm run dev` passes the local
	// dev issuer's for local use only.
}
