{
  "name": "external-vertical",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A Substrat vertical built from published packages — the 'build an app outside the repo' example (first-flow.md slice 2)",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev --var ALLOW_DEV_HEADER:true",
    "cf:deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run --outdir dist-worker"
  },
  "dependencies": {
    "@substrat-run/adapter-cloudflare": "^0.98.1",
    "@substrat-run/contracts": "^0.98.1",
    "@substrat-run/engine-workorder": "^0.10.6",
    "@substrat-run/kernel": "^0.98.1",
    "hono": "^4.12.30",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^7.0.0",
    "wrangler": "^4.110.0"
  }
}
