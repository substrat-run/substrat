---
---

docs: the `freshness` manifest key on the modules and contracts pages; the injected `SUBSTRAT_VERSION_ID` binding and the refused `SUBSTRAT_` namespace on the deploy model page (#1478). Docs site only, no published package changed.
