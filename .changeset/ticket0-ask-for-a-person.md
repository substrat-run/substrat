---
---

ticket0's "Talk to a human" is a route rather than a sentence about one: the widget posts to `/handoff`, which records the ask, acknowledges it to the visitor and notifies the desk in one call — and an escalation on an unassigned conversation now tells every agent instead of nobody (private demo, no published package).
