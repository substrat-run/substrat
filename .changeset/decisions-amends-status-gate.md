---
---

tools: the decision-log gate validates `amends` and `superseded-by`, renders their back-pointers, enforces the status set and writes the README's entry count; private tooling and docs only, nothing published.
