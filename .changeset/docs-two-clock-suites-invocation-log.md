---
---

The contract-tests reference names both clock-bound suites the Cloudflare host cannot mount (`grantExpiryContractSuite` and `facetRecencyContractSuite`), and the agent conventions name `lint:invocation-log` in the refuse-only gate list with the rule it enforces.
