---
---

Callout, Handlebar, Meridian and Shop now hand their declared permission keys to
`definePermissions` as `keys`, so the restatement `defineOperations` needs is checked at
module load instead of maintained by hand. Demo verticals only — no published package
changes.
