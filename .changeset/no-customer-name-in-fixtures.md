---
---

Removes a customer's name from test fixtures, tenant slugs, code comments and internal
docs. Every edit inside a published package is a comment, a test fixture or a doc — no
shipped behaviour changes, so nothing needs a version bump.
