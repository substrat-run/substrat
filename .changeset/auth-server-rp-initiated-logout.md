---
---

auth-server: a registered application can be allowed to sign people out. The issuer refuses
RP-initiated logout unless the client row says yes and lists its own post-logout redirect
URIs, and neither column was reachable from the console — so every client it had was refused.
Both packages are private, so nothing publishes.
