---
---

Internal docs only: `docs/briefs/model-package-adoption.md` records what an external adopter can generate from the model packages today, re-derived against `main`. No workspace package changes.
