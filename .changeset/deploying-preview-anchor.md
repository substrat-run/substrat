---
---

Docs: the "Preview a pull request" heading in `guide/deploying.md` now carries an explicit `{#preview-a-pull-request}` anchor, matching the `{#promote-to-prod}` and `{#deploy-from-ci}` anchors the same page already uses, so all three of its deep-linked headings have a typeable id instead of one containing a raw em dash. Docs-only (`apps/docs` is private).
