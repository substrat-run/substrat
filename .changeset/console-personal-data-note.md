---
"@substrat-run/dashboard-web": patch
"@substrat-run/docs": patch
---

The SQL console and the table browse now say that they read the app's data as stored, including personal data, and that every query is recorded in the access log. The dashboard docs state the same, with the Events explorer's two known limits.
