---
---

Docs: `concepts/scope-host.md` now shows `invoke`'s third argument and documents `InvokeOptions` — `ifMatch`/`onEntityVersion` and `idempotencyKey`/`onIdempotentReplay`. Docs-only (`apps/docs` is private).
