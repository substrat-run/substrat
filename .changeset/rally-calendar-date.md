---
---

demos/rally (private): every date input is the platform's `calendarDate` (#117) — a real month/day check, so an impossible day such as `2030-13-45` is refused — instead of a shape-only regex.
