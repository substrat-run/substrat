---
---

The auth-server console's IA document describes the console that exists: the detail screens and the sign-in log are in its nav and route tables, the return-target allowlist names the detail parsers that are also part of it, and the two follow-ups it deferred — list-to-detail and self-hosted faces — are recorded as landed rather than still pending (#1278).
