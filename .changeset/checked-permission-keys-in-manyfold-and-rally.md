---
---

Manyfold and Rally now hand their declared permission keys to `definePermissions` as
`keys`, finishing the adoption the other six demos already carry, so the restatement
`defineOperations` needs is checked at module load instead of maintained by hand. Demo
verticals only — no published package changes.
