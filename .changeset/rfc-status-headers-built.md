---
---

Docs only: the sub-transactions and error-model documents move from `docs/rfc/` to `docs/architecture/` with `status: built`; code comments in kernel, contracts, contract-tests and boundary-lint point at the new paths.
