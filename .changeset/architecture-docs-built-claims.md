---
---

Docs: `docs/architecture/control-plane.md` and `docs/architecture/dashboard.md` no longer describe shipped behaviour as unbuilt — the K-24 staff access log, the Dashboard's OIDC sign-up, and the version registry's HTTP routes. Architecture documents only — no package changed.
