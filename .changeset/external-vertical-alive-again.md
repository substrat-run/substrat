---
---

The `examples/external-vertical` example installs from the registry again — its pins were a minor behind and its `@cloudflare/workers-types` range no longer satisfied current `wrangler`'s peer. Not a workspace member and not published, so no package version changes.
