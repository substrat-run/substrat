---
---

Both of the builder studio's file-write routes — the local server and the hosted studio — judge a path by where it points, so a `..` after the project prefix no longer writes elsewhere in the repo (#1225).
