---
---

The published status inventory names every engine and vertical that has a page, and `docs-drift` now refuses when it does not. Docs and tooling only — `apps/docs` is private, so nothing published changes.
