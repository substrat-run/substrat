---
---

`examples/external-vertical` signs in through the dev issuer and `@substrat-run/vertical-auth` instead of an `x-principal` dev header; it is not a workspace member, so no published package changes.
