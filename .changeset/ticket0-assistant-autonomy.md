---
---

ticket0's assistant can be let off the leash and put back on it, and a supervised desk stops reporting itself healthy while it withholds every answer. `demos/ticket0` is private and publishes nothing, so no package version moves.
