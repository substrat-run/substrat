---
---

Console only (private): Operations gains a Sweeps view over the fleet's sweep
record (#1232) — every connection polled, schedule fired or skipped, and
freshness verdict, newest first, with kind/outcome/tenant/unit filters and
cursor pagination. The staff twin of the dashboard's per-app strips. Nothing
published changes.
