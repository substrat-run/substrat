---
---

Comment and docs only: the repo's public-repository rule is written down in `CLAUDE.md`, and the customer name it was breaking is removed from `packages/contracts`' doc comments and `docs/rfc/model-phase-plan.md`. No behaviour, no exported surface.
