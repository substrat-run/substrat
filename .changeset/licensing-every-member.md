---
---

Docs: `LICENSING.md` now accounts for every workspace member, with each row's licence and publish state read from that directory's own `package.json`. Root document only — no package changed.
