---
---

Docs only: the identity page's federated sign-in bullet now names the auth server's Custom (OIDC) door beside the closed catalogue — a slug, a label, an issuer URL and a credential, with discovery resolved once at save time (#1247).
