---
---

A missing changelog week is now a red build: `lint:changelog --check` holds the published
entries to a contiguous run of settled weeks. Repo tooling and docs only — no published
package changes.
