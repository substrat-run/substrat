---
---

Docs only: `concepts/modules.md` and `guide/getting-started.md` show `operationInputs` and `operationConcurrency` on a `ModuleRegistration`, and say that the host owns the parse. No published package changed.
