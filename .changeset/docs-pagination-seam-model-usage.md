---
---

Docs: `concepts/model.md` now names `pageOverFold` as the third `sortKey` case and states that a page cursor must be unique and ordered the way the query is, with `CURSOR_FIELD_SEPARATOR` as the answer when the ordering column ties; `reference/contracts.md` gains sections for the engine seam (`engineSeam`, `returns`, `columnsOf`) and model usage (`modelUsageLine`, `MODEL_USAGE_KIND`, `marginFactor`), and its pagination enumeration picks up the two new exports. Docs-only (`apps/docs` is private).
