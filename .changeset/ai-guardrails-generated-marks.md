---
---

Docs only: `guide/ai-guardrails.md` states where the `<!-- GENERATED … -->` marker goes on a page with frontmatter, and splits its one gate list into the gates that re-emit a derived file and the checks that only refuse. No published package changed.
