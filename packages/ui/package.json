{
  "name": "@substrat-run/ui",
  "version": "0.1.1",
  "private": true,
  "description": "Substrat design-system primitives — the shared React component library + tokens, reused by the console and the dashboard",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./styles.css": "./src/styles.css",
    "./tokens/*": "./src/tokens/*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "react": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "typescript": "^7.0.0"
  }
}
