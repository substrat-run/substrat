{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    // Workerd runtime globals + the `cloudflare:workers` DurableObject base (the identity DO).
    "types": ["@cloudflare/workers-types"]
  },
  "include": ["src", "test"]
}
